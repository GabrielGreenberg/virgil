# Next-session prompt — Post-refactor MAINTENANCE management session (Session 17)

> Paste the block below as the opening prompt of the next session. Run it in **ultracode**.
> It is a **management session**: walks-coordination + bug-drain + residue chips. The card-system refactor itself is **COMPLETE and ARCHIVED** — do not re-open it.

---

You are the **post-refactor maintenance management session** for Virgil. The card-system refactor (registry SSOT + `Floatable` presence) is **complete, DoD-verified, merged, and PUSHED** — the archived SSOT with the full verdict lives at `docs/card-refactor/CARD-SYSTEM-REFACTOR.md` (read its **DoD addendum** + the Session-16 Progress entries for the end state and every ratified deferral). Your job is the residue: coordinate the manual walks, drain the bug backlog, and dispatch the named follow-up chips. This is a **manager** role — decompose, dispatch, gate, merge; don't implement cross-cutting work inline.

**CENTRAL RULE (every chip, restated):** prefer **deep, architectural solutions over superficial patches** — diagnose the *class* of bug, derive from `CARD_REGISTRY` rather than adding a parallel switch, and kill analogous bugs alongside the reported one. **Verify feasibility against the code before committing to a preferred design** (let a Plan agent surface concrete options before putting an impl-coupled question to Gabriel — A6's R15 ask sharpened materially from this). When a clean fold hits a real seam wrinkle, **let the consumer own the coupling**.

**RIGHT-SIZE THE GATE.** This is maintenance, not the mega-refactor: a cross-cutting or registry-touching chip still gets the full independent adversarial review Workflow before merge (lenses → refutation → synthesizer re-running tsc/vitest/grep-gates); a small scoped fix can take a single skeptical reviewer agent. Never merge on chip self-verification alone — that rule earned its keep ~16 times.

## Read first
1. **`docs/card-refactor/CARD-SYSTEM-REFACTOR.md`** — the **DoD addendum** (named residue chips + ratified deferrals) and the **Session-16 Progress entries** (deferred-follow-up lists per arena). This is the archived SSOT — read-only; new work gets tracked in THIS session's own notes + the backlog memo.
2. **`MEMO_CARD_REFACTOR_WALKS.md`** (repo root) — walks **W1–W9** (W10 signed off; W8 core passed). Check off / delete as they pass; failures become numbered backlog items.
3. **`MEMO_BUG_BACKLOG.md`** (repo root) — 13 numbered items (#12/#13 are the carried A4 deferrals).
4. **`AGENTS.md`** — keystroke sanctity (non-negotiable: `window.__virgilBusStats().emitCount` flat on plain typing; card-source derivation gates on `useStructuralRevisions`) + the two-kinds rule.

## State (post-push 2026-06-11)
- `main` is **pushed and in sync with origin** (the 98-commit local-only era is over). **Every push to main auto-deploys to GitHub Pages** (`deploy.yml`) — normal flow. CI now includes the refactor's new **typecheck+vitest job** in `coherence.yml`.
- Baseline: **tsc clean, 664 tests / 72 files**. 16 card kinds, one `CARD_REGISTRY` (`src/cards/card-registry.tsx`), one `float:card:<kind>:<id>` grammar (`cardPopKey`), one `Floatable` subsystem (`src/floats/`).
- The skills/`docs/workspace` manifest sync to user papers is **version-stamp-gated** — stale manifest text does NOT ship on a raw push, only on the next `/cleanup-virgil` release-promote. **The doc-rot sweep must land BEFORE that next release.**

## DO (priority order; walks and chips interleave — walks are Gabriel-driven, coordinate don't block)
1. **Walks W1–W9** (`MEMO_CARD_REFACTOR_WALKS.md`): drive headlessly what you can in the dev preview (see the preview memories: `virgil:force-dev-storage`, `el.editor` access, `__virgilBusStats()`), guide Gabriel through the visual/two-client halves, log outcomes in the memo, convert failures to backlog items. W2 (clone/delete cascade — its only execution ever) and W9 (two-client collab; first-ever revision/report presence dots) carry the most risk. W9's CI item may already be closed by the Session-16 push — verify the run went green before re-checking.
2. **`test-hardening` chip** (load-bearing, do early): the `CARD_THEMES`/`panel-theme.defaults.json` frozen-13-key pin (**the 2026-06-09 promote-defaults stale-snapshot incident makes a dropped JSON key → silent black palette a live hazard**) · the marker-meta alias-tautology fix (frozen literal key list) · AI-bridge idempotency pins (existingIdx refresh / `value=false` drop / terminal re-file — the duplicate-on-re-toggle class) · the A5 test-fidelity trio (anchor-state 10-builder coverage; contract files testing the REAL OmniViewPanel memo not a hand-copied mirror; bin `panelScrollRef` parentage) · A6's behavior pins (gutter toggle early-return; error-bridge `selected:false`-must-not-open; `routeAnchorClick` body; morph-gate call sites; capacity=1 grid edge) · the lifecycle wrong-flag CI assertion.
3. **Doc-rot sweep** (BEFORE the next release-promote): `docs/workspace/actions.md:154-156` (THREE deleted hooks: `useFootnoteActions`/`useMarkerActions`/`useSelectionToCardActions`) · `docs/agents/main-text.md` (deleted `marginaliaMarkers` memo) · `docs/workspace/cards.md` (17→16 kinds; deleted `POLYMORPHIC_CARD_PANEL`-era text, `MARKER_KIND_TO_THEME_KEY`, comment-alias wording) · sweep `docs/agents/*` `last-verified` stamps against the new HEAD. Also re-pin AGENTS.md's permitted-subscriber list (LinkConnector was just removed from it — verify the rest).
4. **Drain `MEMO_BUG_BACKLOG.md`** (13 items): batch by area, dispatch right-sized fix chips. #12/#13 (OrphanedFootnoteCard weld; CitationCard draft chevron) are quick A4-pattern fixes.
5. **Remaining named chips** (scoped in the DoD addendum, in rough priority): **`registry-completion`** (RecentlyAddedKind · StackCardKind+snapshot switch incl. the `bibliography`-vs-`bib` drift · `HighlightType`/`CardContentKind` vestiges · write-only `PANEL_REGISTRY` `CardLink` fields · example `stackable:true`-but-snapshot-`null` wart · 2-site revision `kindOptions` · SEAM E-5 `linkedAnchor.kind` attr write-site migration) · **`float-chrome-stage6`** (bib/ai `bareWindow` → full `FloatChrome`; text-object `snapshotForStack` stub) · **`print-chrome`** (one `data-card-chrome` stamp activates the prepared `globals.css` rule — pre-existing wart, R23) · **`error-apparatus-consolidation`** (split error lists/selection/dismissals; **gated on the code-view migration populating `paragraphByErrorId`**) · **`bridge-docId-hardening`** (window-bridge events carry docId) · the `setActivePanelKindBySide`/`openItemInPanel` vestige cleanup + `usePanelColorSubscription` dual-role + orphaned `useExamples`/`usePristineKind` gardening.

## The proven cadence (right-sized)
**Plan agent (read-only, re-pinned to HEAD) → ratify user-facing calls with Gabriel (AskUserQuestion; verify impl-coupled ones against code first) → impl chip (Agent, background, commits PER-STEP) → review gate (full Workflow for cross-cutting; single skeptic for small fixes) → fold load-bearing nits → merge `--no-ff` → update the backlog/walks memos.**

## Hard guards (unchanged — they all earned their keep)
- Impl chips: **commit PER-STEP** (chips died mid-run 4+ times; the committed prefix always survived — and twice a continuation chip finished from the uncommitted tree). **Stage explicit paths, NEVER `git add -A`. Single-quote `git commit -m` (no backticks). Each git command its own Bash call; prefer `git -C`. `timeout` is NOT on this shell** (`npx tsc --noEmit > log 2>&1; echo $?`).
- **NEVER run `tools/sync-defaults.sh`** (even `--check`) — it auto-commits a stale `personal-snapshot.json`. Eyeball `*.defaults.json` diffs by hand.
- Review/audit Workflow agents: **NO working-tree mutation** (forbid checkout/switch/stash/reset/restore/commit/merge + sync-defaults); inspect via `git show <ref>:<path>` / `git diff <range>`; synthesizer runs tsc/vitest on the current checkout without switching branches. Review scripts → repo-root `.tmp-<name>-review.mjs`, invoke via `scriptPath`, **`rm` before merge**; build CTX via array-`join('\n')`, no backticks in prompt strings.
- **Keystroke sanctity** on every chip (`emitCount` flat; no new `editor.on('update'|'transaction')` subscriber without an O(1) justification + AGENTS.md listing) + **two-kinds** (touch text-objects only at the `Floatable` window layer).
- **Pushes now deploy** — a merge to main goes live on push. Batch merges sensibly; nothing half-broken on main.
- SendMessage to a prior background agent is NOT available across sessions — dispatch fresh, seed with the prior plan's anchors. Don't leave a Plan agent running across a session boundary.
- **Re-pin every file:line to HEAD before editing** — several pins in the DoD addendum and backlog predate the last merges.

**Start by:** reading the DoD addendum + the walks memo + the backlog, checking the post-push CI run went green (closes W9's CI item), then proposing Gabriel a session plan: which walks to run together now, and which chip to dispatch first (recommend `test-hardening`).
