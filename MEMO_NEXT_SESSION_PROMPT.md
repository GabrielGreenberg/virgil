# Next-session prompt — Card-System Refactor MANAGEMENT session (Session 16)

> Paste the block below as the opening prompt of the next session. Run it in **ultracode**.
> It is a **management session**: you orchestrate the refactor via **Workflows + dispatched chips**, keeping your own context lean. You do **not** do the cross-cutting implementation inline.

---

You are the **management session** for the Virgil **card-system refactor** — a deep, multi-session, chip-by-chip overhaul. This is a **manager** role: you decompose, dispatch, gate, and merge — you do **not** write the arena implementations inline. Run as much as is appropriate off **Workflows** (review fan-outs) and **dispatched chips** (Agent tool, one arena/cluster at a time) to **preserve the longevity of the manager context**. We are in **ultracode** — author a Workflow for every substantive review/audit and lean toward exhaustive, adversarial verification; token cost is not a constraint.

**CENTRAL RULE (every chip, restated):** prefer **deep, architectural solutions over superficial patches** — diagnose the *class* of problem, unify scattered switches/lists/enums into one registry-derived SSOT, and kill analogous bugs alongside the reported one. When a "gate" framing collides with a real type/seam wrinkle, let the **consumer own the coupling** rather than forcing a half-clean fix (that's how the A2 `EntityCollections` fold got re-scoped to A3, and how A5's `findEntity` slim-type fold avoided the heavy bag). And **verify feasibility against the code before committing to a user's preferred design** — A5's "scroll-beyond-top" was Gabriel's preference but proved architecturally infeasible; the Plan agent caught it before a wasted impl cycle.

## Read first (re-pin every file:line to current HEAD — the tree moves under the refactor)
1. **`CARD-SYSTEM-REFACTOR.md`** (repo root) — the SSOT. Read the **two Session-15 Progress entries** (top: "Session 15 (cont.) — A5" then "Session 15 — A3"; the live state), the **Decisions** section (all ratifications incl. the session-15 A3 + A5 subsets), §5 Arenas, and the **Chip Ledger**.
2. **`docs/card-refactor/WAVE2-seam-sweep.md`** — §5 (Wave-3 sequencing + the contention map) and §6 (R1–R34; A6 = R15/R16/R17, A10 = R26–R29). For A6 also read **SEAM E-7** (the dead marginalia twin), **SEAM D-1** (themeKey as the marker-theme accessor), **Cluster B**. For A10-rest read **SEAM D-2/D-3** + **E-6**.
3. **`AGENTS.md`** — keystroke sanctity (non-negotiable: `window.__virgilBusStats().emitCount` flat on plain typing; card-source derivation gates on `useStructuralRevisions`, never an `editor.on('update')` counter) + the two-kinds rule.

## State (all landed — merged to local `main`, NOT pushed; `main` ~72 ahead of origin; tsc clean, 638 tests / 66 files)
- **BATCH 0 (5/5)** ✅ — A2-B1 · A10-D1 · A8 · AF-follow · A1.
- **BATCH 1 — A4 keystone** (selection ⟂ expansion) ✅ `90c050e`.
- **BATCH 2 (3/3) COMPLETE** ✅ — A9 (`f68fd79`) · A2-rest (`e39ffff`) · **A3 (`e82bfb4`)** — creation pipeline (deleted dead `selection-to-card.ts`; shared `finishCreate`) · `popCardAtAnchor`→`CardKind`+`cardPopKey` · single pristine manager (R21 render-dead delete) + registry-derived `PanelKind` bucket · lifecycle ratification (4 PERMANENT gaps as a criterion + test; R18 archive no-cascade) · `EntityCollections`→slim `EntityCollectionSlots` fold (cycle-safe `src/cards/`).
- **BATCH 3 (1/3)** — **A5 (`32e3c39`)** ✅ — omni single-cascade reflow + the unanchored-card **bin** (deleted the flow `<div>` that displaced the anchored cascade = the overwrite bug; absolute zero-flow collapsed-pill bin; `OmniItem.anchorState` across 10 builders; declared compression symmetry omni=2/docked=1). **Scroll-beyond-top proven infeasible → bin (R6, Gabriel); collapsed-pill default (Gabriel).** **Remaining: A6, A10-rest.**

## DO, in order

### 1. **A6** — marginalia gutter (the bigger structural BATCH-3 arena; do this first)
- **Pipeline collapse (SEAM E-7):** there is a ~300-line **dead `marginaliaMarkers` twin** in `EditorLayout.tsx` (the AF-fix marker migration was verified in *dead code*) — its line range drifted HEAVILY under A3 + A5, **RE-PIN against HEAD**. The deletion is now **unblocked** (A1's `menuLocation` + A3's pristine-manager deletions on `EditorLayout.tsx` already cleared; A5 didn't touch it). **R15 (gutter-click behavior) is the product call here** — port the rich Omni-first routing (`openForCard`+`cardPopKey`+`clickY` align+split-aware citation) into the live EditorPane builder, or keep the simpler current live behavior. **R15 is USER-FACING but impl-coupled** (like A5's scroll-up) → let the **A6 `Plan` agent surface the concrete options first, THEN put R15 to Gabriel** (don't pre-ask a possibly-stale framing).
- **Registry-derived marker metadata (SEAM D-1):** kill the two hand-kept marker→theme maps (`MARKER_TO_THEME_KEY` `Marginalia.tsx`, `MARKER_KIND_TO_THEME_KEY` `EditorLayout.tsx`) by deriving from `CARD_REGISTRY[k].themeKey` (A10-D1 ✓) + `markerType` (A2-B1 ✓); derive `MarkerType`/`MARKER_META`/`MarginItemKind` from the registry (`marginalia.ts`).
- **R16** (overflow affordance — Option A "+K" compress pill [rec]) — borderline UX; can self-ratify or fold into the same Gabriel ask as R15. **R17** (`defaultSide` stays marginalia-local [rec]; `panelId` derives from registry) — self-ratify.
- The note→highlight marker-strand + the A9 chevron-gating-for-paragraph-only-Mode-A-notes follow-up is A6 territory.

### 2. **A10-rest** — cross-cutting integrations
- **Collab claim-scope (SEAM D-2):** derive `collabClaimScope(kind) = CARD_REGISTRY[k].themeKey` (typed, one source docked+float) — replaces the hand-matched literals in `useCollab.ts` + the docked/float card literals. After A9's morph ✓ (a morph that flips `themeKey` must re-derive the scope, same as `remapCardPopKey` follows the key). One `CollabCardTrailing`.
- **AI-request routing (R29):** add `aiRequest?: { kind; linkPanel }` to `CARD_REGISTRY` (kills the 3-enum fan-out; `AiRequestKind` stays the external skill contract). Edits `cards/floats/index.tsx` (trailing slot — serialize after A9's title slot ✓) + `ai-request-bridge.ts` + the 5 data hooks.
- **R5 global error-expansion scoping** (A4 deferred #1, A5 re-confirmed): lift the error `expanded` set to `EditorLayout` (alongside `selectedErrorId`) so docked `ErrorsPanel.expandedIds` + omni `expandedErrorIds` share scope. R26/R28/R29 self-ratifiable technical; D-3 ErrorCard/AiRequestCard body restyle (literal-Tailwind → `theme.*`) is a visible delta — flag for Gabriel.
- A6 ∥ A10-rest are file-disjoint per seam-sweep §5 EXCEPT both may touch `EditorLayout.tsx` — serialize the `EditorLayout.tsx` edits (A6's dead-twin deletion is large; do A6 first, then A10's `MARKER_KIND_TO_THEME_KEY`/error-scoping ride the cleared file).

### 3. Final whole-refactor verification + archive the SSOT (DoD in `CARD-SYSTEM-REFACTOR.md`).

## The proven cadence (every arena)
**read-only `Plan` agent (re-pinned to HEAD) → ratify R-questions (user-facing ones to Gabriel via AskUserQuestion; verify impl-coupled ones against the code first) → impl chip (Agent, commits PER-STEP, background) → INDEPENDENT ADVERSARIAL REVIEW Workflow → fold only the load-bearing nits (folded as a final review-nit commit) → `git merge --no-ff` into `main` → update SSOT Progress + Chip Ledger + Decisions.**

**The review Workflow IS the merge gate (not chip self-verification):** a fan-out of diverse skeptic lenses (each `git diff`/`git show` read-only) → an adversarial-verify stage that tries to REFUTE each CRITICAL/HIGH → a **synthesizer that itself re-runs `tsc` + the full vitest suite + grep-gates** on the current checkout (no branch switch) → **GO / GO-WITH-NITS / NO-GO**. **Merge only on GO / GO-WITH-NITS.** It has repeatedly caught "passes tests but misses the ratified intent" (A9 typography) and dead-module stragglers (A1 snap-grid); for A3/A5 it returned GO with 0 surviving CRITICAL/HIGH and only deferrable nits.

## Hard guards (every Workflow agent + every chip)
- **Review/audit Workflow agents: NO working-tree mutation** — forbid `git checkout/switch/branch/stash/reset/restore/commit/merge/rebase/apply` + `tools/sync-defaults.sh`; inspect via `git show <ref>:<path>` / `git diff <range>`; the synthesizer runs `tsc`/`vitest` on the current checkout **without switching branches**.
- **Impl chips: commit PER-STEP** (kill-resilient — the committed prefix always survives). **Stage explicit paths, NEVER `git add -A`** (an `add -A` once leaked a temp review script into a merge). **Single-quote `git commit -m`** (backticks → shell substitution drops words). **`timeout` is NOT on this shell** — `npx tsc --noEmit > log 2>&1; echo $?`. **Each git command its own Bash call** (no `&&` chaining); prefer `git -C`.
- **Never run `tools/sync-defaults.sh`** (even `--check`) — it auto-commits a stale `personal-snapshot.json`. Eyeball `*.defaults.json` diffs by hand.
- **Keystroke sanctity** (`emitCount` flat; no new `editor.on('update')` subscriber) + **two-kinds** (touch text-objects only at the `Floatable` window layer) on every arena.
- **Commit checkpoints on the chip branch (`chip-A6`/`chip-A10`); merge via `main --no-ff`; do NOT push** (`main` is local-only, ahead of origin by design).
- **Review-script temp files:** write the Workflow script to a repo-root `.tmp-<arena>-review.mjs`, invoke via `scriptPath`, **`rm` it before the merge**. Build `CTX` via array-`join('\n')` — **no backticks inside the prompt strings** (they close the literal → parse error). The A3/A5 review scripts are a good template (8 lenses → refute pipeline → synthesizer with the grep gates).
- **Dev-preview** each arena's user-visible behavior on `virgil-data/doc_devtest`. The **backgrounded preview won't reliably surface the marginalia gutter / docked panel columns / omni column** — flag visual checks you can't drive headlessly as recommended manual walks, don't block on them.
- **SendMessage to a prior background agent is NOT available** — to continue/revise a plan, dispatch a fresh focused Plan agent (seed it with the prior plan's key anchors). Do NOT leave a Plan agent running across a session boundary (orphaned-agent anti-pattern).

## Deferred follow-ups accumulated (route into the right downstream arena / a cleanup chip; don't lose them)
- **A3:** `actOnSuggestion` is now a dead return field in `useSuggestions.ts` (prune); the **E-2 type-level structural guard is a `vitest run` no-op** — only fires under `tsc --noEmit`, which is NOT in CI → **add a typecheck step to `coherence.yml`** (this also enforces the A5 fold-stability pin); strengthen the E-3 R21 guard to exercise the `?? localPristine` fallback; `RecentlyAddedKind` parallel pin-token (`TODO(A3-followup)` in `card-creation.ts`) → reconcile onto the registry; orphaned `useExamples` + `usePristineKind`/`usePristineCardsContext` (zero consumers → a gardening chip).
- **A5:** **3 MEDIUM test-fidelity** → a test-hardening chip (the `anchor-state-classify` test exercises only 3 of 10 builders; both contract files test a HAND-COPIED `split()` mirror not OmniViewPanel's real `useMemo`; the `unanchored-bin` zero-flow guard renders the bin in isolation without asserting `panelScrollRef` parentage). **expand-on-focus** (`omni-host.tsx` `handleCardFocus` no-op) → a dedicated chip (needs the `onCardFocus` prop signature change + a `CardKind→EntityKind` crosswalk + an unratified auto-expand-on-tab UX call). **R5 error-expansion scoping** → **A10-rest** (above).
- **A9:** Examples-panel visual 15px walk (manual); hardcoded revision `kindOptions` vs derived; `bib`/`ai` → full `FloatChrome`; `borrowed-schema.ts` extraction (MEMO_BUG_BACKLOG #11).
- **A1/A8:** `chrome-config.ts` `isActionVisible`/`CALLBACK_TO_ACTION_KIND` zero-consumer (→ A8); `ActionKind` legacy `comment`/`cut` tokens (SEAM E-6 → A10).
- **A2:** the `linkedAnchor.kind` mark-attr removal (R-E / SEAM E-5 — A2 flags, A1 lands, A6 verifies colour parity); the parallel `legacyAnchorKindToCardKind` parser (inverse of `LEGACY_TOKEN_CROSSWALK`).

**Start by:** reading `CARD-SYSTEM-REFACTOR.md` (the two Session-15 entries) + seam-sweep §5/§6/E-7/D-1, re-pinning to HEAD, then dispatching a fresh **A6 `Plan` agent** (re-pin the dead-twin line range — it moved a lot under A3+A5).
