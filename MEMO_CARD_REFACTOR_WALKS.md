# Card-system refactor — manual verification walks (W1–W10)

The refactor is **code-complete and DoD-verified** (`docs/card-refactor/CARD-SYSTEM-REFACTOR.md`, addendum: DONE-PENDING-MANUAL-WALKS, zero blocking gaps). These walks are the remaining human half — the visual/interactive checks that couldn't be driven headlessly. Delete each as it passes; delete the memo when empty.

**SETUP:** refresh the dev doc — `rm -rf virgil-data/doc_devtest && cp -R samples/annotation-history virgil-data/doc_devtest` — and open the dev preview. If "No document open": `localStorage.setItem('virgil:force-dev-storage','1')` + reload.

- [ ] **W1 — Creation + selection axes** *(DoD 9; zero headless coverage of `finishCreate`)*: create a note, a footnote, and a citation from a selection — each lands **selected-but-NOT-expanded** (N1), pinned in its auto-activated panel, focus-scrolled. Then: card-body click = selects+expands · marginalia-marker click = selects WITHOUT expanding · second gutter click = uniform toggle-off (the untested A6 early-return) · omni pin lands at the marker's click-Y.
- [ ] **W2 — Clone/delete cascade** *(the only execution of `duplicate-slice`/`delete-range` post-A3)*: in one paragraph bearing footnote + citation + Mode-B note + revision-comment + cutter-comment, duplicate via drag-handle → fresh atom ids/numbers + cloned sidecar cards (editing a clone's note must NOT edit the original); then delete the duplicated range → clones' cards gone, originals untouched, no orphan `linkedAnchor` marks.
- [ ] **W3 — Float parity + policy** *(DoD 2/5/9)*: pop one card + one text-object → identical FloatChrome skeleton (grip · title · trailing · jump · X), identical drag/resize/position-restore. 3+ mixed floats: 360×280 default + 24px cascade · raise-on-click MRU · Cmd-W closes MRU-top only · edge-drag clamps · long-section lift caps ~55vh · card floats above panel floats. Pop `bib` + `ai` (bespoke `bareWindow` headers EXPECTED — drag/close/jump/clamp must still work). Drag a footnote float AND a text-object float onto the Stack — both serialize + close. Morph a popped revision-comment→suggestion — the float survives the key remap.
- [ ] **W4 — Per-kind surface pass** *(DoD 5)*: one morph pair each of note↔highlight / revision / cutter / report families across docked+omni+float (omni 2-line vs docked 1-line compression) · footnote borrowed serif 15px vs citation sans 12px · example card borrowed 15px + live LaTeX round-trip (the never-run A9 walk) · Mode-A archive/todo marker toggle · errors: docked+omni exist, NO popout button, expansion shared docked↔omni surviving reopen+recompile, code-view sidebar independent, gutter error markers expected ABSENT (pre-existing placeholder) · AiRequestCard tints + ErrorCard severity colors eyeball · the overflow "+K" pill/popover on a one-line paragraph with 3+ cards (click/delete/drag inside; click-away closes WITHOUT dropping selection).
- [ ] **W5 — Omni anchoring residue** *(A5's pending walk)*: a side with anchored + free + orphaned cards — overwrite bug gone (anchored cards align with the bin present) · collapsed pill count correct · expand reveals free+orphaned with orphan badges · pill overlays a first-paragraph card · `emitCount` flat typing with the bin expanded.
- [ ] **W6 — Print** *(A8 fast-follow, never run)*: print dialog shows 11 rows in `PRINT_PANELS` order · Reports appendix renders actual report cards · toggling a panel off removes its appendix · EXPECT appendix cards to print WITH chrome (pre-existing gap — feeds the `print-chrome` chip, not a regression).
- [ ] **W7 — Reader**: open a paper in the Library Reader — only the `READER_CHROME` panel set · note bodies editable · footnote/citation/example/bib read-only · no formatting toolbar · popout works.
- [ ] **W8 — Keystroke sanctity live** *(core already PASSED at archival: emitCount Δ0, console clean)*: optionally repeat at scale — docked panels both sides + omni + several floats, ~50 chars typed → `emitCount` flat, version advances, no margin-card flicker; adversarial: type with a texBlock float open (pre-existing chipped O(doc) walk — expect possible lag, not a refactor regression).
- [ ] **W9 — Collab / AI / release cluster**: two-client walk — claim pills across all 7 claim-bearing kinds docked+float · the first-ever revision + report presence dots · one mixed-version pass · report↔report-request morph under a live partner claim (pill survives) · AI-request round-trip on all 6 routed kinds + `editor/scripts/list_requests.py` reads the frozen tokens · Revisions theme-picker round-trip (persisted key `revision`) · ~~the new `coherence.yml` typecheck job goes green on the next push~~ **DONE 2026-06-11: push `0575076` → coherence + typecheck jobs both green (run 27365394611); deploy green**.
- [x] **W10 — Keyboard-UX sign-off**: ~~focused-but-collapsed intended?~~ **SIGNED OFF by Gabriel 2026-06-11** (recorded in the SSOT Decisions); nothing to walk.
