<!-- last-verified: ad9cb6b0 2026-07-04 -->
<!-- derives-from: docs/architecture/VIRGIL.md#ontology, docs/architecture/VIRGIL.md#code-organization -->
<!-- covers-code: src/panels/panel-registry.ts, src/components/MenuBar.tsx, src/components/EditorLayout.tsx, src/components/panel-primitives.tsx, src/components/menu/, src/components/SkillSyncControls.tsx, src/panels/_shared/card-archive-actions.tsx, src/panels/_shared/card-archive-view.tsx, src/panels/_shared/CardViewModeMenu.tsx, src/cards/predicates.ts -->

# Glossary

Fast lookup from the user's vocabulary → code names + file paths. When the user uses a term you can't resolve, search here first before exploring the codebase.

If a user term isn't yet in this file, add it under **Pending terminology** at the bottom with a best-guess referent and today's date. The cleanup skill consolidates these on the next merge cycle.

---

## Gutter vs Margin (canonical)

These two are **distinct** and must not be conflated (standardized 2026-06-16):

- **Gutter** — the area **OUTSIDE** the text pod: the left/right panel columns / the
  omni-view area where **cards** live, plus the space above/below the pod. *"Outside
  the text."* (The removed `topGutter`/`bottomGutter` pref — backlog #5 — was correctly
  named: it was outside-pod space.)
- **Margin** — the area **INSIDE** the text pod: the left/right text margins where
  **marginalia** (and the paragraph action bolt) live. *"Inside the text."*

Several names predated this standard and were **misnomers** (the
`SelectionActionsMenu` bolt sits in the paragraph's *margin*, marginalia icons sit
in the *margin*, etc.). Backlog **#51** renamed the in-text code/comment usages of
"gutter" → "margin": the `--margin-*` chrome var family (grab-handle / fold-chevron
placement), the marginalia margin markers (`<MarginColumn>`, `data-marginalia-margin`,
the `margin-marker` builder), the `RESTING_MARGIN_TRIGGER_Z` bolt tier, and the
"margin pin" re-anchor. "Gutter" now names **only** outside-pod surfaces: the
left/right panel columns, the omni-view area, dock slots, and the code-view gutter
(`CODE_VIEW_GUTTER_PX`).

---

## Structural UI

| User term | Code name(s) | Where |
|---|---|---|
| **Virgil bar** / **top bar** (horizontal strip across the top, containing tabs + the `+` tab-plus trigger and the right-side button cluster; VIRGIL wordmark dropped in 53d2e39 so the `+` button now leads the strip) | `TopBar` (extracted from EditorLayout + memoized so background ticks don't repaint it; renders the `virgil-bar` div = `TabStrip` (left tab group) + `StatusCluster` (right button cluster)) | [src/components/editor-layout/TopBar.tsx](../../src/components/editor-layout/TopBar.tsx); [src/components/editor-layout/TabStrip.tsx](../../src/components/editor-layout/TabStrip.tsx); [src/components/editor-layout/StatusCluster.tsx](../../src/components/editor-layout/StatusCluster.tsx); fill styled by `--topbar-bg` / `--topbar-bg-bottom` in [src/app/globals.css](../../src/app/globals.css) |
| **Upper tool strip** (the opaque manilla band directly under the Virgil bar, pinned at viewport top so document content scrolls under it; today it holds the text tool bar in the center plus the icon strips at the outer edges — the old left/right action-button segments were removed with `MarginActionToolbar` in bcc583a) | Center segment `[data-tool-strip="text"]` (the `MenuBar`); the `[data-tool-strip="<side>-action-<edge>"]` attrs now mark the decorative `ColumnEdgeFade` gradients (read existence-only by `dock-drag.ts`) | Text segment in [EditorPane.tsx](../../src/components/EditorPane.tsx) ~line 5263; fades in [panel-column.tsx](../../src/components/editor-layout/panel-column.tsx) |
| **Text tool bar** (center segment of the upper tool strip, sitting above the editor; right-aligned 24px-tall row of paragraph-nav arrows, split toggle, and the View menu kebab) | `MenuBar` (code name refers to the pod, not the strip) | [src/components/MenuBar.tsx](../../src/components/MenuBar.tsx) (default export); mounted **inline** as the sticky `[data-tool-strip="text"]` in [src/components/EditorPane.tsx](../../src/components/EditorPane.tsx) ~line 5263. ae15791 slimmed the bar to 24px and dropped the home Format/Actions popups; the actions/formatting vocabulary now lives in `ActionsMenuPanel` (shared dropdown body) triggered from `SelectionActionsMenu` (margin, contextual to a selection) or `DragHandleMenu`. (The redundant strip `ActionsStripButton` lightning-bolt was removed as backlog #6.) |
| **Left action buttons** / **right action buttons** (the old per-column "Add footnote / citation / …" icon rows above each side panel) | **Removed.** Were `MarginActionToolbar` — suppressed in 652572a, deleted in bcc583a. The add-action vocabulary now lives in `SelectionActionsMenu` / `DragHandleMenu` | — |
| **Menu pod** / **menu bar** / **main toolbar** / **menu toolbar** / **floating toolbar** | Same as **Text tool bar** above | Same |
| **Left tool strip** / **left icon strip** / **sidebar navigation** (left) | Inline `<div data-strip-side="left">` — no dedicated component | Rendered by `PaneRail` in `EditorPane.tsx` ~line 6242 |
| **Right tool strip** / **right icon strip** / **sidebar navigation** (right) | Inline `<div data-strip-side="right">` | Same `PaneRail` (right-side instance) |
| **Filter menu** / **kebab at the bottom of the strip** (horizontal 3-dot menu pinned to the bottom of each L/R icon strip; toggles which omni categories show, plus a "Default view" reset) | `OmniFilterMenu` | [src/panels/Omni/OmniViewPanel.tsx](../../src/panels/Omni/OmniViewPanel.tsx); mounted at the bottom of each `PaneRail` strip in `EditorPane.tsx` |
| **Dock outline** / **drop outline** (thin clear-blue outline with static glow that marks the active dock target during a panel drag; persists at the originally captured rect even after the panel undocks) | `DockOutline` (body-portaled, fixed positioning) consuming the module-level `useDockDragTarget` signal in `dock-drag.ts` (`setDockDragTarget` / `getDockDragTarget`) | [src/components/editor-layout/DockOutline.tsx](../../src/components/editor-layout/DockOutline.tsx); [src/components/editor-layout/dock-drag.ts](../../src/components/editor-layout/dock-drag.ts); mounted in `EditorPane.tsx` ~line 4519 |
| **Reading frame** / **viewport-locked reading rectangle** / **letterbox masks** (the visible band between the top and bottom margins, fixed to the viewport; two persistent sticky bands above and below it (`var(--surface)` with an 8px hard fade at the inner edge) mask content scrolling past the rectangle so the editor has a true visible reading frame, not just guide lines. Bottom band is gated on `ready` to avoid showing during initial load) | Inline styled regions in `EditorPane.tsx` driven by `--editor-pt` / `--editor-pb` (column-wrapper CSS vars) and `--scroll-viewport-h` (published by `EditorScrollbar` as the row's `clientHeight`, distinct from `--row-bound-h` = scrollHeight) | [src/components/EditorPane.tsx](../../src/components/EditorPane.tsx) ~lines 5018, 5072, 5220, 5906 |
| **Margin edit mode** / **margin handles** (the editable guide lines and Save/Cancel pill that appear when the user enters margin-edit mode; one drag handler covers all four sides via axis-lookup tables; a sticky glowing-blue X / check icon pill replaces the old text Save/Cancel buttons) | `useMarginEdit` (state machine, keyed on `Margins = Record<MarginSide, number>` with `MARGIN_AXIS` / `MARGIN_OPPOSITE` / `MARGIN_MIN` / `MARGIN_CSS_VAR` lookup tables) | [src/hooks/useMarginEdit.ts](../../src/hooks/useMarginEdit.ts); consumed in `EditorPane.tsx` ~line 3356 |
| **Panel button** / **strip button** (individual icon in a strip) | `StripButton` | [src/components/editor-layout/drag-drop.tsx](../../src/components/editor-layout/drag-drop.tsx) |
| **Panel column** / **sidebar column** (the resizable column on either side) | `PanelColumn` | [src/components/editor-layout/panel-column.tsx](../../src/components/editor-layout/panel-column.tsx) |
| **Current section pod** / **section lozenge** (floating section-path pill at top of editor, shows on scroll, fades after idle) | `SectionLozenge` | [src/components/editor-layout/section-lozenge.tsx](../../src/components/editor-layout/section-lozenge.tsx); `SectionPathEntry` type + main/mirror path builders in `EditorLayout.tsx` ~2107 / ~2220 |
| **Navigation strip** | No dedicated component — paragraph-nav chevrons live inside `MenuBar` | `MenuBar.tsx` around line 866 (in `MenuBarContent`) |
| **Document folder tab** / **manila tab** (the document tabs in the topbar with rounded top + swoop hooks at the bottom) | `DocumentFolderTab` (one self-contained SVG path per tab); path geometry in `folder-path.ts` (`buildActiveTabStrokePath`, `buildTabFillPath`) | [src/components/editor-layout/DocumentFolderTab.tsx](../../src/components/editor-layout/DocumentFolderTab.tsx); [src/components/editor-layout/folder-path.ts](../../src/components/editor-layout/folder-path.ts) |

## Toolbars inside the Virgil bar

| User term | Code name(s) | Where |
|---|---|---|
| **Selection actions menu** / **lightning-bolt button** (small yellow lightning-bolt anchored in the right margin of the current paragraph; click to expand `ActionsMenuPanel` in place. After 1bd614c this is a click-to-expand collapse-button, not the original auto-popping menu; works in cursor-only mode too, with Highlight greyed out when there's no live range) | `SelectionActionsMenu` (the margin button + open-state) → `ActionsMenuPanel` (the inline-formatting grid + vertical action list). Both this menu and `DragHandleMenu` now RENDER FROM the action registry via `cardActionRows()` — the SSOT `VIRGIL_ACTION_REGISTRY` replaced the deleted `MENU_ENTRIES` array. Dispatch through `DragHandleMenuApi.dispatch` | [src/components/SelectionActionsMenu.tsx](../../src/components/SelectionActionsMenu.tsx); [src/components/ActionsMenuPanel.tsx](../../src/components/ActionsMenuPanel.tsx); registry [src/lib/actions/action-registry.ts](../../src/lib/actions/action-registry.ts); PM→React dispatch [src/lib/actions/editor-actions-bridge.ts](../../src/lib/actions/editor-actions-bridge.ts); paired with [src/components/SelectionColorPopover.tsx](../../src/components/SelectionColorPopover.tsx); mounted from `Editor.tsx` |
| **Strip action button** / **stable lightning-bolt** (was a sibling of SelectionActionsMenu in the MenuBar's para-nav group) | **Removed** (backlog #6) — was `ActionsStripButton`, redundant with the margin `SelectionActionsMenu`. The action menu is now reached only from the margin ⚡ and the `DragHandleMenu` | — |
| **Formatting toolbar** / **format popup** (no longer in the docked MenuBar; vocabulary moved into `SelectionActionsMenu` and `DragHandleMenu`) | **Removed.** `FormatButtonsRow` / `DetachedFormattingToolbar` no longer exist — the formatting grid is now driven by the action registry inside `ActionsMenuPanel` | — |
| **Action toolbar** / **actions popup** (no longer in the docked MenuBar; vocabulary moved into `SelectionActionsMenu` and `DragHandleMenu`) | **Removed.** `ActionButtonsRow` / `ACTION_BUTTON_DEFS` are gone — every action now lives in the SSOT `VIRGIL_ACTION_REGISTRY` | [src/lib/actions/action-registry.ts](../../src/lib/actions/action-registry.ts) |
| **Detached actions toolbar** (legacy floating multi-instance toolbar) | **Removed.** `DetachedActionsToolbar` and the `detachedActions[]` state no longer exist | — |
| **Margin action toolbar** | **Removed** (was `MarginActionToolbar`, deleted in bcc583a) — see **Left action buttons** above | — |
| **View menu** / **three-dot menu** (on Virgil bar; now built on the `<Menu>` primitive — arrow-key nav + a roving "selection square" highlight (`--menu-roving-bg`). Gained Left/Right expand-collapse on its three expandable groups + Left/Right color swatch cycling, 8b6fe7a) | `ViewMenu` | `MenuBar.tsx:535` |
| **Block-type dropdown** (body/chapter/section/subsection; its heading items now route through the canonical `headingRun` SET path in `VIRGIL_ACTION_REGISTRY` — always SETs, never toggles back to paragraph — CHIP 5a; migrated onto the `<Menu>` primitive in Phase C, 8047278) | `BlockTypeDropdown` | `MenuBar.tsx:300` |
| **Style mode button** / **document style** / **Manage Styles modal** (Style mode toggle on the Virgil bar that opens a full styles-library modal — apply/edit/duplicate/delete style entries, save current preamble as a new entry, pick default for new docs; the old inline `DocStyleDropdown` was folded in by `9744b71`) | `ManageStylesModal`; per-doc state in `useDocumentStyle`; user-library state in `useStyleLibrary`; presets in `DOCUMENT_STYLES`; the on-apply diff prompt is `StyleApplyDialog` | [src/components/ManageStylesModal.tsx](../../src/components/ManageStylesModal.tsx); [src/components/StyleApplyDialog.tsx](../../src/components/StyleApplyDialog.tsx); [src/lib/document-styles.ts](../../src/lib/document-styles.ts); [src/hooks/useDocumentStyle.ts](../../src/hooks/useDocumentStyle.ts); [src/hooks/useStyleLibrary.ts](../../src/hooks/useStyleLibrary.ts); mode-toggle button now in [StatusCluster.tsx](../../src/components/editor-layout/StatusCluster.tsx) (the extracted right button cluster); modal mount + open-state (`manageStylesOpen`) in `EditorLayout.tsx` ~line 4069 |
| **Print button** / **print dialog** (printer icon on the Virgil bar; opens a dialog with show/hide toggles for paragraph titles, marginalia, footnotes, citations, comments, etc., then triggers `window.print()`) | `PrintDialog` + `PrintAppendices`; print orchestration in `lib/print.ts`; pref state via `useViewPrefs` | [src/components/PrintDialog.tsx](../../src/components/PrintDialog.tsx); [src/components/PrintAppendices.tsx](../../src/components/PrintAppendices.tsx); [src/lib/print.ts](../../src/lib/print.ts) |
| **Help button** / **"?" button** (circle-question-mark icon on the Virgil bar; opens a dropdown with Helper mode toggle) | Rendered inline in the extracted `StatusCluster` (the right button cluster); `helperBtnRef` + open-state still owned by `EditorLayout.tsx` | [src/components/editor-layout/StatusCluster.tsx](../../src/components/editor-layout/StatusCluster.tsx) (~line 282); `EditorLayout.tsx` |
| **Helper mode** / **helper mode indicator** (when active, hovering any button instantly shows the hint bubble describing it; indicator in Virgil bar styled like Focus View) | `useHelperMode` hook; body attr `data-helper-mode="on"` drops the `HintLayer` hover delay to 0 (the old CSS `::after` callout was replaced by the JS hint bubble — see **Hint** below) | [src/hooks/useHelperMode.ts](../../src/hooks/useHelperMode.ts); [src/components/HintLayer.tsx](../../src/components/HintLayer.tsx) |
| **Highlights menu** / **per-kind highlight toggles** (sub-menu in the View menu that hides linked-anchor highlights for individual card kinds) | `HighlightType` union (`note`, `todo`, `comment`, `cut`, `report`); `hiddenHighlightTypes` pref in `useViewPrefs`; toggles rendered inside `ViewMenu` | [src/hooks/useViewPrefs.ts](../../src/hooks/useViewPrefs.ts); `MenuBar.tsx` ViewMenu section |
| **Back / forward buttons** (paragraph nav) | Chevron pair, inline in MenuBar | `MenuBar.tsx`, inline in `MenuBarContent` (~line 866) |
| **Split screen toggle** | Inline button in MenuBar | `MenuBar.tsx` |
| **Close all panels** (X button) | Inline button in MenuBar | `MenuBar.tsx` |
| **Grab handle** / **rotation knob** / **dock-up button** (the old tear-out-pod chrome — drag the Virgil bar out, rotate it horizontal/vertical, re-dock it) | **Removed.** The Virgil bar / `MenuBar` is now docked-only ("No grab handle, no tear-off" — see `MenuBar.tsx`); `PodGrabHandle` was retired with the detached-toolbar removal (it no longer exists; `floating-toolbar-shell.tsx` now exports only the `ToolbarOrientation` type) | — |

## Panels (what "panel" means)

All panels declared in [src/panels/panel-registry.ts](../../src/panels/panel-registry.ts) (`PANEL_REGISTRY` is SSOT). 11 card panels, 4 non-card panels.

### Card panels (have cards anchored in the document)

| Panel kind | Label | Card kind | Default strip | Theme |
|---|---|---|---|---|
| `notes` | Notes | `note` + `highlight` (polymorphic — both kinds coexist in `NotesState.cards`; selection-drop on the panel defaults to a highlight, the toolbar/header "+" creates a note) | right | emerald (highlight reuses the yellow `--accent` warm-yellow swatch via panel-theme `highlight` key) |
| `footnotes` | Footnotes | `footnote` | left | red |
| `citations` | Citations | `citation` | left | warm yellow |
| `bibliography` | Bibliography | `bib` | left | warm tan |
| `reports` | Reports | `report` + `report-request` (polymorphic — `card: null` in registry; membership derives from `CardMeta.panel` in `CARD_REGISTRY`). Report = authored content card with an `author`/timestamp byline (AI or human); Report Request = the user's "ask" with an `aiRequest` flag. Free-form rich-text body like Notes (sans-serif). | left | indigo |
| `examples` | Examples | `example` | left | (default) |
| `todo` | Todo List | `todo` | right | stone |
| `archive` | Archived Text | `archive` | right | amber/blue-grey |
| `revisions` | Revisions | `revision-comment` (key prefix `revision`) + `revision-suggestion` (polymorphic — registry `card.kind` is `revision-comment`; both kinds resolve to this panel via `CardMeta.panel`, registry-derived since 27458d8). Comment cards use `EditableCard` chrome (rich-text body, AI-request checkbox in footer); each card carries a **kind chevron** (the morph dropdown — distinct from the retired expand chevron) that converts in-place between comment ↔ revision-suggestion via `convertCard`. `CARD_TYPE_LABELS` labels `revision-suggestion` "Revision" (3fa5996). NOTE: the spine `CardKind` renamed `comment`→`revision-comment` (27458d8), but `RevisionCard.kind` / `revisions.json` / the Python layer still use `comment`/`suggestion`, bridged at float dispatch | right (omni-eligible) | stone |
| `cutter` | Cutter | `cutter-comment` + `cutter-suggestion` (polymorphic — `card: null` in registry). `cutter-comment` uses `EditableCard` chrome (rich-text body, registry-derived collab/morph, shared `suggestion-fields.tsx` module) — same structure as `revision-comment` (migrated in backlog #35). | right (omni-eligible) | red |
| `errors` | Errors | `error` | right (omni-eligible) | light red |

### Non-card panels

| Panel kind | Label | Default strip | Purpose |
|---|---|---|---|
| `outline` | Outline | left | Heading tree with edit/focus/lock |
| `search` | Search | left | Full-text search |
| `wordcount` | Word Count | right | Live word counts by section |
| `omni` | Omni-view | — | Two-column view aggregating all omni-eligible panels |

Each panel lives in `src/panels/<PanelFolder>/`.

**"N outside focus" bin** (user term) — when Focus View is active, cards anchored OUTSIDE the focused band have a hidden in-text anchor and can't cascade inline; rather than drop them, the Omni view collects them in a collapsed count pill (`N outside focus`, expandable to a bounded list). Code: `OmniOutsideFocusBin` in [src/panels/Omni/OmniViewPanel.tsx](../../src/panels/Omni/OmniViewPanel.tsx); items are stamped `outsideFocus?: boolean` on `OmniItem` ([src/panels/_shared/types.ts](../../src/panels/_shared/types.ts)) by the focus filter in [src/components/editor-layout/panels/omni-host.tsx](../../src/components/editor-layout/panels/omni-host.tsx).

## Main text elements

| User term | Code name(s) | Where |
|---|---|---|
| **Main text** | The TipTap editor document | [src/components/Editor.tsx](../../src/components/Editor.tsx) |
| **Heading** | TipTap `heading` node (levels 0–6 = Part / Chapter / Section / Subsection / Subsubsection / Paragraph / Subparagraph; declared in [src/lib/heading-types.ts](../../src/lib/heading-types.ts)) | TipTap schema; block-type dropdown at `MenuBar.tsx` ~line 300; per-heading lozenge picker at `HeadingTypeMenu.tsx` |
| **Heading lozenge** / **heading control strip** (the floating strip above each heading: type-dropdown chip + numbered (#) toggle + label slot + × delete; f59756b replaced the old redundant-section-number lozenge) | `HeadingTypeMenu` for the type dropdown; lozenge controls inline in `Editor.tsx` heading node-view | [src/components/HeadingTypeMenu.tsx](../../src/components/HeadingTypeMenu.tsx); heading-level vocabulary in [src/lib/heading-types.ts](../../src/lib/heading-types.ts) |
| **Paragraph** | TipTap `paragraph` node (carries `uuid` attr) | See `main-text.md` |
| **Paragraph title** | Stored in `virgil.json` sidecar (`ParagraphMeta.title`), **not** a Tiptap attr | Loaded in [src/hooks/useDocument.ts](../../src/hooks/useDocument.ts); shown in Omni view + search breadcrumbs |
| **Marginalia** | Icons in the left/right margin of main text (INSIDE the text pod — see *Gutter vs Margin*), anchored to paragraphs | [src/components/Marginalia.tsx](../../src/components/Marginalia.tsx); metadata in [src/lib/marginalia.ts](../../src/lib/marginalia.ts); grid math in [src/lib/marginalia-grid.ts](../../src/lib/marginalia-grid.ts) |
| **Linked text** | Text carrying a `linkedAnchor` mark (Mode B) — connects to a card | [src/lib/tiptap/linked-anchor.ts](../../src/lib/tiptap/linked-anchor.ts) |
| **Footnote** (in text) | TipTap atom node `footnote` | [src/lib/tiptap/footnote.ts](../../src/lib/tiptap/footnote.ts) |
| **Citation** (in text) | TipTap atom node `citation` | [src/lib/tiptap/citation.ts](../../src/lib/tiptap/citation.ts) |
| **Inline math** | TipTap atom node `inlineMath` (`$…$`) | [src/lib/tiptap/math.ts](../../src/lib/tiptap/math.ts) |
| **Display math** | TipTap atom node `displayMath` (`$$…$$`) | Same file. Both inline + display math now render via KaTeX with a click-to-edit popover (ef8a8ce) |
| **TeX block** / **raw LaTeX block** (block-level passthrough; CodeMirror-edited pod with syntax highlighting; round-trips wrapped in `%!vtex:begin/end <uuid>` LaTeX comment sentinels so the compiler treats the contents as real LaTeX) | TipTap node `texBlock` (atom block, `selectable:false`, draggable, popoutable like ExampleBlock) | [src/lib/tiptap/tex-block.ts](../../src/lib/tiptap/tex-block.ts); node-view [src/components/TexBlockNodeView.tsx](../../src/components/TexBlockNodeView.tsx) |
| **Figure block** (`\begin{figure}…\end{figure}` env rendered as an embedded image pod with structured attrs: `extras` (env body sans caption/label) / `source[]` / `widthPercent` / `label`; caption lives in a `figureCaption` child node `content: "inline*"`, with a bold "Figure N:" prefix and numbering via the `sectionNumbers` plugin; a blue label lozenge under the caption mirrors the heading annotation — Figure chip, # numbered toggle, label slot with conflict detection, hover-revealed delete) | TipTap node `figureBlock` (`content: "figureCaption?"`); caption inline-editable; click outside the caption opens `FigurePopover` for raw-LaTeX editing of `extras` | [src/lib/tiptap/figure-block.ts](../../src/lib/tiptap/figure-block.ts); [src/lib/tiptap/figure-caption.tsx](../../src/lib/tiptap/figure-caption.tsx); node-view [src/components/FigureBlockNodeView.tsx](../../src/components/FigureBlockNodeView.tsx); popover [src/components/FigurePopover.tsx](../../src/components/FigurePopover.tsx); attr parsing [src/lib/figures/parse-attrs.ts](../../src/lib/figures/parse-attrs.ts); file picker [src/lib/figures/pick-file.ts](../../src/lib/figures/pick-file.ts) (backend-agnostic — works in both FSA and dev backends) |
| **Graphics block** (bare `\includegraphics[...]{source}` outside a `figure` env — same renderer as a figure, just no caption or label) | TipTap node `graphicsBlock` (atom block); shares `FigurePopover` and the figure raster cache | [src/lib/tiptap/graphics-block.ts](../../src/lib/tiptap/graphics-block.ts) |
| **Figure popover** (tex-mode editor that opens on click of a figure / graphics block; lets the user edit the raw LaTeX in `extras`, with the renderer re-rasterizing on save) | `FigurePopover` | [src/components/FigurePopover.tsx](../../src/components/FigurePopover.tsx); save handlers in `EditorLayout.tsx` (~lines 2752 / 2792) route through `extractFigureAttrs` / `extractGraphicsAttrs` |
| **Figure raster cache** (`<paper>/virgil/figures-cache/<sha>.webp`) — backend-agnostic surface for caching rasterized figures; PDF sources rasterize on-the-fly via `pdfjs-dist`, PNG/JPEG sources are stored as-is | `readFigureSource` / `readFigureRaster` / `writeFigureRaster` / `deleteFigureRaster` / `readFigureIndex` / `writeFigureIndex` on the storage backend; client coordination via `useResolvedFigureUrl` | [src/lib/storage-fsa.ts](../../src/lib/storage-fsa.ts) + [src/lib/storage-dev.ts](../../src/lib/storage-dev.ts); [src/hooks/useResolvedFigureUrl.ts](../../src/hooks/useResolvedFigureUrl.ts); rasterizer [src/lib/figures/rasterize.ts](../../src/lib/figures/rasterize.ts) |
| **Slash command popup** (typing `\` in prose opens an inline popup of Virgil's `VIRGIL_COMMANDS` list; arrow-keys + enter inserts the command. Suppressed inside an open `{` group or against another `\name`) | `SlashPopupExtension` (PM plugin) + module-level `slashPopupStore` rendered by the floating popup component | [src/lib/tiptap/slash-popup.ts](../../src/lib/tiptap/slash-popup.ts); state in [src/lib/slash-popup-store.ts](../../src/lib/slash-popup-store.ts); commands declared in [src/lib/tiptap/commands.ts](../../src/lib/tiptap/commands.ts) |
| **LaTeX comment** | TipTap node `latexComment` (`%…`) | [src/lib/tiptap/latex-comment.ts](../../src/lib/tiptap/latex-comment.ts) |
| **Label** | TipTap mark `label` (`\label{ref}`); `LabelRef` node for `\ref{}` / `\getref{}` / `\getfullref{}` with `refCommand` + `targetKind` attrs | [src/lib/tiptap/label.ts](../../src/lib/tiptap/label.ts) |
| **Title field** | TipTap node `titleField` for hoisted `\title{}`, `\author{}`, `\date{}` from preamble | [src/lib/tiptap/title.ts](../../src/lib/tiptap/title.ts) |
| **Example block** / **treatment** (expex `\ex`/`\pex`) | TipTap node `exampleBlock`; sub-items wrapped in `exampleItemList` (recursive — nested xlist tiers reuse the same wrapper for 1 → a → i → A → I marker cycle); each item is `exampleItem`; glosses nest as `exampleGloss` → `alignedGlossRow`/`proseGlossRow` → `glossCell` | [src/lib/tiptap/expex.ts](../../src/lib/tiptap/expex.ts) |

## Cards

| User term | Code name(s) | Where |
|---|---|---|
| **Card** | `PanelCard` (universal wrapper) or `EditableCard` (rich-text variant) | [src/components/panel-primitives.tsx](../../src/components/panel-primitives.tsx) |
| **Highlight card** (Adobe-style yellow-tint card in the Notes panel; wraps a text range with a persistent `tintColor` mark attr, no rich-text body. Selection-drop on Notes creates one of these by default; the toolbar's highlighter-pen button does the same from a live selection. converting a highlight to a Note (and back) is the bidirectional kind-chevron morph — both directions are lossy, so a confirm guards either flip) | `HighlightCard` (component); `HighlightCard` (data type with `kind: "highlight"`); polymorphic siblings of `UserNote` in `NotesState.cards`; morph via `convertCard` (registry `morph:` pair); `deleteHighlightOrNote` on the `cardCreation` context | [src/panels/Notes/HighlightCard.tsx](../../src/panels/Notes/HighlightCard.tsx); `HighlightCard` type in [src/lib/types.ts](../../src/lib/types.ts); creation in [src/components/editor-layout/card-actions/card-creation.ts](../../src/components/editor-layout/card-actions/card-creation.ts) |
| **Card theme** | `CARD_THEMES` — `Record<PanelThemeKey, CardTheme>`, a mechanical fold over `DEFAULT_PANEL_COLORS` (13 keys: citation, bib, footnote, note, highlight, archive, todo, cut, revision, report, example, aiRequest, error — the legacy `comment` alias is gone) | Same file; keys in [src/lib/panel-theme.ts](../../src/lib/panel-theme.ts) |
| **AI request card** | **Retired (#55b).** The legacy `"ai"` `CardKind` / `AiRequestCard` component no longer exists — UNLINKED `ai-requests.json` entries no longer render as a dedicated card. Note/todo requests migrate to real cards carrying the per-card `aiRequest` flag (migration in `useAiRequestCardMigration` / `migrate-ai-request-cards.ts`); footnote/citation requests stay in the AIWindow | [src/hooks/useAiRequestCardMigration.ts](../../src/hooks/useAiRequestCardMigration.ts); [src/lib/migrate-ai-request-cards.ts](../../src/lib/migrate-ai-request-cards.ts) |
| **Orphaned card** (no anchor in document) | `BadgeOrphaned` + disabled `CardTargetIcon` | Same file |
| **Popped-out card** / **floating card** | `FloatHost` resolves each key in `prefs.poppedOutCards` to a `Floatable` (`CARD_REGISTRY[kind].toFloatable`) and mounts a `FloatWindow` with the unified `FloatChrome` header; card floats render on a white `card` surface with a kind-tinted header strip | [src/floats/](../../src/floats/) (FloatHost.tsx, FloatWindow.tsx, FloatChrome.tsx); [src/components/FloatingPanel.tsx](../../src/components/FloatingPanel.tsx) |
| **Floating panel** | `FloatingPanel` (portal, drag + resize) | [src/components/FloatingPanel.tsx](../../src/components/FloatingPanel.tsx) |
| **Quick card** (compact bib-entry card with chip and target icon; pops out via header drag-lift like every docked card. Header restacked into three layers — text → libraries → status — by the bib-card refactor: a `✓ Authenticated` chip on its own status layer (relabelled from the old AUTH/Verified chip — "verified" retired, F#2), the library-processing tier (Bib only / Indexed PDF / Deep-indexed PDF, derived from catalog `indexed.state`), and a shared **Open** entry link) | `BibEntryCard`; status/tier via `LibraryStatusRow` + `LibraryIndexTier` (`library-entry-status.tsx`) | [src/components/BibEntryCard.tsx](../../src/components/BibEntryCard.tsx); [src/components/library/library-entry-status.tsx](../../src/components/library/library-entry-status.tsx) |
| **Open entry link** / **Open entry** (the shared "Open" affordance on bib + citation cards that opens the entry's paper as a new Virgil-bar tab via `openPaperTab` → `PaperOuterView`; reusable primitive `<OpenEntryLink>` + `useOpenLibraryEntry`, routed through the library bridge by a `target: "tab"` discriminator) | `OpenEntryLink` / `useOpenLibraryEntry` / `dispatchOpenLibrary` (`OPEN_LIBRARY_EVENT`, `OpenLibraryTarget = "tab" \| "library"`) | [src/components/library/open-library-entry.tsx](../../src/components/library/open-library-entry.tsx); bridge in [src/components/editor-layout/event-bridges/library.ts](../../src/components/editor-layout/event-bridges/library.ts) |
| **Paragraph float** (popped-out single paragraph with editable title + drag handle) | `ParagraphBody` body component under the unified `FloatWindow`/`FloatChrome` (via `textObjectFloatable`); popout key `textobject:paragraph:${uuid}` in `prefs.poppedOutCards` | [src/text-objects/floats/paragraph-body.tsx](../../src/text-objects/floats/paragraph-body.tsx); [src/text-objects/text-object-floatable.tsx](../../src/text-objects/text-object-floatable.tsx) |
| **Heading float** (popped-out heading + its body section) | `HeadingBody`; popout key `textobject:heading:${uuid}`; section-body extraction in `section-range.ts` | [src/text-objects/floats/heading-body.tsx](../../src/text-objects/floats/heading-body.tsx); [src/lib/section-range.ts](../../src/lib/section-range.ts) |
| **List float** (popped-out bullet / ordered list) | `ListBody`; popout key `textobject:bulletList:${uuid}` or `textobject:orderedList:${uuid}` | [src/text-objects/floats/list-body.tsx](../../src/text-objects/floats/list-body.tsx) |
| **TeX-block float** (popped-out `texBlock` with CodeMirror sync) | `TexBlockBody`; popout key `textobject:texBlock:${uuid}`; hand-rolled CodeMirror-to-main sync stays per-kind | [src/text-objects/floats/tex-block-body.tsx](../../src/text-objects/floats/tex-block-body.tsx) |
| **Example float** (popped-out example block from the editor margin) | `ExampleBlockBody`; popout key `textobject:exampleBlock:${uuid}` (NOTE: distinct from the Examples panel's `ExampleCard` popout, which uses the legacy `example:${uuid}` prefix — both still render correctly per the F-era disambiguation) | [src/text-objects/floats/example-block-body.tsx](../../src/text-objects/floats/example-block-body.tsx) |
| **Selection float** / **linked-range float** (popped-out text range carrying a `linkedAnchor` mark) | `LinkedRangeBody`; popout key `textobject:linkedRange:${anchorId}`. Replaces the session-only SelectionFloat retired in Phase E — the range now persists via the mark + `\vlid{}/\vlidend{}` LaTeX markers and survives reload | [src/text-objects/floats/linked-range-body.tsx](../../src/text-objects/floats/linked-range-body.tsx) |

## General buttons

| User term | Code name(s) | Where |
|---|---|---|
| **Pop-up button** / **popout button** | `PopoutButton` (generic), `CardPopoutButton` (popped card's re-dock X only — docked cards pop via header drag-lift, no button since 2026-06-11), `PanelPopout` (panel level, context-aware) | `panel-primitives.tsx` |
| **Jump-to button** (page-with-arrow icon, jumps from card to in-text anchor) | `CardTargetIcon` / `TargetIcon` | `panel-primitives.tsx` |
| **"Can I request" button** / **request button** / **AI-request button** (8-ray star in panel headers) | `onAiRequest` callback rendered inline in `PanelHeader`; icon is the 8-ray sun-star (never a 5-point star) | `panel-primitives.tsx` (`PanelHeader`); icon spec in [src/STYLE_GUIDE.md](../../src/STYLE_GUIDE.md) |
| **Add button** (+ in panel header) | `onAdd` callback rendered inline in `PanelHeader` | `panel-primitives.tsx` |
| **Delete / trash button** (bottom-right of card, hover-reveal) | `CardTrashButton` | `panel-primitives.tsx` |
| **Archive button** (set-aside sibling of the trash button — bottom-right of card, just left of the trash, hover-reveal; toggles to "Unarchive" when the card is already archived. Distinct from the text-object Archive PANEL — this is per-card archived state. Renders iff `isArchivable(kind)` AND a provider is mounted; gated identically to the trash button) | `CardArchiveButton` (overlay) / `MenuArchive` (the three-dot-menu twin for cards whose delete lives in the header menu); self-wired via `useCardArchiveActions` (the `CardArchiveActionsProvider` context); `isArchivable` / `archiveRemovesAtom` predicates (only highlight is NOT archivable, delete-only; footnote joined the archivable set in bug sweep #3). Atom-bearing kinds (citation + footnote) splice the inline atom out behind a confirm | `panel-primitives.tsx` (`CardArchiveButton` ~line 1710, `MenuArchive` ~line 2745); [src/panels/_shared/card-archive-actions.tsx](../../src/panels/_shared/card-archive-actions.tsx); predicates [src/cards/predicates.ts](../../src/cards/predicates.ts) |
| **View Active / Archives / All menu** (three-item radio group injected into each card panel's three-dot menu that switches whether the panel shows active cards, archived cards, or all; archived cards otherwise hide from the active panel, omni, and the margin) | `CardViewModeMenuItems`; `CardArchiveView = "active" \| "archived" \| "all"` pref (per-`PanelId`, on `useViewPrefs` via `setCardArchiveView`); list filter `filterByArchiveView` consumed in `CardListPanel` via the `CardArchiveViewProvider` / `useCardArchiveView` context | [src/panels/_shared/CardViewModeMenu.tsx](../../src/panels/_shared/CardViewModeMenu.tsx); [src/panels/_shared/card-archive-view.tsx](../../src/panels/_shared/card-archive-view.tsx); pref in [src/hooks/useViewPrefs.ts](../../src/hooks/useViewPrefs.ts); provider wired in `EditorPane.tsx` ~line 4463 |
| **Menu delete item** (in three-dot menu) | `MenuDelete` | `panel-primitives.tsx` |
| **Three-dot menu** | `ItemMenu` (~line 2562 in `panel-primitives.tsx`). Auto-injects a text-size stepper (`PanelTextSizeRow`) at ~line 2593; size persists via `useViewPrefs` keyed by panel kind | `panel-primitives.tsx`; [src/components/PanelTextSizeRow.tsx](../../src/components/PanelTextSizeRow.tsx); sizes via [src/lib/panel-typography.ts](../../src/lib/panel-typography.ts); per-kind context [src/components/panel-kind-context.tsx](../../src/components/panel-kind-context.tsx) |
| **`<Menu>` primitive** / **menu arrow-key nav** / **roving "selection square"** (the unified primitive giving every action menu arrow-key navigation, nested-key ownership, horizontal lists, and a roving highlight square via the `--menu-roving-bg` CSS token; a container `mousedown` `preventDefault` keeps a menu click from blurring the editor. Migrated onto it (Phases A–C): the grab-bar/drag-handle menu, the lightning panel (`ActionsMenuPanel`), `SelectionColorPopover`, `LabelRefPopover`, `HeadingTypeMenu`, `TabPlusMenu`, `BibEntryPickerMenu` combobox path, and MenuBar's `BlockTypeDropdown` + `ViewMenu`. The slash popup is a **documented exception** — not migrated) | `MenuProvider` + `MenuItemsFromRegistry` + `MenuList` / `MenuGrid` (regions) + the `useMenuItem` / `useMenuKeyboard` / `useMenuCombobox` / `useMenuDismiss` hooks + `MenuRegistry`; nav math in `nav-core.ts` | [src/components/menu/](../../src/components/menu/) (`index.ts` is the public surface); design [docs/agents/menu-system-design.md](menu-system-design.md) |
| **Hint** / **hint bubble** / **tooltip** (the small black hover/focus bubble describing a control; the app-wide replacement for native `title=` tooltips and the engine behind Helper mode) | One app-wide controller + portal `HintLayer` (mounted once in `src/app/page.tsx`); authored by `useHint({ label, keys, pos })` / `<Hint>` which stamp `data-hint` / `data-hint-keys` / `data-hint-pos` attrs (legacy `data-helper` read as alias); a shortcut-only hint omits the label and shows just the keycap | [src/components/HintLayer.tsx](../../src/components/HintLayer.tsx); [src/components/Hint.tsx](../../src/components/Hint.tsx); see `ui-chrome.md` → Hint system and [src/STYLE_GUIDE.md](../../src/STYLE_GUIDE.md) → "Hints, tooltips & keyboard shortcuts" |
| **Keycap** / **keyboard-shortcut chip** (the styled `⌘/` chip shown in a hint bubble or menu) | `Kbd` — renders a portable shortcut string (`"Mod+/"`, `"Mod+Shift+N"`) platform-aware: `Mod` → ⌘ on Mac, `Ctrl` elsewhere, via `isMac()` / `useIsMac()`. The only way to render a shortcut (no hardcoded `⌘…` strings) | [src/components/Kbd.tsx](../../src/components/Kbd.tsx); platform probe [src/lib/platform.ts](../../src/lib/platform.ts) |
| **Cmd+/ actions-menu shortcut** (`Mod+/` toggles the actions menu open at the cursor/selection — the keyboard twin of clicking the margin ⚡, toggles closed if already open) | Window-level keydown handler in `SelectionActionsMenu` (~line 313) opening `ActionsMenuPanel`; the margin `SelectionActionsMenu` button carries the `Mod+/` shortcut-only hint | [src/components/SelectionActionsMenu.tsx](../../src/components/SelectionActionsMenu.tsx) |
| **Grab handle** (6-dot grip on card header — decorative signal; the drag-lift gesture lives on the whole wrapping header) | `CardDragHandle` (docked card header); `FloatGrip` mirrors it on the popped `FloatChrome` strip | `panel-primitives.tsx` (`CardDragHandle`); [src/floats/FloatChrome.tsx](../../src/floats/FloatChrome.tsx) |
| **Drop button** (double-chevron-down glyph; rightmost header control on a card, left of the X on a popped-out float — grab it (mousedown-drag) to enter **drop-mode** and (re)anchor the card into the prose at a target position; dropping an unanchored footnote/citation creates the atom inline. Registry-gated: renders only when `isDroppable(kind)`. Mounts on both the docked card and the omni mount; the float twin lives on `FloatChrome`. Supersedes the retired Shift-grab drop entry and the removed panel→margin native drag) | `CardDropButton` (icon `DropChevrons`); gesture in `card-drop-gesture.ts` (`beginCardDropGesture`); `droppable` / `dropPlacement` are CardMeta facets, not new card kinds; predicate `isDroppable` | [src/components/panel-primitives.tsx](../../src/components/panel-primitives.tsx) (`CardDropButton`, ~line 519); [src/components/drop-mode/card-drop-gesture.ts](../../src/components/drop-mode/card-drop-gesture.ts); icon [src/components/icons/DropChevrons.tsx](../../src/components/icons/DropChevrons.tsx); float mount [src/floats/FloatChrome.tsx](../../src/floats/FloatChrome.tsx) |
| **Text drag handle** (~~3-line icon on card body~~) | **Removed in ec38210.** The in-card text-drag grip beside each `RichTextField` was deleted along with the `onTextDragStart` prop, `startTextDrag` helper, and the `MIME_TEXT_INSERT` import from `panel-primitives`. The card's **drop button** (the double-chevron — rightmost on the docked card header, left of the X on a float) enters drop-mode for content drags now; pop-out floats also lost their interior 6-dot grip in 5f2d357. Surviving text-move paths: drag-to-pop-out (6-dot lift in the main editor margin) and drop-mode (grabbing the card's drop button; req-7 retired the legacy Shift-drag-on-the-header entry). | — |
| **Stack** / **visual clipboard** (round button at the editor's bottom-left; click to open a horizontal strip of parked items that can be pulled back into any open doc) | `StackIcon` toggles, `StackStrip` renders, `StackThumbnail` is each item; state via `useStack`; cross-doc localStorage at `virgil-stack-v1` | [src/hooks/useStack.ts](../../src/hooks/useStack.ts); [src/components/stack/](../../src/components/stack/) (StackIcon.tsx, StackStrip.tsx, StackThumbnail.tsx); snapshot helpers in [src/lib/stack/snapshot.ts](../../src/lib/stack/snapshot.ts); see `ui-chrome.md` → Stack for the full flow |
| **Passage-action menu** / **drag-handle menu** (click the paragraph/selection/heading drag handle to pop a vertical menu of Footnote / Citation / Note / Todo / Request report / Request revision / Request cut / Archive — the request-bearing actions were relabeled "Request …" in 43215f9b (labels only; the `suggest-edit`/`cutter`/`report` action ids are unchanged) — each with a single-letter keyboard hint that's active only while the menu is open) | `DragHandleMenu` (anchored popover, react-portal); opened via `DragHandleMenuApi.open(passage, anchorRect)` from `DragHandleMenuProvider`; passage drag-handle UI in `TextObjectGrabHandle` | [src/components/DragHandleMenu.tsx](../../src/components/DragHandleMenu.tsx); [src/components/editor-layout/card-actions/drag-handle-menu-context.tsx](../../src/components/editor-layout/card-actions/drag-handle-menu-context.tsx); [src/text-objects/TextObjectGrabHandle.tsx](../../src/text-objects/TextObjectGrabHandle.tsx); wired in `EditorPane.tsx` ~line 4457 |

## Link model

| User term | Code name(s) | Where |
|---|---|---|
| **Link** | `Link` type | [src/links/_shared/types.ts](../../src/links/_shared/types.ts) |
| **Link kind** | `LinkKind = "footnote" \| "citation" \| "anchor"` | Same |
| **Anchor** (the in-text side of a link) | `LinkAnchor = { type: "inline-atom"; … } \| { type: "textObject"; targetKind: TextObjectKind; textObjectIds: string[]; margin; textRange? }` | Same |
| **TextObject** (the unified abstraction for every graspable text unit — paragraph, heading, list, list item, example item, atom block, linkedRange, etc.) | `TextObjectRef = { kind: TextObjectKind; id }`; closed kind union + meta registry; float chrome (`FloatWindow`/`FloatChrome` via `textObjectFloatable`) + body components + drop-spec all collapse to one pathway routed through `TEXT_OBJECT_REGISTRY` | [src/text-objects/](../../src/text-objects/); [src/text-objects/text-object-registry.ts](../../src/text-objects/text-object-registry.ts); see [TEXT-OBJECT-REFACTOR.md](../../TEXT-OBJECT-REFACTOR.md) |
| **Mode A** (legacy term) | Pre-D8 name for "paragraph-only anchor with no text range." After D8 collapsed Mode A/B into `targetKind`, "Mode A" describes any `targetKind !== "linkedRange"` anchor | `isModeB(link) === false`; see `src/links/links.ts` |
| **Mode B** (legacy term) | Pre-D8 name for "paragraph + text-range anchor (linkedAnchor mark)." After D8 this is `targetKind === "linkedRange"`. `isModeB(link)` is the derived predicate, kept for ripple minimization | `isModeB(link) === true` |
| **DOM contract** | `data-link-id`, `data-link-kind`, `data-link-card` attrs on in-editor markers; `data-link-card` on cards | [src/links/link-registry.ts](../../src/links/link-registry.ts) |
| **Linked surfaces** / **three-surface hover** (text passage + margin icon + panel card all light up together; click-to-select propagates) | Module-level `cardStore` (selection + hover) in `anchored-card-store.ts`; per-card hook `useAnchoredCard` returns the `data-card-key`, mouse handlers and selected/hovered booleans every anchored card needs; `usePlacement` scrolls the editor when card→anchor selection changes. Consumers `useLinkHighlight`, `useAnchorHighlightReconciler` (single idempotent reconciler — 930b9f6 unified the former `useCardHoverHighlight` + `useCardSelectionHighlight` pair), `useLinkedAnchorReconciler`; sources `useTextHoverBridge`, `usePanelCardHoverBridge`, generic margin `onHover` | [src/links/_shared/anchored-card-store.ts](../../src/links/_shared/anchored-card-store.ts); [src/links/_shared/useAnchoredCard.ts](../../src/links/_shared/useAnchoredCard.ts); [src/links/_shared/usePlacement.ts](../../src/links/_shared/usePlacement.ts); see `main-text.md` → Highlight coupling |
| **EntityKind** | The *anchored* card kinds (`ANCHORED_CARD_KINDS`): `note \| highlight \| footnote \| citation \| report \| report-request \| example \| todo \| archive \| revision-comment \| revision-suggestion \| cutter-comment \| cutter-suggestion`. A subset of `CardKind` (drops the non-anchored `bib \| error`; `ai` is no longer a `CardKind` — retired in #55b); drives hover/selection plumbing | [src/links/_shared/entity-hover.ts](../../src/links/_shared/entity-hover.ts) |
| **Label candidate list** (ref popover) | `LabelInfo { label, kind, typeLabel, title }`; `kind ∈ {heading, equation, figure, table, label, example}` | [src/lib/labels.ts](../../src/lib/labels.ts) + [src/components/LabelRefPopover.tsx](../../src/components/LabelRefPopover.tsx) |

## Library tab

Self-contained subsystem under `library/` (sibling of `src/`). See [library/AGENTS.md](../../library/AGENTS.md) for the full map.

| User term | Code name(s) | Where |
|---|---|---|
| **Library tab** / **manila tab** (the "shadow" tab paired with each DocTab in the Virgil bar, in `--library-bg` warm tan) | Rendered alongside `DocumentFolderTab` per open doc — see double-tab pattern in [src/STYLE_GUIDE.md](../../src/STYLE_GUIDE.md) "Library tab" section | [src/components/editor-layout/TabStrip.tsx](../../src/components/editor-layout/TabStrip.tsx) (extracted tab strip); library pane body in [library/components/LibraryApp.tsx](../../library/components/LibraryApp.tsx) |
| **Library pane** (body of the Library tab once activated) | `LibraryApp` → `LibraryView` | [library/components/LibraryApp.tsx](../../library/components/LibraryApp.tsx); [library/components/LibraryView.tsx](../../library/components/LibraryView.tsx) |
| **Inner library tabs** / **Central + curated libraries** (the second layer of tabs inside the library pane: Central catalog plus user-spawned curated slices) | `TabbedLibraryPanel` + `useLibraryTabs` | [library/components/TabbedLibraryPanel.tsx](../../library/components/TabbedLibraryPanel.tsx); [library/hooks/useLibraryTabs.ts](../../library/hooks/useLibraryTabs.ts). The inner strip's "+" / recent-libraries dropdown moved to the navigator pod (below); per-tab pin and close × are now uniform across Central / Project / Custom |
| **Central dashboard** / **stats dashboard** / **Dashboard \| Browse switch** (the Central tab opens to a search-first stats dashboard — honest bib/catalog counts via `computeCatalogStats`, an inline search palette, a "Browse all N matches" footer — instead of the heavy virtualized list; a persistent `[Dashboard \| Browse]` segmented switch toggles between it and the list, and `centralViewMode` defaults to `"dashboard"` so the windowed list stays unmounted until Browse) | `LibraryCentralDashboard` (default export); stats via `computeCatalogStats` / `CatalogStats`; mode pref via `useCentralViewMode` (`"dashboard"` \| `"list"`); switch + `CentralListHeader` live in `TabbedLibraryPanel` | [library/components/LibraryCentralDashboard.tsx](../../library/components/LibraryCentralDashboard.tsx); [library/lib/catalog-stats.ts](../../library/lib/catalog-stats.ts); mode hook + switch in [library/components/TabbedLibraryPanel.tsx](../../library/components/TabbedLibraryPanel.tsx); `useCentralViewMode` in [library/lib/view-session-store.ts](../../library/lib/view-session-store.ts) |
| **Libraries navigator** (leftmost rounded pod inside the library pane: lists Central, Project libraries, and My libraries with a "+" to spawn customs and entry-drop targets on custom rows) | `LibrariesNavigator` (~490 lines); `openLibrary` action on `useLibraryTabs` (paper-style replace-or-append for navigator clicks); `togglePinLibrary` (renamed from `togglePinPaper`) | [library/components/LibrariesNavigator.tsx](../../library/components/LibrariesNavigator.tsx); [library/hooks/useLibraryTabs.ts](../../library/hooks/useLibraryTabs.ts) |
| **My Papers pod** / **"My Papers"** / (user) **"papers column"** (the lower of the two stacked left-rail pods, listing the user's **curated** list of their own Virgil docs — `myPaperIds` from `useMyPapers`, persisted in IndexedDB. **Disambiguate (this confused a session 2026-06-24):** it is **NOT** the currently-open docs; **NOT** the `LeftList` papers-within-a-library; and **NOT** the Libraries pod's "Project libraries" section — which *does* mirror open docs. "+ Add paper" opens a popup mirroring the Virgil-bar `TabPlusMenu` — recent docs + Open folder + Create new. Each row currently shows a hover-× "Remove from My Papers" → `removeMyPaper`) | `MyPapersPod`; curated ids via `useMyPapers`; rendered into the `belowNavigator` slot of `LibraryView` | [src/components/library/MyPapersPod.tsx](../../src/components/library/MyPapersPod.tsx); [src/hooks/useMyPapers.ts](../../src/hooks/useMyPapers.ts) |
| **Nav pod** (shared shape for the stacked Libraries + My Papers pods, with a draggable horizontal resizer between them; height persisted to `virgil-library-papers-height`, both clamped to 100px floor) | `NavPod` | [library/components/NavPod.tsx](../../library/components/NavPod.tsx) |
| **Catalog** (master `catalog.json` on disk + `master.bib`) | `CatalogStore` + `useCatalog` + `useMasterBib` | [library/lib/catalog-store.ts](../../library/lib/catalog-store.ts); [library/hooks/useCatalog.ts](../../library/hooks/useCatalog.ts) |
| **Library view session** (the persistence layer that makes Library view prefs — tab state, list view, scope, `centralViewMode` — survive reload) | `LibraryViewSession` store (`getSession` / `flushNow` / `useCentralViewMode`), keyed `virgil-library-view-session` | [library/lib/view-session-store.ts](../../library/lib/view-session-store.ts) |
| **Catalog search** (the flexible catalog-search adapter onto the fuzzy bib search; powers the dashboard palette + Browse list) | `searchCatalogFuzzy` | [library/lib/catalog-search.ts](../../library/lib/catalog-search.ts) |
| **List window** (the hand-rolled fixed-height list windower behind the Browse list's perf — only ~38 DOM rows for a 200+ paper catalog) | `computeListWindow` / `ListWindow` (`ROW_HEIGHT` / `ROW_OVERSCAN`) | [library/lib/list-window.ts](../../library/lib/list-window.ts) |
| **Import Bibliography** / **imported badge** (per-paper "Import bibliography" row action that folds a paper's `papers/<citekey>/references.bib` into the central `master.bib`; on success the paper's catalog row shows a blue "imported" check/pill, cleared by a later *addition* to that references.bib) | "Import bibliography" item in `RowActionMenu`; enqueued via `bib-edit.ts`; the blue badge is `BibImportedPill` / `BibImportedCheck` (`bib.imported` catalog flag); skill `/library/import-bib` + invalidation sweep `invalidate_bib_imports.py`; UID minting via `bib-uid.ts` + `bib-cite-rewrite.ts` | [library/components/RowActionMenu.tsx](../../library/components/RowActionMenu.tsx); [library/lib/bib-edit.ts](../../library/lib/bib-edit.ts); [library/components/StatusPill.tsx](../../library/components/StatusPill.tsx); [library/skills/import-bib.md](../../library/skills/import-bib.md); [library/scripts/invalidate_bib_imports.py](../../library/scripts/invalidate_bib_imports.py); [src/lib/bib-uid.ts](../../src/lib/bib-uid.ts); [src/lib/identity/bib-cite-rewrite.ts](../../src/lib/identity/bib-cite-rewrite.ts) |
| **Bib card** (single-entry bib display in the right detail pane) | `BibCard` | [library/components/BibCard.tsx](../../library/components/BibCard.tsx) |
| **Bib edit modal** (full-form edit dialog for a `.bib` entry) | `BibEditModal` | [library/components/BibEditModal.tsx](../../library/components/BibEditModal.tsx) |
| **Library entry picker** (floating two-line picker dropdown anchored to a "+" / "Search library…" trigger; per-row truncated authors+year, ellipsized title, ⌄ expansion, provenance chips Local/Central/custom-library, ⊕ add. Used by the Bibliography panel and the Citations card list-maker; replaces the old heavy inline panel mode in 240bfda) | `LibraryEntryMenu` (reusable parameterized component); fuzzy-search via fuse.js with per-array WeakMap cache | [src/components/library/LibraryEntryMenu.tsx](../../src/components/library/LibraryEntryMenu.tsx); consumers: [BibliographyPanel.tsx](../../src/panels/Bibliography/BibliographyPanel.tsx), [CitekeyPicker.tsx](../../src/panels/Citations/CitekeyPicker.tsx) |
| **Citekey picker** (the in-citation-card list-maker that reuses `LibraryEntryMenu`; merges paper bib + library results) | `CitekeyPicker` (gains `keepOpenOnPick` + a `footer` slot so the create popover can stage keys + show the OK strip) | [src/panels/Citations/CitekeyPicker.tsx](../../src/panels/Citations/CitekeyPicker.tsx) |
| **Citation creator popup** / **create-citation popover** / **citation popup** (the deferred-commit popover that opens OVER THE TEXT at the caret when you "create citation" from slash `\cite` / lightning / the grab-bar; cursor auto-focused in a citekey search, picked keys staged as chips, an **OK** button, and nothing written until OK / click-away with ≥1 key. The gutter card + `\cite` pill materialize only on commit) | `CitationCreatePopover` (wraps `CitekeyPicker` `keepOpenOnPick`); opened via the shared create controller below | [src/panels/Citations/CitationCreatePopover.tsx](../../src/panels/Citations/CitationCreatePopover.tsx) |
| **Shared create popover** / **inline-atom create controller** (the ONE deferred-commit lifecycle both `\ref` and citation creation flow through — open-at-caret, capture insertion pos, defer, commit, dismiss; parameterized by kind, mounting `CitationCreatePopover` or `LabelRefPopover` create-mode) | `ctx.openAtomCreate(kind)` seam + `atomCreateRequest` state (EditorLayout) + the `virgil-atom-create-popover` event | [src/lib/actions/atom-create.ts](../../src/lib/actions/atom-create.ts); seam on `ActionContext` in [src/lib/actions/action-registry.ts](../../src/lib/actions/action-registry.ts); consumed in [marker-clicks.ts](../../src/components/editor-layout/event-bridges/marker-clicks.ts) |
| **Paper render** (read-only LaTeX paper view inside the library) | `PaperRender` — mounts `<EditorPane editable={false} chrome={READER_CHROME} />`, sharing Virgil's canonical TipTap extension set; the reader now opens to the PDF by default | [library/components/PaperRender.tsx](../../library/components/PaperRender.tsx) |
| **Page-scroll lozenge** / **"p. N" lozenge** (the floating `p. N` page-number scroll pill in the reader; click/scrub to jump to a source page. Replaced the old `PageScrollStrip`, which was deleted) | `PageScrollLozenge` (default export) | [library/components/PageScrollLozenge.tsx](../../library/components/PageScrollLozenge.tsx); consumed in [library/components/PaperRender.tsx](../../library/components/PaperRender.tsx) |
| **Raw Text / Virgil Text toggle** (reader header button that flips the rendered paper between its raw extraction and the deep-indexed "Virgil" view; the label reads "Virgil Text" when the paper is deep-indexed, else "Raw Text") | toggle button in `PaperHeader` (`indexedState === "deepIndexed" ? "Virgil Text" : "Raw Text"`) | [library/components/PaperHeader.tsx](../../library/components/PaperHeader.tsx) |
| **PDF view** (in-app PDF panel for a paper's compiled output) | `PdfView` | [library/components/PdfView.tsx](../../library/components/PdfView.tsx) |
| **Pgmark** (`\pgmark{}` LaTeX command anchoring text to a source-page number) | `pgmark` TipTap node + Python pipeline | [src/lib/tiptap/pgmark.ts](../../src/lib/tiptap/pgmark.ts) (moved from `library/tiptap/` in Path A 7.8 — now part of the unified extension set); [library/scripts/pgmark.py](../../library/scripts/pgmark.py) |
| **Skill bundle** (the agent skills synced into the user's library folder) | Built by `library/build/build-skill-bundle.mjs` from `library/skills/` + `library/scripts/`; written to `public/skill-bundle/` then copied into the user's library on demand | [library/lib/skill-sync.ts](../../library/lib/skill-sync.ts); built at `predev`/`prebuild` |
| **Skill-sync controls** / **"Skill sync failed" banner** / **Refresh skills** (`SkillSyncControls` is now **banner-only**: a loud dismissible failure banner with Retry / "Grant & retry" on a permission failure, and a "skills updated — restart your cowork session" notice; sits before the collapsed-right-toolbar gate, like the Virgil-update banner, so a failure can't be hidden. Pure presentational — no editor subscription. The always-available manual **Refresh skills** re-sync is NOT a bar button — it's an item in the Virgil-bar help ("?") menu, gated on `hasDoc`, reusing this component's `onResync`) | `SkillSyncControls`; sync state + `SkillSyncError` / `SkillSyncNotice` types in `useFiles` (`skillSyncError` / `skillSyncNotice` / `resyncSkills` / `dismissSkillSyncError` / `dismissSkillSyncNotice`); library twin in `useLibraryHandle` | [src/components/SkillSyncControls.tsx](../../src/components/SkillSyncControls.tsx); [src/hooks/useFiles.ts](../../src/hooks/useFiles.ts); library twin [library/hooks/useLibraryHandle.ts](../../library/hooks/useLibraryHandle.ts); mounted in the extracted [StatusCluster.tsx](../../src/components/editor-layout/StatusCluster.tsx) (~line 183), the top bar's right cluster (after the Virgil-update banner); manual Refresh in the "?" menu |
| **Drop zone** (drag-PDF-here area for triage) | `DropZone` | [library/components/DropZone.tsx](../../library/components/DropZone.tsx) |
| **Example document** / **"Open the example document"** (the bundled, fully-editable onboarding sandbox a new user can open with no folder picker — a static-asset build of `samples/annotation-history/` seeded into OPFS on first open and registered as an ordinary indexed doc; offered only where OPFS works and not on the dev backend (`exampleAvailable`); surfaced as an empty-state CTA lozenge + a `TabPlusMenu` "Open/Reset example document" item) | `ensureExampleSeeded` / `resetExample` (seeder); `opfsAvailable` (OPFS probe); fixed `EXAMPLE_DOC_ID = "example0"`; UI wiring `openExample` / `resetExampleDoc` on `useFiles` | [src/lib/example-doc/example-seeder.ts](../../src/lib/example-doc/example-seeder.ts); [src/lib/example-doc/opfs-doc-location.ts](../../src/lib/example-doc/opfs-doc-location.ts); [src/lib/example-doc/example-identity.ts](../../src/lib/example-doc/example-identity.ts); [src/hooks/useFiles.ts](../../src/hooks/useFiles.ts); CTA in `EditorLayout.tsx` empty-state, menu items in [src/components/TabPlusMenu.tsx](../../src/components/TabPlusMenu.tsx) |
| **Recent papers list** (recently-opened papers shown on the home view) | `RecentPapersList` | [src/components/RecentPapersList.tsx](../../src/components/RecentPapersList.tsx) |
| **Tab plus menu** ("+" menu on the Virgil bar tab strip for adding new tabs / opening papers) | `TabPlusMenu` | [src/components/TabPlusMenu.tsx](../../src/components/TabPlusMenu.tsx) |
| **Install prompt** (PWA install affordance shown when the browser exposes `beforeinstallprompt`) | `InstallPwaPrompt` | [src/components/InstallPwaPrompt.tsx](../../src/components/InstallPwaPrompt.tsx) |

## Persistence & sidecars

| User term | Code name(s) | Where |
|---|---|---|
| **Sidecar** | JSON file alongside `.tex` in `virgil/` folder | Types in [src/lib/types.ts](../../src/lib/types.ts) (`VirgilSidecar`) |
| **Suggestions** | AI line-edit proposals (review cards in the Revisions panel; progress bar in panel header) | `suggestions.json`; [src/hooks/useSuggestions.ts](../../src/hooks/useSuggestions.ts) |
| **Revisions** (comments + suggestions) | Polymorphic `RevisionCommentCard` + `RevisionSuggestionCard` cards, anchored or paper-wide. Shares the same comment/suggestion card structure as Cutter | `revisions.json`; [src/hooks/useRevisions.ts](../../src/hooks/useRevisions.ts); types `RevisionCard` in [src/lib/types.ts](../../src/lib/types.ts) |
| **Revisions tracker** | Optional per-document target for accepted revisions (analogous to CutterGoal for word count) | `RevisionsTracker` on `RevisionsState.tracker` in `revisions.json`; UI in [src/panels/Revisions/RevisionsTracker.tsx](../../src/panels/Revisions/RevisionsTracker.tsx) |
| **Cutter goal** | Per-document word-count cutting target | Stored on `CutterState.goal` in `cutter.json`; [src/hooks/useCutter.ts](../../src/hooks/useCutter.ts); UI in [src/panels/Cutter/CutterGoalStrip.tsx](../../src/panels/Cutter/CutterGoalStrip.tsx) |
| **Collab** / **collaborator mode** | Turn-taking collaboration via sidecar; no co-editing — single-pen coordination for `.tex`, per-card focus claims for sidecars | `collab.json`; [src/lib/collab.ts](../../src/lib/collab.ts); [src/hooks/useCollab.ts](../../src/hooks/useCollab.ts) |
| **AI requests** | Queued requests for an agent to resolve | `ai-requests.json` |
| **AI request bridge** (collapses per-card sticky `aiRequest:true` flags — the 7 kinds whose `CARD_REGISTRY` entry declares `aiRequest` routing: notes / highlights / todos / cutter-comments / revision-comments / report-requests / footnotes — into a single drainable `ai-requests.json` queue with `linkedTo`; fulfillment skill clears the flag and marks the request `complete`) | `bridgeCardFlagsToAiRequests` etc. | [src/lib/ai-request-bridge.ts](../../src/lib/ai-request-bridge.ts) |
| **Doc notifications inbox** (per-doc `notifications.json` sidecar that editor-side skills append completion entries to; the app polls and toasts unseen items, tracking last-seen per doc in localStorage) | `useDocNotificationStream`; `DocNotification`/`DocNotificationsInbox` types | [src/hooks/useDocNotificationStream.ts](../../src/hooks/useDocNotificationStream.ts); types in [src/lib/types.ts](../../src/lib/types.ts) |
| **Bib review requests** | Per-entry bibliography field/notes reviews | `bib-review-requests.json` |
| **FSA** (File System Access API) | Disk boundary — only place disk is touched | [src/lib/storage-fsa.ts](../../src/lib/storage-fsa.ts) |

## System dialogs

| User term | Code name(s) | Where |
|---|---|---|
| **System dialog** (reusable modal primitive) | `SystemDialog` | [src/components/system-dialog.tsx](../../src/components/system-dialog.tsx) |
| **System dialog host** (top-level mount point) | `SystemDialogHost` | [src/components/system-dialog-host.tsx](../../src/components/system-dialog-host.tsx) |
| **Confirm dialog** | `ConfirmDialog` — thin wrapper over `SystemDialog` | [src/components/ConfirmDialog.tsx](../../src/components/ConfirmDialog.tsx) |
| **New document modal** | `NewDocumentModal` — uses `SystemDialog` | [src/components/NewDocumentModal.tsx](../../src/components/NewDocumentModal.tsx) |
| **Tex file picker** | `TexFilePickerModal` — uses `SystemDialog` | [src/components/TexFilePickerModal.tsx](../../src/components/TexFilePickerModal.tsx) |
| **Document class mismatch dialog** | `DocumentClassMismatchDialog` — uses `SystemDialog` | [src/components/DocumentClassMismatchDialog.tsx](../../src/components/DocumentClassMismatchDialog.tsx) |
| **Fonts dialog** / **font picker** (per-category font + size dialog launched from the View menu's "Fonts…" item; one card per font category — body, headings, footnotes, marginalia, etc. — each with a `FontPicker` and `SizeStepper`) | `FontsDialog` (uses `FloatingPanel`); `FontPicker` (typeahead pop-down listing `MAIN_TEXT_FONTS`); `SizeStepper` (− / + numeric stepper, larger hit targets than `PanelTextSizeRow`) | [src/components/FontsDialog.tsx](../../src/components/FontsDialog.tsx); [src/components/FontPicker.tsx](../../src/components/FontPicker.tsx); [src/components/SizeStepper.tsx](../../src/components/SizeStepper.tsx); font catalog in [src/lib/preferences-tree.ts](../../src/lib/preferences-tree.ts) (`MAIN_TEXT_FONTS`); opened from `MenuBar` ViewMenu (`onOpenFontsDialog`); mounted in `EditorLayout.tsx` ~line 4048 |

## Card creation / pristine cards

| User term | Code name(s) | Where |
|---|---|---|
| **Card-creation helpers** (unified create-from-selection flow) | `cardCreation` context (`CardCreationApi`) with `.createNote`, `.createHighlight`, `.createTodo`, `.createFootnote`, `.createCitation`, `.createReport`, `.createReportRequest`, `.createRevisionComment`/`Suggestion`, `.createCutterComment`/`Suggestion`, `.createArchiveSnippet` | [src/components/editor-layout/contexts/card-creation.tsx](../../src/components/editor-layout/contexts/card-creation.tsx) + [src/components/editor-layout/card-actions/card-creation.ts](../../src/components/editor-layout/card-actions/card-creation.ts) |
| **Pristine card** (freshly-created, no user edits yet — auto-discarded if closed untouched) | `usePristineCardManager` + `pristine-cards` context | [src/hooks/usePristineCardManager.ts](../../src/hooks/usePristineCardManager.ts) + [src/components/editor-layout/contexts/pristine-cards.tsx](../../src/components/editor-layout/contexts/pristine-cards.tsx) |

## Name mismatches to keep in mind

- **"Virgil bar"** (user) = the whole horizontal top strip (`<div class="virgil-bar">`, inline in EditorLayout.tsx) — **not** the `MenuBar` component. The `MenuBar` is the menu pod that docks inside the Virgil bar by default. Don't confuse either with the three-dot View menu.
- **"Navigation strip"** (user) — no single component. Paragraph-nav chevrons are inline in `MenuBar`. Panel navigation (what's open) is split between left/right strips plus the Outline panel.
- **"Action toolbar"** (user) — there is no standalone actions toolbar anymore. `ActionButtonsRow` / `ACTION_BUTTON_DEFS` / `DetachedActionsToolbar` / `MarginActionToolbar` are all deleted; every action now lives in the SSOT `VIRGIL_ACTION_REGISTRY` ([src/lib/actions/action-registry.ts](../../src/lib/actions/action-registry.ts)), surfaced via `SelectionActionsMenu` → `ActionsMenuPanel` and `DragHandleMenu`.
- **"Formatting toolbar"** (user) — `FormatButtonsRow` / `DetachedFormattingToolbar` are deleted; the formatting grid now renders from the action registry inside `ActionsMenuPanel`, reached from the margin ⚡ (`SelectionActionsMenu`) or the `DragHandleMenu`.

- **"External-change badge"** / **"changed on disk"** / **"disk-changed indicator"** (user) — `ExternalChangeBadge` ([src/components/ExternalChangeBadge.tsx](../../src/components/ExternalChangeBadge.tsx)), the topbar status-cluster pill (beside the collab pill) that appears when an external writer (Overleaf-via-sync, `git pull`, `vim`, Dropbox) edits the paper's `.tex`/`.bib` on disk. Amber = clean change (Reload); danger = conflict with unsaved edits. Driven by the `DiskWatcher` poller + `diskLedger` ([src/lib/disk-watcher.ts](../../src/lib/disk-watcher.ts), [src/lib/disk-ledger.ts](../../src/lib/disk-ledger.ts)); reconcile actions reuse `useDocument.refetch()`. Design: [docs/memos/external-change-badge/DESIGN.md](../memos/external-change-badge/DESIGN.md).

## Pending terminology

- **"Fold up / compress the top bar"** / **"the fold chevron"** (user, 2026-07-04) — Chrome's installed-PWA **Window Controls Overlay (WCO)**: the chevron collapses Chrome's own toolbar, growing the app's reserved title-bar strip. Code: WCO is opted into `display_override` in [src/app/manifest.ts](../../src/app/manifest.ts); the app's response lives in the `--window-inset-*` CSS SSOT + the `.virgil-bar` rule (src/app/globals.css) and the [useWindowChrome](../../src/hooks/useWindowChrome.ts) store (display mode + px insets + `getWindowInsetTopPx()`). Under WCO the Virgil bar grows to fill the strip and insets its material to clear the controls. See STYLE_GUIDE "Window insets / WCO title bar".
