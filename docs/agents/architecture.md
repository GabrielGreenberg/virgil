<!-- last-verified: 340f8456 2026-09-21 -->
<!-- derives-from: docs/architecture/VIRGIL.md#code-organization, docs/architecture/VIRGIL.md#sidecar-and-panel-inventory -->
<!-- covers-code: src/hooks, src/lib/storage-fsa.ts, src/lib/types.ts, src/panels/panel-registry.ts, src/links/link-dom-contract.ts, src/links/resolve-card-anchor.ts, src/lib/anchor-mint-signal.ts, src/text-objects/text-object-registry.ts, src/text-objects/LiftHost.tsx, src/lib/marginalia.ts, src/lib/actions/action-registry.ts, src/lib/actions/editor-actions-bridge.ts, src/lib/actions/action-icons.tsx, src/lib/tiptap/smart-insert.ts, src/lib/tiptap/insert-inline-atom.ts, src/lib/tiptap/chrome-scroll-margin.ts, src/lib/view-prefs/registry.ts, src/lib/focus-view.ts, src/lib/identity, src/lib/bib-uid.ts, src/cards/has-content.ts, src/cards/lifecycle, src/lib/input-modality.ts, src/lib/pomodoro-timer.ts -->

# Architecture: Registries, Hooks, Persistence, Sidecars

Cross-cutting systems that most features touch.

## Single sources of truth (SSOTs)

| Concern | SSOT | Notes |
|---|---|---|
| Panel taxonomy | [src/panels/panel-registry.ts](../../src/panels/panel-registry.ts) (`PANEL_REGISTRY`) | Display labels, omni eligibility, default strip side. Card-kind satellite tables (`CARD_TYPE_LABELS` / `CARD_TITLE_LABELS`) are now *derived* from `CARD_REGISTRY` (`CARD_KEY_PREFIXES` was DELETED in task 634, with `PanelRegistryEntry.card` collapsed from a `CardLink` object to a plain `CardKind | null` — the panel's PRIMARY kind, not its membership, which derives via `cardKindsForPanel`); `getPanelByCardKind` derives from `CardMeta.panel` (the hand-kept polymorphic-panel map was retired in 27458d8) |
| Card kinds | [src/cards/card-registry.tsx](../../src/cards/card-registry.tsx) (`CARD_REGISTRY: Record<CardKind, CardMeta>`) | Card-system SSOT (27458d8), mirrors `TEXT_OBJECT_REGISTRY`. Per-kind meta: label, theme key, owning panel, origin, anchored flag, marker type, lifecycle caps, stackable, `toFloatable`, plus a declarative `content: CardContentModel | null` descriptor (T4) and `morph` field. Its walker `cardHasContent` was **inverted** in task 401 — a gate written in node types and TEXT could not see a body that is entirely one ATOM, so an atom-only new footnote stayed *pristine* and was reaped on the next click-away; everything now carries content EXCEPT `EMPTY_WRAPPER_NODE_TYPES` ([node-attr-sets.ts](../../src/lib/node-attr-sets.ts)), and the premise is CHECKED against the live schema in both directions. `CardKind` (15 kinds — `comment`→`revision-comment`, bare `suggestion` dropped from the spine, and the legacy `ai` kind retired in #55b: unlinked note/todo AI-requests now migrate to real cards carrying a per-card `aiRequest` flag, while footnote/citation requests stay in the AIWindow) + derived predicates live in [src/cards/types.ts](../../src/cards/types.ts) + [src/cards/predicates.ts](../../src/cards/predicates.ts) (incl. `isArchivable` / `archiveRemovesAtom` for per-card archive). Data layer (`RevisionCard.kind`, `revisions.json`, Python) still uses `comment`/`suggestion`, bridged at float dispatch |
| Link kinds | The `LinkKind` union in [src/links/_shared/types.ts](../../src/links/_shared/types.ts) | **Not a registry, deliberately.** The three kinds are a union with a doc comment; each kind's behaviour is decided by the code that ships it (atom node specs, `createLinkedAnchor`, the highlight reconciler). A per-kind table (`LINK_REGISTRY`) DID sit here and was deleted in task 202 — nothing read it, so its connector strokes described a component deleted in 96675ca1 and its multiplicity rule was enforced by nobody. What survives is the DOM contract: `DATA_LINK_ID` / `DATA_LINK_KIND` / `DATA_LINK_CARD` + `linkCardKey`/`parseLinkCardKey` + (task 204) the selector builders `linkIdSelector` / `linkKindSelector` / `linkCardSelector` / `linkCardIdSelector` in [src/links/link-dom-contract.ts](../../src/links/link-dom-contract.ts), which every marker producer emits through and every query reads through ([link-surface-honesty.test.ts](../../src/links/__tests__/link-surface-honesty.test.ts)). Mode-A/B **membership** has one speller in the same `_shared/types.ts` since task 668 — `isModeB` (membership, the derivation the header documents) and its sibling `isRangedModeB` (PAYLOAD, the eight sites that go on to read `textRange`); twenty-three deciders had hand-written `targetKind === "linkedRange"`, and `ModeBAnchorLink` no longer *requires* `textRange`, because a poisoned linkedRange link is still Mode B and rung 2b exists to heal one |
| TextObject kinds | [src/text-objects/text-object-registry.ts](../../src/text-objects/text-object-registry.ts) (`TEXT_OBJECT_REGISTRY`) | Closed kind union (paragraph, heading, list, list item, example item, atom blocks, linkedRange — 17 kinds total: 14 top-level + 2 sub-objects + 1 range); per-kind meta drives grab-handle layout, float body component, drop adapter (wrap vs direct), move-source collector. (NOT the LaTeX source marker: the `\v*` id-marker vocabulary lives in [src/lib/latex-markers.ts](../../src/lib/latex-markers.ts) — a registry facet claimed that role, was read by nothing, and was deleted in task 255, because the round-trip layer cannot import this editor-coupled registry.) Adding a new TextObject kind = one entry here + `groups: "textObject"` on its node spec. See [TEXT-OBJECT-REFACTOR.md](../../TEXT-OBJECT-REFACTOR.md) |
| Editor actions | [src/lib/actions/action-registry.ts](../../src/lib/actions/action-registry.ts) (`VIRGIL_ACTION_REGISTRY: Readonly<Record<ActionId, ActionSpec>>` — TOTAL since task 260) | The single SSOT for EVERY action across ALL four surfaces (`ActionSurface` = grab-handle menu / gutter lightning / slash command / typed-LaTeX input rule — plus `keyboard`). Per-spec: category (`card`/`atom`/`block`/`format`), `surfaces`, applicability + selection-mode taxonomy, uniform collab read-only gate (rows grey out where the command would no-op). The two live menus (`DragHandleMenu`, `SelectionActionsMenu`→`ActionsMenuPanel`) now RENDER FROM the registry. PM→React dispatch via [src/lib/actions/editor-actions-bridge.ts](../../src/lib/actions/editor-actions-bridge.ts) — a **per-editor registry** (`registerEditorActionsHandle(editor, handle)` / `getEditorActionsHandleFor(view)`, bug sweep #1) so a slash/typed command under multi-doc keep-alive reaches THIS pane's handle, not a stale singleton; `runEditorAction(view, id, seed)` is the ONE plugin-land door (task 642; a census forbids any other production file from calling `.runAction(`), and the pane-keyed fallback supplies only the app-global React APIs — the gesture ORIGIN is per-DOCUMENT and resolves by owner via `owningEditor`, so positions always come from `origin.state`. Icons in [src/lib/actions/action-icons.tsx](../../src/lib/actions/action-icons.tsx); block-atom inserts (display-math/figure/graphics) via [src/lib/tiptap/smart-insert.ts](../../src/lib/tiptap/smart-insert.ts), but **inline**-atom inserts (footnote / citation / `\ref` / inline-math) route through the no-scroll [src/lib/tiptap/insert-inline-atom.ts](../../src/lib/tiptap/insert-inline-atom.ts) primitive so creating one never jumps the viewport (the inline sibling of `smartInsertBlock`; deliberate block scrolls land below the sticky chrome via [src/lib/tiptap/chrome-scroll-margin.ts](../../src/lib/tiptap/chrome-scroll-margin.ts)). **citation + `\ref`** further share ONE deferred-commit create controller ([src/lib/actions/atom-create.ts](../../src/lib/actions/atom-create.ts) — the `openAtomCreate` seam on the actions ctx + the `ATOM_CREATE_POPOVER_EVENT` = `"virgil-atom-create-popover"` window event consumed into EditorLayout's `atomCreateRequest`): any trigger surface opens a popover over the caret with a captured `pos`, and the inline atom is materialized via `insertInlineAtom`'s captured-position `at` only on COMMIT — so the gutter never flashes a blank pristine card mid-pick. **Container gate** (tasks 147–150/153): block-atom inserts, inline-math/`\ref`, slash/menu heading conversion, and the MenuBar `BlockTypeDropdown`'s out-of-scope heading levels all honor the *containing block* — a block-type can refuse a child it would corrupt, via the schema-read SSOT predicates, in caret and RANGE forms — a caller acting over a SELECTION owes the range form, `blockRangeHostsBlockInsert` / `inlineRangeAllowsAtom`, with `posHostsBlockInsert` / `posHostsInlineAtom` their caret reductions (task 641) in [text-object-registry.ts](../../src/text-objects/text-object-registry.ts) (see [workspace/actions.md](../workspace/actions.md) → the container gate; the type-only twin `blockTypeHostsInlineAtom` went **private** in task 396 — it cannot clamp a stale caret, and an exported one invites the smaller question). The 2026-08-21 applicability cluster made greying honest across all four surfaces: **396** put EVERY inline-atom insert through the gate at the DOOR (`insertInlineAtom`, which returns `{ refused: true }` — the callers that mint a card after the splice read that report), **397** made the lightning grid answer per ROW instead of painting one `example`-derived verdict on six cells and one `bullet-list`-derived verdict on three (the wrapper question became the third container predicate, `selectionHostsWrapper`, asking ProseMirror's own `findWrapping`), **398** made the slash popup ASK BEFORE IT OFFERS through [slash-applicability.ts](../../src/lib/tiptap/slash-applicability.ts) + one `commitSlashCommand` door — it used to delete the typed `\name` and only then let the action refuse — and **385** added the `forest` row (slash `\forest` + the grid's tree cell, both calling `forestRun`). Each ships a census with an EMPTY allowlist, because the doors were never the part that could misbehave — a call site that never asks is |
| Card themes | `CARD_THEMES` in [src/components/panel-primitives.tsx](../../src/components/panel-primitives.tsx) | `Record<PanelThemeKey, CardTheme>` — a mechanical fold over `DEFAULT_PANEL_COLORS` ([src/lib/panel-theme.ts](../../src/lib/panel-theme.ts)); 13 keys: citation, bib, footnote, note, highlight, archive, todo, cut, revision, report, example, aiRequest, error. The legacy `comment` alias is gone (registry `themeKey` *is* `PanelThemeKey`); `aiRequest`/`error` are non-overridable system accents (`SYSTEM_THEME_KEYS`). The highlight *text tint* still paints via the `tintColor` mark attr, separate from the `highlight` card accent |
| Auto-title labels | `CARD_TITLE_LABELS` + `nextCardTitle(kind, count)` in [src/panels/panel-registry.ts](../../src/panels/panel-registry.ts) | Per-CardKind label string (or `null` to opt out — comments / suggestions / citations / bib / error don't auto-title). `nextCardTitle("note", n)` → `"Note 4"`. Since T6/C12 a fresh card is created blank + `titleAuto: true` (recorded provenance), not pre-filled; on load `resolveLoadedTitle(kind, title, titleAuto)` decides the effective title from the recorded bit (the `isAutoTitle` string-shape heuristic is demoted to a one-time legacy fallback, stamped via `resolveTitleAuto`) |
| View preferences | [src/lib/view-prefs/registry.ts](../../src/lib/view-prefs/registry.ts) (`VIEW_PREF_REGISTRY`) | Typed table of every view-level pref (View-menu toggles/enums/sets + the Bibliography filter). Per-entry `kind`/`scope`/`default`/menu metadata; the store shape (`RegistryPrefs`), shipped defaults (`REGISTRY_DEFAULTS`), and global-key set (`REGISTRY_GLOBAL_KEYS`) are all *generated* from it, so a pref can't exist in the menu but be missing from persistence — this is why all view prefs now survive reload. Imports ONLY *types* from `useViewPrefs` (no runtime cycle); `useViewPrefs` spreads `REGISTRY_DEFAULTS` and extends `RegistryPrefs` |
| Input modality | [src/lib/input-modality.ts](../../src/lib/input-modality.ts) (`isTypingModality` / `notePointerInput` / `subscribeInputModality`) | Task 336. **A HOVER answer is derived from where the pointer IS, so only pointer input may re-derive it** — a document or selection change INVALIDATES it and never re-answers it. Pointer-derived chrome (today `TextObjectGrabHandle`'s hover branch) reads this and HIDES while the user types, un-hiding when the pointer speaks again. Read the DEVICE, not the derived change: gating on `docChanged`/`selectionUpdate` would hide the handle after a *click*, which moves the selection legitimately. Pure modifiers (`Shift`/`Ctrl`/`Alt`/`Meta`/`CapsLock`) do NOT flip modality — a Cmd-click on a handle begins with a `Meta` keydown. The suppression is an EDGE (one subscriber fire per flip), never a per-event check. Selection-derived chrome is deliberately NOT gated: a shift-arrow extension must keep moving its handle |
| Surface editability | [src/lib/tiptap/surface-editable.ts](../../src/lib/tiptap/surface-editable.ts) | Task 579. **"May the user edit this surface right now?"** — asked by every editor-derived AFFORDANCE (the inline-atom grab, the spellchecker). A card body is read-only exactly when `view.editable` is false; MAIN pins `view.editable = true` for its whole life and carries the user-facing answer in `editableRef`, so `view.editable` ALONE is a lie on MAIN wherever the host mounted it read-only (the Library Reader, a collab non-pen-holder, a cowork-pen hold) and an affordance that asks it offers an edit `readOnlyEnforcer` then silently drops |
| Block-order vocabularies | [src/lib/editor-geometry/block-vocab.ts](../../src/lib/editor-geometry/block-vocab.ts) (`createBlockVocabCache`) | Task 585. Doc-ordered block-uuid lists for the geometry readers that binary-search blocks by position (the breadcrumb's par-titled vocabulary, the active-block probe's). **Cache the ORDER, read positions FRESH**, and key on `structuralVersion` — NOT `DocStructure.version`, which bumps on every keystroke. See [keystroke-sanctity](laws/keystroke-sanctity.md) → "The memo-key half" |
| Design tokens | [src/app/globals.css](../../src/app/globals.css) + [src/STYLE_GUIDE.md](../../src/STYLE_GUIDE.md) | Semantic CSS variables (`--surface`, `--ink-body`, `--accent`, etc.; `--pod-seam` — the derived pod/card-lane seam, see STYLE_GUIDE) |
| Type definitions | [src/lib/types.ts](../../src/lib/types.ts) | `VirgilSidecar`, `EditorStateData`, `Suggestion`, `ReviewRequest`, `Link`, etc. |

Before adding a new panel, link kind, or theme, extend the registry instead of creating a parallel table.

## Key hooks

All in `src/hooks/`. Full list (~70 files) is large; these are the ones most often touched:

| Hook | What it owns |
|---|---|
| `useDocument` | Main doc state + autosave queue; `paragraphUuids`, `paragraphTitles` maps. Exposes `flushNow` — an immediate doc-bundle write that cancels the 1500 ms debounce, fired on an anchor-UUID **mint** transaction (`isAnchorMintTransaction`, [src/lib/anchor-mint-signal.ts](../../src/lib/anchor-mint-signal.ts)) and on a drop-mode re-anchor COMMIT, so a freshly anchored paragraph's `%!v:<uuid>` reaches the `.tex` on the card's fast clock instead of the doc's slow autosave clock (anchor-persistence race). Since task 357 every bundle write that lands BEFORE the user has genuinely edited (`isRealUserEdit` — an UNDOABLE `docChanged` test, never a bare `docChanged`, since a mint is doc-changing too) goes through the write-side preservation gate ([src/lib/write-preservation.ts](../../src/lib/write-preservation.ts)); a refused write returns normally with the file byte-identical, so the save path reads the refusal off `isWriteProtected(docId)` rather than inferring it from the absence of a throw |
| `useCitations` | Citation refs + `.bib` loading (every `.bib` mutation through `mutateProjectBib`, task 558 — the hook keeps no snapshot); `commandFor(id)` serializes a card's `\cite{…}` for the drop spec's create-if-absent branch (returns null for a keyless draft, via the shared `citationCommandOrNull` keyless-citation predicate). When the identity-cascade flag is on, a citekey rename routes through the `IdentityCascade` (registers the `\cite{}` doc-rewrite migrator) and `replaceBibEntry`/`updateBibEntry` carry the durable `BibEntry.uid` so uid-keyed sidecars don't strand. Task 344 made `CitationsState.bibPackage` OPTIONAL and turned detection into a SEED: `refreshBib` no longer writes the detected family over the user's record — absent means detection decides the in-memory view (`stored ?? detected ?? DEFAULT_BIB_FAMILY`), present means the user chose |
| `useFootnotes` | Footnote persistence + numbering; `updateFootnoteContent` mirrors body edits into the `footnotes.json` sidecar (9e53587d) so a footnote you typed into survives click-away, and owns the `FootnoteRef.aiRequest` flag |
| `useOrphanedFootnotes` | Durable per-doc home for orphaned footnotes (marker gone, body recoverable), backed by the `orphaned-footnotes.json` sidecar via `usePersistentState` (load-on-docId, debounced persist, docId-reset so orphans don't bleed cross-doc). Feeds the Footnotes + Search panels. Since 318838cf this per-doc store is the SINGLE store on BOTH flag paths, UNCONDITIONALLY (un-bundled from the gated reconciler); `virgil:inline-atom-lifecycle` now gates only the WRITER — flag OFF, a per-pane, docId-routed legacy event web (`useFootnoteOrphanBridges`) writes it; flag ON, the bus-driven `useInlineAtomLifecycle` reconciler does |
| `useRevisions` | Revision/comment threads |
| `useSuggestions` | AI line-edit suggestion state |
| `useArchive` | Archived snippets |
| `useTodos` | Todo items. Exposes exactly ONE way to remove a todo: the bulk `archiveDone` was DELETED in task 681 (a raw `items.filter(...)` the EditorPane unbridge seam never saw, because that wiring is applied per EXPORTED DOOR by name — so a done todo whose AI box was ticked left its `ai-requests.json` row open forever and every `/editor/review` drain re-served a card that no longer exists). "Clear done" now composes the already-wired single delete at the seam through `makeUnbridgingBulkDelete` ([src/cards/lifecycle/unbridging-delete.ts](../../src/cards/lifecycle/unbridging-delete.ts)), one card at a time through the one lifecycle executor, with the same `terminate`-mode unbridge and settle step. Renamed down the prop chain (`clearDoneTodos` / `onClearDone`) because "archive" is the *reversible* `setArchived` flag on the same card. CI: [card-removal-door-census.test.ts](../../src/cards/__tests__/card-removal-door-census.test.ts) enumerates from the dangerous side — every place a bridging hook drops entries from its own collection must be declared WIRED or EXEMPT |
| `useReports` | Reports (report + report-request cards) |
| `useExamples` | Expex example blocks (harvests `exampleBlock` nodes from editor doc) |
| `useCutter` | Cut items |
| `useWordCount` | Live word counts by section |
| `usePoppedCards` | Floating card registry (reads `prefs.poppedOutCards`) |
| `useLinkHighlight` / `useAnchorHighlightReconciler` / `useLinkedAnchorReconciler` | Three-surface hover/selection coupling (text, margin icon, panel card). 930b9f6 collapsed the previous `useCardHoverHighlight` + `useCardSelectionHighlight` pair into one idempotent reconciler (`useAnchorHighlightReconciler`) that paints both `data-card-hovered` and `data-card-selected` in a single pass. Mode B `.linked-anchor` spans get their own thin reconciler (`useLinkedAnchorReconciler`). All in `src/links/_shared/`; see `main-text.md` → Highlight coupling for the full set including the `useTextHoverBridge` + `usePanelCardHoverBridge` event listeners and the per-doc `cardStore` (the old module-global singleton was scoped per-doc in bcee4b4c via a `CardStoreProvider`/`useCardStore()` context seam + `getCardStore(docId)`/`disposeCardStore` registry, killing cross-doc selection/hover/halo bleed under multi-doc keep-alive) |
| `useViewPrefs` | Panel visibility, layout state, placements, and (since b706812) the per-view editor-decoration prefs that previously lived as EditorLayout local state — marginalia visibility (`showMarginalia`, `hiddenMarginaliaTypes` — since task 672 its `members` ARE `HIDEABLE_MARKER_TYPES` from [src/cards/marker-meta.ts](../../src/cards/marker-meta.ts), i.e. `ALL_MARKER_TYPES` minus the declared `NON_HIDEABLE_MARKER_TYPES` opt-out (`error` alone, an Errors-panel diagnostic rather than user-authored marginalia); the `MarginaliaType` union is DELETED (the stored pref is `MarkerType[]` straight from the card SSOT, so `EditorPane`'s `as MarginaliaType` cast — the one thing keeping the divergence from being a type error — is gone), and `memberLabels` is an exhaustive `Record<HideableMarkerType, string>` so a new marker type can't ship invisible; the menu used to offer note/archive/todo only, leaving revision/cut/report/error unhideable, and now offers every type but the declared `error`), section indicator / heading labels, divider levels + width, omni-view categories + per-side hide-all, four-sided editor margins (`editorLeftMargin` / `editorRightMargin` / `editorTopMargin` / `editorBottomMargin`; top/bottom added in d211464 so the page padding is fully four-sided), and (per-card archive) the per-panel `cardArchiveView` map (`active`/`archived`/`all`, via `setCardArchiveView`) — `suppressArchiveAtomWarning` was RETIRED in task 492 onto the confirm door's `suppressId` and is now a legacy key `loadPrefs` folds across once — plus the opt-in `cardOutlineChrome` toggle (default OFF; paints a colored card outline via `body.card-outline-chrome`, 246fc50f). These view-level prefs are declared in the typed [src/lib/view-prefs/registry.ts](../../src/lib/view-prefs/registry.ts) (`VIEW_PREF_REGISTRY` — see the SSOT table; `useViewPrefs` extends `RegistryPrefs` and spreads `REGISTRY_DEFAULTS`), which is why they all survive reload now. Cross-window mirroring + the personal-prefs promotion script still key off [src/lib/dev-prefs-registry.ts](../../src/lib/dev-prefs-registry.ts) — whose promotion WHITELIST is, since task 676, checked against a derivation of BOTH halves of the global vocabulary: `REGISTRY_PROMOTED_GLOBAL_KEYS` (registry `scope: "global"` minus `promote: false`) ∪ `STRUCTURAL_PROMOTED_GLOBAL_KEYS` ([src/lib/view-prefs/structural-globals.ts](../../src/lib/view-prefs/structural-globals.ts) — the structural globals: page geometry, strip placements, omni filters, print options — carrying the SAME `promote` fact, with a REQUIRED `why` on an exemption). A forgotten structural global is now a test failure rather than a silent omission (`codePaneRatio` was one), and a whitelisted key must actually be declared in `useViewPrefs.defaults.json`. On load, `scrubUnknownPanelIds` ([dropUnknownPanelIds.ts](../../src/hooks/dropUnknownPanelIds.ts)) subtractively drops any panel id that is no longer in its carrier's live registry SSOT, so a retired panel (e.g. `quotations`) can't round-trip through saved prefs or the defaults snapshot; its ADDITIVE twin `applyPanelRenames` ([rename-panel-id.ts](../../src/hooks/rename-panel-id.ts), task 275) makes a panel rename a fact about an ID rather than about one carrier. **Both halves read ONE census** since task 675: `PANEL_ID_CARRIERS` in [src/hooks/panel-id-carriers.ts](../../src/hooks/panel-id-carriers.ts) — a row per persisted carrier stating its SHAPE *and* its VOCABULARY (which of `PANEL_REGISTRY` / `OMNI_PANELS` kinds / `PRINT_PANELS` keys may keep an id it holds), so a new collection-shaped layout field is a compile error until classified and one classification enrols it in both operations. Before that the subtractive half was four hand-inlined functions over three hardcoded allowlists, so `panelHeights` / `panelModes` / `floatPositions` / `cardArchiveView` reached the loader with no key check at all; `panelWidths` is keyed by `Side` and is declared NOT scrubbed (a stated `null` answer, not an omission). The loader's specialised passes stay, each enforcing a rule the generic walk has no business knowing — `filterPlacements` / `filterPrintPanels` run POST-merge so a retired id baked into the shipped defaults is scrubbed too. **Stored bytes become live prefs through ONE door** (task 677): `normalizeGlobalSlice(raw)` — renames → scrub → presentation-pod strip → placement merge → `printOptions` deep merge → print/omni filters → registry value coercion — TOTAL over `GLOBAL_PREF_KEYS`, idempotent, malformed-safe, called by `loadPrefs` AND by `rereadGlobal` (the peer-sync handler on the bus event, the native `storage` event and the same-window listeners). `rereadGlobal` used to spread raw parsed bytes into live state, which `persist` then wrote back out — the live route by which a retired panel id re-entered a window that had just scrubbed it. What stays OUT is stated rather than forgotten: `applyPanelSideMigrations` is one-shot (recorded in `appliedPrefMigrations`), and every window-scoped repair has no second caller because per-window blobs are never broadcast. The registry is also a VALIDATOR now (`coerceRegistryPrefs`): toggles must be boolean, enums must be in `values`, sets are FILTERED to `domain ?? members` — `SetDef.domain` exists because `members` is the MENU vocabulary and filtering against it would drop the `error` marginalia type and the `report` highlight type that stored blobs may legitimately carry. **One fault domain per stored blob** (task 674): the disposable window blob (`virgil-view-prefs/window/<id>`) and the durable global blob (`virgil-view-prefs/global`) are each read by `readPrefBlob(key)` behind its OWN boundary — absent, unparseable, or parseable-but-not-a-plain-object all yield `{}` and leave the other blob untouched. One truncated per-window blob used to throw inside the shared try/catch, drop the whole load to `DEFAULT_PREFS`, and have the user's very next gesture serialize those defaults over a perfectly valid global blob. An unreadable blob is QUARANTINED to `<key>.corrupt` (guarded on the target not already existing; a failed copy never fails the load) rather than left to be overwritten, and the per-window GC skips quarantine keys so a copy can't claim an index slot and evict a live window's layout. A retired pref key is likewise scrubbed from the blob on the first load after upgrade via `RETIRED_PREF_KEYS` (`editorSplit` / `editorSplitRatio` / `suppressArchiveAtomWarning`, joined in task 678 by `poppedOutOrigins` — a persisted field with four maintainers and no reader, retired along with the `Half` type it was the sole referent of, since the ≤2-panel split model it named was replaced by the ordered `dockStack`). The same module's `clampStack` sanitizes the band-stack rework's per-side `dockStack` (drops omni/blank sentinels, unknown/duplicate/cross-side ids, truncates to the stack ceiling — which it takes as a REQUIRED argument threaded from the engine's `MAX_STACK`, never its own literal, so the loader can't own a second ceiling); the panel-layout model moved from the old `activeLeft`/`activeRight` split sentinels to an ordered `dockStack` + `collapsedLeft/Right` + `blankLeft/Right`. **Writes go through THREE registry-driven doors** (task 274) — `setViewPref(key, value)`, `toggleViewPref(key)`, and the `set`-kind door `toggleViewPrefMember(key, member)` that was missing, whose absence had kept minting hand-written twins: ten of them (four boolean togglers, two enum setters, three includes/filter/append copies, one value-setter wearing a setter's clothes) are DELETED, and with all three kinds covered a pref travels by KEY, collapsing `EditorPaneMenuBarBundle`'s twelve per-pref read fields to one `prefs` and MenuBar's twenty-five view props to five. A new pref is now ONE registry row and zero edits elsewhere; a `menuRowId` on each `ToggleDef` builds the View-menu Display block. CI census: [view-pref-writer-ssot.test.tsx](../../src/hooks/__tests__/view-pref-writer-ssot.test.tsx) — no registry key may appear as a literal object key in `useViewPrefs.ts` (the doors write a COMPUTED key, so a literal one is a twin by construction) |
| `view-prefs-derived` (not a hook) | Leaf module of pure derived read-helpers over `ViewPrefs` (`dockedSideOf` / `dockStackTop` / `isPanelDocked`) for the band-stack model. Imports ONLY types from `useViewPrefs` (no runtime cycle / heavy storage chain), so route-derivation code paths import them from here; `useViewPrefs` re-exports them |
| `view-prefs-dock` (not a hook) | The WRITE twin of the leaf above, and the dock-stack engine's ONE home (task 273): `placeInStack` / `removeFromStack` / `closePanel` / `closeAllPanels` / `floatOpen` / `undockToFloat` / `openInMode`, plus `MAX_STACK` + `MIN_BAND_PX` (re-exported from `useViewPrefs` for existing importers). Pure and React-free — same import discipline as `view-prefs-derived` (types only from `useViewPrefs`, so no runtime cycle), so the engine unit-tests without jsdom or the storage mocks. EVERY dock setter in `useViewPrefs` routes through it; the hook body may not spell `dockStack:` / `panelMRU:` / `poppedOutPanels:` itself (CI census in [view-prefs-dock-engine.test.ts](../../src/hooks/__tests__/view-prefs-dock-engine.test.ts)). See [The consolidation half](laws/a-registry-earns-its-name-by-being-read.md) |
| `useMarginEdit` | Margin edit-mode state machine (d211464). Keyed on `Margins = Record<MarginSide, number>` with axis-lookup tables (`MARGIN_AXIS`, `MARGIN_OPPOSITE`, `MARGIN_MIN`, `MARGIN_CSS_VAR`) so one drag handler covers all four sides; adding a fifth side is one table-entry addition. Drives the four guide lines + glowing-blue Save/Cancel pill (see `ui-chrome.md` → Reading frame & margins) |
| `useFloatingMenuPosition` | Shared viewport-clamping placement helper (f7461e2). Action menu / Highlights submenu / DragHandleMenu route through it so popovers never bleed off-screen. RAF-batched + recomputed on scroll/resize. Two opt-in capabilities for the `<Menu>` primitive (off by default, existing callers byte-identical): `maxHeight` (clamps to the space available for the chosen placement + `overflowY:auto` so a tall list scrolls), and `trackAnchor` (a RAF-coalesced capture-phase scroll/resize re-anchor thunk — slash caret / bib-picker / tab-plus scroll re-reads unify onto it) |
| `usePersistentState` | IndexedDB persistence abstraction. `update()` debounces `persist()` ~300ms (2dc963d) with flush-on-unmount + flush-on-docId-change so safety isn't traded for the keystroke-cascade savings |
| `useInTextPositions` | Omni-view positioning. CHIP-B: a card whose first pid isn't a live anchorable node is skipped; deciding a *stored* uuid is dead + needs snapshot/mark recovery is no longer this helper's job — that's the anchor-recovery SSOT (`resolveCardAnchor`), run upstream by the gutter-marker builder, so the pids reaching this helper are already resolved-or-orphan-flagged. Since task 327 the exact-read/interpolate gate is decided on the anchor's document POSITION against a band the VIEW reports (`resolveVisiblePosBand`), never on the card's own last-committed top — a gate that read the estimate it exists to correct had an *absorbing* error state (a wrong retained top classified out-of-band forever, and stayed `DEFAULT_ENTRY_HEIGHT`, hence the arithmetic-exact overlap). The probe fails OPEN and always lands on screen. Since task 370 every trigger (cold mount, `document.fonts.ready`, both ROs, the structural bus, `focusout`, a dirty keep-alive re-show) enters ONE door on the shared convergence controller ([src/lib/editor-geometry/settle-convergence.ts](../../src/lib/editor-geometry/settle-convergence.ts)) — it keeps measuring until TWO consecutive passes commit nothing past the task-328 hysteresis, on a wall-clock budget that a chain cannot refresh; the retired `scrollHeight` proxy was a TOTAL, blind to inner layout. Probe: `window.__settleConvergenceStats()`. Task 544 added the **cascade floor**: the omni bin stack lives in the column's STICKY band frame, which floats over the scrolled pod, so the cascade takes that frame's measured occupancy as its floor (`readStickyOccupancyFloor` in [editor-layout/omni-bin-slot.ts](../../src/components/editor-layout/omni-bin-slot.ts), measured AT SCROLL ZERO so the answer is scroll-invariant, riding the measure pass + the per-card RO + `measureVersion`) — the forward pass binds to it and the backward pull does not. See [The refinement gate](laws/editor-geometry.md), "The settle half" and "The one-surface half" |
| `useLivePosResolver` | Resolves a stored UUID / anchor index to a live in-doc position off the `DocStructureBus` snapshot (not a re-walked array), so jump-to / search-highlight read current positions without drifting on the keystroke that wraps a line (T5). Also serves the Omni cascade: paragraph-anchored cards (note / todo / cutter / revision / report / archive) now carry an `anchorUuid` on their `OmniItem` ([src/panels/_shared/types.ts](../../src/panels/_shared/types.ts)) and resolve a LIVE pos from `structure.blocks.get(uuid).pos` instead of the stale baked `pos` — fixing the "note cards flash/stack at the top while typing" bug (the map is built via the sibling `buildParagraphAnchorMap`). `resolveLiveBlockRange` is the range-anchored sibling (block uuid + intra-block char offset) for main-text search highlights. Its across-an-async-gap cousin is [src/lib/tiptap/block-address.ts](../../src/lib/tiptap/block-address.ts) (task 285): a surface that renders from a debounced SNAPSHOT and writes on a later gesture (the Outline's reorder / scroll / focus-band writes) names its target by `BlockAddress` (`uuid` + a pre-hydration `index` fallback) resolved against the LIVE doc at apply time — a hydrated address resolves by uuid ONLY, and an unhydrated one degrades for a READ but is REFUSED for a WRITE. Extents are re-derived live (`sectionExtentFromHeadings`), never carried |
| `useReconcileModeAAnchors` | Shared factory wrapping a card-source hook's `usePersistentState` `update` setter into a uniform `reconcileAnchors(editor)`, called once per doc-open. Repairs a Mode-A margin card whose stored paragraph UUID was lost to a reload race (the `%!v:` write didn't round-trip): funnels every card through the `resolveCardAnchor` ladder (uuid → mark → rung-2b → snapshot → orphan) in [src/links/resolve-card-anchor.ts](../../src/links/resolve-card-anchor.ts) + the `reconcileCardToResolved` mutator (backfills the self-healing snapshot, strips dead-mark residue, or rewrites the dead pid to a live same-text paragraph). Load-only (`buildResolveIndex` is O(doc), one index per pass), idempotent, never on a keystroke. Wired into `useNotes` / `useTodos` / `useArchive` / `useReports` / `useRevisions` / `useCutter` |
| `usePristineCardManager` | Tracks freshly-created cards so they auto-discard if closed without edits; exposed via the `pristine-cards` context |
| `useDocumentStyle` | Per-document preamble preset. Reads/writes the style id to the doc settings sidecar and rewrites the preamble in place when the user picks a new style |
| `useStyleLibrary` | User-curated style entries shown in `ManageStylesModal` (apply / edit / duplicate / delete / save current preamble as a new entry) |
| `useZenMode` | Zen-mode pref + chrome-hide orchestration. Hides Virgil bar / strips / panels and extends the editor to window edges; restores on exit |
| `useHelperMode` | Helper-mode toggle — sets `data-helper-mode="on"` on `<body>`, enabling CSS hover callouts on all `[data-helper]` elements |
| `usePreservationNotice` | `useSyncExternalStore` reader over the per-doc preservation-refusal store ([src/lib/preservation-notice.ts](../../src/lib/preservation-notice.ts)) — a gate refusal is published once, write-protects the doc until acknowledged, and surfaces as the topbar `PreservationNoticeBadge` (task 357) |
| `usePomodoro` | `useSyncExternalStore` reader over the Virgil bar's timer session store ([src/lib/pomodoro-timer.ts](../../src/lib/pomodoro-timer.ts), task 354). Notifies on real TRANSITIONS only (start / pause / complete / duration / open) — never per second: the seconds readout is local state in the widget leaf, which is what keeps a running timer from repainting the memoized bar. The clock is timestamps (`endAt` / `remainingMs`), not accumulated ticks, so background-tab throttling costs display freshness and never accuracy; a wall-clock watchdog in the `DiskWatcher` exemption class lands the completion behind a closed widget. Nothing here touches `localStorage`, so the cross-window rule below does not reach it — one timer per window, not persisted across reload (stated in the store header) |
| `useCollab` | Turn-taking collaboration state machine: pen ownership, heartbeat, polling of `collab.json` sidecar, per-card focus claims, cursor-paragraph presence broadcast |
| `useRecentlyAddedTracker` | One-slot-per-kind tracker for just-created cards so panels can sort them to the top; auto-cleared when selection moves away |
| `useLatexCompile` | SwiftLaTeX pdfTeX compile + parsed-error extraction. On success, persists the resulting PDF next to the `.tex` (`pdfFilenameFromTex`) so the in-app PDF view (`library/components/PdfView.tsx`) can re-render without recompiling. Task 454 made a long compile *survivable*: every package the worker fetches is streamed to IndexedDB the instant it is cached (so a timed-out compile keeps 100% of its downloads and the next attempt continues from a warmer cache), and a per-doc progress store ([src/lib/compile/compile-progress.ts](../../src/lib/compile/compile-progress.ts)) drives `CompilePaneStatus` in the PDF pane — see [The compile path](laws/the-compile-path.md) |
| `useMyPapers` | Global "My Papers" list shown in the Library's My Papers pod (IndexedDB single shared record + BroadcastChannel sync). Decoupled from open document tabs: opening a doc anywhere never auto-adds, and removing a row never closes a tab |
| `useStack` | Cross-doc visual clipboard at the editor's bottom-left. Versioned envelope in localStorage (`virgil-stack-v1`), 200-item FIFO cap, cross-tab sync via the `storage` event. See `ui-chrome.md` → Stack |
| `useScrollActivityTracker` | Auto-hide scrollbars: paints `data-scroll-active` on the container while the user is actively scrolling/hovering, fades when idle |
| `useUpdateAvailable` | Service-worker update store (`UpdateState`: `none` / `waiting` / `activated-elsewhere`, task 610) that drives the Virgil bar's update banner. The waiting worker's version is the build's own content hash, stamped into `out/sw.js` at postbuild by `scripts/stamp-service-worker.mjs` (task 611) — no hand-bumped literal |
| `useAutoAddLibraryEntriesForCitations` | Watches new citation keys in the doc and auto-adds matching entries from the user's Virgil Library into the paper's bibliography (after the bibliography-search redesign in 91f253c / 240bfda) |
| `useEditorUIState` | Per-doc UI state hook factored out of EditorPane — tracks click-time selection / focus-mode / hover state etc. that several sibling hooks consume |
| `useResolvedFigureUrl` | Resolves a figure block's `source` to a renderable blob URL: looks up the cached raster by source fingerprint, rasterizes the source on miss (PDF → webp via `pdfjs-dist`; PNG/JPEG pass-through), and manages the blob-URL lifecycle |
| `useViewportFrame` (in [src/lib/editor-geometry/use-viewport-frame.ts](../../src/lib/editor-geometry/use-viewport-frame.ts)) | React adapter for the geometry service's viewport frame (`editorRight`, `scrollTop`, pod rect, portal context) so the placement overlays (`SelectionActionsMenu`, `TextObjectGrabHandle`, `PendingChangePill`, `LiftHost`) place themselves without re-reading layout on every keystroke (a fifth reader, `Marginalia`'s `useLaneCols`, takes the service's viewport channel directly for the marker lane's resolved per-side column count — tasks 214/325) — the successor of the deleted per-consumer `useEditorViewportCache` (perf Wave 2 C7: 4 private ROs per pane → the service's one engine) |
| `useCardLifecycleReconciler` | Takes this doc's per-doc `store: CardStore` (resolved `getCardStore(docId)` in the EditorPane body, bcee4b4c) and subscribes to the card-lifecycle signal channel ([src/cards/lifecycle/card-lifecycle-signal.ts](../../src/cards/lifecycle/card-lifecycle-signal.ts)), prunes/re-keys that store's `cardStore` slots (selection/hover/expand) on a sidecar-card DELETE or kind-MORPH — the obligation the `DocStructureBus` can't carry for sidecar-backed kinds (report/note/cutter/revision) that have no doc node. Single emitter (`runCardLifecycleEvent` in [src/cards/lifecycle/run-event.ts](../../src/cards/lifecycle/run-event.ts)), single consumer; NOT a bus subscription (fires only on a trash/morph click) |
| `useIdentityBusConsumer` | The single inline-atom `DocStructureBus` consumer (the "+1 not +3"): opens ONE `onAnyChange` subscription, registers T1's id-regen policy, and lets Wave-2 themes register on the returned `IdentityBusConsumer` dispatcher via `registerPolicy` rather than opening their own subscriptions. Behind `virgil:identity-cascade` (default OFF). See the [keystroke-sanctity law](laws/keystroke-sanctity.md)'s permitted-consumer list and the Identity cascade subsystem below |
| `useDocumentInterruption` | `useSyncExternalStore` reader (task 545) that folds the cowork pen, the `DiskWatcher` external-change store, the preservation notice and the save-state channel into the ONE `deriveDocumentInterruption` view every surface renders — the in-card `DocumentInterruptionBanner`, the external-change pill and the cowork pill alike, so the five registers cannot disagree. Provenance at `window.__interruptionStats()` ([src/lib/interruption-log.ts](../../src/lib/interruption-log.ts)) |
| `useAiRequestCardMigration` | Mints Note/Todo cards from unlinked `ai-requests.json` rows and writes THREE sidecars at once, automatically, with no user gesture. Its gate is `sourcesAreAuthoritative(...sources)` — NOT `loaded` (task 679): `loaded` flips on a read that THREW as well as on one that succeeded, leaving the collection at its EMPTY default, so a corrupt `notes.json` gave notes `{cards: []}`, `loaded` true, a migration mint, and `notes.json` rewritten as only the migrated cards — every note in the paper destroyed by a write the user never asked for. The stand-down happens BEFORE the `migratedDocs` claim, so a failed-read session doesn't burn the doc's one-per-session migration chance. The question has one owner and the gate carries the contract in its name |
| `useCitationResync` / `useInlineAtomLifecycle` | Wave-2 policies registered on `useIdentityBusConsumer`'s dispatcher (citation add-resync T5; inline-atom lifecycle T2) — in [src/links/_shared/](../../src/links/_shared/) alongside their `citation-resync-policy.ts` / `inline-atom-lifecycle-policy.ts`. Both gated behind the identity-cascade / inline-atom-lifecycle flags |

## Editor hot-path conventions (2dc963d)

Per-keystroke perf regressions chase one anti-pattern: a synchronous TipTap subscriber that does a doc traversal / LaTeX serialization / forced layout read / IndexedDB write / wide-tree React setter. The 2dc963d refactor codifies the shape of a well-behaved hot-path subscriber:

- **Subscribe to `update`, not `transaction`/`selectionUpdate`**, and gate on `tr.docChanged` so mark-only / selection-only transactions don't trigger a layout-reading recompute (see `TextObjectGrabHandle`). (The EditorLayout focus-mode hide now rides a ProseMirror node decoration — `focusViewPlugin` ([src/lib/focus-view.ts](../../src/lib/focus-view.ts)) stamps `.focus-hidden` on out-of-band top-level blocks, fed the UUID band by `useFocusMode`; this replaced the old injected `<style>` nth-child stylesheet + child-count tracker, and reaches React-NodeView blocks + the mirror pane for free.)
- **RAF-batch any compute that reads layout** (`useMarginalia.compute()`, `Marginalia` host-detection notify, `SelectionDragHandle` placement, `EditorMirror.updateState()`).
- **Debounce React state setters** that fan out via useMemos (`usePersistentState.update()` ~300ms with flush-on-unmount/docId-change). (The former per-keystroke `docVersion`/`editorDocVersion` counters are gone — derived doc products now come from the doc-products pipeline's interactive/idle tiers, not a wide-tree setter; see [src/lib/doc-products/](../../src/lib/doc-products/).)
- **WeakMap-memoize per-node serialization** keyed on the immutable PM node (`getExamples()` runs once per edited example block instead of N-per-keystroke).
- **`useRef` instead of `useState`** for values that are only read on transitions (`lastEditTime` → `pdfStale`); avoids a full re-render per character.
- **Lazy hook activation**: `useLibraryMasterBib(enabled)` — pass `false` until the doc actually has unresolved citation keys; the hook stays mounted (no conditional-hook violation) but skips the citation-js parse. `catalog-store` polling is refcounted via a single shared interval, zero polling when no consumers are mounted.

## Persistence layers

### File System Access API (the disk)

**Single boundary: [src/lib/storage-fsa.ts](../../src/lib/storage-fsa.ts).** Every disk read/write goes through here. If you're tempted to call `handle.getFile()` or `writable.write()` anywhere else, route it through storage-fsa instead.

Every writer funnels through `enqueueDocWrite`, and that funnel is where the **library-paper write guard** lives (153f714 / b93739b, renegotiated by task 556): a `library-paper:<citekey>` doc opened in the Library Reader persists ONLY the sidecars DERIVED from `READER_CHROME.editableCardKinds` — today `notes.json`, so a note annotation written while reading lands in the paper folder — and every other write (the `.tex`, the bundle, the bib, the PDF, figures, every other sidecar) is refused. The derivation is ONE import-free leaf, [src/lib/host-writability.ts](../../src/lib/host-writability.ts), read by the funnel in BOTH backends and by the UI-layer permit `isSidecarWriteAllowed`, so the chrome and the storage layer cannot disagree about what the Reader may write. Reader read handles are resolved **on demand** (1c2a6a0) via the `library-paper:` directory-handle fallback rather than requiring per-doc registration.

**Cross-window doc ownership is WRITE-ORDERED (task 596).** Two windows on the same paper arbitrate through a Web Lock; holding it is what makes this window's own writes cheap (`withDocLock` short-circuits whenever this window owns the doc). So `releaseDoc` DRAINS first and drops the hold second — the ordering lives inside the door rather than being hand-re-derived at each of its four call sites, and the drain is INJECTED (`registerDocDrain`, called once by `@/lib/storage`) because `drainDoc` lives in storage and storage's FSA backend imports `withDocLock`. Handing a paper between windows is likewise ONE door, `claimDocWithHandoff` ([src/lib/multi-window/handoff.ts](../../src/lib/multi-window/handoff.ts)), with the confirm dialog injected. See [per-doc services under multi-pane keep-alive](laws/per-doc-services-under-multi-pane-keep-alive.md) → "The WINDOW half".

**`references.bib` has its own serialized read-modify-write door (task 558).** `mutateBib` in both backends is the bib twin of `mutateSidecar` — the base read runs INSIDE the queued, doc-locked critical section as the write, and it is deliberately non-stamping so the external-change badge keeps its baseline — and the whole-snapshot `writeBib` is RETIRED. [src/lib/project-bib.ts](../../src/lib/project-bib.ts) is the ONE authority above it (`mutateProjectBib`: the parse/serialize pair + the post-write publish); every writer — the Library drop and remove, and all five `useCitations` mutators — enters it with a pure `BibEntry[] → BibEntry[] | null` mutator applied twice (to the view at once, to the disk under the lock), uids minted ONCE outside the mutator, a stale edit DECLINING rather than resurrecting, and the view converging on the published result.

On load, `readDocBundle` assigns paragraph UUIDs and now writes the re-stamped `.tex` (+ `virgil.json` sidecar) back to disk opportunistically (`writeReStampedTexOnLoad`, 10e86d6) — parity with storage-dev, so a UUID minted for a paragraph that lacked a `%!v:` marker is durable BEFORE the editor mounts (not volatile until the next 1500 ms autosave; closes the production-only anchor-orphan-on-reload window). Fire-and-forget, routed through `enqueueDocWrite` with the active-handle / pipeline staleness guard so a read during a doc switch can't write to the wrong file, and preamble-preserving like `writeDocBundle`. Since task 350-D this automatic write is GATED: `checkTexPreservation` ([src/lib/tex-preservation.ts](../../src/lib/tex-preservation.ts)) weighs the preamble and the body SEPARATELY in WORD tokens (Virgil's own `%!v:` / `\v*` markers projected away on both sides) and a shrink is a REFUSAL — the file is left byte-identical and the refusal is recorded to the preservation-notice store.

On a fresh paper mount, `readSidecarBundle(docId)` (`storage-fsa.ts:504`) coalesces what used to be ~17 per-sidecar reads into ONE `virgil/` directory acquire — cache-first, reading every `ALL_SIDECAR_FILENAMES` entry in a single parallel pass. Since task 363 that list is DERIVED (the `mount: true` subset of [src/lib/sidecar-value.ts](../../src/lib/sidecar-value.ts), the SSOT for every file Virgil writes and what each one is WORTH — plus, since task 417, a `store: "disk" | "local"` column the four sidecar doors in both backends route on); `sidecar-files.ts` is a re-export under its historical name. The `tier` column decides the write cadence (`sidecarWriteDebounceMs`) and how a cloud-sync "conflicted copy" fork is reported — see [The daemon half](laws/cross-window-store-stability.md). This is the L1 layer that made the paper↔Library tab bounce cheap (5f718bd8); per-hook `readSidecar` calls hit the warm bundle instead of re-acquiring the directory each.

Files on disk (per paper):
- `<name>.tex` — the paper (source of truth)
- `<name>.bib` (optional) — bibliography
- `virgil/` folder — sidecars (see below)
- `figures/<name>.<ext>` (optional) — binary assets copied in by the figure picker. **Where a picked asset lands is [src/lib/figures/asset-import.ts](../../src/lib/figures/asset-import.ts)** (task 533), an import-free leaf both backends read: a candidate name that is ABSENT is written, one holding the SAME bytes is reused (no write at all), one holding DIFFERENT bytes is someone else's and the resolver tries `<stem>-2`, `<stem>-3`, … A picked asset never overwrites a byte already on disk — `figures/` has no `.history/` slot and FSA has no trash, so the pre-533 truncating write let Figure 5's `plot.png` destroy Figure 1's silently.
- `virgil/figures-cache/<sha>.webp` (optional) — rasterized output cached by source-content sha. PDF sources rasterize on the fly via `pdfjs-dist`; PNG / JPEG / WebP pass through unchanged. Companion `virgil/figures-cache/index.json` tracks `{sourcePath → sha}` so multiple `\includegraphics` with the same source share one raster. Surface: `readFigureSource` / `readFigureRaster` / `writeFigureRaster` / `deleteFigureRaster` / `readFigureIndex` / `writeFigureIndex` on the storage backend.

### IndexedDB

Used for: user preferences (`useViewPrefs`), tab state, folder handles, doc index. Not for paper content — that's always on disk.

The **doc index** has ONE mutation door, `mutateIndex(fn)` in [src/lib/doc-index.ts](../../src/lib/doc-index.ts) (task 601; read + write in one readwrite IDB transaction — `writeIndex` is gone), and ONE retirement door, `purgeDoc` (task 604: folder/bib handles, emergency mirror + recovery offer, every local-store sidecar slot). **Tab records outlive the page** (task 603): no page event deletes them; a startup `sweepTabRecords` drops only records whose window is not alive — liveness is a per-window Web Lock ([src/lib/multi-window/window-liveness.ts](../../src/lib/multi-window/window-liveness.ts)), which replaced the old windows registry + heartbeat. In `useFiles`, every open funnels through `admitDoc` and every departure through `retireOpenDoc`; the skill-bundle auto-sync is an effect on the `currentDocId` edge (task 602).

### Sidecars in `virgil/`

All are JSON files. Schemas in [src/lib/types.ts](../../src/lib/types.ts).

| Sidecar | Purpose | Surfaced as |
|---|---|---|
| `virgil.json` | Per-paragraph metadata: titles, fingerprints | Omni view, search breadcrumbs |
| `suggestions.json` | AI line-edit proposals | **Revisions panel** (suggestion cards beside comments; `y`/`n`/`s` keyboard controls); progress bar lives in the Revisions panel header |
| `revisions.json` | Comment threads (anchored or paper-wide); legacy `GeneralRevision`/`TextRevision` shapes fold forward to one `Comment` type on read | Revisions panel |
| `ai-requests.json` | Queued requests for an agent to resolve | Per-panel "ask" affordances (Footnotes, Notes, Reports, Citations, Todo). Also fed by the **AI request bridge** ([src/lib/ai-request-bridge.ts](../../src/lib/ai-request-bridge.ts)), which collapses per-card sticky `aiRequest:true` flags (the 7 kinds whose `CARD_REGISTRY` entry declares `aiRequest` routing: notes / highlights / todos / cutter-comments / revision-comments / report-requests / footnotes — footnotes via `FootnoteRef.aiRequest` in `footnotes.json`, #55a/b) into a single drainable queue with `linkedTo`. Editor-side skills under `editor/` (see [editor/AGENTS.md](../../editor/AGENTS.md)) drain it. **Three writers, one authority** (task 220): every in-app mutation goes through `mutateAiRequests` ([src/lib/ai-requests-store.ts](../../src/lib/ai-requests-store.ts)), which read-modify-merges over the on-disk list inside one serialized write via `mutateSidecar` ([storage-fsa.ts](../../src/lib/storage-fsa.ts)) and publishes on success; `useAiRequests` re-hydrates off the `SidecarWatcher` so a peer window (or an `/editor/*` skill writing the file directly) converges instead of being clobbered. **The INBOUND direction got the same treatment in task 682.** Two of the three writers sit outside the type system (the Python skills read-modify-write the file on disk while the paper is open; it is hand-editable besides), so `AiRequest.kind` is typed `AiRequestKindOnDisk` — the honest type — and every reader that keys a per-kind table must pass through `isAiRequestKind` ([src/lib/ai-request-kind.ts](../../src/lib/ai-request-kind.ts): the vocabulary stated once, compile-pinned exhaustive in both directions, with a live-source parity pin against its Python twin `_common.AI_REQUEST_KINDS`). `normalizeAiRequestRows` is the read gate, applied at the two CONSUMER exits (`readAiRequests`, and the list `mutateAiRequests` publishes) and deliberately NOT to the merge base, since normalising there would write the normalised value back and let an older build overwrite a newer one's kind. It makes every row renderable while overwriting no value that carries meaning — an off-union kind survives verbatim and `panelDisplayKind` resolves it at list-build time to the window's `unknown` row, where before the next dereference THREW and took the user's view of every other request in the paper with it. `useAiRequests` also exposes `loadError` beside `loaded` (task 679), because `loaded` flips on a read that threw as well as on one that succeeded |
| `notifications.json` | Per-doc inbox of completion entries appended by editor-side skills (Python: `editor/scripts/_common.py`) | Written on every skill completion; the client-side toaster hook (`useDocNotificationStream`) was never wired to a UI host and has been removed (task 033) — not yet surfaced in-app. Shape: `DocNotification` in [src/lib/types.ts](../../src/lib/types.ts) |
| `bib-review-requests.json` | Per-entry bibliography field/note reviews. `BibReviewRequest.entryUid` (T1) targets the durable `BibEntry.uid` so a citekey rename re-points nothing; `bibKey` is a human-readable mirror / legacy fallback | Bibliography cards |
| `annotations.json` | Per-entry bibliography annotations. Legacy flat `citekey → html` (`AnnotationsState`); under `virgil:identity-cascade` migrates non-destructively to uid-keyed `AnnotationsStateV2` (`{ v:2, byUid, orphanByKey }`) so a citekey rename never strands a note (BIB-A2-01) | Bibliography annotation editor |
| `footnotes.json` | Footnote bodies (Tiptap `JSONContent`) + the per-card `FootnoteRef.aiRequest`/`archived` flags. Numbering and in-text positions derive live from the `.tex`, NOT here | Footnotes panel / omni; [src/hooks/useFootnotes.ts](../../src/hooks/useFootnotes.ts) |
| `orphaned-footnotes.json` | Durable orphaned-footnote bodies (versioned `{ version:1, orphans }`; absent ⇒ empty) | [src/hooks/useOrphanedFootnotes.ts](../../src/hooks/useOrphanedFootnotes.ts); Footnotes + Search panels |
| `editor-state.json` | Last-edited paragraph + scroll offset + collapsed section folds. Schema moved from PM positions to paragraph UUIDs in 7d702de so structural edits between sessions don't lose the target. Declared VIEW state (task 363), so its write coalesces at the view cadence rather than firing on every scroll pause. **Since task 417 it is `store: "local"`** — it lives in this browser's IndexedDB ([src/lib/local-sidecar.ts](../../src/lib/local-sidecar.ts)), not the synced `virgil/` folder, with a one-time migration on the first local miss; per-machine view state was the loudest conflicted-copy base in the measured Dropbox folder and holds nothing a second machine wants. `readDocBundle` no longer reads it | Restored on reopen — scrolls back to the last paragraph and reapplies folds |
| `cutter.json` | Cutter cards (comments + suggestions) and optional word-count goal | Cutter panel; [src/hooks/useCutter.ts](../../src/hooks/useCutter.ts) |
| `collab.json` | Turn-taking collab state: pen holder, heartbeat timestamps, per-user presence entries (cursor paragraph, card focus claims) | [src/hooks/useCollab.ts](../../src/hooks/useCollab.ts); types/constants in [src/lib/collab.ts](../../src/lib/collab.ts) |
| `document-settings.json` | Per-document settings (currently: `style` id for the preamble preset) | `useDocumentStyle` reads/writes; schema in [src/lib/document-settings.ts](../../src/lib/document-settings.ts) |

Agents never touch this app — they read the same `.tex`/`.bib` and write these sidecars. Virgil polls/watches and surfaces changes.

### localStorage stores + the cross-window rule

Prefs that aren't per-document live in `localStorage` behind small module-global stores consumed through `useSyncExternalStore` (panel colors, panel typography, Outline view prefs, the Stack, …).

> **A store that caches a localStorage snapshot at module scope MUST re-hydrate on the native `storage` event.** Multi-window is first-class (`openNewVirgilWindow`), and the common store shape — hydrate ONCE behind a `loaded` latch, then serialize the WHOLE snapshot on every setter — is silently unsafe without it: window B never learns about A's write, so B's snapshot is permanently stale and B's next write clobbers A's change from that stale base. The loss is silent and the two windows disagree until one reloads.

The listener contract lives in ONE place — [src/lib/cross-window-storage.ts](../../src/lib/cross-window-storage.ts) (`subscribeToStorageKey(key, onChange)`) — because it has two guards that are easy to get subtly wrong: foreign keys must be ignored, and `key === null` is a `clear()` that must be honored **only** when `storageArea === localStorage` (a peer's `sessionStorage.clear()` fires with a null key too). On the event, re-read through the store's own parse/validate path and notify — one validation path, so a peer's blob is filtered exactly like a local one.

The same module carries three companions for the **hook-state** variant of the shape (state in `useState` rather than a module global): `useStorageKeySync(keys, onPeerChange)` puts the listener in an effect (multi-key, ref-held handler, content-keyed so an inline array doesn't re-subscribe), `writeStorageIfChanged(key, value)` makes a write idempotent, and `useMirroredDraft` (tasks 628/629) is the DRAFT-mirror door that gives the localStorage family the sidecar family's two guarantees — a debounced write cancelled on teardown still lands, and a peer's change is adopted rather than clobbered from a stale base. The second exists because a store that persists from a *state-watching effect* turns a peer sync into a write — B syncs from A, B's effect persists, A hears it, syncs, persists… a ping-pong that never settles. Skipping the unchanged write kills it on the first bounce (and stops a no-op re-render from waking every other window). Persist from the setters where you can; route through `writeStorageIfChanged` where you can't.

Riding it today: `panel-theme.ts`, `panel-typography.ts`, `outline-prefs-store.ts`, `useStack`, `useStyleLibrary`, and — since task 179 — `usePreferences`, `useZenMode`, `pref-links`, `useWordCountConfig`, `useLibraryTabs`, `library/lib/view-session-store`, and the `ActionsMenuPanel` palette. Since task 599 also `useViewPrefs` (its `global-pref-changed` bus in `src/lib/multi-window/bus.ts` stays the fast path, the `storage` event is the floor), `useHelperMode`, `BugReportWindow`'s and `AIWindow`'s drafts (both on the `useMirroredDraft` door) and `useNotificationStream`'s seen-at mark — the guard now runs a store-shape census (every shipped file that both reads and writes localStorage subscribes or sits in a checked ledger). Also cross-window-safe by other means: `style-library` / `library-store` / `row-viewed-store` (no module cache — they re-read per call). `view-session-store` is the one member that needs more than a re-read: its write is debounced 250 ms, so a peer event routinely lands while a local change is still only in memory. It does a three-way merge (base = `lastPersisted`, the snapshot last known to be on disk and advanced only on a write that actually landed; ours = the live session; theirs = the peer blob), recursing per key and letting ours win a genuine leaf conflict, then lets the pending timer flush the merged result — so neither window's edit is lost and both converge. The base is a value snapshot rather than a set of declared dirty paths, because nearly all of this store lives under the single `scopes[""]` key: any path-level granularity would swap a whole subtree and still drop a peer's edit inside it. An unparseable peer blob (corrupt, or a newer `schemaVersion`) is ignored rather than adopted.

`InstallPwaPrompt` (a monotonic `"1"` sentinel) is recorded as degenerate-but-safe.

**A shipped default is DERIVED, never stored** (task 624). `usePreferences` persists only the user's *own* presets: `persistPresets` filters the built-ins out of the write, `loadPresets` drops any stored built-in (migration) and prepends the live `BUILT_IN_PRESETS`. Storing one froze a copy of that day's `DEFAULT_PREFS`, so once `promote-defaults` shipped new colours "Reset to defaults" and picking "Default" disagreed — and the stored copy won, writing the stale values back as the user's prefs. Built-in names are reserved (`isReservedPresetName`, read by BOTH the hook's refusal and the Save button); a stored shadow is RENAMED on read rather than dropped. The picker is an ACTION, not a bound value — it applies and returns to its placeholder — so re-picking the preset you are on re-applies it, and the control never claims a preset is active after an edit or a reset.

### External-change detection

[src/lib/disk-watcher.ts](../../src/lib/disk-watcher.ts) (mounted by `DiskWatcherProvider`) is a per-doc wall-clock `setInterval` poller (~3 s, paused while `document.hidden`, immediate on tab-focus) that detects out-of-band edits to the `.tex`/`.bib` on disk — Overleaf-via-sync, `git pull`, `vim`, Dropbox — and raises the `ExternalChangeBadge` topbar pill ([src/components/ExternalChangeBadge.tsx](../../src/components/ExternalChangeBadge.tsx); amber = clean reload; a **stronger amber, NOT danger** = conflict with unsaved edits — since task 364 red is reserved for the one action that discards a side, and a conflict now offers BOTH doors, each archiving both sides into one `virgil/.history/` slot first via `snapshotConflictSides`, with the order stated once in [src/lib/conflict-resolution.ts](../../src/lib/conflict-resolution.ts)). It compares a content fingerprint owned by the `diskLedger` ([src/lib/disk-ledger.ts](../../src/lib/disk-ledger.ts)) — stamped ONLY on load + writes, never on plain reads (the anti-flicker lynchpin) — against fresh `statFiles` / `readTextFile` reads (`getBibFilename` resolves the `.bib` name). Autosave pauses on a detected change; reconcile reuses `useDocument.refetch()` and never auto-merges. The watcher *pulls* the dirty flag at poll time rather than subscribing to the editor, so it does zero per-keystroke work (a wall-clock-service keystroke-sanctity exemption — see AGENTS.md).

### Emergency mirror (task 391)

Every gate above answers *may this write land?*; this is the half they presuppose. **When a refusal or a pause makes the editor's memory the only copy of the user's work, every door that DROPS memory stays fully armed** — a service-worker reload, a badge reload, a tab close, a crash. That is the 2026-08-19 loss: the clobber guard correctly paused autosave, the writer worked ~70 minutes behind a quiet pill, then clicked the "Update available" banner.

Four pieces, in the order a byte travels:

- **The channel** — [src/lib/unsaved-work.ts](../../src/lib/unsaved-work.ts) (`dirtySince` / `lastLandedAt` / `reason` per doc). Explicitly *not* `saveTimerRef.current !== null`, which is unsound in both directions: the debounce callback nulls its handle BEFORE calling `save`, so a REFUSED write leaves the doc dirty with the flag already cleared. Cleared only by a write that actually LANDED, read off the 357 refusal channel rather than from the absence of a throw.
- **The mirror** — [src/lib/emergency-mirror.ts](../../src/lib/emergency-mirror.ts), a rolling per-doc snapshot of the live MODEL in IndexedDB (the same `virgil`/`kv` store), on a 5 s wall clock, armed whenever the doc is unlanded and either BLOCKED (at once) or merely AGING. A model, not `.tex` bytes, so a restore goes back through the same mount and preservation gates any load does. Keystroke-sane: `noteUnsavedEdit` emits only on the clean→dirty EDGE, and the ticker adds no editor subscription.
- **The doors** — [src/lib/reload-door.ts](../../src/lib/reload-door.ts): flush every doc, RE-READ the channel, force-mirror what still hasn't landed, then report. Because `SKIP_WAITING` reloads every window, the update banner asks EVERY live window first (`prepareAllWindowsForReload`, task 610; a silent window counts as unknown), and a window holding unsaved work survives a peer-started `controllerchange` via `reloadIfClean`. `beforeunload` prompts off the same channel. CI census: no production file may call `location.reload()` outside the door.
- **The recovery** — [src/lib/mirror-recovery.ts](../../src/lib/mirror-recovery.ts) + `MirrorRecoveryBadge` (see ui-chrome.md). A mirror is cleared by exactly one thing, so a mirror that SURVIVES to the next open is by construction work that never reached disk.

Two later halves. **Evidence, never inference (task 557):** `writeDocBundle` returns a `DocWriteReceipt` (`landed` / `blocked` / `read-only`, [src/lib/storage-types.ts](../../src/lib/storage-types.ts)) in both backends, so `save()` reads a write's verdict off the receipt rather than off the absence of a flag or a throw; `dropMirror(docId, ticker, reason)` names its evidence (`landed` | `discarded`); `lastSavedRef` is no longer a rung of `currentModel()` — it is by definition the disk copy, and the pre-557 forced unmount tick mirrored it OVER the work; the unmount takes ONE capture read by both cleanups; and a `library-paper:` read-only answer is given BEFORE the funnel. **The reload door flushes every debounce a document holds (task 559):** `registerPendingFlusher` ([src/lib/multi-window/pending-saves.ts](../../src/lib/multi-window/pending-saves.ts)) is a per-doc MULTI-set in two phases — `settle` (the code pane's 600 ms re-parse, registered by `CodeEditor`) completes before any `write` (the bundle autosave, every `usePersistentState` sidecar, `useEditorUIState`) starts — and `drainDoc` inherits it. `RichTextField`'s 250 ms body debounce is a declared non-registrant (its write is armed inside a lazy `setState` updater).

### Save state (task 392)

The voice for all of the above. Every path that declines to write REPORTS on the one channel, and the topbar renders the one tier ladder derived from it — [src/lib/save-state.ts](../../src/lib/save-state.ts) (`clean` / `pending` / `unsaved` / `blocked`, plus `isSaveTierProtected` and `describeBlockReason`, which names both what a reason MEANS and which flow can resolve it). The manual door is [src/lib/save-request.ts](../../src/lib/save-request.ts): it respects the clobber guard rather than walking past it, and a Save that cannot land ROUTES to the blocking flow instead of re-refusing. The old `useDocument` `saveStatus` — declared, written at six sites, read by no pixel — is retired with it. See ui-chrome.md → "SaveStateBadge". Task 545 gave all of it ONE voice: [src/lib/document-interruption.ts](../../src/lib/document-interruption.ts) derives, per doc, WHAT happened (naming the writer where the app can — the AI is attributed off the pen's release trace rather than reported as "another app or a sync service"), what Virgil is DOING, ONE recommended action phrased as an OUTCOME and the alternatives behind it; the doors are untouched, and the band + every pill read that one derivation.

## EditorPane vs EditorLayout

After Path A 7.8: [src/components/EditorPane.tsx](../../src/components/EditorPane.tsx) is the **canonical editor surface**. Both the main app's doc branch (via [src/components/EditorLayout.tsx](../../src/components/EditorLayout.tsx)) and the Library Reader (via [library/components/PaperRender.tsx](../../library/components/PaperRender.tsx)) mount `<EditorPane>`. EditorPane owns the per-doc hooks (`useDocument`, `useLatexCompile`, `useNotes`, `useTodos`, `useCitations`, `useCollab`, `usePristineCardManager`, …), the docked MenuBar + detached toolbars, the panel rail (icon strip + active panel column), the floating panels block, and the canonical `DockOutline` / `CardLiftOutline`.

EditorLayout shrinks to the **shell wrapper**: tab/file management (`useFiles`), `useViewPrefs` ownership (the bundle is then handed back to EditorPane via the `viewPrefs` prop), top-bar dialogs (Preferences, Fonts, Margins, NewDoc, TexFilePicker, DocumentClassMismatch), the Virgil bar (which reads per-doc state from `paneState` populated via `onPaneStateChange`), the `activePane` switch (paper / library-outer / doc routing), the PDF view branch, and Code view. The PDF branch is a sibling of the EditorPane mount in the doc-branch ternary; Code view became a draggable **split-pane alongside EditorPane** in 8b9659c ([split-with-code.tsx](../../src/components/editor-layout/split-with-code.tsx) + `CodePaneSplitContext`), with code↔TipTap sync via [code-pane-bridge.ts](../../src/lib/code-pane-bridge.ts) (TipTap canonical). The `CodeEditor` (CodeMirror) state still lives in EditorLayout.

The two bundles passed to EditorPane:
- `viewPrefs: EditorPaneViewPrefs` — dock/float-shaped state (panel placements, focus state, undock/redock, card popouts, OutlineHost wiring). **Both** surfaces pass it: the main app from the persisted `useViewPrefs()`, the Reader from `useReaderViewPrefs()` (the same engine in `"ephemeral"` session-only mode), both assembled by the shared `buildEditorPaneViewPrefs(...)` builder. So the rail/dock/float/omni machinery is LIVE in the Reader too (session-only); the only Editor/Reader delta is the named `EditorMutationHandlers` set (mostly no-op in the read-only Reader, but `onScrollToHeading` is real).
- `menuBar: EditorPaneMenuBarBundle` — toggle state + setters, para-nav, dialog openers, detached-toolbar refs from the shell. Reader passes none → docked MenuBar + detached toolbars stay dormant. (This is the one bundle the Reader genuinely omits.)

The `chrome` prop ([src/components/editor-layout/chrome-config.ts](../../src/components/editor-layout/chrome-config.ts)) gates feature visibility per surface: main app passes `FULL_CHROME`, Reader passes `READER_CHROME` (note-only action toolbar, no formatting toolbar, no MenuBar edit items, 7-kind panel whitelist — Search joined in task 485).

**Tab-switch keep-alive.** The main doc editor is hoisted into a `display:none` `KeepAliveSlot` ([src/lib/keep-alive/KeepAliveSlot.tsx](../../src/lib/keep-alive/KeepAliveSlot.tsx)) so the paper↔Library tab bounce doesn't remount it — `currentDocId` stays stable, so the autosave wall holds and the in-memory doc + selection survive the bounce instead of reloading fresh. A hidden editor is inert (keystroke-safe: nothing runs while it's `display:none`). `useKeepAliveLRU.ts` keeps the last N Library readers warm; `visibility-context.tsx` threads the visible/hidden signal so children can pause their own work — and (task 598) carries `usePaneScopedListener` / `useIsVisibleRef`, the door every per-pane `window`/`document` listener goes through so a hidden warm pane ignores events; orphan-card events ([src/lib/tiptap/orphan-events.ts](../../src/lib/tiptap/orphan-events.ts)) carry their editor's `docId` and each card hook answers only its own doc. **Multi-doc keep-alive (568f0d88)** extends this to paper↔paper: an LRU of warm doc panes ([src/components/editor-layout/DocKeepAliveLRU.tsx](../../src/components/editor-layout/DocKeepAliveLRU.tsx)) keeps the last N papers mounted-but-hidden so a tab switch is instant, gated by `isMultiDocKeepAliveOn()` ([src/lib/multi-doc-keepalive-flag.ts](../../src/lib/multi-doc-keepalive-flag.ts), default ON, capacity>1; `active-editor-probe.ts` resolves which warm pane is live — `pickActiveByEditor` / `pickProbeEditor`, the ONE focused → visible → sole → null ladder). **A module-level value that is per-DOCUMENT is a keyed REGISTRY, never a single slot** (task 329): the drop-mode `DropCtx` was a last-writer-wins global, so a warm paper↔paper switch silently pointed drag-and-drop at the wrong document (and an LRU eviction nulled it app-wide). It is now keyed by an owner token, disposed identity-guarded, and a `DropSession` carries the ctx it *began* in. See [Per-doc services under multi-pane keep-alive](laws/per-doc-services-under-multi-pane-keep-alive.md); CI: `dropctx-multipane-registry.test.tsx`. **The DOM half is the same rule** (task 438): a per-PANE marker (`[data-strip-side]`, `[data-panel-column-side]`, `[data-flex-col]`, `[data-stack-frame]`, `[data-dock-slot]`) is resolved through ONE door, [editor-layout/pane-dom.ts](../../src/components/editor-layout/pane-dom.ts) — a document-global `querySelector` takes the FIRST match, which is a hidden warm pane whenever the Library Reader is up; measurement readers fail OPEN, portal targets fail CLOSED. A warm re-show republishes cached geometry instead of re-measuring (604f7b22); cold mount restores scroll via `EditorStateData.scrollTop` (Phase D), owned by `useEditorUIState`.

## Panel / card rendering

Entry points for rendering a panel instance — both inside EditorPane:

1. **Rail-mounted**: `<PaneRail side="left|right">` (consumes `chrome.visiblePanelKinds` + `viewPrefs.prefs.placements` to split kinds across sides).
2. **Floating**: panels in `viewPrefs.prefs.poppedOutPanels` plus dock-slot panels render through `<FloatingPanel>` portals to body.

Cards inside a `CardListPanel`:
1. **In list** — iterated by `renderCard(item)`.
2. **In-text** — positioned via `inTextRenderItem` (uses `useInTextPositions`).
3. **Popped out** — registered in `prefs.poppedOutCards` with the live key `buildFloatKey` → `float:card:<kind>:<id>` (`${prefix}:${id}` is only the legacy persisted shape `migrateLegacyKeyToFloat` converts); the generic dispatcher `FloatHost` ([src/floats/FloatHost.tsx](../../src/floats/FloatHost.tsx) — AF's successor to `renderPoppedCard`) parses each key, resolves a `Floatable` via `CARD_REGISTRY[kind].toFloatable(id, ctx)` (ctx bag built in [editor-layout/floating-cards.tsx](../../src/components/editor-layout/floating-cards.tsx)), and mounts it in a `FloatWindow` under the unified `FloatChrome` header.

The popouts mount lives inside [EditorPane.tsx](../../src/components/EditorPane.tsx) at the editor root, gated on `viewPrefs && !viewPrefs.zenMode`, with a memoized `popoutsDeps: PoppedCardDeps` bag wired from EditorPane's per-doc hooks. The Reader now passes ephemeral `viewPrefs`, so this mount is LIVE there (session-only) — popouts work in the Reader. Zen mode hides the floats but retains their state.

Popout key prefixes for cards (DO NOT rename without migration — they're persisted; SSOT is `LEGACY_TOKEN_CROSSWALK.legacyKeyPrefix` in [legacy-token-crosswalk.ts](../../src/cards/legacy-token-crosswalk.ts) since task 634, the registry facet and `CARD_KEY_PREFIXES` both deleted; frozen history, read only by `migrateLegacyKeyToFloat`):
`note`, `highlight`, `footnote`, `archive`, `todo`, `bib`, `citation`, `revision` (for `revision-comment` cards), `cutter-comment`, `cutter-suggestion`, `revision-suggestion` (prefix kept for the LEGACY persisted key `revision:s:<id>`, dual-read + migrated on load), `report`, `report-request`, `example`, `error`. (The bare `suggestion` prefix, the legacy `cut` prefix, and the `ai` prefix were retired with the spine refactor / Cutter rebuild / #55b "ai"-kind removal.)

**Block popouts** also live in `prefs.poppedOutCards` but use one unified prefix shape: `textobject:<kind>:<uuid>` (or `textobject:linkedRange:<anchorId>` for mark-backed range popouts). Emitted by `textObjectPopoutKey` in [src/text-objects/text-object-registry.ts](../../src/text-objects/text-object-registry.ts); parsed by `parseTextObjectPopoutKey`. `FloatHost` resolves these through `textObjectFloatable` ([src/text-objects/text-object-floatable.tsx](../../src/text-objects/text-object-floatable.tsx)), which reads `meta.floatBodyComponent` from `TEXT_OBJECT_REGISTRY` and renders the body inside the same unified `FloatWindow`/`FloatChrome`. Body components live in [src/text-objects/floats/](../../src/text-objects/floats/) — after the L3g–L3n chips **all 16 graspable kinds register a body** via `registerFloatBody` (from `floats/index.ts`): per-kind `ParagraphBody` / `HeadingBody` / `ListBody` (both list kinds) / `TexBlockBody` / `ExampleBlockBody` / `LinkedRangeBody` / `ListItemBody` / `ExampleItemBody` / `FigureBody` (figure + graphics), plus a shared `SingleBlockBody` for the bodyless single-block kinds (blockquote, codeBlock, displayMath, latexComment, titleField).

Legacy popout-key migration: boot-time read-side rewriter in `useViewPrefs.loadPrefs` rewrites `paragraph:` / `heading:` / `texBlock:` to the unified shape; `list:<uuid>` and the in-editor `example:<uuid>` keys go through a doc-aware post-load sweep in [src/text-objects/post-load-migrations.ts](../../src/text-objects/post-load-migrations.ts) wired from `EditorLayout.tsx`. The Examples panel-card prefix `example:<id>` (a sibling of `note:` / `todo:` / `bib:`) is stable and intentionally NOT migrated — the disambiguation is doc-aware (a uuid that resolves to an exampleBlock node is the in-editor popout; otherwise it's the panel-card prefix).

Floats spawn near their trigger element via `popCardAtAnchor` in EditorPane (which routes through `viewPrefs.toggleCardPopout` + `viewPrefs.setCardFloatPosition`); position helper at [src/components/editor-layout/spawn-position.ts](../../src/components/editor-layout/spawn-position.ts). A **header drag-lift** instead spawns the float at the docked card's own measured rect (`liftSpawnRect` in [src/floats/float-policy.ts](../../src/floats/float-policy.ts) — pop-out continuity; collapsed lifts grow to content, capped at 55 vh via `POPOUT_MAX_VH`). Initial float size per kind is declared in the registry (`meta.initialFloatSize`).

**Per-panel omni-item builders.** Each omni-eligible panel owns an `omni.tsx` next to its panel folder (e.g. [src/panels/Cutter/omni.tsx](../../src/panels/Cutter/omni.tsx), [src/panels/Errors/omni.tsx](../../src/panels/Errors/omni.tsx), [src/panels/Revisions/omni.tsx](../../src/panels/Revisions/omni.tsx), and the older builders for notes/footnotes/citations/etc.) exporting a `buildXOmniItems(args): OmniItem[]` function. The orchestrator-side host [src/components/editor-layout/panels/omni-host.tsx](../../src/components/editor-layout/panels/omni-host.tsx) imports each builder and concatenates the results into the per-side omni columns. When adding a new omni-eligible panel: flip `omniEligible: true` + set `omniSide` in `panel-registry.ts`, write `<panel>/omni.tsx` exporting the builder, re-export it from `<panel>/index.ts`, and import + invoke from `omni-host.tsx`.

**Dock-drag signal.** [src/components/editor-layout/dock-drag.ts](../../src/components/editor-layout/dock-drag.ts) exposes a module-level (not React Context) `{slotKey, rect}` store via `setDockDragTarget` / `getDockDragTarget` / `useDockDragTarget`. Producer (panel shell, on mousedown) writes the captured slot rect; consumers — the body-portaled [DockOutline](../../src/components/editor-layout/DockOutline.tsx) and EditorLayout's redock-on-mouseup handler — read it. Module-level scope is required because the producer and consumers sit in different parts of the React tree.

**Jump-to alignment.** All `onJump` callbacks accept an optional `sourceEl: HTMLElement | null` argument — typically the clicked card element (use `(e.currentTarget as HTMLElement).closest('[data-card]')`). When passed, `jumpToCard` aligns the in-text marker's vertical position to the card so the page doesn't lurch.

## Panel context

`PanelChromeProvider` in `panel-primitives.tsx` supplies the surrounding panel's `onClose` so the header's context-aware close button (`PanelClose`) can close the right panel without prop drilling. It carries nothing else — `PanelClose` is its sole consumer.

## Card creation + pristine cards

Two related abstractions, both now mounted inside EditorPane:

- **`cardCreation` context** ([contexts/card-creation.tsx](../../src/components/editor-layout/contexts/card-creation.tsx) + [card-actions/card-creation.ts](../../src/components/editor-layout/card-actions/card-creation.ts)) — collapses the historical "create + select + pop-at-anchor" dance into single `cardCreation.createNote/createCut/createTodo/createFootnote/createCitation/createReport/createReportRequest` calls. Each action trigger (the gutter `SelectionActionsMenu`, the `DragHandleMenu`) routes through it, so creation behavior stays consistent across entry points. (The old margin `MarginActionToolbar` and the redundant strip `ActionsStripButton` that also fed it were deleted in bcc583a and backlog #6 respectively.) The pop-at-anchor side now flows through `popCardAtAnchor` inside EditorPane (gated on `viewPrefs`). On a **user-initiated** create of an editable-body kind (`cardKindHasEditableBody` SSOT), the central `finishCreate` chokepoint now auto-expands + selects + **focuses** the new card's body via `focusNewCard` (picking the visible instance); AI/programmatic callers pass `autoFocus:false` and citation keeps its own popover focus (5424f49a).
- **`pristine-cards` context** + `usePristineCardManager` — tracks cards that were just created but never edited by the user; auto-discards them if the user closes/blurs without typing. Every card-bearing hook (`useNotes`, `useCutter`, `useTodos`, `useReports`, `useCitations`, `useFootnotes`) plugs into this. EditorPane owns the manager; EditorLayout still constructs a parallel manager because AIWindow + the click-away mutex effect read selection state on the shell side.

## System dialog primitive

[src/components/system-dialog.tsx](../../src/components/system-dialog.tsx) + [src/components/system-dialog-host.tsx](../../src/components/system-dialog-host.tsx) provide a shared modal primitive. `ConfirmDialog`, `NewDocumentModal`, `TexFilePickerModal`, and `DocumentClassMismatchDialog` are thin wrappers over it. See [src/STYLE_GUIDE.md](../../src/STYLE_GUIDE.md) for the dialog conventions.

Since task 389 the shell also owns **keyboard ownership and initial focus**. A visible cued default is a promise `Enter` must keep, so [dialog-enter-policy.ts](../../src/components/dialog-enter-policy.ts) activates the focused in-frame button or the REGISTERED cue — never gated on where DOM focus happens to sit (the pre-389 claim was a one-shot rAF scheduled from a commit that renders `null`, so on a busy thread it missed and `Return` did nothing). Exceptions are asked of the TARGET, not of focus. Dialogs stack, so [dialog-stack.ts](../../src/components/dialog-stack.ts) is a LIFO where only the OWNER answers a key — topmost MODAL first, else the window CONTAINING focus, else mount order — which also retired a live bug where one `Escape` closed two stacked dialogs. `SystemDialog` takes an `initialFocus` door and stands down when the dialog's own body claims focus (389b: that branch was DEAD in all three dialogs written for it, because a caller's `useEffect` fires in the commit where the body is not yet in the DOM). `confirmDialogCuedDefault()` derives `autoFocus` for every caller, so a `tone="danger"` action is never the cued default; a footer that deliberately cues nothing must DECLARE `noCuedDefault`.

## Unanchored-card safety (21933e0)

Anchored cards (notes, todos, cuts, archives, revisions, reports) no longer disappear silently when their host paragraph is deleted or their last anchor is dropped. Two coordinated pieces:

- **Unified delete path** — [src/cards/delete-margin-item.ts](../../src/cards/delete-margin-item.ts) (`deleteMarginItem`) is the single entry point every gutter-marker delete and panel-trash delete routes through. If the gesture removes the card's last anchor, it prompts to delete the whole card (using the "This item has text" confirm via `cardHasContent` in [src/cards/has-content.ts](../../src/cards/has-content.ts)); if other anchors remain, it just drops this link. `cardHasContent` is now a single declarative walker over each kind's `content: CardContentModel` descriptor on `CARD_REGISTRY` (T4) — replacing the per-kind `switch`, so panel-trash and gutter-marker delete-confirms can't miss a content field; the `morph` field's drop-set drives the lossy-morph confirm copy.
- **Paragraph-deletion guard** — `MarginaliaAnchorGuard` (TipTap extension in [src/lib/tiptap/linked-anchor.ts](../../src/lib/tiptap/linked-anchor.ts)) reads a ref of currently-anchored UUIDs from EditorPane (`anchoredUuidsRef`, populated in `EditorPane.tsx` ~line 3661) and, when a transaction would delete a paragraph carrying an anchor or a `linkedAnchor` mark, re-inserts an empty placeholder at the mapped original position with the same UUID. Covers both gutter-marked paragraphs and Mode-B text-range paragraphs. Configured in `buildEditorExtensions` ([src/lib/editor-extensions.ts](../../src/lib/editor-extensions.ts) ~line 1924).

Together: only explicit user gestures (gutter delete or panel trash) destroy a card; incidental editor edits can't orphan one.

**Resolver-driven render + orphan dock (RC2).** The gutter-marker builder resolves each card's live paragraph through the anchor-recovery SSOT (`resolveCardAnchor`) rather than a raw uuid→pos lookup. A card that resolves to `source:'orphan'` (its uuid + mark + text-snapshot are all dead) is no longer silently culled — its `MarginaliaMarker` carries an `unanchored` flag ([src/lib/marginalia.ts](../../src/lib/marginalia.ts)). **Since task 410 that affordance has LEFT the lane**: the in-margin `OrphanDock` overlapped the first blocks' cells and STOLE their clicks, was culled with them at a cramped margin, and — pinned at `top: 6` inside a non-scrolling pod — was unreachable on any scrolled document. `computeMarkerPositions` now skips an `m.unanchored` marker before the side is even resolved (no `orphans` bucket survives). **Since task 544 the affordance's ONE owner is the gutter's "N unanchored" omni bin** (`OmniUnanchoredBin`, in the column's sticky bin slot — see ui-chrome.md "Omni bins"); the "N unanchored" pill in the pod's STICKY chrome header, [src/components/UnanchoredCardsChip.tsx](../../src/components/UnanchoredCardsChip.tsx), is its FALLBACK for a side with no bin surface — fed from the UNFILTERED marker set (a lost anchor is a data-integrity fact, not hideable by a layout preference; archived cards ARE excluded), rendering nothing at zero, and reusing the same `MarkerButton` so click-to-open and grab-to-re-anchor are unchanged. `useMarginaliaRegistry` ([src/hooks/useMarginaliaRegistry.ts](../../src/hooks/useMarginaliaRegistry.ts)) also heals a gutter marker when its anchor block's DOM is swapped (list-item / heading hover-cull) with bounded observe-retry.

**Lane hosting, the left lane's bands, and a total frontier walk** (tasks 670–673, all in [src/lib/marginalia.ts](../../src/lib/marginalia.ts) + [marginalia-grid.ts](../../src/lib/marginalia-grid.ts)). "Does this pane host the marker lane?" is ONE resolution: `resolveMarginaliaLane` returns both `hosted` (does this pane paint markers) and `reserved` (is its margin floored for the lane), `reserved` implying `hosted` by construction and narrower only by the compressed code-split, which is about the FLOOR rather than the markers. `EditorPane` makes exactly one call and both gates read its halves, so the render filter and the margin floor can no longer disagree — zen HIDES the markers (it used to paint every icon while dropping the floor that guarantees them room) and the Reader HOSTS them (the old `!!menuBar` term meant "not the Reader" only until F#16 gave it `readerMenuBar`). The LEFT margin's two occupants ride opposite edges — the marker grid is POD-anchored, the fold chevron CONTENT-anchored — so that lane is an ordered band list (`LEFT_LANE_BANDS` + `leftLaneOffset` + `resolveLeftLane`, each band declaring the edge it is measured from); `MARGINALIA_MIN_MARGIN_LEFT` is now Σ of both halves (88) instead of an alias of the marker container's width (80), and `LANE_MEASUREMENT_EPSILON_PX` (0.5) keeps an exact-sum floor from reading as an overlap on either side. `computeMarkerPositions` itself became TOTAL over its declared `AnchorNodeMetrics` input: the drift bound is asked at EVERY row rather than only row 0 (the "byte-identical to the pre-366 grid" claim is now a stated conditional on a pitch ≥ 24 px — below it each row's push accumulated, and the surplus now rides the node's own "+K" pill), the list sorts on the literal `rowY(node, 0)` rather than `node.top` (a textless block measures `lineHeight` as its full height and so anchors at its CENTRE), a per-node `sanitizeMetrics` gate refuses a non-finite measurement so one bad `lineHeight` costs one node instead of NaN-ing every marker below it, and a fractional `lineCount` is floored.

## Identity cascade (behind `virgil:identity-cascade`, default OFF)

[src/lib/identity/](../../src/lib/identity/) gives bibliography entries and inline atoms a durable internal id decoupled from the renameable citekey, so a rename / markerless re-parse never strands the surfaces that key on the old string (annotations, bib-review requests, float, panel selection — the BIB-A2-01 DATA-LOSS class). Pieces:

- **`BibEntry.uid`** — minted once and round-tripped via a `\vbid{}` marker in the `.bib` ([src/lib/bib-uid.ts](../../src/lib/bib-uid.ts)). A rename mutates `key`, never `uid`.
- **`IdentityCascade`** ([identity-cascade.ts](../../src/lib/identity/identity-cascade.ts)) — the single writer for any identity-changing op (`runIdentityChange`). Pure-logic; every uid-keyed surface registers a migrator so the cascade fans a rename out atomically (the `\cite{}` doc-rewrite is itself a migrator the citation hook registers, passing the live editor). Fires only on an explicit rename, never on a keystroke.
- **`useIdentityBusConsumer`** ([useIdentityBusConsumer.ts](../../src/lib/identity/useIdentityBusConsumer.ts)) + **`IdentityBusConsumer`** ([identity-bus-consumer.ts](../../src/lib/identity/identity-bus-consumer.ts)) — the single inline-atom `DocStructureBus` consumer (one `onAnyChange` subscription, `emitCount`-gated + O(1) bail); Wave-2 themes register ordered policies (`registerPolicy`) rather than opening their own subscriptions.
- Sidecar migration: **`sidecar-uid-migrate.ts`** re-keys legacy citekey-keyed sidecars to uids non-destructively (`AnnotationsStateV2.orphanByKey` catches unresolved keys); **`bib-cite-rewrite.ts`** rewrites `\cite{}` commands through the deep atom-rewrite door `rewriteInlineAtomsDeep` ([src/lib/inline-content.ts](../../src/lib/inline-content.ts), task 606 — also reaches footnote-body atoms; the label rename uses the same door).
- Flag gates: **`identity-flag.ts`** (`virgil:identity-cascade`) + **`inline-atom-lifecycle-flag.ts`** (`virgil:inline-atom-lifecycle`). Flag-off keeps the legacy `updateBibKeyAndType` path so the existing suite stays green.

## Per-panel color overrides

Users can recolor any panel via the header color picker. The override routes through `deriveCardPalette` in [src/lib/panel-theme.ts](../../src/lib/panel-theme.ts), which derives a full card palette (marker, badge, border, header, selected variants) from a single accent color. The override store is cross-window synced — see "localStorage stores + the cross-window rule" above.

## Drag / drop MIME map

The MIME constants live in [src/lib/marginalia.ts](../../src/lib/marginalia.ts). After the drop-button / unified drop-mode rework (chip H) the native **panel→gutter** and **gutter-pin re-anchor** paragraph-anchor drags were folded onto the unified drop-mode controller (re-anchoring now flows through the card drop button → `beginCardDropGesture`, not HTML5 DnD); the per-kind anchor MIMEs (`MIME_NOTE` / `MIME_TODO` / `MIME_ARCHIVE_ANCHOR` / `MIME_CUT` / `MIME_REPORT`) and the `panel-drops.ts` / `anchor-rebind.ts` event-bridges + `Revisions/mime.ts` were deleted. What remains:

- **`ANCHOR_DRAG_TYPES`** (vertical drop indicator suppress-set): now the lone residual `MIME_MARGINALIA_MOVE` — no live code produces it, kept only so `isAnchorDrag` stays a guard for any future native paragraph-anchor drag that opts back in.
- **Inline-insert** (no vertical indicator): citation, archive (restore), footnote. `MIME_TEXT_INSERT` was **deleted** by task 590 (a reader with no producer since `ec382103`, the `EditableCard` text-drag grip), along with `MIME_SELECTION_ANCHOR` (never produced, never read).
- **Whether anyone PRODUCES a MIME is now DATA, not folklore:** each surviving constant declares `produced: true | false` in `DRAG_MIME_PRODUCTION`, checked against the repo's real `setData` call sites by [drag-mime-production.test.ts](../../src/lib/__tests__/drag-mime-production.test.ts). A reader waiting on a drag that can never arrive is a test failure rather than dead code two readers keep honouring.
- **Card-merge discriminator** (`MIME_BIB_MERGE`): a `BibEntryCard` drag advertises this *alongside* `MIME_CITATION` so a `CitationCard`'s drop ring lights iff the drop would actually merge (a bib-entry drag, payload `{ bibKey }`) and never for a citation card's own atom-move drag (which carries `MIME_CITATION` alone). `dragover` sees `types` but not `getData`, so a distinct type is the only way the ring can predict the drop. Card-merge target only — kept out of `EDITOR_INSERT_DRAG_TYPES` and `ANCHOR_DRAG_TYPES`.

When a card kind is re-anchorable via the drop button, declare the `droppable` + `dropPlacement` facets on its `CARD_REGISTRY` entry and register its `dropSpec` (see `src/cards/types.ts` `CardMeta`) — these are facets on existing kinds, not new card kinds. For a genuinely new inline-insert DnD payload, add the `MIME_*` constant and keep it out of `ANCHOR_DRAG_TYPES`.

**Text moves** are deliberately narrow. The canonical text-move gestures are:

1. **Drag-to-pop-out** — the lift gesture on any TextObject via the unified [TextObjectGrabHandle](../../src/text-objects/TextObjectGrabHandle.tsx) (the single grab-handle component for paragraph / heading / list / list item / example item / atom block / selection-as-linkedRange). Selection lifts hydrate into `linkedRange` TextObjects at lift commit via `hydrateSelectionToTextObject` ([src/text-objects/hydrate-selection.ts](../../src/text-objects/hydrate-selection.ts)). Custom mousedown protocol, NOT HTML5 drag. The post-threshold lifted-overlay core (the translucent ghost↔popout clone) is hoisted out of the grab handle into the shared [LiftHost](../../src/text-objects/LiftHost.tsx) provider (`beginLift`, `terminalPolicy`), so a second producer — the popped-out text-object float's drop button — drives the same machinery.
2. **Drop-mode** — grabbing the card's drop button (the double-chevron, on the docked header / float chrome; the float-chrome drop button also lifts via `LiftHost`; req-7 retired the legacy Shift-drag-on-a-float-header entry) re-drops the block back into the doc at a visible placement bar. The block-source drop spec in [src/components/drop-mode/specs/textobject.ts](../../src/components/drop-mode/specs/textobject.ts) routes through `meta.dropAdapter` (wrap vs drop-direct) and `meta.collectMoveSource` (single node vs section range). A lifted `linkedRange` selection (cardKey `textobject:linkedRange:<id>`) is routed to a sibling [specs/text-range-move.ts](../../src/components/drop-mode/specs/text-range-move.ts) instead — it moves a text *slice* (not a whole node) to an inline cursor, or drops it as block content in a block gap (L3f-2). Feature A1–A3 also let block payloads drop into an expex example via a single left vertical drop-bar.
3. **Inline-Atom grab** — the inline cousin of (1): grab an **Atom** (footnote / citation / `\ref` / inline math) directly in the prose and drag it to a new inline cursor. The atom *is* its own handle. A ProseMirror plugin ([src/lib/tiptap/inline-atom-grab.ts](../../src/lib/tiptap/inline-atom-grab.ts), in `buildEditorExtensions`) runs a mousedown→threshold→`beginDropSession` gesture (same drop-mode pipeline, inline-cursor placement); a no-drag press still fires the atom's click (open Card / edit popover). The single drop spec ([specs/in-text-atom-grab.ts](../../src/components/drop-mode/specs/in-text-atom-grab.ts), `atom-grab` prefix) resolves the source captured at grab and moves it same-editor, preserving the node. The 4-kind SSOT is `ATOM_REGISTRY` ([src/lib/tiptap/atom-registry.ts](../../src/lib/tiptap/atom-registry.ts)) — the inline sibling of `TEXT_OBJECT_REGISTRY`. No atom uses native HTML5 drag anymore.

`MIME_TEXTOBJECT = "application/x-virgil-textobject"` ([src/text-objects/types.ts](../../src/text-objects/types.ts)) is defined for future HTML5 drag-out producers (e.g. cross-app drag-to-Stack). Today no producer emits it — the grab handle is mouse-driven and the float-header drop-mode stays in-app via the `virgil-stack-drop` event. The legacy `MIME_PAR_CAPTURE` / `MIME_TEXT_CAPTURE` MIMEs were retired in D5+D6.

## Keyboard: Tab in prose fields

Substantive prose fields (every TipTap editor, every multi-line `<textarea>`, the BibEntryCard annotation contentEditable) treat **Tab as indent**, not as focus-move. Tab inserts a literal `\t` at the cursor; Shift-Tab is a no-op in plain prose. The escape hatch is **Esc** — it blurs the field so the next Tab navigates panels normally. Single-line `<input>` fields (search, card titles, citation/bib keys, numeric/dialog inputs) are intentionally *not* affected and keep default focus-moving Tab.

Implementation: [src/lib/tiptap/tab-indent.ts](../../src/lib/tiptap/tab-indent.ts) (TipTap extension at priority 50, so list `sinkListItem` and the expex Tab handlers in [src/lib/tiptap/expex.ts](../../src/lib/tiptap/expex.ts) win first); [src/hooks/useTabIndent.ts](../../src/hooks/useTabIndent.ts) (textarea / contentEditable handler).

## Preview / dev caveats

From the existing memory: the File System Access folder picker doesn't work inside the preview iframe. For UI verification, load the dev doc (`virgil-data/doc_devtest`) via the helper, not the picker. Worktrees need the symlink present; see `dev_doc_loading.md` in the agent's personal memory.

**Relocating `library-data/`.** The dev-library API route honors `VIRGIL_LIBRARY_PATH` (introduced in 6ad177f). Setting it in `.env.local` (or your shell) repoints `DATA_DIR` from `process.cwd() + "/library-data"` to the resolved path. Recommended fix for Turbopack instability when `library-data/.virgil/models/` (multi-GB ML weights) and `library-data/.virgil/queue/` (churning under skill runs) overwhelm the watcher's startup walk.

## Where NOT to look for business logic

- `src/app/` — almost pure Next.js scaffolding (manifest, layout, page, global styles). Real work happens in `components/`, `hooks/`, `lib/`, `links/`, `panels/`.
- Top-level config files (`next.config.ts`, `tsconfig.json`, `tailwind.config.*`) — only relevant when changing build/bundling behavior.

## Print

[src/lib/print.ts](../../src/lib/print.ts) orchestrates the print path triggered from the Virgil-bar print button → `PrintDialog` ([src/components/PrintDialog.tsx](../../src/components/PrintDialog.tsx)) → `window.print()`. Both print doors — that dialog and the browser's own File → Print (`beforeprint`) — enter ONE synchronous `enterPrintPosture(options)` (task 608); the native door prints the user's SAVED options, which `EditorLayout` publishes into the single slot in [src/lib/print-intent.ts](../../src/lib/print-intent.ts). Isolation anchors on the VISIBLE pane, fail-closed (task 597). Show/hide toggles for marginalia, footnotes, citations, comments, paragraph titles, etc. live in `useViewPrefs`. Appendix collection (e.g. all footnotes / comments rendered as a tail section) is in [src/components/PrintAppendices.tsx](../../src/components/PrintAppendices.tsx).

## Related docs

- UI structure → `ui-chrome.md`
- Editor content → `main-text.md`
- User vocabulary → `glossary.md`
- Library subsystem → [library/AGENTS.md](../../library/AGENTS.md) (sibling tree under `library/`, with its own components/hooks/lib/tiptap/scripts/skills)
- Editor skill bundle → [editor/AGENTS.md](../../editor/AGENTS.md) (`/editor/review` umbrella + per-kind subskills that fulfill `ai-requests.json` entries; bridged via [src/lib/ai-request-bridge.ts](../../src/lib/ai-request-bridge.ts))
- Project README → [README.md](../../README.md)
- Style guide → [src/STYLE_GUIDE.md](../../src/STYLE_GUIDE.md)
