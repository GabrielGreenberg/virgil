<!-- last-verified: cb8bc044 2026-09-02 -->
<!-- derives-from: docs/architecture/VIRGIL.md#code-organization, docs/architecture/VIRGIL.md#ontology -->
<!-- covers-code: src/app, src/components, src/hooks, src/lib, src/links, src/cards, src/floats, src/panels, src/text-objects, src/types, library, editor, virgil, package.json -->

# Virgil Overview

**Current version:** 0.1.103 (mirrors `package.json`; bumped by `/cleanup-virgil`)


## What it is

Virgil is a browser-based visual LaTeX editor for academic writing, designed to be co-edited with AI agents. It runs 100% client-side: files live on the user's disk via the File System Access API, nothing goes over the network. The user's `.tex` and `.bib` files remain the source of truth; Virgil renders them meaningfully while preserving the original LaTeX.

It is **not** WYSIWYG: the editor's rendering never compiles — it is driven entirely by the parse/serialize round trip. (A *separate*, optional in-browser SwiftLaTeX pdfTeX compile does exist — `src/lib/compile/` + the PDF pane — but nothing the editor draws depends on it.) It renders inline formatting (italics look italic, footnotes sit in margins, math has its own color) and lets the user click any rendered element to edit the raw LaTeX underneath.

## Who it's for

Academic writers working in LaTeX who want to cowork with Claude or another agent. Virgil doesn't call any model itself — instead, the agent reads the same files and drops structured JSON sidecars into a `virgil/` folder. Virgil surfaces those sidecars as UI (suggestions, revisions, AI requests, bib reviews).

## Tech stack

- **Framework**: Next.js 16 (App Router, static export) — breaking changes from training data; see `AGENTS.md`
- **Editor**: TipTap (ProseMirror-based)
- **Styling**: Tailwind CSS v4 + semantic CSS variables
- **Storage**: File System Access API (disk) + IndexedDB (prefs, tab state, folder handles)
- **Bibliography**: citation-js
- **Language**: TypeScript, React 19

## Top-level `src/` map

- `src/app/` — Next.js app router root, global styles (`globals.css`), manifest, layout, dev-only API routes
- `src/components/` — React components. Biggest files: `EditorLayout.tsx` (~3820 lines; shed ~650 lines this cycle as the top-bar shell was extracted into memoized sub-components under `src/components/editor-layout/` — `TopBar.tsx`, `TabStrip.tsx`, `StatusCluster.tsx`, `InlineTabLabel.tsx`, `TabSeparator.tsx`, `PaperDropIndicator.tsx` — so a warm paper-switch no longer repaints the bar; plus `bar-occupancy.ts` + `useBarOccupancy.ts`, task 395's priority ladder for the fixed-width strip: protected status > tabs > collapsible tools, resolved from ONE ResizeObserver over three boxes, over a structural floor where the tab strip clips its own overflow so an overlap is unrepresentable), `EditorPane.tsx` (~8200 lines, canonical editor surface used by both the main app and the Library Reader; also owns the single load-time anchor-recovery pass — its reconcile effect invokes `applyLinkedAnchorsImpl`, see `src/links/`), `Editor.tsx` (~1820 lines, TipTap wrapper), `panel-primitives.tsx` (~3285 lines), `MenuBar.tsx` (~825 lines, docked menu pod; its `BlockTypeDropdown` + `ViewMenu` now ride the `<Menu>` primitive), `dialog-enter-policy.ts` + `dialog-stack.ts` (task 389 — a VISIBLE cued default is a promise `Enter` must keep, and only the OWNING dialog of a LIFO stack answers a key), `confirm-cue-policy.ts` (task 528 — the `tone` → BUTTONS mapping, stated once for BOTH imperative confirm doors, since the second hand-derived it and cued the DESTRUCTIVE button on a `danger` confirm; its other rule is that a button which COMMITS NOTHING is never painted destructive), `field-draft.ts` + `HexColorField.tsx` (task 532 — a field whose value is owned elsewhere holds a DRAFT, commits on a stated edge and RECONCILES while not dirty; two sites rendered a color field and only one worked, the other a controlled input that could not be typed into at all), `SkillSyncControls.tsx` (the loud-and-recoverable skill-bundle sync UI — now banner-only: failure banner with Retry/regrant + an "updated" notice, symmetric in paper + library; the manual "Refresh skills" re-sync moved into the Virgil-bar help "?" menu). See `architecture.md` → "EditorPane vs EditorLayout" for the split.
  - `src/components/menu/` — the unified `<Menu>` primitive (`MenuProvider`, `MenuItemsFromRegistry`, `useMenuKeyboard`/`useMenuCombobox`/`useMenuItem`/`useMenuDismiss`, `nav-core.ts`, `registry.ts`, `regions.tsx`, `MenuToggleRow.tsx` — the shared checkbox/toggle row, extracted in task 180; `AnchoredMenu.tsx` — the TRIGGER half added in task 295, after the primitive's owning behavior-but-no-surface left twelve menus authoring their own chrome and each dropping a different guard): one keyboard/roving-highlight model, one menu surface, and one trigger contract behind every action menu (grab-bar menu, lightning `ActionsMenuPanel`, `SelectionColorPopover`, `LabelRefPopover`, `HeadingTypeMenu`, `TabPlusMenu`, `BibEntryPickerMenu`, MenuBar dropdowns, and — since task 180 — the Outline panel-header kebab, now on `ItemMenu` like every other panel kebab). The slash popup is a documented exception. Design: [menu-system-design.md](menu-system-design.md).
  - `src/components/drop-mode/` — drop-mode controller + specs (block move, in-text-atom grab, text-range move, textobject re-anchor) plus `card-drop-gesture.ts`: the neutral card drop button (docked + omni + popped-out-float via `FloatChrome`) enters drop-mode to (re)anchor a card; dropping an unanchored footnote/citation creates the atom inline. Replaced the retired panel→margin native drag (deleted `event-bridges/panel-drops.ts` + `anchor-rebind.ts`). Card `droppable`/`dropPlacement` live as `CardMeta` facets in `src/cards/`. Three SSOTs govern what a drag may promise and where a payload may land — `placement-policy.ts` (`winningPlacementKind`, the hit-test's priority switch, read by both the hover and the commit so a spec can't offer a placement it will refuse), `specs/drop-context.ts` → `fitNodesAtInsert` (the container-fit ladder over the schema-level `fitNodeInContainer` in `src/text-objects/drop-adapters.ts`), and `planned-spec.ts` (`plannedDropSpec` — one pure `planDrop` resolution from which both `classifyDrop` and `applyDrop` derive, so a refused drop cancels the session with the float intact instead of reporting success over an untouched document). Each is CI-censused; the laws are written up in `AGENTS.md` → "Capture/schema symmetry". Task 351 put the content drag itself on the four bespoke-gesture obligations: `move-geometry.ts` is its lazy snapshot door (every geometry the per-move math needs captured once at the gesture edge, re-armed off the `LayoutGestureBus` SET channel), the controller's hit-test is RAF-coalesced at the LIVE pointer instead of running inline per raw `mousemove`, `auto-scroll.ts` reads its edge zones off the snapshot, and everything painted above the editor is click-through for the session so `posAtCoords` can reach it.
- `src/hooks/` — React hooks for state management (~72 files), plus `view-prefs-derived.ts` (a pure leaf module of derived `ViewPrefs` read-helpers, re-exported by `useViewPrefs` so route-derivation code can read prefs without dragging in the heavy storage chain) and `useReconcileModeAAnchors.ts` (the shared Mode-A reload reconcile factory each panel hook calls — UUID-first, paragraph-snapshot fallback, funneled through the `resolve-card-anchor.ts` SSOT), and `useOrphanedFootnotes.ts` (the live orphan-footnote list fed to the Footnotes + Search panels)
- `src/lib/` — Core business logic: LaTeX parse/serialize, TipTap extensions, storage, types (~120 files). Notable subsystems: `src/lib/input-modality.ts` (task 336 — the SSOT for "only pointer input answers a pointer question"; pointer-derived chrome hides while the user types, and the flip is an EDGE so a 40-key burst schedules one resolve), `src/lib/latex-lexer.ts` (`skipOpaqueConstructAt` — task 338's one grammar-derived answer to "which nested construct is opaque to my scan?", read by every body splitter and all three environment terminators), the zero-import `.tex` round-trip vocabulary leaves both the parser and the serializer read — `latex-markers.ts` (the `\v*` marker commands), `node-attr-sets.ts` (task 343 — which node types declare `uuid` / `parTitle` / `collapsed`, the schema facts the TipTap-free layers cannot ask the live schema, so the sidecar write and read can't disagree), `latex-typography.ts`, `cite-commands.ts` — `src/lib/tex-preservation.ts` + `write-preservation.ts` + `preservation-notice.ts` (tasks 350-D / 357 — the preservation gates: a `.tex` write the user did not ask for, whether the load-writeback or a pre-first-edit bundle flush, is weighed in WORD tokens against what was READ, preamble and body separately, and a shrink is a REFUSAL that leaves the file byte-identical; the refusal is published once to one store, write-protects the doc until acknowledged, and surfaces as the topbar `PreservationNoticeBadge`) — joined by `mount-preservation.ts` over `src/lib/tiptap/schema-mount.ts` (a model that a gate has MEASURED is not yet a document: `createNodeFromContent` swallows a `nodeFromJSON` throw and returns an EMPTY doc, so a model naming a node type this build's schema lacks would open the paper blank and step the write gate aside on the first keystroke) and `serialize-refusal.ts` (task 357's last member — the serializer THROWS `UnserializableNodeError` rather than emitting less, since "less" is byte-indistinguishable from a correct shorter document; the four bundle writers catch and publish, every read-only `.tex` projection fails OPEN and keeps its last good text), `src/lib/conflict-resolution.ts` (task 364 — a disk/model conflict has TWO sides, so it gets two doors, and the order in which BOTH are archived to one `virgil/.history/` slot is stated here once so "Keep mine" and "Load the disk version" cannot disagree about what is kept), `src/lib/public-asset-url.ts` (task 365 — the ONE door every URL reaching a `public/` asset is built by, since Next prefixes only the URLs it generates itself and Virgil may be served from a subdirectory; a path stored in a DATA table is public-RELATIVE and each consumer applies its own base, which is why the SW's precache table needs its own spelling), `src/lib/compile/` (the in-browser SwiftLaTeX pdfTeX compile — `compile-service.ts` orchestrates recovery/multi-pass, `compile-progress.ts` is the per-doc progress store the PDF pane's `CompilePaneStatus` renders, and every package the worker fetches is written through to IndexedDB the moment it lands so a timed-out compile keeps its downloads; and since task 520 the pgf/tikz + forest families are VENDORED into the offline bundle — 92 files / 2.26 MB paid once at install, replacing 60–100 serial blocking mirror round trips on the first compile of any paper using them. A live capture is the wrong instrument for a KNOWN closure, so `scripts/tex-bundle-families.mjs` DECLARES each family (seeds plus the two things a source scan cannot infer — a macro that is not a filename, and what the format already carries) and `scripts/vendor-tex-family.mjs` resolves it against the mirror; both producers write through the ONE manifest door, which merges BY FAMILY. see `AGENTS.md` → "The compile path" / "The vendoring half"), `src/lib/prose-index.ts` (task 517 — the ONE owner of "which characters are prose, and where": the PROSE character runs of a doc with their ProseMirror positions, every carrier run / markless block / atom excluded, the exclusion vocabulary DERIVED from the SSOTs and the live schema rather than hand-listed; Search reads it, so a query no longer matches inside raw LaTeX or a `%` comment, and it is the foundation the spellchecker is built on), `src/lib/spellcheck-policy.ts` (task 517's other half — the browser's native spellcheck as ONE inherited `spellcheck` attribute on `<body>` written from one `VIEW_PREF_REGISTRY` row (`checkSpelling`, default ON), with `NEVER_SPELLCHECK_ATTRS` / `NEVER_SPELLCHECK_PROPS` the door the eleven deliberate opt-outs spell rather than a bare literal), `src/lib/spell/` (task 518 — Virgil's OWN LaTeX-aware spellchecker, built on the prose index: `prose-words.ts` turns the 517 run table into WORDS, where THE RUN GAP IS THE WORD BOUNDARY (an ATOM ends a word, an excluded raw-LaTeX text run makes a FRAGMENT that is not checked); `accepted-words.ts` is the ONE authority for "this word is fine", composing the paper's `dictionary.json`, the user's global list and every bibliography `author`/`editor` name; `spell-core.ts` is a pure function of its two assets, so it tests from a five-word inline `.dic` rather than a 550 KB one, with `spell.worker.ts` / `spell-client.ts` / `spell-port.ts` keeping the BASE dictionary off the main thread — the user's exceptions live on it, which is what lets a dictionary edit re-derive with no worker round trip; the squiggle is a VIEW like every other, so `src/lib/tiptap/spellcheck-decorator.ts` paints a `DecorationSet` replaced by META-ONLY transactions, its `apply` maps and takes the transaction's touched textblocks through the shared `touchedTextblocks` door with the ONE whole-document arm in the plugin VIEW behind a 300 ms debounce), `src/lib/tiptap/autocorrect.ts` + `typed-prose-gate.ts` (task 518's phase 3 — a CURATED table of unambiguous word typos applied at type time, never nearest-dictionary-word, since a word swap has no reverse map where a glyph has one; the gate asks TWO rungs because a construct is raw LaTeX before it is FINISHED, and CI checks every row against the shipped Hunspell dictionary in both spellings so the list is safe to grow), `src/lib/view-only-chrome.ts` (tasks 408/523 — the ONE vocabulary for "this paint is the EDITOR's state, not the document's", read by the writers that paint it AND by the `@media print` block that must not, so what reaches paper is the document; the `viewOnly()` stamp is what let the census ask the QUESTION rather than rediscovering the population per mechanism), `src/lib/field-edit-session.ts` (task 529 — an edit session ends exactly ONCE: `element.blur()` dispatches `focusout` SYNCHRONOUSLY and React delegates `onBlur` off it, so Escape inside a keydown ran the very commit it was cancelling, from a stale closure, in four separate fields), `src/lib/example-dialect.ts` (task 355 — the `expex | linguex` vocabulary and the mint rule for a new example), `src/lib/doc-products/` (the single derived-doc-product pipeline; since task 337 its caches RECURSE into containers, so a keystroke inside item 50 of a list re-derives that item's subtree rather than the whole list), `src/lib/editor-geometry/` (the one per-editor screen-geometry service — near-zone culling, the viewport frame, `glyph-anchor.ts`'s kind-gated probe, and since task 370 `settle-convergence.ts` — the ONE door every settle trigger enters, measuring until two consecutive passes agree on the consumer's own fixed point on an unrefreshable wall-clock budget, probe `window.__settleConvergenceStats()`, `viewport-probe.ts`'s fail-open position band), `src/lib/view-prefs/registry.ts` (the typed `VIEW_PREF_REGISTRY` SSOT that generates `ViewPrefs`/`DEFAULT_PREFS`/`GLOBAL_PREF_KEYS` and auto-wires the ViewMenu — all view/display prefs now survive reload; task 274 completed its WRITE side with the missing `set`-kind door, so a pref now travels by key through `setViewPref`/`toggleViewPref`/`toggleViewPrefMember` and a new one is one registry row rather than nine hand-threaded artifacts), `src/lib/actions/` (the multi-surface `VIRGIL_ACTION_REGISTRY` SSOT — one action set rendered by the grab-handle menu, margin lightning, slash commands, and typed-LaTeX input rules; PM→React dispatch via `editor-actions-bridge.ts`, icons in `action-icons.tsx`), `src/lib/focus-view.ts` (UUID-anchored focus-mode band lib + `focusViewPlugin`), `src/lib/tiptap/` (the TipTap extension set, including `insert-inline-atom.ts` — the unified no-scroll `insertInlineAtom` primitive used by all of footnote/citation/`\ref`/inline-math create — and `chrome-scroll-margin.ts`, the chrome-aware `scrollMargin`, so creating an inline atom no longer jumps the viewport; `slash-applicability.ts` — task 398's verdict publisher, so the slash popup asks the SAME question at the offer that it will ask at the commit, instead of deleting the typed `\name` and only then refusing; `card-body-capture.ts` — task 393's ONE capture door, deriving the payload once (normalize → validate → return) so the object a destructive capture VALIDATES is the object it STORES; `source-pod-static.ts` — the shared static child spec both source-pod nodes emit, since a block atom keeps its payload in ATTRS and a `renderHTML` emitting only a wrapper projects to an empty element in the T1 card tier and on the CLIPBOARD; the carrier marks `latexCommand` / `latexVerbatim` / `latexCommentTail` — the first now applied at TYPE time by the `latexCommandCarrier` plugin (task 360) so bare text is prose by construction and `CHAR_ESCAPE_TABLE` escapes its whole vocabulary unconditionally; the last added in task 347, because a `%` comment is CONTENT Virgil re-emits verbatim rather than prose the escape table may rewrite — are what keep unmodeled LaTeX byte-literal through a round trip). Code-pane support: `code-position-map.ts` (the cached UUID↔`.tex` source-position map keyed by the CodeMirror doc, SSOT for cursor/selection sync) and `code-band.ts` (the passive code-side cursor band; manual sync arrows replaced auto-align). Anchor durability: `anchor-mint-signal.ts` (tags an anchor-UUID mint tx so the autosaver flushes the doc bundle immediately, closing the card-clock vs doc-clock reload race) and `src/lib/tiptap/anchor-highlight-deco.ts` (the `AnchorHighlightDecorator` extension — paints card hover/selection on in-editor node/atom anchors via a `Decoration.node`, replacing the foreign-mutation redraw; Mode-B text-range highlight stays on raw `setAttribute`). Security: `sanitize-html.ts` (allowlist HTML sanitizer wired into the bibliography annotation editor; BIB-F5-01). Typography: `font-stacks.ts` (the SSOT for the three chrome font CHAINS — user override pref → `next/font` variable → load-window fallbacks — with zero imports so `library/` can take it as a leaf; task 170 extracted it after `--mono`/`--serif` turned out to be spelled 48 times and defined nowhere, so every "monospace" page-picker / tab label / citekey pill and every serif dialog heading had silently rendered in the ambient sans. Never re-spell a chain, and never reach for a bare `var(--font-mono)` — that skips the user's override; `src/__tests__/phantom-css-var.test.ts` censuses every `var()` in both stylesheets and both silos against three definition channels). Identity: `src/lib/identity/` (the identity-cascade subsystem giving footnote/citation inline atoms stable UIDs so selection/float/pin survive a markerless re-parse — `identity-cascade.ts` dispatcher, `identity-bus-consumer.ts` + `useIdentityBusConsumer.ts` the SINGLE inline-atom `DocStructureBus` consumer that lifecycle/citation-resync register on as ordered policies, `sidecar-uid-migrate.ts`, `bib-cite-rewrite.ts`, plus `src/lib/bib-uid.ts` for stable bib-UID minting; all gated behind the DEFAULT-OFF flags `virgil:identity-cascade` / `virgil:inline-atom-lifecycle` via `identity-flag.ts` / `inline-atom-lifecycle-flag.ts`). Keep-alive: `src/lib/keep-alive/` (`KeepAliveSlot.tsx` hoists an editor into a `display:none` slot so it survives a tab bounce without remount — `currentDocId` stays stable so the autosave wall holds — plus `useKeepAliveLRU.ts` for the last-N warm panes and `visibility-context.tsx`). Multi-doc keep-alive (instant paper↔paper switching) keeps up to 2 authored docs warm-but-hidden alongside the visible one via `src/components/editor-layout/DocKeepAliveLRU.tsx`, gated by `src/lib/multi-doc-keepalive-flag.ts` (DEFAULT-ON; the localStorage key `virgil:multi-doc-keepalive` is the opt-out — set to `"0"` for the legacy single-doc behavior) with `src/lib/active-editor-probe.ts` resolving the live editor; the Library Reader has its own warm-pane LRU in `library/components/ReaderLRU.tsx`. Disk watch: `disk-watcher.ts` + `disk-ledger.ts` (the wall-clock `DiskWatcher` poller + `diskLedger` fingerprint that detect out-of-band `.tex`/`.bib` edits — Overleaf-sync, `git pull`, `vim` — surfacing the `ExternalChangeBadge` topbar pill; autosave-paused on detect, never auto-merge). Sidecar placement: `sidecar-value.ts` (the value SSOT — `tier` decides the write cadence and how a cloud-sync fork is reported, `mount` derives `ALL_SIDECAR_FILENAMES`, and since task 417 a `store: "disk" | "local"` column the four sidecar doors in BOTH backends route on) + `local-sidecar.ts` (the IndexedDB store `"local"` files live in — today just `editor-state.json`, because per-machine view state in a synced folder is a conflict waiting to be minted). Cross-window: `cross-window-storage.ts` (the ONE encoding of the native `storage`-event contract — `subscribeToStorageKey`/`subscribeToStorageKeys` for module-global stores, `useStorageKeySync` + `writeStorageIfChanged` for the hook-state variant; every localStorage store that caches a snapshot rides it so a second window can't clobber the first from a stale base — see `architecture.md` → "localStorage stores + the cross-window rule"). Onboarding: `src/lib/example-doc/` (the OPFS-seeded editable EXAMPLE document for new users — prod-only sandbox: `ensureExampleSeeded`/`resetExample` in `example-seeder.ts`, the `opfs-doc-location.ts` OPFS primitive, consts in `example-identity.ts`; rides the unchanged `storage-fsa` core as an ordinary indexed doc)
- `src/links/` — Unified link architecture (link registry, resolvers, types) for cross-references between editor and panel cards. Anchor-recovery SSOT: `resolve-card-anchor.ts` (one pure `resolveCardAnchor` ladder — uuid → live mark → snapshot → orphan — plus `buildResolveIndex` + `reconcileCardToResolved`, the single owner every recovery path funnels through), `_shared/reapply-mode-b-anchors.ts` (the single load-time Mode-B mark re-apply WRITER), `_shared/apply-linked-anchors.ts` (the ONE `applyLinkedAnchorsImpl` — the load-time linked-anchor reconcile, imported by `Editor.tsx`'s `applyLinkedAnchors` handle and invoked from the EditorPane reconcile effect; makes the SIDECAR authoritative — re-stamps absent OR disagreeing ranges so a revision/cutter/todo/report/highlight span no longer reloads mislabeled as a note. The load-time reconcile role retired from `EditorLayout.applyLinkedAnchors` now lives here), and `_shared/normalize-text.ts` (the canonical snapshot normalize-at-capture form shared by both ends of the match). Mode-A cards carry a self-healing paragraph snapshot captured at creation and at drop re-anchor
- `src/cards/` — Card-system SSOT (27458d8): `CARD_REGISTRY` (mirrors `TEXT_OBJECT_REGISTRY`), `CardKind`/`CardMeta` types (incl. the `droppable`/`dropPlacement` drop facets that gate the card drop button, plus the declarative `content: CardContentModel | null` facet + `MorphDropField` — one descriptor that `cardHasContent` walks so panel-trash + margin-marker delete-confirms never miss content and morph drop-fields drive the confirm copy), derived predicates, card floats + `drop-specs/`, the unified `deleteMarginItem` path (moved here from `src/lib/cards/`), and `src/cards/lifecycle/` (the card-lifecycle reconciler — `card-lifecycle-signal.ts`, `run-event.ts` executor, `useCardLifecycleReconciler.ts`)
- `src/text-objects/` — TextObject SSOT (`TEXT_OBJECT_REGISTRY`), grab handle, float chrome + per-kind bodies, drop adapters, selection-hydration, `LiftHost.tsx` (the shared grab-handle lift core extracted into a provider — drives the lifted-overlay ghost for both the grab handle and the drop button on popped-out text-object floats), `block-frame.ts` (the one canonical per-block margin geometry — grab handle, drop indicator, and figure chrome all read it so they align by construction)
- `src/floats/` — The `Floatable` contract (the shared shape cards and TextObjects both satisfy), plus the Stack-capture seam (task 332): `stack-capture.ts` (`canCaptureToStack` — the registry-derived declaration `FloatingPanel` resolves ONCE at mousedown, so the icon's capture ring lights only for a kind the capture accepts) and `resolve-floatable.ts` (`captureFloatToStack`, the ONE capture door; the float closes only on a snapshot that actually landed)
- `src/panels/` — Sidebar panel implementations, one folder per panel + `_shared/` + `panel-registry.ts`. `_shared/types.ts`'s `OmniItem` carries an `anchorUuid?` for paragraph-anchored kinds (note/todo/cutter/revision/report/archive) so `useLivePosResolver` re-resolves a LIVE document position from the `DocStructureObserver` block snapshot instead of the stale baked pos (fixes "note cards flash/stack at the top while typing"). Per-card archive lives in `_shared/`: `card-archive-actions.tsx` (the archive button beside trash), `CardViewModeMenu.tsx` + `card-archive-view.tsx` (the View Active/Archives/All menu + `filterByArchiveView`); archived cards hide from active/omni/margin (only highlights are delete-only; footnote became archivable in bug sweep #3 — mirrors citations, with an Archives view in the Footnotes panel). `Omni/omni-archived.ts` (task 476) states the omni's ARCHIVED rule once and applies it to the ASSEMBLED item array — the three renderers of one card (docked list, margin marker, omni gutter) read ONE resolution, so the eleventh builder inherits the rule by existing. The Outline panel (`Outline/`) persists its display prefs across reload + popout via a shared `useSyncExternalStore` store, `Outline/outline-prefs-store.ts`
- `src/types/` — Shared type definitions
- `src/STYLE_GUIDE.md` — Design tokens, panel architecture, UI conventions

## Sibling subsystem: `library/`

The Library tab is its own self-contained tree at the repo root, sibling to `src/`. It has its own components, hooks, lib (parser/serializer/store), Python skill scripts, and `library/styles/library.css`. Path A 7.8 deleted the parallel `library/tiptap/` extension set; the Library Reader now mounts the canonical `<EditorPane>` and shares Virgil's TipTap extensions (PgMarkChip moved to [src/lib/tiptap/pgmark.ts](../../src/lib/tiptap/pgmark.ts)). Cross-tree imports go through the `@library/*` path alias (see `tsconfig.json`, `vitest.config.ts`). The Central tab opens to a stats dashboard (`library/components/LibraryCentralDashboard.tsx` + `library/lib/catalog-stats.ts`; search-first inline palette, a Dashboard|Browse switch); the reader is PDF-by-default with a "Raw Text"/"Virgil Text" toggle and a `p. N` scroll lozenge (`library/components/PageScrollLozenge.tsx`, replacing the deleted `PageScrollStrip.tsx`). View-pref persistence + perf live in `library/lib/`: `view-session-store.ts` (versioned Library view-pref store), `catalog-search.ts` (a flexible catalog-search adapter onto the fuzzy bib search), `list-window.ts` (a hand-rolled fixed-height list windower). Detailed map in [library/AGENTS.md](../../library/AGENTS.md). The static dev sample lives in `library-data/`.

## Sibling subsystem: `editor/`

Editor-side skill bundle (companion to `library/`): the `/editor/review` umbrella plus per-kind subskills that fulfill `ai-requests.json` entries. Self-contained under `editor/` (skills, Python helpers, build script). Wired into the Virgil app via [src/lib/ai-request-bridge.ts](../../src/lib/ai-request-bridge.ts) (collapses per-card `aiRequest:true` flags into the unified queue). (Skills also append completion entries to a per-doc `notifications.json`; the client-side toaster hook that would surface them was never wired and has been removed — task 033.) Built into `public/skill-bundle/` at `predev`/`prebuild` alongside the library bundle. Detailed map in [editor/AGENTS.md](../../editor/AGENTS.md).

## Sibling subsystem: `virgil/`

The **front door** (silo since 2026-05-16 `7be3b5f7`, first documented here; substantially reworked by tasks 473–475): `virgil/skills/start.md` = the `/virgil:start` skill a user reaches by addressing "Virgil" by name ("Virgil, review my doc", "hey Virgil") or asking for help in general terms inside a paper folder. It composes nothing and writes nothing — it resolves the session's doc context and library **mode** (`editor/scripts/library_path.py --mode` → `no-library` | `in-library` | `paper-session`, one question with one cause: *is a library configured, and is this session IN it*), states that context back in one line, and dispatches an `editor:*` / `library:*` specialist **as a subagent**, routing off those skills' own `description:` triggers rather than a hand-kept copy of the skill set. Heavy library ops gate on the mode via each skill's own declared weight. `virgil/build/build-virgil-bundle.mjs` builds it into `public/skill-bundle/` alongside the library + editor bundles; `virgil/dev/rewrite-descriptions.mjs` is the dev-only description rewriter; guards in `virgil/skills/__tests__/`.


## Core user-facing concepts

- **Main text editor** — central prose area with visual rendering of LaTeX commands
- **TextObjects** — the single canonical abstraction for every graspable text unit (paragraph, heading, list, list item, example item, atom block, linkedRange). Adding a new graspable kind = one entry in `TEXT_OBJECT_REGISTRY` + one schema-group annotation; grab handle, float chrome, drop adapter, and marginalia positioning route through that one registry. See `src/text-objects/` and [TEXT-OBJECT-REFACTOR.md](../../TEXT-OBJECT-REFACTOR.md)
- **Paragraph UUIDs** — every TextObject node carries a `uuid` attr so margins and panels can anchor to specific locations (linkedRange uses the `linkedAnchor` mark's `anchorId` instead)
- **Marginalia margin** — left/right strips of icon markers anchored to paragraphs (or any TextObject), representing linked cards (notes, reports, todos, cuts, archives, revisions). Backlog #51 renamed the in-pod "gutter" → "margin" (var family `--margin-*`, `<MarginColumn>`, `MARGINALIA_MARGIN_WIDTH`); "gutter" now names only outside-pod surfaces (code view, panel/dock columns)
- **Side panels** — configurable stack per side: notes, footnotes, citations, bibliography, reports, examples, todo, archive, revisions (comments + suggestions), cutter, outline, search, wordcount, errors, omni
- **Cards** — every panel displays items as themed cards; cards can be popped out as floating windows
- **Links** — unified model connecting editor content to panel cards (three kinds: footnote, citation, anchor). The anchor side parameterizes on `targetKind: TextObjectKind`, so cards can anchor to any TextObject
- **AI exchange** — sidecar JSON files (`suggestions.json`, `revisions.json`, `ai-requests.json`, `bib-review-requests.json`) in `virgil/` folder drive structured UI affordances
- **Citations & bibliography** — natbib + biblatex command families; formatted via citation-js
- **Library tab** — sibling pane to each open document. Outer tabs (DocTab + LibraryTab pair) live in the Virgil bar; inside the library pane there's a second layer of tabs (Central catalog + curated libraries). Self-contained code under `library/`

## Where to look next

- UI vocabulary / user terms → `glossary.md`
- Panels, strips, toolbars → `ui-chrome.md`
- Editor content model, links, marginalia → `main-text.md`
- Registries, hooks, storage, sidecars → `architecture.md`
