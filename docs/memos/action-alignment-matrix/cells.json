[
  {
    "id": "archive@atom-only:citation#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "atom-only:citation",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "citation-only line archives; confirm shown",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@atom-only:citation#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "atom-only:citation",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "citation-only line archives; confirm shown",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@atom-only:footnote#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "atom-only:footnote",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@atom-only:footnote#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "atom-only:footnote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@atom-only:inline-math#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "atom-only:inline-math",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "$\\lambda$-only line: NOT silently no-op'd - slice content.size>0 so it archives; confirm surfaces. Fixed in 80170b3/63ccace.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@atom-only:inline-math#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "atom-only:inline-math",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "$\\lambda$-only line: NOT silently no-op'd - slice content.size>0 so it archives; confirm surfaces. Fixed in 80170b3/63ccace.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@blockquote#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "blockquote",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@blockquote#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@bulletList#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "bulletList",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "descriptorForContainer (item count)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@bulletList#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "bulletList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "descriptorForContainer (item count)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@codeBlock#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "codeBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@codeBlock#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "codeBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@displayMath#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "displayMath",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "atom block -> always-warn confirm; NodeSelection extraction",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@displayMath#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "atom block -> always-warn confirm; NodeSelection extraction",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@exampleBlock#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "exampleBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@exampleBlock#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "exampleBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@exampleItem#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "exampleItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@exampleItem#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "exampleItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@figureBlock#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "figureBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@figureBlock#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "figureBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@graphicsBlock#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "graphicsBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@graphicsBlock#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@heading#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "heading",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "LIFECYCLE -> whole SECTION; descriptorForHeading wide-scope confirm",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@heading#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "LIFECYCLE -> whole SECTION; descriptorForHeading wide-scope confirm",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@latexComment#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "latexComment",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "confirmDestructive returns null (never warn - author noise)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@latexComment#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "confirmDestructive returns null (never warn - author noise)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@linkedRange#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "linkedRange",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "always-danger confirm (underlying text also archived)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@linkedRange#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "linkedRange",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "always-danger confirm (underlying text also archived)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@listItem#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "listItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "cascade: last item collapses the list wrapper too",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@listItem#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "cascade: last item collapses the list wrapper too",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@orderedList#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "orderedList",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@orderedList#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "orderedList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@paragraph#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "paragraph",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "descriptorForSimpleBlock; silent (null confirm) iff empty AND no anchors/atoms",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@paragraph#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "descriptorForSimpleBlock; silent (null confirm) iff empty AND no anchors/atoms",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@selection#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "selection",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "sub-range archive; source paragraph survives so keeps its uuid as anchor; word-count confirm",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@selection#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "sub-range archive; source paragraph survives so keeps its uuid as anchor; word-count confirm",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@texBlock#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "texBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@texBlock#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@titleField#grab",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "titleField",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "TITLE_FIELD_ACTIONS DROPS archive",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "archive@titleField#lightning",
    "category": "Card actions (11)",
    "action": "archive",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "TITLE_FIELD_ACTIONS DROPS archive",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "A (menu-scoped letter hint, grab/lightning surface - NOT global)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:486 (case \"archive\"); destructive confirm: drag-handle-actions.ts:172,711 (resolveDestructiveConfirm) + per-kind confirmDestructive in text-object-registry.ts",
    "backbone": "",
    "expected": {
      "behavior": "Bail ONLY when the resolved range has NO content at all (slice content.size===0) - an atom-only line has empty textContent but non-empty slice, so it archives. Resolve outer range, expandCascadeRange, snapshot rich slice JSON (sliceToDocJson), resolve reanchor target (findPreviousAnchorableBlock for block / keep source paragraph for selection), cleanupLinksInRange (lifecycle.delete each sidecar), delete with LIFECYCLE_DELETE_META, then createArchiveSnippet.",
      "docDelta": "the outer/cascade-extended range deleted from the doc (block removed; wrapper swallowed if emptied)",
      "sidecarDelta": "archive.json +1 ArchivedSnippet (rich content snapshot + reanchored paragraphId/targetKind); any linkedAnchor/atom sidecars inside the range deleted via cardLifecycle",
      "texDelta": "the archived block's .tex removed; its content preserved in the archive snippet sidecar (not the .tex)",
      "lifecycle": "snippet selected + pinned in archive omni; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind confirm gates (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('archive', ref) directly - byte-identical. The destructive-confirm resolution (resolveDestructiveConfirm -> per-kind meta.confirmDestructive, or confirmSelectionDestructive for selection refs) is shared in ONE place.",
    "knownRisk": "HIGH - drive live first. Atom-only line archive was a real bug (silently no-op'd, fixed 80170b3): verify $\\lambda$-only / citation-only / footnote-only lines DO archive AND surface the destructive confirm. The empty-content bail uses slice content.size, NOT textContent. Cascade (last listItem/exampleItem collapses wrapper) and heading whole-section archive both need live checks. Reanchor: block source walks to previous anchorable; selection source keeps source paragraph."
  },
  {
    "id": "citation@blockquote#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "blockquote",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@blockquote#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@blockquote#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "blockquote",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@blockquote#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "blockquote",
    "surface": "typed",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@bulletList#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "bulletList",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@bulletList#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "bulletList",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@bulletList#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "bulletList",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@bulletList#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "bulletList",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@codeBlock#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "codeBlock",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@codeBlock#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "codeBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@codeBlock#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "codeBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@codeBlock#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "codeBlock",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@cursor#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "cursor",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "slash/typed: atom already inserted, register card with same citationId",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@cursor#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "cursor",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "slash/typed: atom already inserted, register card with same citationId",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@cursor#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "cursor",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "slash/typed: atom already inserted, register card with same citationId",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@cursor#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "cursor",
    "surface": "typed",
    "applies": "ok",
    "kindNote": "slash/typed: atom already inserted, register card with same citationId",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@displayMath#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "displayMath",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS DROPS citation",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@displayMath#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS DROPS citation",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@displayMath#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "displayMath",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS DROPS citation",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@displayMath#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "displayMath",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS DROPS citation",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@exampleBlock#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "exampleBlock",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@exampleBlock#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "exampleBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@exampleBlock#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "exampleBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@exampleBlock#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "exampleBlock",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@exampleItem#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "exampleItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@exampleItem#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "exampleItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@exampleItem#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "exampleItem",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@exampleItem#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "exampleItem",
    "surface": "typed",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@figureBlock#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "figureBlock",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@figureBlock#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "figureBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@figureBlock#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "figureBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@figureBlock#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "figureBlock",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@graphicsBlock#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "graphicsBlock",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@graphicsBlock#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@graphicsBlock#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "graphicsBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@graphicsBlock#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "graphicsBlock",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@heading#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "heading",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@heading#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@heading#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "heading",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@heading#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "heading",
    "surface": "typed",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@latexComment#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "latexComment",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@latexComment#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@latexComment#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "latexComment",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@latexComment#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "latexComment",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@linkedRange#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "linkedRange",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@linkedRange#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "linkedRange",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@linkedRange#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "linkedRange",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@linkedRange#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "linkedRange",
    "surface": "typed",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@listItem#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "listItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@listItem#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@listItem#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "listItem",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@listItem#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "listItem",
    "surface": "typed",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@orderedList#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "orderedList",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@orderedList#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "orderedList",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@orderedList#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "orderedList",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@orderedList#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "orderedList",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@paragraph#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "paragraph",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@paragraph#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@paragraph#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "paragraph",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@paragraph#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "paragraph",
    "surface": "typed",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@selection#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "selection",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "collapse to range.to, insert [cite] pill, anchored card",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@selection#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "collapse to range.to, insert [cite] pill, anchored card",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@selection#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "selection",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "collapse to range.to, insert [cite] pill, anchored card",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@selection#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "selection",
    "surface": "typed",
    "applies": "ok",
    "kindNote": "collapse to range.to, insert [cite] pill, anchored card",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@texBlock#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "texBlock",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@texBlock#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@texBlock#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "texBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@texBlock#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "texBlock",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@titleField#grab",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "titleField",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "TITLE_FIELD_ACTIONS DROPS citation (a title has no bibliography)",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@titleField#lightning",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "TITLE_FIELD_ACTIONS DROPS citation (a title has no bibliography)",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@titleField#slash",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "titleField",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "TITLE_FIELD_ACTIONS DROPS citation (a title has no bibliography)",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "citation@titleField#typed",
    "category": "Card actions (11)",
    "action": "citation",
    "kind": "titleField",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "TITLE_FIELD_ACTIONS DROPS citation (a title has no bibliography)",
    "slashName": "\\cite",
    "inputRulePattern": "CITE_RE_FULL (\\cite{key}) canonical; CITE_RE_BARE (\\cite ) second trigger - @/lib/cite-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1070 (citationRun); grab/lightning -> drag-handle-actions.ts:273; slash -> commands.ts:138; typed full -> citation.ts:149, bare -> citation.ts:182",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse to range.to, mint citationId, handle.insertCitation('\\cite{}', id, '') drops the pill, createCitation({command:'\\cite{}', citationId, unanchored:false}) registers the anchored card. slash/typed: PM caller already inserted the citation atom; citationRun registers the card with the SAME citationId+command and soft-routes into citations-side omni, focusing the library-picker.",
      "docDelta": "one inline citation atom inserted (grab/lightning) or already-present (slash/typed)",
      "sidecarDelta": "citations.json +1 CitationRef {id, command, keys (parsed from command), createdAt, unanchored absent (anchored)}; pristine marked when keys empty",
      "texDelta": "\\cite{} (empty) or \\cite{key} (typed full) at the passage-end/caret",
      "lifecycle": "anchored card selected + pinned; slash/typed focus the library-picker input and soft-route only if citations side collapsed/blank"
    },
    "crossSurfaceIdentity": "Atom attrs {citationId, command, displayText:''} must be byte-identical across surfaces. CitationRef sidecar shape identical (id===citationId; keys parsed from command). Difference: typed \\cite{key} carries the FULL command (renders keys) while menu/slash carry empty \\cite{}.",
    "knownRisk": "Typed \\cite{key} was the CHIP 4a-ii bug fix (previously made NO card). Drive typed full-command live: verify the card renders the parsed keys (command preserved) and is anchored. Verify bare \\cite followed by space also registers an anchored card (citation.ts:182). Both must share the citationId of the synchronously-inserted atom."
  },
  {
    "id": "cutter@blockquote#grab",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "blockquote",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@blockquote#lightning",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@bulletList#grab",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "bulletList",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@bulletList#lightning",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "bulletList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@codeBlock#grab",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "codeBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@codeBlock#lightning",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "codeBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@displayMath#grab",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "displayMath",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS keeps cutter",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@displayMath#lightning",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS keeps cutter",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@exampleItem#grab",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "exampleItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@exampleItem#lightning",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "exampleItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@figureBlock#grab",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "figureBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@figureBlock#lightning",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "figureBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@graphicsBlock#grab",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "graphicsBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@graphicsBlock#lightning",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@heading#grab",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "heading",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@heading#lightning",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@latexComment#grab",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "latexComment",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@latexComment#lightning",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@linkedRange#grab",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "linkedRange",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@linkedRange#lightning",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "linkedRange",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@listItem#grab",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "listItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@listItem#lightning",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@orderedList#grab",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "orderedList",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@orderedList#lightning",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "orderedList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@paragraph#grab",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "paragraph",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@paragraph#lightning",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@selection#grab",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "selection",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "range -> 'cutter-comment' linkedAnchor + bound card",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@selection#lightning",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "range -> 'cutter-comment' linkedAnchor + bound card",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@texBlock#grab",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "texBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@texBlock#lightning",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@titleField#grab",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "titleField",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "TITLE_FIELD_ACTIONS includes cutter",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "cutter@titleField#lightning",
    "category": "Card actions (11)",
    "action": "cutter",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "TITLE_FIELD_ACTIONS includes cutter",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:356 (case \"cutter\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a CutterComment card ('Suggest cut') via createCutterComment in the Cutter panel. Drops a 'cutter-comment' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "cutter.json +1 CutterCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in cutter omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('cutter', ref) directly - byte-identical. Produces a cutter-COMMENT (not cutter-suggestion).",
    "knownRisk": "Low. Verify the produced kind is cutter-comment. cutter is in NON_PROSE_BLOCK_ACTIONS (unlike suggest-edit), so it stays enabled on non-prose blocks - confirm the asymmetry with suggest-edit."
  },
  {
    "id": "delete@atom-only:citation#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "atom-only:citation",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@atom-only:citation#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "atom-only:citation",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@atom-only:footnote#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "atom-only:footnote",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@atom-only:footnote#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "atom-only:footnote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@atom-only:inline-math#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "atom-only:inline-math",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "math/cite/footnote-only line is deletable AND surfaces the destructive confirm (was silently deletable before 63ccace)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@atom-only:inline-math#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "atom-only:inline-math",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "math/cite/footnote-only line is deletable AND surfaces the destructive confirm (was silently deletable before 63ccace)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@blockquote#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "blockquote",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@blockquote#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@bulletList#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "bulletList",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "descriptorForContainer (item count)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@bulletList#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "bulletList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "descriptorForContainer (item count)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@codeBlock#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "codeBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@codeBlock#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "codeBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@displayMath#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "displayMath",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "atom block -> always-warn; danger tone",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@displayMath#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "atom block -> always-warn; danger tone",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@exampleBlock#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "exampleBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@exampleBlock#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "exampleBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@exampleItem#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "exampleItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@exampleItem#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "exampleItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@figureBlock#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "figureBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "deletes figure + caption",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@figureBlock#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "figureBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "deletes figure + caption",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@graphicsBlock#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "graphicsBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@graphicsBlock#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@heading#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "heading",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "LIFECYCLE -> whole SECTION; descriptorForHeading; tone danger",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@heading#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "LIFECYCLE -> whole SECTION; descriptorForHeading; tone danger",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@latexComment#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "latexComment",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "confirmDestructive null -> no warning (author noise, cheap to redo)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@latexComment#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "confirmDestructive null -> no warning (author noise, cheap to redo)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@linkedRange#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "linkedRange",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "always-danger confirm (underlying text + anchored cards removed)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@linkedRange#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "linkedRange",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "always-danger confirm (underlying text + anchored cards removed)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@listItem#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "listItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "cascade: last item swallows the list wrapper (no PM auto-fill placeholder)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@listItem#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "cascade: last item swallows the list wrapper (no PM auto-fill placeholder)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@orderedList#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "orderedList",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@orderedList#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "orderedList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@paragraph#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "paragraph",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "descriptorForSimpleBlock w/ preview; null (silent) iff empty AND no anchors/atoms",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@paragraph#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "descriptorForSimpleBlock w/ preview; null (silent) iff empty AND no anchors/atoms",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@selection#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "selection",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "word-count confirm (confirmSelectionDestructive)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@selection#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "word-count confirm (confirmSelectionDestructive)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@texBlock#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "texBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@texBlock#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@titleField#grab",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "titleField",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "TITLE_FIELD_ACTIONS DROPS delete",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "delete@titleField#lightning",
    "category": "Card actions (11)",
    "action": "delete",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "TITLE_FIELD_ACTIONS DROPS delete",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "Backspace / Delete (menu-scoped, grab surface - fires only while the menu is open, DragHandleMenu.tsx:148; NOT global). Letter hint del.",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:578 (case \"delete\"); destructive confirm: drag-handle-actions.ts:172,711",
    "backbone": "",
    "expected": {
      "behavior": "Resolve outer range (heading -> whole section), expandCascadeRange (swallow a wrapper that would be emptied - so PM content-rule auto-fill never injects a placeholder), cleanupLinksInRange (lifecycle.delete each sidecar-bearing footnote/citation atom + linkedAnchor mark in range), then delete with LIFECYCLE_DELETE_META. NO snapshot/archive - the content is gone.",
      "docDelta": "the outer/cascade-extended range deleted; emptied wrapper removed too",
      "sidecarDelta": "every linkedAnchor/atom sidecar inside the range deleted via cardLifecycle.delete (no orphan sidecar entries left)",
      "texDelta": "the block's .tex removed entirely (along with nested \\footnote/\\cite/\\vlid markers)",
      "lifecycle": "no card produced; LIFECYCLE_DELETE_META suppresses MarginaliaAnchorGuard re-insert; TextObjectOrphanGuard sweeps orphaned Mode-A cards; per-kind destructive confirm (danger tone) gates - cancel = silent no-op; on no-card path ed.view.focus() restores focus so Cmd-Z reaches the doc (B4)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('delete', ref) directly - byte-identical. Shares the destructive-confirm resolution + cascade + cleanup with archive (delete is archive minus the snapshot+snippet).",
    "knownRisk": "HIGH - drive live first. Atom-only/atom-bearing: a math/cite/footnote-only line was SILENTLY DELETABLE without a confirm before 63ccace - verify the destructive confirm now surfaces. Cleanup must remove the nested footnote/citation sidecars (no orphans). Cascade on last listItem/exampleItem must swallow the wrapper. Heading delete = whole section, danger tone. Confirm focus restoration (B4) after the confirm dialog closes so Cmd-Z works. titleField delete must be disabled (singleton protection)."
  },
  {
    "id": "duplicate@blockquote#grab",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "blockquote",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@blockquote#lightning",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@bulletList#grab",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "bulletList",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@bulletList#lightning",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "bulletList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@codeBlock#grab",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "codeBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@codeBlock#lightning",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "codeBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@displayMath#grab",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "displayMath",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "atom block -> NodeSelection on the clone, not TextSelection.near",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@displayMath#lightning",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "atom block -> NodeSelection on the clone, not TextSelection.near",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@exampleBlock#grab",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "exampleBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@exampleBlock#lightning",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "exampleBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@exampleItem#grab",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "exampleItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@exampleItem#lightning",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "exampleItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@figureBlock#grab",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "figureBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@figureBlock#lightning",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "figureBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@graphicsBlock#grab",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "graphicsBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@graphicsBlock#lightning",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@heading#grab",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "heading",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "LIFECYCLE action -> whole SECTION duplicated; wide-scope confirm (confirmHeadingLifecycle), non-destructive so NOT via confirmDestructive",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@heading#lightning",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "LIFECYCLE action -> whole SECTION duplicated; wide-scope confirm (confirmHeadingLifecycle), non-destructive so NOT via confirmDestructive",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@latexComment#grab",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "latexComment",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@latexComment#lightning",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@linkedRange#grab",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "linkedRange",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "LINKED_RANGE_ACTIONS DROPS duplicate (cloning a mark-backed range mints two marks with the same id -> linkedAnchor id-uniqueness violation)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@linkedRange#lightning",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "linkedRange",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "LINKED_RANGE_ACTIONS DROPS duplicate (cloning a mark-backed range mints two marks with the same id -> linkedAnchor id-uniqueness violation)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@listItem#grab",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "listItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@listItem#lightning",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@orderedList#grab",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "orderedList",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@orderedList#lightning",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "orderedList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@paragraph#grab",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "paragraph",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@paragraph#lightning",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@selection#grab",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "selection",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@selection#lightning",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@texBlock#grab",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "texBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@texBlock#lightning",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@titleField#grab",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "titleField",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "TITLE_FIELD_ACTIONS DROPS duplicate (singleton; also caught by pre-dispatch tr.doc.check())",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "duplicate@titleField#lightning",
    "category": "Card actions (11)",
    "action": "duplicate",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "TITLE_FIELD_ACTIONS DROPS duplicate (singleton; also caught by pre-dispatch tr.doc.check())",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "D (menu-scoped letter hint, grab/lightning surface - NOT a global keybinding)",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:402 (case \"duplicate\"); heading confirm: drag-handle-actions.ts:180,664 (confirmHeadingLifecycle)",
    "backbone": "",
    "expected": {
      "behavior": "Resolves the OUTER node range (outerRangeFor - heading -> whole section), slices the doc, clones via duplicateSlice (mints fresh anchorIds + new linkCard keys + forks sidecar entries via cardLifecycle.clone), validates the new doc with tr.doc.check() BEFORE dispatch, inserts the clone after outer.to, places NodeSelection (atom blocks) or caret-near (text), then rewireClonedAnchors binds each cloned card's links to the new mark.",
      "docDelta": "block(s) added - a duplicate of the outer node range inserted immediately after it (whole section for heading)",
      "sidecarDelta": "for each cloned linkedAnchor/atom in the slice: cardLifecycle.clone forks a new sidecar entry (fresh id); cloned cards' links rewired to the clone",
      "texDelta": "duplicated block(s) re-serialized with fresh \\vlid/\\footnote/\\cite ids; section duplicate for heading",
      "lifecycle": "no card produced (duplicate is structural); clone selected via NodeSelection/caret; heading x duplicate shows a section-scope confirm first (cancel = silent no-op)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('duplicate', ref) directly - byte-identical. The whole duplicate flow (fail-loud B1: stale ref/empty slice/schema reject -> notify+abort) lives in ONE dispatch case.",
    "knownRisk": "HIGH - drive live first. Atom-only / atom-bearing: cloning a slice carrying footnote/citation/inline-math atoms must mint FRESH atom ids (else duplicate ids corrupt renumber - the atom_drag observer bug class). titleField x duplicate is double-guarded (action-set drop + tr.doc.check); confirm both. linkedRange x duplicate is dropped (id-uniqueness). Heading duplicate must copy the WHOLE section and warn. The pre-dispatch tr.doc.check() is the only thing preventing a doomed transaction."
  },
  {
    "id": "footnote@blockquote#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "blockquote",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@blockquote#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@blockquote#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "blockquote",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@blockquote#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "blockquote",
    "surface": "typed",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@bulletList#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "bulletList",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@bulletList#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "bulletList",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@bulletList#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "bulletList",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@bulletList#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "bulletList",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@codeBlock#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "codeBlock",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@codeBlock#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "codeBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@codeBlock#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "codeBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@codeBlock#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "codeBlock",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@cursor#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "cursor",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "slash/typed at caret -> adopt the synchronously-inserted atom",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@cursor#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "cursor",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "slash/typed at caret -> adopt the synchronously-inserted atom",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@cursor#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "cursor",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "slash/typed at caret -> adopt the synchronously-inserted atom",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@cursor#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "cursor",
    "surface": "typed",
    "applies": "ok",
    "kindNote": "slash/typed at caret -> adopt the synchronously-inserted atom",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@displayMath#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "displayMath",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS DROPS footnote - no inline slot in a non-prose block",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@displayMath#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS DROPS footnote - no inline slot in a non-prose block",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@displayMath#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "displayMath",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS DROPS footnote - no inline slot in a non-prose block",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@displayMath#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "displayMath",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS DROPS footnote - no inline slot in a non-prose block",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@exampleBlock#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "exampleBlock",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@exampleBlock#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "exampleBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@exampleBlock#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "exampleBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@exampleBlock#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "exampleBlock",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@exampleItem#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "exampleItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@exampleItem#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "exampleItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@exampleItem#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "exampleItem",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@exampleItem#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "exampleItem",
    "surface": "typed",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@figureBlock#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "figureBlock",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@figureBlock#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "figureBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@figureBlock#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "figureBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@figureBlock#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "figureBlock",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@graphicsBlock#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "graphicsBlock",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@graphicsBlock#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@graphicsBlock#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "graphicsBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@graphicsBlock#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "graphicsBlock",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@heading#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "heading",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "TITLE_FIELD/PROSE both include footnote; annotation -> heading line, collapse to to",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@heading#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "TITLE_FIELD/PROSE both include footnote; annotation -> heading line, collapse to to",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@heading#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "heading",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "TITLE_FIELD/PROSE both include footnote; annotation -> heading line, collapse to to",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@heading#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "heading",
    "surface": "typed",
    "applies": "ok",
    "kindNote": "TITLE_FIELD/PROSE both include footnote; annotation -> heading line, collapse to to",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@latexComment#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "latexComment",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@latexComment#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@latexComment#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "latexComment",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@latexComment#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "latexComment",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@linkedRange#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "linkedRange",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@linkedRange#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "linkedRange",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@linkedRange#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "linkedRange",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@linkedRange#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "linkedRange",
    "surface": "typed",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@listItem#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "listItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@listItem#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@listItem#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "listItem",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@listItem#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "listItem",
    "surface": "typed",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@orderedList#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "orderedList",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@orderedList#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "orderedList",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@orderedList#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "orderedList",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@orderedList#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "orderedList",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@paragraph#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "paragraph",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@paragraph#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@paragraph#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "paragraph",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@paragraph#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "paragraph",
    "surface": "typed",
    "applies": "ok",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@selection#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "selection",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "collapse to range.to, insert empty footnote atom",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@selection#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "collapse to range.to, insert empty footnote atom",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@selection#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "selection",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "collapse to range.to, insert empty footnote atom",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@selection#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "selection",
    "surface": "typed",
    "applies": "ok",
    "kindNote": "collapse to range.to, insert empty footnote atom",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@texBlock#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "texBlock",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@texBlock#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@texBlock#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "texBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@texBlock#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "texBlock",
    "surface": "typed",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@titleField#grab",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "titleField",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "TITLE_FIELD_ACTIONS includes footnote (round-trips as \\thanks{})",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@titleField#lightning",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "TITLE_FIELD_ACTIONS includes footnote (round-trips as \\thanks{})",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@titleField#slash",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "titleField",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "TITLE_FIELD_ACTIONS includes footnote (round-trips as \\thanks{})",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "footnote@titleField#typed",
    "category": "Card actions (11)",
    "action": "footnote",
    "kind": "titleField",
    "surface": "typed",
    "applies": "ok",
    "kindNote": "TITLE_FIELD_ACTIONS includes footnote (round-trips as \\thanks{})",
    "slashName": "\\footnote",
    "inputRulePattern": "FOOTNOTE_RE_FULL (\\footnote{...}) - @/lib/footnote-commands",
    "keybinding": "",
    "runRef": "action-registry.ts:1158 (footnoteRun); grab/lightning -> drag-handle-actions.ts:253; slash -> commands.ts:177; typed -> footnote.ts:106 (handleTextInput)",
    "backbone": "",
    "expected": {
      "behavior": "grab/lightning: collapse selection to range.to, createFootnote({fromSelection:false}) -> inserts empty footnote atom + pristine+pinned card. slash/typed: PM caller ALREADY inserted the \\footnote{} atom synchronously; footnoteRun ADOPTS it via createFootnote({existingFootnoteId, pristine}) - pristine:true for blank, false for typed \\footnote{body} - then soft-routes into footnotes-side omni only if collapsed/blank.",
      "docDelta": "one inline footnote atom inserted (grab/lightning) or already-present (slash/typed, adopted not re-inserted)",
      "sidecarDelta": "footnotes sidecar +1; pristine flag set when body blank; renumberFootnotes() reflows numbers",
      "texDelta": "\\footnote{body} at the caret/passage-end (or \\thanks{} when inside a titleField/author)",
      "lifecycle": "card pristine (blank -> click-away-discardable), pinned, selected; slash/typed soft-route never force-opens Footnotes panel; menu mode omni"
    },
    "crossSurfaceIdentity": "All four surfaces must land the SAME footnote atom (footnoteId, number:0 pre-renumber, empty content) AND the SAME pristine+pinned+selected card lifecycle. Slash/typed adopt (no double-insert); menu inserts. Card footnoteId === atom footnoteId on every surface. CHIP 4b aligned slash/typed to the menu.",
    "knownRisk": "Pristine alignment: typed \\footnote{realtext} must pass pristine:false (footnote.ts:172 computes match[1].trim().length===0) so the click-away discarder doesn't reap a typed body. Drive typed-with-body live to confirm it is NOT reaped. Also: slash/typed adopt path runs renumberFootnotes but skips re-insert - verify no double-insert."
  },
  {
    "id": "highlight@atom-bearing:citation#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "atom-bearing:citation",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@atom-bearing:citation#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "atom-bearing:citation",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@atom-only:inline-math#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "atom-only:inline-math",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "math-only line selected/blocked-ref still has a range -> ok",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@atom-only:inline-math#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "atom-only:inline-math",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "math-only line selected/blocked-ref still has a range -> ok",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@blockquote#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "blockquote",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@blockquote#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@bulletList#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "bulletList",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@bulletList#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "bulletList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@codeBlock#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "codeBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@codeBlock#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "codeBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@displayMath#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "displayMath",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS keeps highlight (only F/C/suggest-edit dropped)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@displayMath#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS keeps highlight (only F/C/suggest-edit dropped)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@exampleItem#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "exampleItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@exampleItem#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "exampleItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@figureBlock#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "figureBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@figureBlock#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "figureBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@heading#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "heading",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "annotation action -> wraps the HEADING LINE only (collectAnnotationRange), never the section body",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@heading#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "annotation action -> wraps the HEADING LINE only (collectAnnotationRange), never the section body",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@latexComment#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "latexComment",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@latexComment#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@linkedRange#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "linkedRange",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "LINKED_RANGE_ACTIONS keeps highlight; grab menu never opens for linkedRange but applies() still computes ok",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@linkedRange#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "linkedRange",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "LINKED_RANGE_ACTIONS keeps highlight; grab menu never opens for linkedRange but applies() still computes ok",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@listItem#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "listItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@listItem#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@orderedList#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "orderedList",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@orderedList#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "orderedList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@paragraph#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "paragraph",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "block ref -> selection spans whole content range, mark wraps entire passage",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@paragraph#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "block ref -> selection spans whole content range, mark wraps entire passage",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@selection#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "selection",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "non-empty range wraps; empty selection -> disabled via selection:'required'",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@selection#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "non-empty range wraps; empty selection -> disabled via selection:'required'",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@selection:cursor-empty#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "selection:cursor-empty",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "empty/collapsed selection (mode 'cursor') -> disabled; the ONE card action with selection:'required'",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@selection:cursor-empty#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "selection:cursor-empty",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "empty/collapsed selection (mode 'cursor') -> disabled; the ONE card action with selection:'required'",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@texBlock#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "texBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@texBlock#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@titleField#grab",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "titleField",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "TITLE_FIELD_ACTIONS includes highlight",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "highlight@titleField#lightning",
    "category": "Card actions (11)",
    "action": "highlight",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "TITLE_FIELD_ACTIONS includes highlight",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:311 (case \"highlight\")",
    "backbone": "",
    "expected": {
      "behavior": "Wraps the resolved range in a linkedAnchor mark (tintColor #fbbf24), mints a HighlightCard via createHighlight, binds the card to the anchor, selects+pins it in the notes panel omni view, focuses the new card. No-ops (break) if textBetween is empty.",
      "docDelta": "linkedAnchor mark added over the range (no block added/removed)",
      "sidecarDelta": "highlights/notes sidecar +1 HighlightCard; anchor linkCard attr stamped on the mark",
      "texDelta": "\\vlid{id}...\\vlidend{id} paired markers wrap the highlighted text on round-trip (linkedRange sourceMarker 'vlid')",
      "lifecycle": "card selected + pinned (recentlyAdded) in notes panel, NOT popped as a float (mode omni); focusNewCard drops caret into card"
    },
    "crossSurfaceIdentity": "grab and lightning both route to dragHandleMenu.dispatch('highlight', ref) - NOT row.run() - so they are byte-identical by construction (same dispatch case). The registry run() (cardRun->ctx.dispatch) is the declarative twin only.",
    "knownRisk": "Heading x highlight: must wrap heading LINE only (collectAnnotationRange) - a regression here corrupts \\section{} braces and strips every linkedAnchor on reload (the C9/C11 bug). Drive heading x highlight live first. Also verify empty-selection grey-out in cursor mode (lightning mode:'cursor')."
  },
  {
    "id": "report@blockquote#grab",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "blockquote",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@blockquote#lightning",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@bulletList#grab",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "bulletList",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@bulletList#lightning",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "bulletList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@codeBlock#grab",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "codeBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@codeBlock#lightning",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "codeBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@displayMath#grab",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "displayMath",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS keeps report",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@displayMath#lightning",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS keeps report",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@exampleItem#grab",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "exampleItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@exampleItem#lightning",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "exampleItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@figureBlock#grab",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "figureBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@figureBlock#lightning",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "figureBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@graphicsBlock#grab",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "graphicsBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@graphicsBlock#lightning",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@heading#grab",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "heading",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@heading#lightning",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@latexComment#grab",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "latexComment",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@latexComment#lightning",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@linkedRange#grab",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "linkedRange",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@linkedRange#lightning",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "linkedRange",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@listItem#grab",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "listItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@listItem#lightning",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@orderedList#grab",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "orderedList",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@orderedList#lightning",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "orderedList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@paragraph#grab",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "paragraph",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@paragraph#lightning",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@selection#grab",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "selection",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "range -> 'report-request' linkedAnchor + bound card",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@selection#lightning",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "range -> 'report-request' linkedAnchor + bound card",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@texBlock#grab",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "texBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@texBlock#lightning",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@titleField#grab",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "titleField",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "TITLE_FIELD_ACTIONS includes report",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "report@titleField#lightning",
    "category": "Card actions (11)",
    "action": "report",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "TITLE_FIELD_ACTIONS includes report",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:378 (case \"report\")",
    "backbone": "",
    "expected": {
      "behavior": "Files a Report REQUEST (the ask), NOT an authored Report - createReportRequest in the Reports panel. Drops a 'report-request' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "reports.json +1 ReportRequestCard (kind report-request, not report); targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in reports omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('report', ref) directly - byte-identical. The id 'report' produces a report-REQUEST card (the quick gesture files the ask; an AI authors the actual Report later).",
    "knownRisk": "Low-medium. Verify the produced kind is report-request (not report). Both report and report-request pin under the same 'reports' RecentlyAddedKind bucket."
  },
  {
    "id": "suggest-edit@blockquote#grab",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "blockquote",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@blockquote#lightning",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@bulletList#grab",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "bulletList",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@bulletList#lightning",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "bulletList",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@codeBlock#grab",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "codeBlock",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@codeBlock#lightning",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "codeBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@displayMath#grab",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "displayMath",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS DROPS suggest-edit",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@displayMath#lightning",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS DROPS suggest-edit",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@exampleBlock#grab",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "exampleBlock",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@exampleBlock#lightning",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "exampleBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@exampleItem#grab",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "exampleItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@exampleItem#lightning",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "exampleItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@figureBlock#grab",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "figureBlock",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@figureBlock#lightning",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "figureBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@graphicsBlock#grab",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "graphicsBlock",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@graphicsBlock#lightning",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@heading#grab",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "heading",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@heading#lightning",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@latexComment#grab",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "latexComment",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@latexComment#lightning",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@linkedRange#grab",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "linkedRange",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@linkedRange#lightning",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "linkedRange",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@listItem#grab",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "listItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@listItem#lightning",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@orderedList#grab",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "orderedList",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@orderedList#lightning",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "orderedList",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@paragraph#grab",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "paragraph",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@paragraph#lightning",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@selection#grab",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "selection",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "range source -> 'revision' linkedAnchor + bound card",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@selection#lightning",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "range source -> 'revision' linkedAnchor + bound card",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@texBlock#grab",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "texBlock",
    "surface": "grab",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@texBlock#lightning",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@titleField#grab",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "titleField",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "TITLE_FIELD_ACTIONS includes suggest-edit",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "suggest-edit@titleField#lightning",
    "category": "Card actions (11)",
    "action": "suggest-edit",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "TITLE_FIELD_ACTIONS includes suggest-edit",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:341 (case \"suggest-edit\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a RevisionComment card (createRevisionComment) in the Revisions panel - the quick gesture drafts a revision-COMMENT (the conversation opener), NOT a suggestion-with-rewrite. Drops a 'revision' linkedAnchor for a range source and binds the card.",
      "docDelta": "range source -> linkedAnchor mark; block source -> none (Mode-A)",
      "sidecarDelta": "revisions.json +1 RevisionCommentCard; targetKind recorded",
      "texDelta": "range source -> \\vlid markers; block source -> none",
      "lifecycle": "card selected + pinned in revisions omni, focusNewCard; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('suggest-edit', ref) directly - byte-identical. The action id 'suggest-edit' maps to a revision-COMMENT card (not revision-suggestion); the suggestion-with-rewrite is a separate responder-skill output.",
    "knownRisk": "Low-medium. The label says 'suggest edit' but the gesture produces a comment card - verify the produced card kind is revision-comment (not revision-suggestion). The NON_PROSE_BLOCK drop of suggest-edit (alongside F/C) is easy to get wrong if NON_PROSE_BLOCK_ACTIONS is edited."
  },
  {
    "id": "author@cursor#slash",
    "category": "Heading + title (7)",
    "action": "author",
    "kind": "cursor",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "Slash builds a CursorRef -> blockApplies 'ok'. Ref ignored by run; doc-top find-or-insert.",
    "slashName": "author",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun) via titleFieldRow(\"author\"):1382; routed by runViewOnlyAction(\"author\") commands.ts:94",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-or-insert of a \\author titleField. Same mechanics as 'title' but field:'author', canonical order index 1 (between title=0 and date=2). If exists -> caret to end, no duplicate. If absent -> insert at order position 1, empty content, caret inside. Collab read-only -> no-op.",
      "docDelta": "If absent: +1 titleField{field:'author'} at order index 1 (after any \\title, before any \\date / non-title). If present: zero doc mutation (caret move only).",
      "texDelta": "If absent: +\\author{} on serialize. If present: no change.",
      "sidecarDelta": "None.",
      "lifecycle": "No card, no float - in-doc lozenge."
    },
    "crossSurfaceIdentity": "Slash-only - no cross-surface match required. Invariant: idempotency (no duplicate author field) + canonical ordering (author between title and date). Minted attrs round-trip identically to a parsed \\author.",
    "knownRisk": "Ordering insert is the risk: with both \\title and \\date present, \\author must land at index 1 (between them). Verify live. Also verify idempotent no-op on a second \\author."
  },
  {
    "id": "author@displayMath#slash",
    "category": "Heading + title (7)",
    "action": "author",
    "kind": "displayMath",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled' (theoretical; slash-only).",
    "slashName": "author",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun) via titleFieldRow(\"author\"):1382; routed by runViewOnlyAction(\"author\") commands.ts:94",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-or-insert of a \\author titleField. Same mechanics as 'title' but field:'author', canonical order index 1 (between title=0 and date=2). If exists -> caret to end, no duplicate. If absent -> insert at order position 1, empty content, caret inside. Collab read-only -> no-op.",
      "docDelta": "If absent: +1 titleField{field:'author'} at order index 1 (after any \\title, before any \\date / non-title). If present: zero doc mutation (caret move only).",
      "texDelta": "If absent: +\\author{} on serialize. If present: no change.",
      "sidecarDelta": "None.",
      "lifecycle": "No card, no float - in-doc lozenge."
    },
    "crossSurfaceIdentity": "Slash-only - no cross-surface match required. Invariant: idempotency (no duplicate author field) + canonical ordering (author between title and date). Minted attrs round-trip identically to a parsed \\author.",
    "knownRisk": "Ordering insert is the risk: with both \\title and \\date present, \\author must land at index 1 (between them). Verify live. Also verify idempotent no-op on a second \\author."
  },
  {
    "id": "author@figureBlock#slash",
    "category": "Heading + title (7)",
    "action": "author",
    "kind": "figureBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled' (theoretical).",
    "slashName": "author",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun) via titleFieldRow(\"author\"):1382; routed by runViewOnlyAction(\"author\") commands.ts:94",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-or-insert of a \\author titleField. Same mechanics as 'title' but field:'author', canonical order index 1 (between title=0 and date=2). If exists -> caret to end, no duplicate. If absent -> insert at order position 1, empty content, caret inside. Collab read-only -> no-op.",
      "docDelta": "If absent: +1 titleField{field:'author'} at order index 1 (after any \\title, before any \\date / non-title). If present: zero doc mutation (caret move only).",
      "texDelta": "If absent: +\\author{} on serialize. If present: no change.",
      "sidecarDelta": "None.",
      "lifecycle": "No card, no float - in-doc lozenge."
    },
    "crossSurfaceIdentity": "Slash-only - no cross-surface match required. Invariant: idempotency (no duplicate author field) + canonical ordering (author between title and date). Minted attrs round-trip identically to a parsed \\author.",
    "knownRisk": "Ordering insert is the risk: with both \\title and \\date present, \\author must land at index 1 (between them). Verify live. Also verify idempotent no-op on a second \\author."
  },
  {
    "id": "author@selection#slash",
    "category": "Heading + title (7)",
    "action": "author",
    "kind": "selection",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "blockApplies base 'ok'; selection 'ignored' so range never greys.",
    "slashName": "author",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun) via titleFieldRow(\"author\"):1382; routed by runViewOnlyAction(\"author\") commands.ts:94",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-or-insert of a \\author titleField. Same mechanics as 'title' but field:'author', canonical order index 1 (between title=0 and date=2). If exists -> caret to end, no duplicate. If absent -> insert at order position 1, empty content, caret inside. Collab read-only -> no-op.",
      "docDelta": "If absent: +1 titleField{field:'author'} at order index 1 (after any \\title, before any \\date / non-title). If present: zero doc mutation (caret move only).",
      "texDelta": "If absent: +\\author{} on serialize. If present: no change.",
      "sidecarDelta": "None.",
      "lifecycle": "No card, no float - in-doc lozenge."
    },
    "crossSurfaceIdentity": "Slash-only - no cross-surface match required. Invariant: idempotency (no duplicate author field) + canonical ordering (author between title and date). Minted attrs round-trip identically to a parsed \\author.",
    "knownRisk": "Ordering insert is the risk: with both \\title and \\date present, \\author must land at index 1 (between them). Verify live. Also verify idempotent no-op on a second \\author."
  },
  {
    "id": "author@texBlock#slash",
    "category": "Heading + title (7)",
    "action": "author",
    "kind": "texBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled' (theoretical).",
    "slashName": "author",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun) via titleFieldRow(\"author\"):1382; routed by runViewOnlyAction(\"author\") commands.ts:94",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-or-insert of a \\author titleField. Same mechanics as 'title' but field:'author', canonical order index 1 (between title=0 and date=2). If exists -> caret to end, no duplicate. If absent -> insert at order position 1, empty content, caret inside. Collab read-only -> no-op.",
      "docDelta": "If absent: +1 titleField{field:'author'} at order index 1 (after any \\title, before any \\date / non-title). If present: zero doc mutation (caret move only).",
      "texDelta": "If absent: +\\author{} on serialize. If present: no change.",
      "sidecarDelta": "None.",
      "lifecycle": "No card, no float - in-doc lozenge."
    },
    "crossSurfaceIdentity": "Slash-only - no cross-surface match required. Invariant: idempotency (no duplicate author field) + canonical ordering (author between title and date). Minted attrs round-trip identically to a parsed \\author.",
    "knownRisk": "Ordering insert is the risk: with both \\title and \\date present, \\author must land at index 1 (between them). Verify live. Also verify idempotent no-op on a second \\author."
  },
  {
    "id": "author@titleField#slash",
    "category": "Heading + title (7)",
    "action": "author",
    "kind": "titleField",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "Idempotent: existing author field -> caret to content end, no duplicate.",
    "slashName": "author",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun) via titleFieldRow(\"author\"):1382; routed by runViewOnlyAction(\"author\") commands.ts:94",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-or-insert of a \\author titleField. Same mechanics as 'title' but field:'author', canonical order index 1 (between title=0 and date=2). If exists -> caret to end, no duplicate. If absent -> insert at order position 1, empty content, caret inside. Collab read-only -> no-op.",
      "docDelta": "If absent: +1 titleField{field:'author'} at order index 1 (after any \\title, before any \\date / non-title). If present: zero doc mutation (caret move only).",
      "texDelta": "If absent: +\\author{} on serialize. If present: no change.",
      "sidecarDelta": "None.",
      "lifecycle": "No card, no float - in-doc lozenge."
    },
    "crossSurfaceIdentity": "Slash-only - no cross-surface match required. Invariant: idempotency (no duplicate author field) + canonical ordering (author between title and date). Minted attrs round-trip identically to a parsed \\author.",
    "knownRisk": "Ordering insert is the risk: with both \\title and \\date present, \\author must land at index 1 (between them). Verify live. Also verify idempotent no-op on a second \\author."
  },
  {
    "id": "date@cursor#slash",
    "category": "Heading + title (7)",
    "action": "date",
    "kind": "cursor",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "Slash CursorRef -> blockApplies 'ok'. Ref ignored; doc-top find-or-insert.",
    "slashName": "date",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun) via titleFieldRow(\"date\"):1382; routed by runViewOnlyAction(\"date\") commands.ts:95",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-or-insert of a \\date titleField, with a DATE-SPECIFIC pre-fill. On INSERT: attrs include isToday:true, nodeContent pre-filled with today's date pretty-printed (toLocaleDateString en-US); isToday:true tells the serializer to emit \\date{\\today} rather than the literal. On FIND-EXISTING: caret to end, NO re-stamp of today. Collab read-only -> no-op.",
      "docDelta": "If absent: +1 titleField{field:'date', isToday:true, content=pretty-printed today} at order index 2. If present: zero doc mutation (caret move only).",
      "texDelta": "If absent: +\\date{\\today} on serialize (because isToday:true, NOT the expanded literal). If present: no change.",
      "sidecarDelta": "None.",
      "lifecycle": "No card, no float - in-doc lozenge showing the pretty date."
    },
    "crossSurfaceIdentity": "Slash-only. Invariant: idempotency + the \\today special-case must round-trip: an inserted date (isToday:true, pretty content) serializes to \\date{\\today}, and re-parsing \\date{\\today} must reproduce isToday:true + the same pretty rendering. Canonical order index 2.",
    "knownRisk": "Drive live FIRST - date carries the most behavior. (1) Verify INSERT pre-fills today's pretty date AND serializes to \\date{\\today} (NOT \\date{June 14, 2026}). (2) Verify FIND-EXISTING does NOT overwrite a user-edited date with today. (3) Verify canonical ordering: \\date lands LAST among title fields (index 2); with \\maketitle present it goes BEFORE \\maketitle (order:99 for non-titles). This \\maketitle-ordering is the subtlest case."
  },
  {
    "id": "date@displayMath#slash",
    "category": "Heading + title (7)",
    "action": "date",
    "kind": "displayMath",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled' (theoretical; slash-only).",
    "slashName": "date",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun) via titleFieldRow(\"date\"):1382; routed by runViewOnlyAction(\"date\") commands.ts:95",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-or-insert of a \\date titleField, with a DATE-SPECIFIC pre-fill. On INSERT: attrs include isToday:true, nodeContent pre-filled with today's date pretty-printed (toLocaleDateString en-US); isToday:true tells the serializer to emit \\date{\\today} rather than the literal. On FIND-EXISTING: caret to end, NO re-stamp of today. Collab read-only -> no-op.",
      "docDelta": "If absent: +1 titleField{field:'date', isToday:true, content=pretty-printed today} at order index 2. If present: zero doc mutation (caret move only).",
      "texDelta": "If absent: +\\date{\\today} on serialize (because isToday:true, NOT the expanded literal). If present: no change.",
      "sidecarDelta": "None.",
      "lifecycle": "No card, no float - in-doc lozenge showing the pretty date."
    },
    "crossSurfaceIdentity": "Slash-only. Invariant: idempotency + the \\today special-case must round-trip: an inserted date (isToday:true, pretty content) serializes to \\date{\\today}, and re-parsing \\date{\\today} must reproduce isToday:true + the same pretty rendering. Canonical order index 2.",
    "knownRisk": "Drive live FIRST - date carries the most behavior. (1) Verify INSERT pre-fills today's pretty date AND serializes to \\date{\\today} (NOT \\date{June 14, 2026}). (2) Verify FIND-EXISTING does NOT overwrite a user-edited date with today. (3) Verify canonical ordering: \\date lands LAST among title fields (index 2); with \\maketitle present it goes BEFORE \\maketitle (order:99 for non-titles). This \\maketitle-ordering is the subtlest case."
  },
  {
    "id": "date@figureBlock#slash",
    "category": "Heading + title (7)",
    "action": "date",
    "kind": "figureBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled' (theoretical).",
    "slashName": "date",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun) via titleFieldRow(\"date\"):1382; routed by runViewOnlyAction(\"date\") commands.ts:95",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-or-insert of a \\date titleField, with a DATE-SPECIFIC pre-fill. On INSERT: attrs include isToday:true, nodeContent pre-filled with today's date pretty-printed (toLocaleDateString en-US); isToday:true tells the serializer to emit \\date{\\today} rather than the literal. On FIND-EXISTING: caret to end, NO re-stamp of today. Collab read-only -> no-op.",
      "docDelta": "If absent: +1 titleField{field:'date', isToday:true, content=pretty-printed today} at order index 2. If present: zero doc mutation (caret move only).",
      "texDelta": "If absent: +\\date{\\today} on serialize (because isToday:true, NOT the expanded literal). If present: no change.",
      "sidecarDelta": "None.",
      "lifecycle": "No card, no float - in-doc lozenge showing the pretty date."
    },
    "crossSurfaceIdentity": "Slash-only. Invariant: idempotency + the \\today special-case must round-trip: an inserted date (isToday:true, pretty content) serializes to \\date{\\today}, and re-parsing \\date{\\today} must reproduce isToday:true + the same pretty rendering. Canonical order index 2.",
    "knownRisk": "Drive live FIRST - date carries the most behavior. (1) Verify INSERT pre-fills today's pretty date AND serializes to \\date{\\today} (NOT \\date{June 14, 2026}). (2) Verify FIND-EXISTING does NOT overwrite a user-edited date with today. (3) Verify canonical ordering: \\date lands LAST among title fields (index 2); with \\maketitle present it goes BEFORE \\maketitle (order:99 for non-titles). This \\maketitle-ordering is the subtlest case."
  },
  {
    "id": "date@selection#slash",
    "category": "Heading + title (7)",
    "action": "date",
    "kind": "selection",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "blockApplies base 'ok'; selection 'ignored'.",
    "slashName": "date",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun) via titleFieldRow(\"date\"):1382; routed by runViewOnlyAction(\"date\") commands.ts:95",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-or-insert of a \\date titleField, with a DATE-SPECIFIC pre-fill. On INSERT: attrs include isToday:true, nodeContent pre-filled with today's date pretty-printed (toLocaleDateString en-US); isToday:true tells the serializer to emit \\date{\\today} rather than the literal. On FIND-EXISTING: caret to end, NO re-stamp of today. Collab read-only -> no-op.",
      "docDelta": "If absent: +1 titleField{field:'date', isToday:true, content=pretty-printed today} at order index 2. If present: zero doc mutation (caret move only).",
      "texDelta": "If absent: +\\date{\\today} on serialize (because isToday:true, NOT the expanded literal). If present: no change.",
      "sidecarDelta": "None.",
      "lifecycle": "No card, no float - in-doc lozenge showing the pretty date."
    },
    "crossSurfaceIdentity": "Slash-only. Invariant: idempotency + the \\today special-case must round-trip: an inserted date (isToday:true, pretty content) serializes to \\date{\\today}, and re-parsing \\date{\\today} must reproduce isToday:true + the same pretty rendering. Canonical order index 2.",
    "knownRisk": "Drive live FIRST - date carries the most behavior. (1) Verify INSERT pre-fills today's pretty date AND serializes to \\date{\\today} (NOT \\date{June 14, 2026}). (2) Verify FIND-EXISTING does NOT overwrite a user-edited date with today. (3) Verify canonical ordering: \\date lands LAST among title fields (index 2); with \\maketitle present it goes BEFORE \\maketitle (order:99 for non-titles). This \\maketitle-ordering is the subtlest case."
  },
  {
    "id": "date@texBlock#slash",
    "category": "Heading + title (7)",
    "action": "date",
    "kind": "texBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled' (theoretical).",
    "slashName": "date",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun) via titleFieldRow(\"date\"):1382; routed by runViewOnlyAction(\"date\") commands.ts:95",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-or-insert of a \\date titleField, with a DATE-SPECIFIC pre-fill. On INSERT: attrs include isToday:true, nodeContent pre-filled with today's date pretty-printed (toLocaleDateString en-US); isToday:true tells the serializer to emit \\date{\\today} rather than the literal. On FIND-EXISTING: caret to end, NO re-stamp of today. Collab read-only -> no-op.",
      "docDelta": "If absent: +1 titleField{field:'date', isToday:true, content=pretty-printed today} at order index 2. If present: zero doc mutation (caret move only).",
      "texDelta": "If absent: +\\date{\\today} on serialize (because isToday:true, NOT the expanded literal). If present: no change.",
      "sidecarDelta": "None.",
      "lifecycle": "No card, no float - in-doc lozenge showing the pretty date."
    },
    "crossSurfaceIdentity": "Slash-only. Invariant: idempotency + the \\today special-case must round-trip: an inserted date (isToday:true, pretty content) serializes to \\date{\\today}, and re-parsing \\date{\\today} must reproduce isToday:true + the same pretty rendering. Canonical order index 2.",
    "knownRisk": "Drive live FIRST - date carries the most behavior. (1) Verify INSERT pre-fills today's pretty date AND serializes to \\date{\\today} (NOT \\date{June 14, 2026}). (2) Verify FIND-EXISTING does NOT overwrite a user-edited date with today. (3) Verify canonical ordering: \\date lands LAST among title fields (index 2); with \\maketitle present it goes BEFORE \\maketitle (order:99 for non-titles). This \\maketitle-ordering is the subtlest case."
  },
  {
    "id": "date@titleField#slash",
    "category": "Heading + title (7)",
    "action": "date",
    "kind": "titleField",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "Idempotent: existing date field -> caret to content end, no duplicate (does NOT re-stamp today).",
    "slashName": "date",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun) via titleFieldRow(\"date\"):1382; routed by runViewOnlyAction(\"date\") commands.ts:95",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-or-insert of a \\date titleField, with a DATE-SPECIFIC pre-fill. On INSERT: attrs include isToday:true, nodeContent pre-filled with today's date pretty-printed (toLocaleDateString en-US); isToday:true tells the serializer to emit \\date{\\today} rather than the literal. On FIND-EXISTING: caret to end, NO re-stamp of today. Collab read-only -> no-op.",
      "docDelta": "If absent: +1 titleField{field:'date', isToday:true, content=pretty-printed today} at order index 2. If present: zero doc mutation (caret move only).",
      "texDelta": "If absent: +\\date{\\today} on serialize (because isToday:true, NOT the expanded literal). If present: no change.",
      "sidecarDelta": "None.",
      "lifecycle": "No card, no float - in-doc lozenge showing the pretty date."
    },
    "crossSurfaceIdentity": "Slash-only. Invariant: idempotency + the \\today special-case must round-trip: an inserted date (isToday:true, pretty content) serializes to \\date{\\today}, and re-parsing \\date{\\today} must reproduce isToday:true + the same pretty rendering. Canonical order index 2.",
    "knownRisk": "Drive live FIRST - date carries the most behavior. (1) Verify INSERT pre-fills today's pretty date AND serializes to \\date{\\today} (NOT \\date{June 14, 2026}). (2) Verify FIND-EXISTING does NOT overwrite a user-edited date with today. (3) Verify canonical ordering: \\date lands LAST among title fields (index 2); with \\maketitle present it goes BEFORE \\maketitle (order:99 for non-titles). This \\maketitle-ordering is the subtlest case."
  },
  {
    "id": "heading-chapter@atom-only:inline-math#lightning",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "atom-only:inline-math",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "A CURSOR ref (slash) is always base 'ok' regardless of surrounding atom - slash fires from a caret. setBlockType converts the CONTAINING paragraph.",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@atom-only:inline-math#slash",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "atom-only:inline-math",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "A CURSOR ref (slash) is always base 'ok' regardless of surrounding atom - slash fires from a caret. setBlockType converts the CONTAINING paragraph.",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@blockquote#lightning",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "Non-atom-block, text-bearing -> base 'ok'.",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@blockquote#slash",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "blockquote",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "Non-atom-block, text-bearing -> base 'ok'.",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@codeBlock#lightning",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "codeBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "Non-atom-block per registry -> 'ok' (codeBlock is text-bearing in the schema).",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@codeBlock#slash",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "codeBlock",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "Non-atom-block per registry -> 'ok' (codeBlock is text-bearing in the schema).",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@displayMath#lightning",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> base 'disabled' (no text to convert).",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@displayMath#slash",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "displayMath",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> base 'disabled' (no text to convert).",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@figureBlock#lightning",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "figureBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@figureBlock#slash",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "figureBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@graphicsBlock#lightning",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@graphicsBlock#slash",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "graphicsBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@heading#lightning",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "Already a heading; setBlockType re-sets level+numbered (idempotent-ish re-level).",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@heading#slash",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "heading",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "Already a heading; setBlockType re-sets level+numbered (idempotent-ish re-level).",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@latexComment#lightning",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@latexComment#slash",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "latexComment",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@paragraph#lightning",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "TextObjectRef path - non-atom-block kind -> base 'ok' (text-bearing, convertible).",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@paragraph#slash",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "paragraph",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "TextObjectRef path - non-atom-block kind -> base 'ok' (text-bearing, convertible).",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@selection#lightning",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "selection/cursor base 'ok' (always sits in a convertible text block). Gated only by collab via gateApplies (DA-5 range no-op for 'ignored').",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@selection#slash",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "selection",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "selection/cursor base 'ok' (always sits in a convertible text block). Gated only by collab via gateApplies (DA-5 range no-op for 'ignored').",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@texBlock#lightning",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@texBlock#slash",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "texBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@titleField#lightning",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "Non-atom-block -> 'ok'; in practice only invoked from a caret (slash/dropdown) inside body text.",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-chapter@titleField#slash",
    "category": "Heading + title (7)",
    "action": "heading-chapter",
    "kind": "titleField",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "Non-atom-block -> 'ok'; in practice only invoked from a caret (slash/dropdown) inside body text.",
    "slashName": "chapter",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-chapter\"); level 1",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 1 (\\chapter), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:1, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:1, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\chapter{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash (chapter) and lightning (BlockTypeDropdown level 1 via applyHeadingFromDropdown->spec.run) MUST produce a byte-identical setBlockType to heading{level:1, numbered:true} on selection.from..to. Both build a {kind:'cursor', pos:selection.head} ref and invoke the SAME headingRun. canEdit: slash reads view.editable, lightning reads editor.isEditable - same flag.",
    "knownRisk": "REAL multi-surface divergence OUTSIDE the registry: the Heading extension is configure({levels:[0..6]}), and that comment states levels drive BOTH 'input rules and keyboard shortcuts'. So StarterKit's Mod-Alt-1 and the markdown '# ' input rule are LIVE and call TipTap's toggleHeading/setHeading - a TOGGLE path that the alignment effort deliberately replaced with always-SET. A user pressing Mod-Alt-1 on an existing level-1 heading will toggle it back to paragraph - diverging from slash/dropdown SET. Drive this live: Mod-Alt-1 twice toggles, vs \\chapter twice stays a heading. Also confirm '# ' input rule numbered:true consistency."
  },
  {
    "id": "heading-section@displayMath#lightning",
    "category": "Heading + title (7)",
    "action": "heading-section",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "section",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-section\"); level 2",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 2 (\\section), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:2, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:2, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\section{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\section and lightning BlockTypeDropdown level 2 route through the SAME headingRun(2); identical setBlockType{level:2, numbered:true} tx, identical serialized \\section{}.",
    "knownRisk": "Same keyboard/input-rule TOGGLE divergence as heading-chapter (Mod-Alt-2 / a markdown rule toggle vs the registry SET). The dropdown's prior behavior toggled; CHIP 5a unified on SET. Drive live: dropdown 'Section' on an existing section must STAY a section (no revert); 'Body' (setParagraph) is the ONLY way out."
  },
  {
    "id": "heading-section@displayMath#slash",
    "category": "Heading + title (7)",
    "action": "heading-section",
    "kind": "displayMath",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "section",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-section\"); level 2",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 2 (\\section), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:2, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:2, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\section{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\section and lightning BlockTypeDropdown level 2 route through the SAME headingRun(2); identical setBlockType{level:2, numbered:true} tx, identical serialized \\section{}.",
    "knownRisk": "Same keyboard/input-rule TOGGLE divergence as heading-chapter (Mod-Alt-2 / a markdown rule toggle vs the registry SET). The dropdown's prior behavior toggled; CHIP 5a unified on SET. Drive live: dropdown 'Section' on an existing section must STAY a section (no revert); 'Body' (setParagraph) is the ONLY way out."
  },
  {
    "id": "heading-section@figureBlock#lightning",
    "category": "Heading + title (7)",
    "action": "heading-section",
    "kind": "figureBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "section",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-section\"); level 2",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 2 (\\section), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:2, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:2, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\section{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\section and lightning BlockTypeDropdown level 2 route through the SAME headingRun(2); identical setBlockType{level:2, numbered:true} tx, identical serialized \\section{}.",
    "knownRisk": "Same keyboard/input-rule TOGGLE divergence as heading-chapter (Mod-Alt-2 / a markdown rule toggle vs the registry SET). The dropdown's prior behavior toggled; CHIP 5a unified on SET. Drive live: dropdown 'Section' on an existing section must STAY a section (no revert); 'Body' (setParagraph) is the ONLY way out."
  },
  {
    "id": "heading-section@figureBlock#slash",
    "category": "Heading + title (7)",
    "action": "heading-section",
    "kind": "figureBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "section",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-section\"); level 2",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 2 (\\section), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:2, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:2, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\section{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\section and lightning BlockTypeDropdown level 2 route through the SAME headingRun(2); identical setBlockType{level:2, numbered:true} tx, identical serialized \\section{}.",
    "knownRisk": "Same keyboard/input-rule TOGGLE divergence as heading-chapter (Mod-Alt-2 / a markdown rule toggle vs the registry SET). The dropdown's prior behavior toggled; CHIP 5a unified on SET. Drive live: dropdown 'Section' on an existing section must STAY a section (no revert); 'Body' (setParagraph) is the ONLY way out."
  },
  {
    "id": "heading-section@graphicsBlock#lightning",
    "category": "Heading + title (7)",
    "action": "heading-section",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "section",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-section\"); level 2",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 2 (\\section), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:2, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:2, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\section{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\section and lightning BlockTypeDropdown level 2 route through the SAME headingRun(2); identical setBlockType{level:2, numbered:true} tx, identical serialized \\section{}.",
    "knownRisk": "Same keyboard/input-rule TOGGLE divergence as heading-chapter (Mod-Alt-2 / a markdown rule toggle vs the registry SET). The dropdown's prior behavior toggled; CHIP 5a unified on SET. Drive live: dropdown 'Section' on an existing section must STAY a section (no revert); 'Body' (setParagraph) is the ONLY way out."
  },
  {
    "id": "heading-section@graphicsBlock#slash",
    "category": "Heading + title (7)",
    "action": "heading-section",
    "kind": "graphicsBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "section",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-section\"); level 2",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 2 (\\section), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:2, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:2, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\section{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\section and lightning BlockTypeDropdown level 2 route through the SAME headingRun(2); identical setBlockType{level:2, numbered:true} tx, identical serialized \\section{}.",
    "knownRisk": "Same keyboard/input-rule TOGGLE divergence as heading-chapter (Mod-Alt-2 / a markdown rule toggle vs the registry SET). The dropdown's prior behavior toggled; CHIP 5a unified on SET. Drive live: dropdown 'Section' on an existing section must STAY a section (no revert); 'Body' (setParagraph) is the ONLY way out."
  },
  {
    "id": "heading-section@heading#lightning",
    "category": "Heading + title (7)",
    "action": "heading-section",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "Re-levels an existing heading (e.g. chapter->section). SET, not toggle.",
    "slashName": "section",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-section\"); level 2",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 2 (\\section), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:2, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:2, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\section{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\section and lightning BlockTypeDropdown level 2 route through the SAME headingRun(2); identical setBlockType{level:2, numbered:true} tx, identical serialized \\section{}.",
    "knownRisk": "Same keyboard/input-rule TOGGLE divergence as heading-chapter (Mod-Alt-2 / a markdown rule toggle vs the registry SET). The dropdown's prior behavior toggled; CHIP 5a unified on SET. Drive live: dropdown 'Section' on an existing section must STAY a section (no revert); 'Body' (setParagraph) is the ONLY way out."
  },
  {
    "id": "heading-section@heading#slash",
    "category": "Heading + title (7)",
    "action": "heading-section",
    "kind": "heading",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "Re-levels an existing heading (e.g. chapter->section). SET, not toggle.",
    "slashName": "section",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-section\"); level 2",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 2 (\\section), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:2, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:2, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\section{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\section and lightning BlockTypeDropdown level 2 route through the SAME headingRun(2); identical setBlockType{level:2, numbered:true} tx, identical serialized \\section{}.",
    "knownRisk": "Same keyboard/input-rule TOGGLE divergence as heading-chapter (Mod-Alt-2 / a markdown rule toggle vs the registry SET). The dropdown's prior behavior toggled; CHIP 5a unified on SET. Drive live: dropdown 'Section' on an existing section must STAY a section (no revert); 'Body' (setParagraph) is the ONLY way out."
  },
  {
    "id": "heading-section@latexComment#lightning",
    "category": "Heading + title (7)",
    "action": "heading-section",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "section",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-section\"); level 2",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 2 (\\section), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:2, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:2, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\section{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\section and lightning BlockTypeDropdown level 2 route through the SAME headingRun(2); identical setBlockType{level:2, numbered:true} tx, identical serialized \\section{}.",
    "knownRisk": "Same keyboard/input-rule TOGGLE divergence as heading-chapter (Mod-Alt-2 / a markdown rule toggle vs the registry SET). The dropdown's prior behavior toggled; CHIP 5a unified on SET. Drive live: dropdown 'Section' on an existing section must STAY a section (no revert); 'Body' (setParagraph) is the ONLY way out."
  },
  {
    "id": "heading-section@latexComment#slash",
    "category": "Heading + title (7)",
    "action": "heading-section",
    "kind": "latexComment",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "section",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-section\"); level 2",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 2 (\\section), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:2, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:2, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\section{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\section and lightning BlockTypeDropdown level 2 route through the SAME headingRun(2); identical setBlockType{level:2, numbered:true} tx, identical serialized \\section{}.",
    "knownRisk": "Same keyboard/input-rule TOGGLE divergence as heading-chapter (Mod-Alt-2 / a markdown rule toggle vs the registry SET). The dropdown's prior behavior toggled; CHIP 5a unified on SET. Drive live: dropdown 'Section' on an existing section must STAY a section (no revert); 'Body' (setParagraph) is the ONLY way out."
  },
  {
    "id": "heading-section@paragraph#lightning",
    "category": "Heading + title (7)",
    "action": "heading-section",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "Non-atom-block text block -> convertible.",
    "slashName": "section",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-section\"); level 2",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 2 (\\section), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:2, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:2, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\section{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\section and lightning BlockTypeDropdown level 2 route through the SAME headingRun(2); identical setBlockType{level:2, numbered:true} tx, identical serialized \\section{}.",
    "knownRisk": "Same keyboard/input-rule TOGGLE divergence as heading-chapter (Mod-Alt-2 / a markdown rule toggle vs the registry SET). The dropdown's prior behavior toggled; CHIP 5a unified on SET. Drive live: dropdown 'Section' on an existing section must STAY a section (no revert); 'Body' (setParagraph) is the ONLY way out."
  },
  {
    "id": "heading-section@paragraph#slash",
    "category": "Heading + title (7)",
    "action": "heading-section",
    "kind": "paragraph",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "Non-atom-block text block -> convertible.",
    "slashName": "section",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-section\"); level 2",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 2 (\\section), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:2, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:2, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\section{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\section and lightning BlockTypeDropdown level 2 route through the SAME headingRun(2); identical setBlockType{level:2, numbered:true} tx, identical serialized \\section{}.",
    "knownRisk": "Same keyboard/input-rule TOGGLE divergence as heading-chapter (Mod-Alt-2 / a markdown rule toggle vs the registry SET). The dropdown's prior behavior toggled; CHIP 5a unified on SET. Drive live: dropdown 'Section' on an existing section must STAY a section (no revert); 'Body' (setParagraph) is the ONLY way out."
  },
  {
    "id": "heading-section@selection#lightning",
    "category": "Heading + title (7)",
    "action": "heading-section",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "Same applies() as heading-chapter - selection/cursor -> 'ok'.",
    "slashName": "section",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-section\"); level 2",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 2 (\\section), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:2, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:2, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\section{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\section and lightning BlockTypeDropdown level 2 route through the SAME headingRun(2); identical setBlockType{level:2, numbered:true} tx, identical serialized \\section{}.",
    "knownRisk": "Same keyboard/input-rule TOGGLE divergence as heading-chapter (Mod-Alt-2 / a markdown rule toggle vs the registry SET). The dropdown's prior behavior toggled; CHIP 5a unified on SET. Drive live: dropdown 'Section' on an existing section must STAY a section (no revert); 'Body' (setParagraph) is the ONLY way out."
  },
  {
    "id": "heading-section@selection#slash",
    "category": "Heading + title (7)",
    "action": "heading-section",
    "kind": "selection",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "Same applies() as heading-chapter - selection/cursor -> 'ok'.",
    "slashName": "section",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-section\"); level 2",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 2 (\\section), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:2, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:2, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\section{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\section and lightning BlockTypeDropdown level 2 route through the SAME headingRun(2); identical setBlockType{level:2, numbered:true} tx, identical serialized \\section{}.",
    "knownRisk": "Same keyboard/input-rule TOGGLE divergence as heading-chapter (Mod-Alt-2 / a markdown rule toggle vs the registry SET). The dropdown's prior behavior toggled; CHIP 5a unified on SET. Drive live: dropdown 'Section' on an existing section must STAY a section (no revert); 'Body' (setParagraph) is the ONLY way out."
  },
  {
    "id": "heading-section@texBlock#lightning",
    "category": "Heading + title (7)",
    "action": "heading-section",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "section",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-section\"); level 2",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 2 (\\section), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:2, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:2, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\section{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\section and lightning BlockTypeDropdown level 2 route through the SAME headingRun(2); identical setBlockType{level:2, numbered:true} tx, identical serialized \\section{}.",
    "knownRisk": "Same keyboard/input-rule TOGGLE divergence as heading-chapter (Mod-Alt-2 / a markdown rule toggle vs the registry SET). The dropdown's prior behavior toggled; CHIP 5a unified on SET. Drive live: dropdown 'Section' on an existing section must STAY a section (no revert); 'Body' (setParagraph) is the ONLY way out."
  },
  {
    "id": "heading-section@texBlock#slash",
    "category": "Heading + title (7)",
    "action": "heading-section",
    "kind": "texBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "section",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-section\"); level 2",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 2 (\\section), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:2, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:2, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\section{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\section and lightning BlockTypeDropdown level 2 route through the SAME headingRun(2); identical setBlockType{level:2, numbered:true} tx, identical serialized \\section{}.",
    "knownRisk": "Same keyboard/input-rule TOGGLE divergence as heading-chapter (Mod-Alt-2 / a markdown rule toggle vs the registry SET). The dropdown's prior behavior toggled; CHIP 5a unified on SET. Drive live: dropdown 'Section' on an existing section must STAY a section (no revert); 'Body' (setParagraph) is the ONLY way out."
  },
  {
    "id": "heading-subsection@displayMath#lightning",
    "category": "Heading + title (7)",
    "action": "heading-subsection",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "subsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsection\"); level 3",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 3 (\\subsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:3, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:3, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsection and lightning dropdown level 3 -> same headingRun(3); identical tx + \\subsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-3) / input-rule divergence as the other heading rows. Lower priority to drive than chapter/section (less common), but the same root: levels[] enables StarterKit shortcuts that toggle."
  },
  {
    "id": "heading-subsection@displayMath#slash",
    "category": "Heading + title (7)",
    "action": "heading-subsection",
    "kind": "displayMath",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "subsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsection\"); level 3",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 3 (\\subsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:3, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:3, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsection and lightning dropdown level 3 -> same headingRun(3); identical tx + \\subsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-3) / input-rule divergence as the other heading rows. Lower priority to drive than chapter/section (less common), but the same root: levels[] enables StarterKit shortcuts that toggle."
  },
  {
    "id": "heading-subsection@figureBlock#lightning",
    "category": "Heading + title (7)",
    "action": "heading-subsection",
    "kind": "figureBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "subsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsection\"); level 3",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 3 (\\subsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:3, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:3, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsection and lightning dropdown level 3 -> same headingRun(3); identical tx + \\subsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-3) / input-rule divergence as the other heading rows. Lower priority to drive than chapter/section (less common), but the same root: levels[] enables StarterKit shortcuts that toggle."
  },
  {
    "id": "heading-subsection@figureBlock#slash",
    "category": "Heading + title (7)",
    "action": "heading-subsection",
    "kind": "figureBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "subsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsection\"); level 3",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 3 (\\subsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:3, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:3, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsection and lightning dropdown level 3 -> same headingRun(3); identical tx + \\subsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-3) / input-rule divergence as the other heading rows. Lower priority to drive than chapter/section (less common), but the same root: levels[] enables StarterKit shortcuts that toggle."
  },
  {
    "id": "heading-subsection@graphicsBlock#lightning",
    "category": "Heading + title (7)",
    "action": "heading-subsection",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "subsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsection\"); level 3",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 3 (\\subsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:3, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:3, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsection and lightning dropdown level 3 -> same headingRun(3); identical tx + \\subsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-3) / input-rule divergence as the other heading rows. Lower priority to drive than chapter/section (less common), but the same root: levels[] enables StarterKit shortcuts that toggle."
  },
  {
    "id": "heading-subsection@graphicsBlock#slash",
    "category": "Heading + title (7)",
    "action": "heading-subsection",
    "kind": "graphicsBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "subsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsection\"); level 3",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 3 (\\subsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:3, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:3, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsection and lightning dropdown level 3 -> same headingRun(3); identical tx + \\subsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-3) / input-rule divergence as the other heading rows. Lower priority to drive than chapter/section (less common), but the same root: levels[] enables StarterKit shortcuts that toggle."
  },
  {
    "id": "heading-subsection@heading#lightning",
    "category": "Heading + title (7)",
    "action": "heading-subsection",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "Re-levels to subsection.",
    "slashName": "subsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsection\"); level 3",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 3 (\\subsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:3, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:3, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsection and lightning dropdown level 3 -> same headingRun(3); identical tx + \\subsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-3) / input-rule divergence as the other heading rows. Lower priority to drive than chapter/section (less common), but the same root: levels[] enables StarterKit shortcuts that toggle."
  },
  {
    "id": "heading-subsection@heading#slash",
    "category": "Heading + title (7)",
    "action": "heading-subsection",
    "kind": "heading",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "Re-levels to subsection.",
    "slashName": "subsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsection\"); level 3",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 3 (\\subsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:3, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:3, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsection and lightning dropdown level 3 -> same headingRun(3); identical tx + \\subsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-3) / input-rule divergence as the other heading rows. Lower priority to drive than chapter/section (less common), but the same root: levels[] enables StarterKit shortcuts that toggle."
  },
  {
    "id": "heading-subsection@paragraph#lightning",
    "category": "Heading + title (7)",
    "action": "heading-subsection",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "Convertible text block.",
    "slashName": "subsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsection\"); level 3",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 3 (\\subsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:3, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:3, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsection and lightning dropdown level 3 -> same headingRun(3); identical tx + \\subsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-3) / input-rule divergence as the other heading rows. Lower priority to drive than chapter/section (less common), but the same root: levels[] enables StarterKit shortcuts that toggle."
  },
  {
    "id": "heading-subsection@paragraph#slash",
    "category": "Heading + title (7)",
    "action": "heading-subsection",
    "kind": "paragraph",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "Convertible text block.",
    "slashName": "subsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsection\"); level 3",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 3 (\\subsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:3, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:3, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsection and lightning dropdown level 3 -> same headingRun(3); identical tx + \\subsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-3) / input-rule divergence as the other heading rows. Lower priority to drive than chapter/section (less common), but the same root: levels[] enables StarterKit shortcuts that toggle."
  },
  {
    "id": "heading-subsection@selection#lightning",
    "category": "Heading + title (7)",
    "action": "heading-subsection",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "selection/cursor -> 'ok'.",
    "slashName": "subsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsection\"); level 3",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 3 (\\subsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:3, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:3, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsection and lightning dropdown level 3 -> same headingRun(3); identical tx + \\subsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-3) / input-rule divergence as the other heading rows. Lower priority to drive than chapter/section (less common), but the same root: levels[] enables StarterKit shortcuts that toggle."
  },
  {
    "id": "heading-subsection@selection#slash",
    "category": "Heading + title (7)",
    "action": "heading-subsection",
    "kind": "selection",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "selection/cursor -> 'ok'.",
    "slashName": "subsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsection\"); level 3",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 3 (\\subsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:3, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:3, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsection and lightning dropdown level 3 -> same headingRun(3); identical tx + \\subsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-3) / input-rule divergence as the other heading rows. Lower priority to drive than chapter/section (less common), but the same root: levels[] enables StarterKit shortcuts that toggle."
  },
  {
    "id": "heading-subsection@texBlock#lightning",
    "category": "Heading + title (7)",
    "action": "heading-subsection",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "subsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsection\"); level 3",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 3 (\\subsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:3, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:3, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsection and lightning dropdown level 3 -> same headingRun(3); identical tx + \\subsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-3) / input-rule divergence as the other heading rows. Lower priority to drive than chapter/section (less common), but the same root: levels[] enables StarterKit shortcuts that toggle."
  },
  {
    "id": "heading-subsection@texBlock#slash",
    "category": "Heading + title (7)",
    "action": "heading-subsection",
    "kind": "texBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "subsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsection\"); level 3",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 3 (\\subsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:3, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:3, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsection and lightning dropdown level 3 -> same headingRun(3); identical tx + \\subsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-3) / input-rule divergence as the other heading rows. Lower priority to drive than chapter/section (less common), but the same root: levels[] enables StarterKit shortcuts that toggle."
  },
  {
    "id": "heading-subsubsection@displayMath#lightning",
    "category": "Heading + title (7)",
    "action": "heading-subsubsection",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "subsubsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsubsection\"); level 4",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 4 (\\subsubsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:4, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:4, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsubsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsubsection and lightning dropdown level 4 -> same headingRun(4); identical tx + \\subsubsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-4) / input-rule divergence. NOTE the dropdown also lists out-of-registry levels 0 (Part), 5 (Paragraph heading), 6 (Subparagraph heading) which fall to a DIRECT setNode fallback - also SET+numbered but NOT through a registry row, so no slash twin and no coverage row. If a future chip adds \\part etc., verify the fallback stays SET-consistent."
  },
  {
    "id": "heading-subsubsection@displayMath#slash",
    "category": "Heading + title (7)",
    "action": "heading-subsubsection",
    "kind": "displayMath",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "subsubsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsubsection\"); level 4",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 4 (\\subsubsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:4, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:4, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsubsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsubsection and lightning dropdown level 4 -> same headingRun(4); identical tx + \\subsubsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-4) / input-rule divergence. NOTE the dropdown also lists out-of-registry levels 0 (Part), 5 (Paragraph heading), 6 (Subparagraph heading) which fall to a DIRECT setNode fallback - also SET+numbered but NOT through a registry row, so no slash twin and no coverage row. If a future chip adds \\part etc., verify the fallback stays SET-consistent."
  },
  {
    "id": "heading-subsubsection@figureBlock#lightning",
    "category": "Heading + title (7)",
    "action": "heading-subsubsection",
    "kind": "figureBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "subsubsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsubsection\"); level 4",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 4 (\\subsubsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:4, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:4, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsubsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsubsection and lightning dropdown level 4 -> same headingRun(4); identical tx + \\subsubsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-4) / input-rule divergence. NOTE the dropdown also lists out-of-registry levels 0 (Part), 5 (Paragraph heading), 6 (Subparagraph heading) which fall to a DIRECT setNode fallback - also SET+numbered but NOT through a registry row, so no slash twin and no coverage row. If a future chip adds \\part etc., verify the fallback stays SET-consistent."
  },
  {
    "id": "heading-subsubsection@figureBlock#slash",
    "category": "Heading + title (7)",
    "action": "heading-subsubsection",
    "kind": "figureBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "subsubsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsubsection\"); level 4",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 4 (\\subsubsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:4, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:4, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsubsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsubsection and lightning dropdown level 4 -> same headingRun(4); identical tx + \\subsubsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-4) / input-rule divergence. NOTE the dropdown also lists out-of-registry levels 0 (Part), 5 (Paragraph heading), 6 (Subparagraph heading) which fall to a DIRECT setNode fallback - also SET+numbered but NOT through a registry row, so no slash twin and no coverage row. If a future chip adds \\part etc., verify the fallback stays SET-consistent."
  },
  {
    "id": "heading-subsubsection@graphicsBlock#lightning",
    "category": "Heading + title (7)",
    "action": "heading-subsubsection",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "subsubsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsubsection\"); level 4",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 4 (\\subsubsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:4, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:4, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsubsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsubsection and lightning dropdown level 4 -> same headingRun(4); identical tx + \\subsubsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-4) / input-rule divergence. NOTE the dropdown also lists out-of-registry levels 0 (Part), 5 (Paragraph heading), 6 (Subparagraph heading) which fall to a DIRECT setNode fallback - also SET+numbered but NOT through a registry row, so no slash twin and no coverage row. If a future chip adds \\part etc., verify the fallback stays SET-consistent."
  },
  {
    "id": "heading-subsubsection@graphicsBlock#slash",
    "category": "Heading + title (7)",
    "action": "heading-subsubsection",
    "kind": "graphicsBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "subsubsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsubsection\"); level 4",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 4 (\\subsubsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:4, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:4, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsubsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsubsection and lightning dropdown level 4 -> same headingRun(4); identical tx + \\subsubsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-4) / input-rule divergence. NOTE the dropdown also lists out-of-registry levels 0 (Part), 5 (Paragraph heading), 6 (Subparagraph heading) which fall to a DIRECT setNode fallback - also SET+numbered but NOT through a registry row, so no slash twin and no coverage row. If a future chip adds \\part etc., verify the fallback stays SET-consistent."
  },
  {
    "id": "heading-subsubsection@heading#lightning",
    "category": "Heading + title (7)",
    "action": "heading-subsubsection",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "Re-levels to subsubsection.",
    "slashName": "subsubsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsubsection\"); level 4",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 4 (\\subsubsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:4, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:4, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsubsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsubsection and lightning dropdown level 4 -> same headingRun(4); identical tx + \\subsubsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-4) / input-rule divergence. NOTE the dropdown also lists out-of-registry levels 0 (Part), 5 (Paragraph heading), 6 (Subparagraph heading) which fall to a DIRECT setNode fallback - also SET+numbered but NOT through a registry row, so no slash twin and no coverage row. If a future chip adds \\part etc., verify the fallback stays SET-consistent."
  },
  {
    "id": "heading-subsubsection@heading#slash",
    "category": "Heading + title (7)",
    "action": "heading-subsubsection",
    "kind": "heading",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "Re-levels to subsubsection.",
    "slashName": "subsubsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsubsection\"); level 4",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 4 (\\subsubsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:4, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:4, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsubsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsubsection and lightning dropdown level 4 -> same headingRun(4); identical tx + \\subsubsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-4) / input-rule divergence. NOTE the dropdown also lists out-of-registry levels 0 (Part), 5 (Paragraph heading), 6 (Subparagraph heading) which fall to a DIRECT setNode fallback - also SET+numbered but NOT through a registry row, so no slash twin and no coverage row. If a future chip adds \\part etc., verify the fallback stays SET-consistent."
  },
  {
    "id": "heading-subsubsection@paragraph#lightning",
    "category": "Heading + title (7)",
    "action": "heading-subsubsection",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "Convertible text block.",
    "slashName": "subsubsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsubsection\"); level 4",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 4 (\\subsubsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:4, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:4, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsubsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsubsection and lightning dropdown level 4 -> same headingRun(4); identical tx + \\subsubsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-4) / input-rule divergence. NOTE the dropdown also lists out-of-registry levels 0 (Part), 5 (Paragraph heading), 6 (Subparagraph heading) which fall to a DIRECT setNode fallback - also SET+numbered but NOT through a registry row, so no slash twin and no coverage row. If a future chip adds \\part etc., verify the fallback stays SET-consistent."
  },
  {
    "id": "heading-subsubsection@paragraph#slash",
    "category": "Heading + title (7)",
    "action": "heading-subsubsection",
    "kind": "paragraph",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "Convertible text block.",
    "slashName": "subsubsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsubsection\"); level 4",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 4 (\\subsubsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:4, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:4, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsubsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsubsection and lightning dropdown level 4 -> same headingRun(4); identical tx + \\subsubsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-4) / input-rule divergence. NOTE the dropdown also lists out-of-registry levels 0 (Part), 5 (Paragraph heading), 6 (Subparagraph heading) which fall to a DIRECT setNode fallback - also SET+numbered but NOT through a registry row, so no slash twin and no coverage row. If a future chip adds \\part etc., verify the fallback stays SET-consistent."
  },
  {
    "id": "heading-subsubsection@selection#lightning",
    "category": "Heading + title (7)",
    "action": "heading-subsubsection",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "selection/cursor -> 'ok'.",
    "slashName": "subsubsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsubsection\"); level 4",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 4 (\\subsubsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:4, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:4, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsubsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsubsection and lightning dropdown level 4 -> same headingRun(4); identical tx + \\subsubsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-4) / input-rule divergence. NOTE the dropdown also lists out-of-registry levels 0 (Part), 5 (Paragraph heading), 6 (Subparagraph heading) which fall to a DIRECT setNode fallback - also SET+numbered but NOT through a registry row, so no slash twin and no coverage row. If a future chip adds \\part etc., verify the fallback stays SET-consistent."
  },
  {
    "id": "heading-subsubsection@selection#slash",
    "category": "Heading + title (7)",
    "action": "heading-subsubsection",
    "kind": "selection",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "selection/cursor -> 'ok'.",
    "slashName": "subsubsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsubsection\"); level 4",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 4 (\\subsubsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:4, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:4, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsubsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsubsection and lightning dropdown level 4 -> same headingRun(4); identical tx + \\subsubsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-4) / input-rule divergence. NOTE the dropdown also lists out-of-registry levels 0 (Part), 5 (Paragraph heading), 6 (Subparagraph heading) which fall to a DIRECT setNode fallback - also SET+numbered but NOT through a registry row, so no slash twin and no coverage row. If a future chip adds \\part etc., verify the fallback stays SET-consistent."
  },
  {
    "id": "heading-subsubsection@texBlock#lightning",
    "category": "Heading + title (7)",
    "action": "heading-subsubsection",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "subsubsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsubsection\"); level 4",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 4 (\\subsubsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:4, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:4, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsubsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsubsection and lightning dropdown level 4 -> same headingRun(4); identical tx + \\subsubsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-4) / input-rule divergence. NOTE the dropdown also lists out-of-registry levels 0 (Part), 5 (Paragraph heading), 6 (Subparagraph heading) which fall to a DIRECT setNode fallback - also SET+numbered but NOT through a registry row, so no slash twin and no coverage row. If a future chip adds \\part etc., verify the fallback stays SET-consistent."
  },
  {
    "id": "heading-subsubsection@texBlock#slash",
    "category": "Heading + title (7)",
    "action": "heading-subsubsection",
    "kind": "texBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled'.",
    "slashName": "subsubsection",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1236 (headingRun, factory); headingRow(\"heading-subsubsection\"); level 4",
    "backbone": "",
    "expected": {
      "behavior": "Convert the current block (containing selection.from..to) to heading level 4 (\\subsubsection), ALWAYS SET (never toggle-off) with numbered:true. setBlockType(selection.from, selection.to, heading, {level:4, numbered:true}); view.dispatch. No card, no popover. Collab read-only (canEdit===false) -> no-op.",
      "docDelta": "One block setBlockType'd to heading{level:4, numbered:true}; existing uuid preserved; no block count change.",
      "sidecarDelta": "None - pure-PM, no sidecar.",
      "texDelta": "On serialize: the line becomes \\subsubsection{<content>}; section auto-numbering recomputed by the heading-extension appendTransaction (editor-extensions.ts:1560).",
      "lifecycle": "No card produced. No float, no pin/pristine."
    },
    "crossSurfaceIdentity": "slash \\subsubsection and lightning dropdown level 4 -> same headingRun(4); identical tx + \\subsubsection{} output.",
    "knownRisk": "Same keyboard-toggle (Mod-Alt-4) / input-rule divergence. NOTE the dropdown also lists out-of-registry levels 0 (Part), 5 (Paragraph heading), 6 (Subparagraph heading) which fall to a DIRECT setNode fallback - also SET+numbered but NOT through a registry row, so no slash twin and no coverage row. If a future chip adds \\part etc., verify the fallback stays SET-consistent."
  },
  {
    "id": "title@cursor#slash",
    "category": "Heading + title (7)",
    "action": "title",
    "kind": "cursor",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "The actual invocation ref (slash builds a CursorRef). titleFieldRun ignores the ref and reads the live doc top to find-or-insert.",
    "slashName": "title",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun, factory) via titleFieldRow(\"title\"):1382; routed by runViewOnlyAction(\"title\") commands.ts:93",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-existing-or-insert of a \\title titleField at the doc top. Scans top-level children for titleField{field:'title'}. IF FOUND: no node mutation, place caret at end of content, scrollIntoView - NO duplicate. IF ABSENT: insert titleField{field:'title', rawPrefix:null, isToday:false, uuid:fresh} at canonical order position 0, caret inside empty content. Collab read-only -> no-op. SLASH-ONLY (a doc-top singleton, not a card).",
      "docDelta": "If absent: +1 titleField node at the doc top (order index 0). If present: ZERO doc mutation (caret move only).",
      "texDelta": "If absent: +\\title{} line on serialize. If present: no .tex change.",
      "sidecarDelta": "None - pure-PM, no card sidecar.",
      "lifecycle": "No card, no float. The titleField renders as an in-doc lozenge, not a panel card."
    },
    "crossSurfaceIdentity": "SLASH-ONLY - no cross-surface byte-match required. The invariant is INTERNAL idempotency: a second \\title must NOT create a second titleField. Minted attrs (field:'title', rawPrefix:null, isToday:false, fresh uuid) must match what the .tex parser produces on reload.",
    "knownRisk": "Drive live FIRST: the IDEMPOTENT branch is divergence-prone. (1) Run \\title twice - second must be a no-op caret-move, NOT a duplicate node. (2) Run \\title when a titleField exists but is NOT at index 0 - find scans ALL children so it should dedupe; confirm. (3) Canonical-order insert walks childOrder; verify a \\title inserted when only \\date exists lands BEFORE \\date (title order 0 < date order 2)."
  },
  {
    "id": "title@displayMath#slash",
    "category": "Heading + title (7)",
    "action": "title",
    "kind": "displayMath",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "blockApplies isAtomBlock -> 'disabled'. Unreachable in practice (slash-only).",
    "slashName": "title",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun, factory) via titleFieldRow(\"title\"):1382; routed by runViewOnlyAction(\"title\") commands.ts:93",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-existing-or-insert of a \\title titleField at the doc top. Scans top-level children for titleField{field:'title'}. IF FOUND: no node mutation, place caret at end of content, scrollIntoView - NO duplicate. IF ABSENT: insert titleField{field:'title', rawPrefix:null, isToday:false, uuid:fresh} at canonical order position 0, caret inside empty content. Collab read-only -> no-op. SLASH-ONLY (a doc-top singleton, not a card).",
      "docDelta": "If absent: +1 titleField node at the doc top (order index 0). If present: ZERO doc mutation (caret move only).",
      "texDelta": "If absent: +\\title{} line on serialize. If present: no .tex change.",
      "sidecarDelta": "None - pure-PM, no card sidecar.",
      "lifecycle": "No card, no float. The titleField renders as an in-doc lozenge, not a panel card."
    },
    "crossSurfaceIdentity": "SLASH-ONLY - no cross-surface byte-match required. The invariant is INTERNAL idempotency: a second \\title must NOT create a second titleField. Minted attrs (field:'title', rawPrefix:null, isToday:false, fresh uuid) must match what the .tex parser produces on reload.",
    "knownRisk": "Drive live FIRST: the IDEMPOTENT branch is divergence-prone. (1) Run \\title twice - second must be a no-op caret-move, NOT a duplicate node. (2) Run \\title when a titleField exists but is NOT at index 0 - find scans ALL children so it should dedupe; confirm. (3) Canonical-order insert walks childOrder; verify a \\title inserted when only \\date exists lands BEFORE \\date (title order 0 < date order 2)."
  },
  {
    "id": "title@figureBlock#slash",
    "category": "Heading + title (7)",
    "action": "title",
    "kind": "figureBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled' (theoretical).",
    "slashName": "title",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun, factory) via titleFieldRow(\"title\"):1382; routed by runViewOnlyAction(\"title\") commands.ts:93",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-existing-or-insert of a \\title titleField at the doc top. Scans top-level children for titleField{field:'title'}. IF FOUND: no node mutation, place caret at end of content, scrollIntoView - NO duplicate. IF ABSENT: insert titleField{field:'title', rawPrefix:null, isToday:false, uuid:fresh} at canonical order position 0, caret inside empty content. Collab read-only -> no-op. SLASH-ONLY (a doc-top singleton, not a card).",
      "docDelta": "If absent: +1 titleField node at the doc top (order index 0). If present: ZERO doc mutation (caret move only).",
      "texDelta": "If absent: +\\title{} line on serialize. If present: no .tex change.",
      "sidecarDelta": "None - pure-PM, no card sidecar.",
      "lifecycle": "No card, no float. The titleField renders as an in-doc lozenge, not a panel card."
    },
    "crossSurfaceIdentity": "SLASH-ONLY - no cross-surface byte-match required. The invariant is INTERNAL idempotency: a second \\title must NOT create a second titleField. Minted attrs (field:'title', rawPrefix:null, isToday:false, fresh uuid) must match what the .tex parser produces on reload.",
    "knownRisk": "Drive live FIRST: the IDEMPOTENT branch is divergence-prone. (1) Run \\title twice - second must be a no-op caret-move, NOT a duplicate node. (2) Run \\title when a titleField exists but is NOT at index 0 - find scans ALL children so it should dedupe; confirm. (3) Canonical-order insert walks childOrder; verify a \\title inserted when only \\date exists lands BEFORE \\date (title order 0 < date order 2)."
  },
  {
    "id": "title@paragraph#slash",
    "category": "Heading + title (7)",
    "action": "title",
    "kind": "paragraph",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "Non-atom-block -> 'ok' by blockApplies. Theoretical TextObjectRef path; no menu surface exposes it (slash-only).",
    "slashName": "title",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun, factory) via titleFieldRow(\"title\"):1382; routed by runViewOnlyAction(\"title\") commands.ts:93",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-existing-or-insert of a \\title titleField at the doc top. Scans top-level children for titleField{field:'title'}. IF FOUND: no node mutation, place caret at end of content, scrollIntoView - NO duplicate. IF ABSENT: insert titleField{field:'title', rawPrefix:null, isToday:false, uuid:fresh} at canonical order position 0, caret inside empty content. Collab read-only -> no-op. SLASH-ONLY (a doc-top singleton, not a card).",
      "docDelta": "If absent: +1 titleField node at the doc top (order index 0). If present: ZERO doc mutation (caret move only).",
      "texDelta": "If absent: +\\title{} line on serialize. If present: no .tex change.",
      "sidecarDelta": "None - pure-PM, no card sidecar.",
      "lifecycle": "No card, no float. The titleField renders as an in-doc lozenge, not a panel card."
    },
    "crossSurfaceIdentity": "SLASH-ONLY - no cross-surface byte-match required. The invariant is INTERNAL idempotency: a second \\title must NOT create a second titleField. Minted attrs (field:'title', rawPrefix:null, isToday:false, fresh uuid) must match what the .tex parser produces on reload.",
    "knownRisk": "Drive live FIRST: the IDEMPOTENT branch is divergence-prone. (1) Run \\title twice - second must be a no-op caret-move, NOT a duplicate node. (2) Run \\title when a titleField exists but is NOT at index 0 - find scans ALL children so it should dedupe; confirm. (3) Canonical-order insert walks childOrder; verify a \\title inserted when only \\date exists lands BEFORE \\date (title order 0 < date order 2)."
  },
  {
    "id": "title@selection#slash",
    "category": "Heading + title (7)",
    "action": "title",
    "kind": "selection",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "blockApplies base 'ok'. selection-mode 'ignored' so range never greys it. In practice ONLY invoked from a caret (slash).",
    "slashName": "title",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun, factory) via titleFieldRow(\"title\"):1382; routed by runViewOnlyAction(\"title\") commands.ts:93",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-existing-or-insert of a \\title titleField at the doc top. Scans top-level children for titleField{field:'title'}. IF FOUND: no node mutation, place caret at end of content, scrollIntoView - NO duplicate. IF ABSENT: insert titleField{field:'title', rawPrefix:null, isToday:false, uuid:fresh} at canonical order position 0, caret inside empty content. Collab read-only -> no-op. SLASH-ONLY (a doc-top singleton, not a card).",
      "docDelta": "If absent: +1 titleField node at the doc top (order index 0). If present: ZERO doc mutation (caret move only).",
      "texDelta": "If absent: +\\title{} line on serialize. If present: no .tex change.",
      "sidecarDelta": "None - pure-PM, no card sidecar.",
      "lifecycle": "No card, no float. The titleField renders as an in-doc lozenge, not a panel card."
    },
    "crossSurfaceIdentity": "SLASH-ONLY - no cross-surface byte-match required. The invariant is INTERNAL idempotency: a second \\title must NOT create a second titleField. Minted attrs (field:'title', rawPrefix:null, isToday:false, fresh uuid) must match what the .tex parser produces on reload.",
    "knownRisk": "Drive live FIRST: the IDEMPOTENT branch is divergence-prone. (1) Run \\title twice - second must be a no-op caret-move, NOT a duplicate node. (2) Run \\title when a titleField exists but is NOT at index 0 - find scans ALL children so it should dedupe; confirm. (3) Canonical-order insert walks childOrder; verify a \\title inserted when only \\date exists lands BEFORE \\date (title order 0 < date order 2)."
  },
  {
    "id": "title@texBlock#slash",
    "category": "Heading + title (7)",
    "action": "title",
    "kind": "texBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock -> 'disabled' (theoretical).",
    "slashName": "title",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun, factory) via titleFieldRow(\"title\"):1382; routed by runViewOnlyAction(\"title\") commands.ts:93",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-existing-or-insert of a \\title titleField at the doc top. Scans top-level children for titleField{field:'title'}. IF FOUND: no node mutation, place caret at end of content, scrollIntoView - NO duplicate. IF ABSENT: insert titleField{field:'title', rawPrefix:null, isToday:false, uuid:fresh} at canonical order position 0, caret inside empty content. Collab read-only -> no-op. SLASH-ONLY (a doc-top singleton, not a card).",
      "docDelta": "If absent: +1 titleField node at the doc top (order index 0). If present: ZERO doc mutation (caret move only).",
      "texDelta": "If absent: +\\title{} line on serialize. If present: no .tex change.",
      "sidecarDelta": "None - pure-PM, no card sidecar.",
      "lifecycle": "No card, no float. The titleField renders as an in-doc lozenge, not a panel card."
    },
    "crossSurfaceIdentity": "SLASH-ONLY - no cross-surface byte-match required. The invariant is INTERNAL idempotency: a second \\title must NOT create a second titleField. Minted attrs (field:'title', rawPrefix:null, isToday:false, fresh uuid) must match what the .tex parser produces on reload.",
    "knownRisk": "Drive live FIRST: the IDEMPOTENT branch is divergence-prone. (1) Run \\title twice - second must be a no-op caret-move, NOT a duplicate node. (2) Run \\title when a titleField exists but is NOT at index 0 - find scans ALL children so it should dedupe; confirm. (3) Canonical-order insert walks childOrder; verify a \\title inserted when only \\date exists lands BEFORE \\date (title order 0 < date order 2)."
  },
  {
    "id": "title@titleField#slash",
    "category": "Heading + title (7)",
    "action": "title",
    "kind": "titleField",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "The idempotent path: if a 'title' titleField already exists, places the caret at end of its content (no duplicate).",
    "slashName": "title",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1276 (titleFieldRun, factory) via titleFieldRow(\"title\"):1382; routed by runViewOnlyAction(\"title\") commands.ts:93",
    "backbone": "",
    "expected": {
      "behavior": "IDEMPOTENT find-existing-or-insert of a \\title titleField at the doc top. Scans top-level children for titleField{field:'title'}. IF FOUND: no node mutation, place caret at end of content, scrollIntoView - NO duplicate. IF ABSENT: insert titleField{field:'title', rawPrefix:null, isToday:false, uuid:fresh} at canonical order position 0, caret inside empty content. Collab read-only -> no-op. SLASH-ONLY (a doc-top singleton, not a card).",
      "docDelta": "If absent: +1 titleField node at the doc top (order index 0). If present: ZERO doc mutation (caret move only).",
      "texDelta": "If absent: +\\title{} line on serialize. If present: no .tex change.",
      "sidecarDelta": "None - pure-PM, no card sidecar.",
      "lifecycle": "No card, no float. The titleField renders as an in-doc lozenge, not a panel card."
    },
    "crossSurfaceIdentity": "SLASH-ONLY - no cross-surface byte-match required. The invariant is INTERNAL idempotency: a second \\title must NOT create a second titleField. Minted attrs (field:'title', rawPrefix:null, isToday:false, fresh uuid) must match what the .tex parser produces on reload.",
    "knownRisk": "Drive live FIRST: the IDEMPOTENT branch is divergence-prone. (1) Run \\title twice - second must be a no-op caret-move, NOT a duplicate node. (2) Run \\title when a titleField exists but is NOT at index 0 - find scans ALL children so it should dedupe; confirm. (3) Canonical-order insert walks childOrder; verify a \\title inserted when only \\date exists lands BEFORE \\date (title order 0 < date order 2)."
  },
  {
    "id": "display-math@cursor#lightning",
    "category": "Block inserts (6)",
    "action": "display-math",
    "kind": "cursor",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "Collapsed caret -> 'ok'. Insert placeholder '\\int f(x)\\,dx' displayMath.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1877 (mathRun('display')); DISPLAY_MATH_ACTION_ROW:2060; grid call ActionsMenuPanel:520 (runGridAction('display-math'))",
    "backbone": "",
    "expected": {
      "behavior": "WRAP the selection: latex = textBetween(from,to,' '), or placeholder '\\int f(x)\\,dx' when empty. deleteSelection() then insertContent({type:'displayMath', attrs:{latex}}) via editor.chain().focus(). The displayMath uuid is NOT pre-minted - it hydrates lazily via ensureAnchorUuid on first interaction. DATA-LOSS GUARD: an atom-only selection BAILS. No-op when ctx.canEdit===false.",
      "docDelta": "One displayMath block atom inserted (attrs:{latex}; uuid absent/lazy - hydrated by ensureAnchorUuid on first interaction). On a text-bearing selection the range is deleted first. displayMath is a block atom - PM lifts the insert out of an inline container automatically.",
      "texDelta": "\\[\\n<latex>\\n\\]<anchor>\\n\\n (serializer line 386; the %!v anchor appended only when the equation carries a uuid - absent until lazy-hydrated). Empty selection -> \\[\\n\\int f(x)\\,dx\\n\\].",
      "sidecarDelta": "None - displayMath is a pure in-doc block atom, no JSON sidecar.",
      "lifecycle": "No card produced. uuid hydrated lazily (ensureAnchorUuid) on first interaction, NOT at insert."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only). No cross-surface identity to enforce today.",
    "knownRisk": "Drive the lazy-uuid behavior live FIRST: displayMath is NOT pre-minted a uuid (unlike tex/figure/graphics/example), so a freshly inserted display equation serializes WITHOUT a %!v anchor until ensureAnchorUuid runs on first interaction. Verify the round-trip is stable (insert->serialize->reparse) for an un-interacted equation, and that a card anchored to it later isn't orphaned by the late uuid. Also verify the atom-only-selection data-loss guard (same as inline-math)."
  },
  {
    "id": "display-math@displayMath#lightning",
    "category": "Block inserts (6)",
    "action": "display-math",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled' (self-kind also excluded).",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1877 (mathRun('display')); DISPLAY_MATH_ACTION_ROW:2060; grid call ActionsMenuPanel:520 (runGridAction('display-math'))",
    "backbone": "",
    "expected": {
      "behavior": "WRAP the selection: latex = textBetween(from,to,' '), or placeholder '\\int f(x)\\,dx' when empty. deleteSelection() then insertContent({type:'displayMath', attrs:{latex}}) via editor.chain().focus(). The displayMath uuid is NOT pre-minted - it hydrates lazily via ensureAnchorUuid on first interaction. DATA-LOSS GUARD: an atom-only selection BAILS. No-op when ctx.canEdit===false.",
      "docDelta": "One displayMath block atom inserted (attrs:{latex}; uuid absent/lazy - hydrated by ensureAnchorUuid on first interaction). On a text-bearing selection the range is deleted first. displayMath is a block atom - PM lifts the insert out of an inline container automatically.",
      "texDelta": "\\[\\n<latex>\\n\\]<anchor>\\n\\n (serializer line 386; the %!v anchor appended only when the equation carries a uuid - absent until lazy-hydrated). Empty selection -> \\[\\n\\int f(x)\\,dx\\n\\].",
      "sidecarDelta": "None - displayMath is a pure in-doc block atom, no JSON sidecar.",
      "lifecycle": "No card produced. uuid hydrated lazily (ensureAnchorUuid) on first interaction, NOT at insert."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only). No cross-surface identity to enforce today.",
    "knownRisk": "Drive the lazy-uuid behavior live FIRST: displayMath is NOT pre-minted a uuid (unlike tex/figure/graphics/example), so a freshly inserted display equation serializes WITHOUT a %!v anchor until ensureAnchorUuid runs on first interaction. Verify the round-trip is stable (insert->serialize->reparse) for an un-interacted equation, and that a card anchored to it later isn't orphaned by the late uuid. Also verify the atom-only-selection data-loss guard (same as inline-math)."
  },
  {
    "id": "display-math@graphicsBlock#lightning",
    "category": "Block inserts (6)",
    "action": "display-math",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1877 (mathRun('display')); DISPLAY_MATH_ACTION_ROW:2060; grid call ActionsMenuPanel:520 (runGridAction('display-math'))",
    "backbone": "",
    "expected": {
      "behavior": "WRAP the selection: latex = textBetween(from,to,' '), or placeholder '\\int f(x)\\,dx' when empty. deleteSelection() then insertContent({type:'displayMath', attrs:{latex}}) via editor.chain().focus(). The displayMath uuid is NOT pre-minted - it hydrates lazily via ensureAnchorUuid on first interaction. DATA-LOSS GUARD: an atom-only selection BAILS. No-op when ctx.canEdit===false.",
      "docDelta": "One displayMath block atom inserted (attrs:{latex}; uuid absent/lazy - hydrated by ensureAnchorUuid on first interaction). On a text-bearing selection the range is deleted first. displayMath is a block atom - PM lifts the insert out of an inline container automatically.",
      "texDelta": "\\[\\n<latex>\\n\\]<anchor>\\n\\n (serializer line 386; the %!v anchor appended only when the equation carries a uuid - absent until lazy-hydrated). Empty selection -> \\[\\n\\int f(x)\\,dx\\n\\].",
      "sidecarDelta": "None - displayMath is a pure in-doc block atom, no JSON sidecar.",
      "lifecycle": "No card produced. uuid hydrated lazily (ensureAnchorUuid) on first interaction, NOT at insert."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only). No cross-surface identity to enforce today.",
    "knownRisk": "Drive the lazy-uuid behavior live FIRST: displayMath is NOT pre-minted a uuid (unlike tex/figure/graphics/example), so a freshly inserted display equation serializes WITHOUT a %!v anchor until ensureAnchorUuid runs on first interaction. Verify the round-trip is stable (insert->serialize->reparse) for an un-interacted equation, and that a card anchored to it later isn't orphaned by the late uuid. Also verify the atom-only-selection data-loss guard (same as inline-math)."
  },
  {
    "id": "display-math@latexComment#lightning",
    "category": "Block inserts (6)",
    "action": "display-math",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1877 (mathRun('display')); DISPLAY_MATH_ACTION_ROW:2060; grid call ActionsMenuPanel:520 (runGridAction('display-math'))",
    "backbone": "",
    "expected": {
      "behavior": "WRAP the selection: latex = textBetween(from,to,' '), or placeholder '\\int f(x)\\,dx' when empty. deleteSelection() then insertContent({type:'displayMath', attrs:{latex}}) via editor.chain().focus(). The displayMath uuid is NOT pre-minted - it hydrates lazily via ensureAnchorUuid on first interaction. DATA-LOSS GUARD: an atom-only selection BAILS. No-op when ctx.canEdit===false.",
      "docDelta": "One displayMath block atom inserted (attrs:{latex}; uuid absent/lazy - hydrated by ensureAnchorUuid on first interaction). On a text-bearing selection the range is deleted first. displayMath is a block atom - PM lifts the insert out of an inline container automatically.",
      "texDelta": "\\[\\n<latex>\\n\\]<anchor>\\n\\n (serializer line 386; the %!v anchor appended only when the equation carries a uuid - absent until lazy-hydrated). Empty selection -> \\[\\n\\int f(x)\\,dx\\n\\].",
      "sidecarDelta": "None - displayMath is a pure in-doc block atom, no JSON sidecar.",
      "lifecycle": "No card produced. uuid hydrated lazily (ensureAnchorUuid) on first interaction, NOT at insert."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only). No cross-surface identity to enforce today.",
    "knownRisk": "Drive the lazy-uuid behavior live FIRST: displayMath is NOT pre-minted a uuid (unlike tex/figure/graphics/example), so a freshly inserted display equation serializes WITHOUT a %!v anchor until ensureAnchorUuid runs on first interaction. Verify the round-trip is stable (insert->serialize->reparse) for an un-interacted equation, and that a card anchored to it later isn't orphaned by the late uuid. Also verify the atom-only-selection data-loss guard (same as inline-math)."
  },
  {
    "id": "display-math@paragraph#lightning",
    "category": "Block inserts (6)",
    "action": "display-math",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1877 (mathRun('display')); DISPLAY_MATH_ACTION_ROW:2060; grid call ActionsMenuPanel:520 (runGridAction('display-math'))",
    "backbone": "",
    "expected": {
      "behavior": "WRAP the selection: latex = textBetween(from,to,' '), or placeholder '\\int f(x)\\,dx' when empty. deleteSelection() then insertContent({type:'displayMath', attrs:{latex}}) via editor.chain().focus(). The displayMath uuid is NOT pre-minted - it hydrates lazily via ensureAnchorUuid on first interaction. DATA-LOSS GUARD: an atom-only selection BAILS. No-op when ctx.canEdit===false.",
      "docDelta": "One displayMath block atom inserted (attrs:{latex}; uuid absent/lazy - hydrated by ensureAnchorUuid on first interaction). On a text-bearing selection the range is deleted first. displayMath is a block atom - PM lifts the insert out of an inline container automatically.",
      "texDelta": "\\[\\n<latex>\\n\\]<anchor>\\n\\n (serializer line 386; the %!v anchor appended only when the equation carries a uuid - absent until lazy-hydrated). Empty selection -> \\[\\n\\int f(x)\\,dx\\n\\].",
      "sidecarDelta": "None - displayMath is a pure in-doc block atom, no JSON sidecar.",
      "lifecycle": "No card produced. uuid hydrated lazily (ensureAnchorUuid) on first interaction, NOT at insert."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only). No cross-surface identity to enforce today.",
    "knownRisk": "Drive the lazy-uuid behavior live FIRST: displayMath is NOT pre-minted a uuid (unlike tex/figure/graphics/example), so a freshly inserted display equation serializes WITHOUT a %!v anchor until ensureAnchorUuid runs on first interaction. Verify the round-trip is stable (insert->serialize->reparse) for an un-interacted equation, and that a card anchored to it later isn't orphaned by the late uuid. Also verify the atom-only-selection data-loss guard (same as inline-math)."
  },
  {
    "id": "display-math@selection#lightning",
    "category": "Block inserts (6)",
    "action": "display-math",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "SelectionRef -> 'ok'. WRAP selected text into the displayMath latex.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1877 (mathRun('display')); DISPLAY_MATH_ACTION_ROW:2060; grid call ActionsMenuPanel:520 (runGridAction('display-math'))",
    "backbone": "",
    "expected": {
      "behavior": "WRAP the selection: latex = textBetween(from,to,' '), or placeholder '\\int f(x)\\,dx' when empty. deleteSelection() then insertContent({type:'displayMath', attrs:{latex}}) via editor.chain().focus(). The displayMath uuid is NOT pre-minted - it hydrates lazily via ensureAnchorUuid on first interaction. DATA-LOSS GUARD: an atom-only selection BAILS. No-op when ctx.canEdit===false.",
      "docDelta": "One displayMath block atom inserted (attrs:{latex}; uuid absent/lazy - hydrated by ensureAnchorUuid on first interaction). On a text-bearing selection the range is deleted first. displayMath is a block atom - PM lifts the insert out of an inline container automatically.",
      "texDelta": "\\[\\n<latex>\\n\\]<anchor>\\n\\n (serializer line 386; the %!v anchor appended only when the equation carries a uuid - absent until lazy-hydrated). Empty selection -> \\[\\n\\int f(x)\\,dx\\n\\].",
      "sidecarDelta": "None - displayMath is a pure in-doc block atom, no JSON sidecar.",
      "lifecycle": "No card produced. uuid hydrated lazily (ensureAnchorUuid) on first interaction, NOT at insert."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only). No cross-surface identity to enforce today.",
    "knownRisk": "Drive the lazy-uuid behavior live FIRST: displayMath is NOT pre-minted a uuid (unlike tex/figure/graphics/example), so a freshly inserted display equation serializes WITHOUT a %!v anchor until ensureAnchorUuid runs on first interaction. Verify the round-trip is stable (insert->serialize->reparse) for an un-interacted equation, and that a card anchored to it later isn't orphaned by the late uuid. Also verify the atom-only-selection data-loss guard (same as inline-math)."
  },
  {
    "id": "display-math@selection:collab-readonly#lightning",
    "category": "Block inserts (6)",
    "action": "display-math",
    "kind": "selection:collab-readonly",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "ctx.canEdit===false -> 'disabled' via gateApplies.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1877 (mathRun('display')); DISPLAY_MATH_ACTION_ROW:2060; grid call ActionsMenuPanel:520 (runGridAction('display-math'))",
    "backbone": "",
    "expected": {
      "behavior": "WRAP the selection: latex = textBetween(from,to,' '), or placeholder '\\int f(x)\\,dx' when empty. deleteSelection() then insertContent({type:'displayMath', attrs:{latex}}) via editor.chain().focus(). The displayMath uuid is NOT pre-minted - it hydrates lazily via ensureAnchorUuid on first interaction. DATA-LOSS GUARD: an atom-only selection BAILS. No-op when ctx.canEdit===false.",
      "docDelta": "One displayMath block atom inserted (attrs:{latex}; uuid absent/lazy - hydrated by ensureAnchorUuid on first interaction). On a text-bearing selection the range is deleted first. displayMath is a block atom - PM lifts the insert out of an inline container automatically.",
      "texDelta": "\\[\\n<latex>\\n\\]<anchor>\\n\\n (serializer line 386; the %!v anchor appended only when the equation carries a uuid - absent until lazy-hydrated). Empty selection -> \\[\\n\\int f(x)\\,dx\\n\\].",
      "sidecarDelta": "None - displayMath is a pure in-doc block atom, no JSON sidecar.",
      "lifecycle": "No card produced. uuid hydrated lazily (ensureAnchorUuid) on first interaction, NOT at insert."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only). No cross-surface identity to enforce today.",
    "knownRisk": "Drive the lazy-uuid behavior live FIRST: displayMath is NOT pre-minted a uuid (unlike tex/figure/graphics/example), so a freshly inserted display equation serializes WITHOUT a %!v anchor until ensureAnchorUuid runs on first interaction. Verify the round-trip is stable (insert->serialize->reparse) for an un-interacted equation, and that a card anchored to it later isn't orphaned by the late uuid. Also verify the atom-only-selection data-loss guard (same as inline-math)."
  },
  {
    "id": "display-math@texBlock#lightning",
    "category": "Block inserts (6)",
    "action": "display-math",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1877 (mathRun('display')); DISPLAY_MATH_ACTION_ROW:2060; grid call ActionsMenuPanel:520 (runGridAction('display-math'))",
    "backbone": "",
    "expected": {
      "behavior": "WRAP the selection: latex = textBetween(from,to,' '), or placeholder '\\int f(x)\\,dx' when empty. deleteSelection() then insertContent({type:'displayMath', attrs:{latex}}) via editor.chain().focus(). The displayMath uuid is NOT pre-minted - it hydrates lazily via ensureAnchorUuid on first interaction. DATA-LOSS GUARD: an atom-only selection BAILS. No-op when ctx.canEdit===false.",
      "docDelta": "One displayMath block atom inserted (attrs:{latex}; uuid absent/lazy - hydrated by ensureAnchorUuid on first interaction). On a text-bearing selection the range is deleted first. displayMath is a block atom - PM lifts the insert out of an inline container automatically.",
      "texDelta": "\\[\\n<latex>\\n\\]<anchor>\\n\\n (serializer line 386; the %!v anchor appended only when the equation carries a uuid - absent until lazy-hydrated). Empty selection -> \\[\\n\\int f(x)\\,dx\\n\\].",
      "sidecarDelta": "None - displayMath is a pure in-doc block atom, no JSON sidecar.",
      "lifecycle": "No card produced. uuid hydrated lazily (ensureAnchorUuid) on first interaction, NOT at insert."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only). No cross-surface identity to enforce today.",
    "knownRisk": "Drive the lazy-uuid behavior live FIRST: displayMath is NOT pre-minted a uuid (unlike tex/figure/graphics/example), so a freshly inserted display equation serializes WITHOUT a %!v anchor until ensureAnchorUuid runs on first interaction. Verify the round-trip is stable (insert->serialize->reparse) for an un-interacted equation, and that a card anchored to it later isn't orphaned by the late uuid. Also verify the atom-only-selection data-loss guard (same as inline-math)."
  },
  {
    "id": "example@cursor#lightning",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "cursor",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "CursorRef (slash caret) -> base 'ok'. INSERT empty single example at caret.",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "example@cursor#slash",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "cursor",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "CursorRef (slash caret) -> base 'ok'. INSERT empty single example at caret.",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "example@displayMath#lightning",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled' (no caret to insert at).",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "example@displayMath#slash",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "displayMath",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled' (no caret to insert at).",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "example@graphicsBlock#lightning",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "example@graphicsBlock#slash",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "graphicsBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "example@heading#lightning",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "example@heading#slash",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "heading",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "example@latexComment#lightning",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "example@latexComment#slash",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "latexComment",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "example@paragraph#lightning",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> base 'ok' via blockApplies.",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "example@paragraph#slash",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "paragraph",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> base 'ok' via blockApplies.",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "example@selection#lightning",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "SelectionRef -> blockApplies base 'ok'. The WRAP path: harvests inline content from the slice.",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "example@selection#slash",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "selection",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "SelectionRef -> blockApplies base 'ok'. The WRAP path: harvests inline content from the slice.",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "example@selection:collab-readonly#lightning",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "selection:collab-readonly",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "When ctx.canEdit === false: gateApplies layer (1) -> 'disabled' uniformly, regardless of kind.",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "example@selection:collab-readonly#slash",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "selection:collab-readonly",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "When ctx.canEdit === false: gateApplies layer (1) -> 'disabled' uniformly, regardless of kind.",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "example@texBlock#lightning",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "example@texBlock#slash",
    "category": "Block inserts (6)",
    "action": "example",
    "kind": "texBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "ex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1748 (exampleRun); template buildExampleNode:1655; EXAMPLE_ACTION_ROW:1823",
    "backbone": "",
    "expected": {
      "behavior": "Wrap-if-selection-else-insert. Non-empty selection -> WRAP: extractInlineFromSlice harvests ONLY inline nodes, deleteSelection() then insert a SINGLE exampleBlock seeded with that inline content in its first item paragraph. Whitespace-only harvest -> empty-template fallback. Collapsed caret -> INSERT an empty single example. After insert, caret PARKED inside the new block's first editable paragraph (by uuid). scrollIntoView. Slash additionally soft-selects in an ALREADY-open Examples panel. No-op when ctx.canEdit===false.",
      "docDelta": "One exampleBlock added (kind:'single', attrs uuid + tag/label/exnoOverride:null/suppressSpace:false/number:0), content = [paragraph] (seeded inline on wrap, empty on insert). On wrap the selected range is deleted first. buildExampleNode is the SSOT.",
      "texDelta": "\\ex <inline content>\\vexid{<uuid>}\\n (serializeExampleBlock). Empty insert -> \\ex with empty body. Requires \\providecommand{\\vexid}[1]{}.",
      "sidecarDelta": "None - exampleBlock is a pure in-doc node. The only out-of-doc effect is the transient Examples-panel selectedExampleId state (slash surface).",
      "lifecycle": "No card produced. Caret parked in new block's first paragraph. Slash: selectedExampleId set to new uuid (soft panel-select, no force-open)."
    },
    "crossSurfaceIdentity": "Both surfaces route through the ONE exampleRun: same exampleBlock node, same firstParagraph seeding, same uuid-collision scan, same caret-park, same .tex round-trip. The ONLY intentional difference: slash threads panelRouting.selectExample (soft panel-select); the lightning grid omits panelRouting entirely - by design.",
    "knownRisk": "Drive the WRAP-on-atom-only-selection case live FIRST: when the selection holds ONLY an inline atom (citation/$\\lambda$/\\ref) and no text, extractInlineFromSlice returns it via hasUsable=true and WRAPS it - unlike texRun/mathRun which BAIL on an atom-only selection. example's atom-only behavior INTENTIONALLY diverges from tex/math: example MOVES the atom into the item paragraph. Verify the atom survives the deleteSelection->insert round-trip (the observer multi-step move-bug class). Also: the lightning grid uses a SEPARATE hand-rolled ctx builder (wrapSelectionInExample, ActionsMenuPanel:274) that calls exampleRun directly - verify it stays in sync with the bridge ctx."
  },
  {
    "id": "figure@cursor#lightning",
    "category": "Block inserts (6)",
    "action": "figure",
    "kind": "cursor",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "Collapsed caret -> 'ok'. Plain cursor-insert.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1940 (figureRun); FIGURE_ACTION_ROW:2069; smart-insert smart-insert.ts:119; attrs figure-attrs.ts:42",
    "backbone": "",
    "expected": {
      "behavior": "INSERT an opaque figureBlock atom at the caret (replacing any non-empty selection per smartInsertBlock's REPLACE policy), with a freshFigureBlockAttrs stub (centered \\includegraphics empty path, label 'fig:', numbered:true, widthPercent:60, collision-free uuid) and a single figureCaption child. Then ONE rAF after insert, open the SOURCE popover anchored to the block via ctx.openFigurePopover(seed). No-op when ctx.canEdit===false.",
      "docDelta": "One figureBlock added via smartInsertBlock (deleteSelection if non-empty, then replaceSelectionWith), content=[figureCaption]. uuid minted collision-free. Position relocated post-dispatch by uuid for the popover anchor.",
      "texDelta": "\\begin{figure}...\\centering \\includegraphics[width=0.6\\textwidth]{} \\caption{} (label \\label{fig:} when present)...\\end{figure}. The fresh stub has an EMPTY image path until the user fills the popover.",
      "sidecarDelta": "None - figureBlock is a pure in-doc node (attrs + figureCaption sub-node), no JSON sidecar.",
      "lifecycle": "No card produced. The source popover opens (one rAF later) for the user to fill the \\includegraphics path/label."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today - no cross-surface byte-identity to enforce. But figureRun IS the SSOT the standalone insertFigureBlock helper and any future FILE-DROP path delegate to (DA-2): all figure-insert paths must produce the byte-identical smartInsertBlock result.",
    "knownRisk": "Drive the rAF-deferred popover live FIRST in the backgrounded preview - rAF is paused when backgrounded, so the popover-open + relocateBlock(uuid) lookup inside the rAF may never fire under test; shim rAF->setTimeout. Also verify the REPLACE-selection policy on a non-empty selection: smartInsertBlock deletes the selected range and drops the figure in its place - confirm this is the intended (documented) behavior vs surprising data loss."
  },
  {
    "id": "figure@displayMath#lightning",
    "category": "Block inserts (6)",
    "action": "figure",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1940 (figureRun); FIGURE_ACTION_ROW:2069; smart-insert smart-insert.ts:119; attrs figure-attrs.ts:42",
    "backbone": "",
    "expected": {
      "behavior": "INSERT an opaque figureBlock atom at the caret (replacing any non-empty selection per smartInsertBlock's REPLACE policy), with a freshFigureBlockAttrs stub (centered \\includegraphics empty path, label 'fig:', numbered:true, widthPercent:60, collision-free uuid) and a single figureCaption child. Then ONE rAF after insert, open the SOURCE popover anchored to the block via ctx.openFigurePopover(seed). No-op when ctx.canEdit===false.",
      "docDelta": "One figureBlock added via smartInsertBlock (deleteSelection if non-empty, then replaceSelectionWith), content=[figureCaption]. uuid minted collision-free. Position relocated post-dispatch by uuid for the popover anchor.",
      "texDelta": "\\begin{figure}...\\centering \\includegraphics[width=0.6\\textwidth]{} \\caption{} (label \\label{fig:} when present)...\\end{figure}. The fresh stub has an EMPTY image path until the user fills the popover.",
      "sidecarDelta": "None - figureBlock is a pure in-doc node (attrs + figureCaption sub-node), no JSON sidecar.",
      "lifecycle": "No card produced. The source popover opens (one rAF later) for the user to fill the \\includegraphics path/label."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today - no cross-surface byte-identity to enforce. But figureRun IS the SSOT the standalone insertFigureBlock helper and any future FILE-DROP path delegate to (DA-2): all figure-insert paths must produce the byte-identical smartInsertBlock result.",
    "knownRisk": "Drive the rAF-deferred popover live FIRST in the backgrounded preview - rAF is paused when backgrounded, so the popover-open + relocateBlock(uuid) lookup inside the rAF may never fire under test; shim rAF->setTimeout. Also verify the REPLACE-selection policy on a non-empty selection: smartInsertBlock deletes the selected range and drops the figure in its place - confirm this is the intended (documented) behavior vs surprising data loss."
  },
  {
    "id": "figure@graphicsBlock#lightning",
    "category": "Block inserts (6)",
    "action": "figure",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1940 (figureRun); FIGURE_ACTION_ROW:2069; smart-insert smart-insert.ts:119; attrs figure-attrs.ts:42",
    "backbone": "",
    "expected": {
      "behavior": "INSERT an opaque figureBlock atom at the caret (replacing any non-empty selection per smartInsertBlock's REPLACE policy), with a freshFigureBlockAttrs stub (centered \\includegraphics empty path, label 'fig:', numbered:true, widthPercent:60, collision-free uuid) and a single figureCaption child. Then ONE rAF after insert, open the SOURCE popover anchored to the block via ctx.openFigurePopover(seed). No-op when ctx.canEdit===false.",
      "docDelta": "One figureBlock added via smartInsertBlock (deleteSelection if non-empty, then replaceSelectionWith), content=[figureCaption]. uuid minted collision-free. Position relocated post-dispatch by uuid for the popover anchor.",
      "texDelta": "\\begin{figure}...\\centering \\includegraphics[width=0.6\\textwidth]{} \\caption{} (label \\label{fig:} when present)...\\end{figure}. The fresh stub has an EMPTY image path until the user fills the popover.",
      "sidecarDelta": "None - figureBlock is a pure in-doc node (attrs + figureCaption sub-node), no JSON sidecar.",
      "lifecycle": "No card produced. The source popover opens (one rAF later) for the user to fill the \\includegraphics path/label."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today - no cross-surface byte-identity to enforce. But figureRun IS the SSOT the standalone insertFigureBlock helper and any future FILE-DROP path delegate to (DA-2): all figure-insert paths must produce the byte-identical smartInsertBlock result.",
    "knownRisk": "Drive the rAF-deferred popover live FIRST in the backgrounded preview - rAF is paused when backgrounded, so the popover-open + relocateBlock(uuid) lookup inside the rAF may never fire under test; shim rAF->setTimeout. Also verify the REPLACE-selection policy on a non-empty selection: smartInsertBlock deletes the selected range and drops the figure in its place - confirm this is the intended (documented) behavior vs surprising data loss."
  },
  {
    "id": "figure@latexComment#lightning",
    "category": "Block inserts (6)",
    "action": "figure",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1940 (figureRun); FIGURE_ACTION_ROW:2069; smart-insert smart-insert.ts:119; attrs figure-attrs.ts:42",
    "backbone": "",
    "expected": {
      "behavior": "INSERT an opaque figureBlock atom at the caret (replacing any non-empty selection per smartInsertBlock's REPLACE policy), with a freshFigureBlockAttrs stub (centered \\includegraphics empty path, label 'fig:', numbered:true, widthPercent:60, collision-free uuid) and a single figureCaption child. Then ONE rAF after insert, open the SOURCE popover anchored to the block via ctx.openFigurePopover(seed). No-op when ctx.canEdit===false.",
      "docDelta": "One figureBlock added via smartInsertBlock (deleteSelection if non-empty, then replaceSelectionWith), content=[figureCaption]. uuid minted collision-free. Position relocated post-dispatch by uuid for the popover anchor.",
      "texDelta": "\\begin{figure}...\\centering \\includegraphics[width=0.6\\textwidth]{} \\caption{} (label \\label{fig:} when present)...\\end{figure}. The fresh stub has an EMPTY image path until the user fills the popover.",
      "sidecarDelta": "None - figureBlock is a pure in-doc node (attrs + figureCaption sub-node), no JSON sidecar.",
      "lifecycle": "No card produced. The source popover opens (one rAF later) for the user to fill the \\includegraphics path/label."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today - no cross-surface byte-identity to enforce. But figureRun IS the SSOT the standalone insertFigureBlock helper and any future FILE-DROP path delegate to (DA-2): all figure-insert paths must produce the byte-identical smartInsertBlock result.",
    "knownRisk": "Drive the rAF-deferred popover live FIRST in the backgrounded preview - rAF is paused when backgrounded, so the popover-open + relocateBlock(uuid) lookup inside the rAF may never fire under test; shim rAF->setTimeout. Also verify the REPLACE-selection policy on a non-empty selection: smartInsertBlock deletes the selected range and drops the figure in its place - confirm this is the intended (documented) behavior vs surprising data loss."
  },
  {
    "id": "figure@paragraph#lightning",
    "category": "Block inserts (6)",
    "action": "figure",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1940 (figureRun); FIGURE_ACTION_ROW:2069; smart-insert smart-insert.ts:119; attrs figure-attrs.ts:42",
    "backbone": "",
    "expected": {
      "behavior": "INSERT an opaque figureBlock atom at the caret (replacing any non-empty selection per smartInsertBlock's REPLACE policy), with a freshFigureBlockAttrs stub (centered \\includegraphics empty path, label 'fig:', numbered:true, widthPercent:60, collision-free uuid) and a single figureCaption child. Then ONE rAF after insert, open the SOURCE popover anchored to the block via ctx.openFigurePopover(seed). No-op when ctx.canEdit===false.",
      "docDelta": "One figureBlock added via smartInsertBlock (deleteSelection if non-empty, then replaceSelectionWith), content=[figureCaption]. uuid minted collision-free. Position relocated post-dispatch by uuid for the popover anchor.",
      "texDelta": "\\begin{figure}...\\centering \\includegraphics[width=0.6\\textwidth]{} \\caption{} (label \\label{fig:} when present)...\\end{figure}. The fresh stub has an EMPTY image path until the user fills the popover.",
      "sidecarDelta": "None - figureBlock is a pure in-doc node (attrs + figureCaption sub-node), no JSON sidecar.",
      "lifecycle": "No card produced. The source popover opens (one rAF later) for the user to fill the \\includegraphics path/label."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today - no cross-surface byte-identity to enforce. But figureRun IS the SSOT the standalone insertFigureBlock helper and any future FILE-DROP path delegate to (DA-2): all figure-insert paths must produce the byte-identical smartInsertBlock result.",
    "knownRisk": "Drive the rAF-deferred popover live FIRST in the backgrounded preview - rAF is paused when backgrounded, so the popover-open + relocateBlock(uuid) lookup inside the rAF may never fire under test; shim rAF->setTimeout. Also verify the REPLACE-selection policy on a non-empty selection: smartInsertBlock deletes the selected range and drops the figure in its place - confirm this is the intended (documented) behavior vs surprising data loss."
  },
  {
    "id": "figure@selection#lightning",
    "category": "Block inserts (6)",
    "action": "figure",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "Grid passes a SelectionRef -> blockApplies base 'ok'. A non-empty selection is REPLACED by the block (smartInsertBlock policy).",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1940 (figureRun); FIGURE_ACTION_ROW:2069; smart-insert smart-insert.ts:119; attrs figure-attrs.ts:42",
    "backbone": "",
    "expected": {
      "behavior": "INSERT an opaque figureBlock atom at the caret (replacing any non-empty selection per smartInsertBlock's REPLACE policy), with a freshFigureBlockAttrs stub (centered \\includegraphics empty path, label 'fig:', numbered:true, widthPercent:60, collision-free uuid) and a single figureCaption child. Then ONE rAF after insert, open the SOURCE popover anchored to the block via ctx.openFigurePopover(seed). No-op when ctx.canEdit===false.",
      "docDelta": "One figureBlock added via smartInsertBlock (deleteSelection if non-empty, then replaceSelectionWith), content=[figureCaption]. uuid minted collision-free. Position relocated post-dispatch by uuid for the popover anchor.",
      "texDelta": "\\begin{figure}...\\centering \\includegraphics[width=0.6\\textwidth]{} \\caption{} (label \\label{fig:} when present)...\\end{figure}. The fresh stub has an EMPTY image path until the user fills the popover.",
      "sidecarDelta": "None - figureBlock is a pure in-doc node (attrs + figureCaption sub-node), no JSON sidecar.",
      "lifecycle": "No card produced. The source popover opens (one rAF later) for the user to fill the \\includegraphics path/label."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today - no cross-surface byte-identity to enforce. But figureRun IS the SSOT the standalone insertFigureBlock helper and any future FILE-DROP path delegate to (DA-2): all figure-insert paths must produce the byte-identical smartInsertBlock result.",
    "knownRisk": "Drive the rAF-deferred popover live FIRST in the backgrounded preview - rAF is paused when backgrounded, so the popover-open + relocateBlock(uuid) lookup inside the rAF may never fire under test; shim rAF->setTimeout. Also verify the REPLACE-selection policy on a non-empty selection: smartInsertBlock deletes the selected range and drops the figure in its place - confirm this is the intended (documented) behavior vs surprising data loss."
  },
  {
    "id": "figure@selection:collab-readonly#lightning",
    "category": "Block inserts (6)",
    "action": "figure",
    "kind": "selection:collab-readonly",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "ctx.canEdit===false -> 'disabled' via gateApplies (the grid threads canEdit through runGridAction).",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1940 (figureRun); FIGURE_ACTION_ROW:2069; smart-insert smart-insert.ts:119; attrs figure-attrs.ts:42",
    "backbone": "",
    "expected": {
      "behavior": "INSERT an opaque figureBlock atom at the caret (replacing any non-empty selection per smartInsertBlock's REPLACE policy), with a freshFigureBlockAttrs stub (centered \\includegraphics empty path, label 'fig:', numbered:true, widthPercent:60, collision-free uuid) and a single figureCaption child. Then ONE rAF after insert, open the SOURCE popover anchored to the block via ctx.openFigurePopover(seed). No-op when ctx.canEdit===false.",
      "docDelta": "One figureBlock added via smartInsertBlock (deleteSelection if non-empty, then replaceSelectionWith), content=[figureCaption]. uuid minted collision-free. Position relocated post-dispatch by uuid for the popover anchor.",
      "texDelta": "\\begin{figure}...\\centering \\includegraphics[width=0.6\\textwidth]{} \\caption{} (label \\label{fig:} when present)...\\end{figure}. The fresh stub has an EMPTY image path until the user fills the popover.",
      "sidecarDelta": "None - figureBlock is a pure in-doc node (attrs + figureCaption sub-node), no JSON sidecar.",
      "lifecycle": "No card produced. The source popover opens (one rAF later) for the user to fill the \\includegraphics path/label."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today - no cross-surface byte-identity to enforce. But figureRun IS the SSOT the standalone insertFigureBlock helper and any future FILE-DROP path delegate to (DA-2): all figure-insert paths must produce the byte-identical smartInsertBlock result.",
    "knownRisk": "Drive the rAF-deferred popover live FIRST in the backgrounded preview - rAF is paused when backgrounded, so the popover-open + relocateBlock(uuid) lookup inside the rAF may never fire under test; shim rAF->setTimeout. Also verify the REPLACE-selection policy on a non-empty selection: smartInsertBlock deletes the selected range and drops the figure in its place - confirm this is the intended (documented) behavior vs surprising data loss."
  },
  {
    "id": "figure@texBlock#lightning",
    "category": "Block inserts (6)",
    "action": "figure",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1940 (figureRun); FIGURE_ACTION_ROW:2069; smart-insert smart-insert.ts:119; attrs figure-attrs.ts:42",
    "backbone": "",
    "expected": {
      "behavior": "INSERT an opaque figureBlock atom at the caret (replacing any non-empty selection per smartInsertBlock's REPLACE policy), with a freshFigureBlockAttrs stub (centered \\includegraphics empty path, label 'fig:', numbered:true, widthPercent:60, collision-free uuid) and a single figureCaption child. Then ONE rAF after insert, open the SOURCE popover anchored to the block via ctx.openFigurePopover(seed). No-op when ctx.canEdit===false.",
      "docDelta": "One figureBlock added via smartInsertBlock (deleteSelection if non-empty, then replaceSelectionWith), content=[figureCaption]. uuid minted collision-free. Position relocated post-dispatch by uuid for the popover anchor.",
      "texDelta": "\\begin{figure}...\\centering \\includegraphics[width=0.6\\textwidth]{} \\caption{} (label \\label{fig:} when present)...\\end{figure}. The fresh stub has an EMPTY image path until the user fills the popover.",
      "sidecarDelta": "None - figureBlock is a pure in-doc node (attrs + figureCaption sub-node), no JSON sidecar.",
      "lifecycle": "No card produced. The source popover opens (one rAF later) for the user to fill the \\includegraphics path/label."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today - no cross-surface byte-identity to enforce. But figureRun IS the SSOT the standalone insertFigureBlock helper and any future FILE-DROP path delegate to (DA-2): all figure-insert paths must produce the byte-identical smartInsertBlock result.",
    "knownRisk": "Drive the rAF-deferred popover live FIRST in the backgrounded preview - rAF is paused when backgrounded, so the popover-open + relocateBlock(uuid) lookup inside the rAF may never fire under test; shim rAF->setTimeout. Also verify the REPLACE-selection policy on a non-empty selection: smartInsertBlock deletes the selected range and drops the figure in its place - confirm this is the intended (documented) behavior vs surprising data loss."
  },
  {
    "id": "graphics@cursor#lightning",
    "category": "Block inserts (6)",
    "action": "graphics",
    "kind": "cursor",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "Collapsed caret -> 'ok'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1968 (graphicsRun); GRAPHICS_ACTION_ROW:2078; smart-insert smart-insert.ts:119; attrs figure-attrs.ts:94",
    "backbone": "",
    "expected": {
      "behavior": "INSERT an opaque graphicsBlock atom at the caret (replacing any non-empty selection per smartInsertBlock's REPLACE policy) with a freshGraphicsBlockAttrs stub (command = '\\includegraphics[width=0.5\\textwidth]{}', source '', widthPercent:50, collision-free uuid) and NO content child. Then ONE rAF after insert, open the SOURCE popover anchored to the block via ctx.openFigurePopover(seed) (raw = attrs.command). No-op when ctx.canEdit===false.",
      "docDelta": "One graphicsBlock added via smartInsertBlock, no content. uuid minted collision-free. Position relocated post-dispatch by uuid for the popover anchor.",
      "texDelta": "Verbatim \\includegraphics[width=0.5\\textwidth]{} (standalone, serializeGraphicsBlock emits attrs.command verbatim; \\vxid{<uuid>} marker). Empty path until the user fills the popover.",
      "sidecarDelta": "None - graphicsBlock is a pure in-doc node (attrs-only), no JSON sidecar.",
      "lifecycle": "No card produced. Source popover opens (one rAF later) seeded with attrs.command."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today. graphicsRun is the SSOT the standalone insertGraphicsBlock helper and any future FILE-DROP path delegate to (DA-2): all must produce the byte-identical smartInsertBlock result.",
    "knownRisk": "Same rAF-pause caveat as figure (shim rAF->setTimeout in the backgrounded preview). Note the self-kind 'disabled' (graphicsBlock x graphics) is correct-by-design but is the kind of cell to spot-check it isn't accidentally 'absent'/missing in the grid. REPLACE-on-selection: confirm a non-empty selection is intentionally consumed."
  },
  {
    "id": "graphics@displayMath#lightning",
    "category": "Block inserts (6)",
    "action": "graphics",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1968 (graphicsRun); GRAPHICS_ACTION_ROW:2078; smart-insert smart-insert.ts:119; attrs figure-attrs.ts:94",
    "backbone": "",
    "expected": {
      "behavior": "INSERT an opaque graphicsBlock atom at the caret (replacing any non-empty selection per smartInsertBlock's REPLACE policy) with a freshGraphicsBlockAttrs stub (command = '\\includegraphics[width=0.5\\textwidth]{}', source '', widthPercent:50, collision-free uuid) and NO content child. Then ONE rAF after insert, open the SOURCE popover anchored to the block via ctx.openFigurePopover(seed) (raw = attrs.command). No-op when ctx.canEdit===false.",
      "docDelta": "One graphicsBlock added via smartInsertBlock, no content. uuid minted collision-free. Position relocated post-dispatch by uuid for the popover anchor.",
      "texDelta": "Verbatim \\includegraphics[width=0.5\\textwidth]{} (standalone, serializeGraphicsBlock emits attrs.command verbatim; \\vxid{<uuid>} marker). Empty path until the user fills the popover.",
      "sidecarDelta": "None - graphicsBlock is a pure in-doc node (attrs-only), no JSON sidecar.",
      "lifecycle": "No card produced. Source popover opens (one rAF later) seeded with attrs.command."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today. graphicsRun is the SSOT the standalone insertGraphicsBlock helper and any future FILE-DROP path delegate to (DA-2): all must produce the byte-identical smartInsertBlock result.",
    "knownRisk": "Same rAF-pause caveat as figure (shim rAF->setTimeout in the backgrounded preview). Note the self-kind 'disabled' (graphicsBlock x graphics) is correct-by-design but is the kind of cell to spot-check it isn't accidentally 'absent'/missing in the grid. REPLACE-on-selection: confirm a non-empty selection is intentionally consumed."
  },
  {
    "id": "graphics@graphicsBlock#lightning",
    "category": "Block inserts (6)",
    "action": "graphics",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled' (self-kind also excluded - no caret in an atom block).",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1968 (graphicsRun); GRAPHICS_ACTION_ROW:2078; smart-insert smart-insert.ts:119; attrs figure-attrs.ts:94",
    "backbone": "",
    "expected": {
      "behavior": "INSERT an opaque graphicsBlock atom at the caret (replacing any non-empty selection per smartInsertBlock's REPLACE policy) with a freshGraphicsBlockAttrs stub (command = '\\includegraphics[width=0.5\\textwidth]{}', source '', widthPercent:50, collision-free uuid) and NO content child. Then ONE rAF after insert, open the SOURCE popover anchored to the block via ctx.openFigurePopover(seed) (raw = attrs.command). No-op when ctx.canEdit===false.",
      "docDelta": "One graphicsBlock added via smartInsertBlock, no content. uuid minted collision-free. Position relocated post-dispatch by uuid for the popover anchor.",
      "texDelta": "Verbatim \\includegraphics[width=0.5\\textwidth]{} (standalone, serializeGraphicsBlock emits attrs.command verbatim; \\vxid{<uuid>} marker). Empty path until the user fills the popover.",
      "sidecarDelta": "None - graphicsBlock is a pure in-doc node (attrs-only), no JSON sidecar.",
      "lifecycle": "No card produced. Source popover opens (one rAF later) seeded with attrs.command."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today. graphicsRun is the SSOT the standalone insertGraphicsBlock helper and any future FILE-DROP path delegate to (DA-2): all must produce the byte-identical smartInsertBlock result.",
    "knownRisk": "Same rAF-pause caveat as figure (shim rAF->setTimeout in the backgrounded preview). Note the self-kind 'disabled' (graphicsBlock x graphics) is correct-by-design but is the kind of cell to spot-check it isn't accidentally 'absent'/missing in the grid. REPLACE-on-selection: confirm a non-empty selection is intentionally consumed."
  },
  {
    "id": "graphics@latexComment#lightning",
    "category": "Block inserts (6)",
    "action": "graphics",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1968 (graphicsRun); GRAPHICS_ACTION_ROW:2078; smart-insert smart-insert.ts:119; attrs figure-attrs.ts:94",
    "backbone": "",
    "expected": {
      "behavior": "INSERT an opaque graphicsBlock atom at the caret (replacing any non-empty selection per smartInsertBlock's REPLACE policy) with a freshGraphicsBlockAttrs stub (command = '\\includegraphics[width=0.5\\textwidth]{}', source '', widthPercent:50, collision-free uuid) and NO content child. Then ONE rAF after insert, open the SOURCE popover anchored to the block via ctx.openFigurePopover(seed) (raw = attrs.command). No-op when ctx.canEdit===false.",
      "docDelta": "One graphicsBlock added via smartInsertBlock, no content. uuid minted collision-free. Position relocated post-dispatch by uuid for the popover anchor.",
      "texDelta": "Verbatim \\includegraphics[width=0.5\\textwidth]{} (standalone, serializeGraphicsBlock emits attrs.command verbatim; \\vxid{<uuid>} marker). Empty path until the user fills the popover.",
      "sidecarDelta": "None - graphicsBlock is a pure in-doc node (attrs-only), no JSON sidecar.",
      "lifecycle": "No card produced. Source popover opens (one rAF later) seeded with attrs.command."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today. graphicsRun is the SSOT the standalone insertGraphicsBlock helper and any future FILE-DROP path delegate to (DA-2): all must produce the byte-identical smartInsertBlock result.",
    "knownRisk": "Same rAF-pause caveat as figure (shim rAF->setTimeout in the backgrounded preview). Note the self-kind 'disabled' (graphicsBlock x graphics) is correct-by-design but is the kind of cell to spot-check it isn't accidentally 'absent'/missing in the grid. REPLACE-on-selection: confirm a non-empty selection is intentionally consumed."
  },
  {
    "id": "graphics@paragraph#lightning",
    "category": "Block inserts (6)",
    "action": "graphics",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1968 (graphicsRun); GRAPHICS_ACTION_ROW:2078; smart-insert smart-insert.ts:119; attrs figure-attrs.ts:94",
    "backbone": "",
    "expected": {
      "behavior": "INSERT an opaque graphicsBlock atom at the caret (replacing any non-empty selection per smartInsertBlock's REPLACE policy) with a freshGraphicsBlockAttrs stub (command = '\\includegraphics[width=0.5\\textwidth]{}', source '', widthPercent:50, collision-free uuid) and NO content child. Then ONE rAF after insert, open the SOURCE popover anchored to the block via ctx.openFigurePopover(seed) (raw = attrs.command). No-op when ctx.canEdit===false.",
      "docDelta": "One graphicsBlock added via smartInsertBlock, no content. uuid minted collision-free. Position relocated post-dispatch by uuid for the popover anchor.",
      "texDelta": "Verbatim \\includegraphics[width=0.5\\textwidth]{} (standalone, serializeGraphicsBlock emits attrs.command verbatim; \\vxid{<uuid>} marker). Empty path until the user fills the popover.",
      "sidecarDelta": "None - graphicsBlock is a pure in-doc node (attrs-only), no JSON sidecar.",
      "lifecycle": "No card produced. Source popover opens (one rAF later) seeded with attrs.command."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today. graphicsRun is the SSOT the standalone insertGraphicsBlock helper and any future FILE-DROP path delegate to (DA-2): all must produce the byte-identical smartInsertBlock result.",
    "knownRisk": "Same rAF-pause caveat as figure (shim rAF->setTimeout in the backgrounded preview). Note the self-kind 'disabled' (graphicsBlock x graphics) is correct-by-design but is the kind of cell to spot-check it isn't accidentally 'absent'/missing in the grid. REPLACE-on-selection: confirm a non-empty selection is intentionally consumed."
  },
  {
    "id": "graphics@selection#lightning",
    "category": "Block inserts (6)",
    "action": "graphics",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "SelectionRef -> 'ok'. Non-empty selection REPLACED by the block.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1968 (graphicsRun); GRAPHICS_ACTION_ROW:2078; smart-insert smart-insert.ts:119; attrs figure-attrs.ts:94",
    "backbone": "",
    "expected": {
      "behavior": "INSERT an opaque graphicsBlock atom at the caret (replacing any non-empty selection per smartInsertBlock's REPLACE policy) with a freshGraphicsBlockAttrs stub (command = '\\includegraphics[width=0.5\\textwidth]{}', source '', widthPercent:50, collision-free uuid) and NO content child. Then ONE rAF after insert, open the SOURCE popover anchored to the block via ctx.openFigurePopover(seed) (raw = attrs.command). No-op when ctx.canEdit===false.",
      "docDelta": "One graphicsBlock added via smartInsertBlock, no content. uuid minted collision-free. Position relocated post-dispatch by uuid for the popover anchor.",
      "texDelta": "Verbatim \\includegraphics[width=0.5\\textwidth]{} (standalone, serializeGraphicsBlock emits attrs.command verbatim; \\vxid{<uuid>} marker). Empty path until the user fills the popover.",
      "sidecarDelta": "None - graphicsBlock is a pure in-doc node (attrs-only), no JSON sidecar.",
      "lifecycle": "No card produced. Source popover opens (one rAF later) seeded with attrs.command."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today. graphicsRun is the SSOT the standalone insertGraphicsBlock helper and any future FILE-DROP path delegate to (DA-2): all must produce the byte-identical smartInsertBlock result.",
    "knownRisk": "Same rAF-pause caveat as figure (shim rAF->setTimeout in the backgrounded preview). Note the self-kind 'disabled' (graphicsBlock x graphics) is correct-by-design but is the kind of cell to spot-check it isn't accidentally 'absent'/missing in the grid. REPLACE-on-selection: confirm a non-empty selection is intentionally consumed."
  },
  {
    "id": "graphics@selection:collab-readonly#lightning",
    "category": "Block inserts (6)",
    "action": "graphics",
    "kind": "selection:collab-readonly",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "ctx.canEdit===false -> 'disabled' via gateApplies.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1968 (graphicsRun); GRAPHICS_ACTION_ROW:2078; smart-insert smart-insert.ts:119; attrs figure-attrs.ts:94",
    "backbone": "",
    "expected": {
      "behavior": "INSERT an opaque graphicsBlock atom at the caret (replacing any non-empty selection per smartInsertBlock's REPLACE policy) with a freshGraphicsBlockAttrs stub (command = '\\includegraphics[width=0.5\\textwidth]{}', source '', widthPercent:50, collision-free uuid) and NO content child. Then ONE rAF after insert, open the SOURCE popover anchored to the block via ctx.openFigurePopover(seed) (raw = attrs.command). No-op when ctx.canEdit===false.",
      "docDelta": "One graphicsBlock added via smartInsertBlock, no content. uuid minted collision-free. Position relocated post-dispatch by uuid for the popover anchor.",
      "texDelta": "Verbatim \\includegraphics[width=0.5\\textwidth]{} (standalone, serializeGraphicsBlock emits attrs.command verbatim; \\vxid{<uuid>} marker). Empty path until the user fills the popover.",
      "sidecarDelta": "None - graphicsBlock is a pure in-doc node (attrs-only), no JSON sidecar.",
      "lifecycle": "No card produced. Source popover opens (one rAF later) seeded with attrs.command."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today. graphicsRun is the SSOT the standalone insertGraphicsBlock helper and any future FILE-DROP path delegate to (DA-2): all must produce the byte-identical smartInsertBlock result.",
    "knownRisk": "Same rAF-pause caveat as figure (shim rAF->setTimeout in the backgrounded preview). Note the self-kind 'disabled' (graphicsBlock x graphics) is correct-by-design but is the kind of cell to spot-check it isn't accidentally 'absent'/missing in the grid. REPLACE-on-selection: confirm a non-empty selection is intentionally consumed."
  },
  {
    "id": "graphics@texBlock#lightning",
    "category": "Block inserts (6)",
    "action": "graphics",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1968 (graphicsRun); GRAPHICS_ACTION_ROW:2078; smart-insert smart-insert.ts:119; attrs figure-attrs.ts:94",
    "backbone": "",
    "expected": {
      "behavior": "INSERT an opaque graphicsBlock atom at the caret (replacing any non-empty selection per smartInsertBlock's REPLACE policy) with a freshGraphicsBlockAttrs stub (command = '\\includegraphics[width=0.5\\textwidth]{}', source '', widthPercent:50, collision-free uuid) and NO content child. Then ONE rAF after insert, open the SOURCE popover anchored to the block via ctx.openFigurePopover(seed) (raw = attrs.command). No-op when ctx.canEdit===false.",
      "docDelta": "One graphicsBlock added via smartInsertBlock, no content. uuid minted collision-free. Position relocated post-dispatch by uuid for the popover anchor.",
      "texDelta": "Verbatim \\includegraphics[width=0.5\\textwidth]{} (standalone, serializeGraphicsBlock emits attrs.command verbatim; \\vxid{<uuid>} marker). Empty path until the user fills the popover.",
      "sidecarDelta": "None - graphicsBlock is a pure in-doc node (attrs-only), no JSON sidecar.",
      "lifecycle": "No card produced. Source popover opens (one rAF later) seeded with attrs.command."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today. graphicsRun is the SSOT the standalone insertGraphicsBlock helper and any future FILE-DROP path delegate to (DA-2): all must produce the byte-identical smartInsertBlock result.",
    "knownRisk": "Same rAF-pause caveat as figure (shim rAF->setTimeout in the backgrounded preview). Note the self-kind 'disabled' (graphicsBlock x graphics) is correct-by-design but is the kind of cell to spot-check it isn't accidentally 'absent'/missing in the grid. REPLACE-on-selection: confirm a non-empty selection is intentionally consumed."
  },
  {
    "id": "inline-math@cursor#lightning",
    "category": "Block inserts (6)",
    "action": "inline-math",
    "kind": "cursor",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "Collapsed caret -> 'ok'. Insert placeholder 'x' inlineMath.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1877 (mathRun('inline')); INLINE_MATH_ACTION_ROW:2051; grid call ActionsMenuPanel:511 (runGridAction('inline-math'))",
    "backbone": "",
    "expected": {
      "behavior": "WRAP the selection: latex = textBetween(from,to,' '), or placeholder 'x' when empty. deleteSelection() then insertContent({type:'inlineMath', attrs:{latex}}) via editor.chain().focus(). NOTE 'inline-math' is category:'block' in the registry but produces an INLINE atom (inlineMath, NO uuid attr). DATA-LOSS GUARD: a non-empty selection holding an inline atom but no text BAILS so the atom isn't destroyed. No-op when ctx.canEdit===false.",
      "docDelta": "One inlineMath inline atom inserted (attrs:{latex}, NO uuid). On a text-bearing selection the range is deleted first.",
      "texDelta": "$<latex>$ inline. Empty selection -> $x$.",
      "sidecarDelta": "None - inlineMath is a pure in-doc inline atom, no JSON sidecar.",
      "lifecycle": "No card produced. Caret after the inserted inline atom (insertContent default)."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only). No cross-surface identity to enforce today. (mathRun preserves the former grid wrapSelectionInMath semantics verbatim - a temporal/refactor identity, not multi-surface.)",
    "knownRisk": "Drive the atom-only-selection data-loss guard live FIRST (mathRun:1889): selecting a lone citation/\\ref/$math$ and hitting inline-math must BAIL, NOT replace the atom with $x$. Same guard texRun has and example INTENTIONALLY lacks - confirm the divergence is correct. Lower priority: 'inline-math' is mislabeled category:'block' though it yields an inline atom; note it for the oracle's category-consistency check."
  },
  {
    "id": "inline-math@displayMath#lightning",
    "category": "Block inserts (6)",
    "action": "inline-math",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled' via blockApplies.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1877 (mathRun('inline')); INLINE_MATH_ACTION_ROW:2051; grid call ActionsMenuPanel:511 (runGridAction('inline-math'))",
    "backbone": "",
    "expected": {
      "behavior": "WRAP the selection: latex = textBetween(from,to,' '), or placeholder 'x' when empty. deleteSelection() then insertContent({type:'inlineMath', attrs:{latex}}) via editor.chain().focus(). NOTE 'inline-math' is category:'block' in the registry but produces an INLINE atom (inlineMath, NO uuid attr). DATA-LOSS GUARD: a non-empty selection holding an inline atom but no text BAILS so the atom isn't destroyed. No-op when ctx.canEdit===false.",
      "docDelta": "One inlineMath inline atom inserted (attrs:{latex}, NO uuid). On a text-bearing selection the range is deleted first.",
      "texDelta": "$<latex>$ inline. Empty selection -> $x$.",
      "sidecarDelta": "None - inlineMath is a pure in-doc inline atom, no JSON sidecar.",
      "lifecycle": "No card produced. Caret after the inserted inline atom (insertContent default)."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only). No cross-surface identity to enforce today. (mathRun preserves the former grid wrapSelectionInMath semantics verbatim - a temporal/refactor identity, not multi-surface.)",
    "knownRisk": "Drive the atom-only-selection data-loss guard live FIRST (mathRun:1889): selecting a lone citation/\\ref/$math$ and hitting inline-math must BAIL, NOT replace the atom with $x$. Same guard texRun has and example INTENTIONALLY lacks - confirm the divergence is correct. Lower priority: 'inline-math' is mislabeled category:'block' though it yields an inline atom; note it for the oracle's category-consistency check."
  },
  {
    "id": "inline-math@graphicsBlock#lightning",
    "category": "Block inserts (6)",
    "action": "inline-math",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1877 (mathRun('inline')); INLINE_MATH_ACTION_ROW:2051; grid call ActionsMenuPanel:511 (runGridAction('inline-math'))",
    "backbone": "",
    "expected": {
      "behavior": "WRAP the selection: latex = textBetween(from,to,' '), or placeholder 'x' when empty. deleteSelection() then insertContent({type:'inlineMath', attrs:{latex}}) via editor.chain().focus(). NOTE 'inline-math' is category:'block' in the registry but produces an INLINE atom (inlineMath, NO uuid attr). DATA-LOSS GUARD: a non-empty selection holding an inline atom but no text BAILS so the atom isn't destroyed. No-op when ctx.canEdit===false.",
      "docDelta": "One inlineMath inline atom inserted (attrs:{latex}, NO uuid). On a text-bearing selection the range is deleted first.",
      "texDelta": "$<latex>$ inline. Empty selection -> $x$.",
      "sidecarDelta": "None - inlineMath is a pure in-doc inline atom, no JSON sidecar.",
      "lifecycle": "No card produced. Caret after the inserted inline atom (insertContent default)."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only). No cross-surface identity to enforce today. (mathRun preserves the former grid wrapSelectionInMath semantics verbatim - a temporal/refactor identity, not multi-surface.)",
    "knownRisk": "Drive the atom-only-selection data-loss guard live FIRST (mathRun:1889): selecting a lone citation/\\ref/$math$ and hitting inline-math must BAIL, NOT replace the atom with $x$. Same guard texRun has and example INTENTIONALLY lacks - confirm the divergence is correct. Lower priority: 'inline-math' is mislabeled category:'block' though it yields an inline atom; note it for the oracle's category-consistency check."
  },
  {
    "id": "inline-math@latexComment#lightning",
    "category": "Block inserts (6)",
    "action": "inline-math",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1877 (mathRun('inline')); INLINE_MATH_ACTION_ROW:2051; grid call ActionsMenuPanel:511 (runGridAction('inline-math'))",
    "backbone": "",
    "expected": {
      "behavior": "WRAP the selection: latex = textBetween(from,to,' '), or placeholder 'x' when empty. deleteSelection() then insertContent({type:'inlineMath', attrs:{latex}}) via editor.chain().focus(). NOTE 'inline-math' is category:'block' in the registry but produces an INLINE atom (inlineMath, NO uuid attr). DATA-LOSS GUARD: a non-empty selection holding an inline atom but no text BAILS so the atom isn't destroyed. No-op when ctx.canEdit===false.",
      "docDelta": "One inlineMath inline atom inserted (attrs:{latex}, NO uuid). On a text-bearing selection the range is deleted first.",
      "texDelta": "$<latex>$ inline. Empty selection -> $x$.",
      "sidecarDelta": "None - inlineMath is a pure in-doc inline atom, no JSON sidecar.",
      "lifecycle": "No card produced. Caret after the inserted inline atom (insertContent default)."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only). No cross-surface identity to enforce today. (mathRun preserves the former grid wrapSelectionInMath semantics verbatim - a temporal/refactor identity, not multi-surface.)",
    "knownRisk": "Drive the atom-only-selection data-loss guard live FIRST (mathRun:1889): selecting a lone citation/\\ref/$math$ and hitting inline-math must BAIL, NOT replace the atom with $x$. Same guard texRun has and example INTENTIONALLY lacks - confirm the divergence is correct. Lower priority: 'inline-math' is mislabeled category:'block' though it yields an inline atom; note it for the oracle's category-consistency check."
  },
  {
    "id": "inline-math@paragraph#lightning",
    "category": "Block inserts (6)",
    "action": "inline-math",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1877 (mathRun('inline')); INLINE_MATH_ACTION_ROW:2051; grid call ActionsMenuPanel:511 (runGridAction('inline-math'))",
    "backbone": "",
    "expected": {
      "behavior": "WRAP the selection: latex = textBetween(from,to,' '), or placeholder 'x' when empty. deleteSelection() then insertContent({type:'inlineMath', attrs:{latex}}) via editor.chain().focus(). NOTE 'inline-math' is category:'block' in the registry but produces an INLINE atom (inlineMath, NO uuid attr). DATA-LOSS GUARD: a non-empty selection holding an inline atom but no text BAILS so the atom isn't destroyed. No-op when ctx.canEdit===false.",
      "docDelta": "One inlineMath inline atom inserted (attrs:{latex}, NO uuid). On a text-bearing selection the range is deleted first.",
      "texDelta": "$<latex>$ inline. Empty selection -> $x$.",
      "sidecarDelta": "None - inlineMath is a pure in-doc inline atom, no JSON sidecar.",
      "lifecycle": "No card produced. Caret after the inserted inline atom (insertContent default)."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only). No cross-surface identity to enforce today. (mathRun preserves the former grid wrapSelectionInMath semantics verbatim - a temporal/refactor identity, not multi-surface.)",
    "knownRisk": "Drive the atom-only-selection data-loss guard live FIRST (mathRun:1889): selecting a lone citation/\\ref/$math$ and hitting inline-math must BAIL, NOT replace the atom with $x$. Same guard texRun has and example INTENTIONALLY lacks - confirm the divergence is correct. Lower priority: 'inline-math' is mislabeled category:'block' though it yields an inline atom; note it for the oracle's category-consistency check."
  },
  {
    "id": "inline-math@selection#lightning",
    "category": "Block inserts (6)",
    "action": "inline-math",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "SelectionRef -> blockApplies base 'ok'. WRAP the selected text into the atom's latex.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1877 (mathRun('inline')); INLINE_MATH_ACTION_ROW:2051; grid call ActionsMenuPanel:511 (runGridAction('inline-math'))",
    "backbone": "",
    "expected": {
      "behavior": "WRAP the selection: latex = textBetween(from,to,' '), or placeholder 'x' when empty. deleteSelection() then insertContent({type:'inlineMath', attrs:{latex}}) via editor.chain().focus(). NOTE 'inline-math' is category:'block' in the registry but produces an INLINE atom (inlineMath, NO uuid attr). DATA-LOSS GUARD: a non-empty selection holding an inline atom but no text BAILS so the atom isn't destroyed. No-op when ctx.canEdit===false.",
      "docDelta": "One inlineMath inline atom inserted (attrs:{latex}, NO uuid). On a text-bearing selection the range is deleted first.",
      "texDelta": "$<latex>$ inline. Empty selection -> $x$.",
      "sidecarDelta": "None - inlineMath is a pure in-doc inline atom, no JSON sidecar.",
      "lifecycle": "No card produced. Caret after the inserted inline atom (insertContent default)."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only). No cross-surface identity to enforce today. (mathRun preserves the former grid wrapSelectionInMath semantics verbatim - a temporal/refactor identity, not multi-surface.)",
    "knownRisk": "Drive the atom-only-selection data-loss guard live FIRST (mathRun:1889): selecting a lone citation/\\ref/$math$ and hitting inline-math must BAIL, NOT replace the atom with $x$. Same guard texRun has and example INTENTIONALLY lacks - confirm the divergence is correct. Lower priority: 'inline-math' is mislabeled category:'block' though it yields an inline atom; note it for the oracle's category-consistency check."
  },
  {
    "id": "inline-math@selection:collab-readonly#lightning",
    "category": "Block inserts (6)",
    "action": "inline-math",
    "kind": "selection:collab-readonly",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "ctx.canEdit===false -> 'disabled' via gateApplies (grid threads canEdit through runGridAction).",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1877 (mathRun('inline')); INLINE_MATH_ACTION_ROW:2051; grid call ActionsMenuPanel:511 (runGridAction('inline-math'))",
    "backbone": "",
    "expected": {
      "behavior": "WRAP the selection: latex = textBetween(from,to,' '), or placeholder 'x' when empty. deleteSelection() then insertContent({type:'inlineMath', attrs:{latex}}) via editor.chain().focus(). NOTE 'inline-math' is category:'block' in the registry but produces an INLINE atom (inlineMath, NO uuid attr). DATA-LOSS GUARD: a non-empty selection holding an inline atom but no text BAILS so the atom isn't destroyed. No-op when ctx.canEdit===false.",
      "docDelta": "One inlineMath inline atom inserted (attrs:{latex}, NO uuid). On a text-bearing selection the range is deleted first.",
      "texDelta": "$<latex>$ inline. Empty selection -> $x$.",
      "sidecarDelta": "None - inlineMath is a pure in-doc inline atom, no JSON sidecar.",
      "lifecycle": "No card produced. Caret after the inserted inline atom (insertContent default)."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only). No cross-surface identity to enforce today. (mathRun preserves the former grid wrapSelectionInMath semantics verbatim - a temporal/refactor identity, not multi-surface.)",
    "knownRisk": "Drive the atom-only-selection data-loss guard live FIRST (mathRun:1889): selecting a lone citation/\\ref/$math$ and hitting inline-math must BAIL, NOT replace the atom with $x$. Same guard texRun has and example INTENTIONALLY lacks - confirm the divergence is correct. Lower priority: 'inline-math' is mislabeled category:'block' though it yields an inline atom; note it for the oracle's category-consistency check."
  },
  {
    "id": "inline-math@texBlock#lightning",
    "category": "Block inserts (6)",
    "action": "inline-math",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1877 (mathRun('inline')); INLINE_MATH_ACTION_ROW:2051; grid call ActionsMenuPanel:511 (runGridAction('inline-math'))",
    "backbone": "",
    "expected": {
      "behavior": "WRAP the selection: latex = textBetween(from,to,' '), or placeholder 'x' when empty. deleteSelection() then insertContent({type:'inlineMath', attrs:{latex}}) via editor.chain().focus(). NOTE 'inline-math' is category:'block' in the registry but produces an INLINE atom (inlineMath, NO uuid attr). DATA-LOSS GUARD: a non-empty selection holding an inline atom but no text BAILS so the atom isn't destroyed. No-op when ctx.canEdit===false.",
      "docDelta": "One inlineMath inline atom inserted (attrs:{latex}, NO uuid). On a text-bearing selection the range is deleted first.",
      "texDelta": "$<latex>$ inline. Empty selection -> $x$.",
      "sidecarDelta": "None - inlineMath is a pure in-doc inline atom, no JSON sidecar.",
      "lifecycle": "No card produced. Caret after the inserted inline atom (insertContent default)."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only). No cross-surface identity to enforce today. (mathRun preserves the former grid wrapSelectionInMath semantics verbatim - a temporal/refactor identity, not multi-surface.)",
    "knownRisk": "Drive the atom-only-selection data-loss guard live FIRST (mathRun:1889): selecting a lone citation/\\ref/$math$ and hitting inline-math must BAIL, NOT replace the atom with $x$. Same guard texRun has and example INTENTIONALLY lacks - confirm the divergence is correct. Lower priority: 'inline-math' is mislabeled category:'block' though it yields an inline atom; note it for the oracle's category-consistency check."
  },
  {
    "id": "tex@cursor#lightning",
    "category": "Block inserts (6)",
    "action": "tex",
    "kind": "cursor",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "CursorRef (slash caret) -> 'ok'. Insert empty texBlock at caret.",
    "slashName": "tex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1441 (texRun); TEX_ACTION_ROW:1490; lightning delegate tex-block.ts:125 (insertTexBlock->texRun); slash commands.ts:225 (runViewOnlyAction('tex'))",
    "backbone": "",
    "expected": {
      "behavior": "Seed-from-selection. Non-empty selection -> seedCode = textBetween(from,to,'\\n', hardBreak->'\\n'), deleteSelection() then replaceSelectionWith a texBlock carrying that code. Collapsed caret -> empty texBlock (code:''). DATA-LOSS GUARD: a non-empty selection that yields seedCode==='' but a non-empty slice (an inline atom selected alone) BAILS - the atom is preserved. scrollIntoView. No-op when read-only.",
      "docDelta": "One texBlock added (attrs: uuid via the single collision-free scan, code = seedCode | ''). On a text-bearing selection the range is deleted first.",
      "texDelta": "%!vtex:begin <uuid>\\n<escaped code>\\n%!vtex:end <uuid>\\n\\n (serializeTexBlock; raw passthrough between sentinels). \\vtex sentinels are comments, no command needed.",
      "sidecarDelta": "None - texBlock is a pure in-doc node, no JSON sidecar.",
      "lifecycle": "No card produced. Caret left at the inserted block. No panel hop on either surface."
    },
    "crossSurfaceIdentity": "Both surfaces route through texRun: same seedCode harvest, same ONE uuid-collision scan, same deleteSelection->replaceSelectionWith, same data-loss guard, same %!vtex sentinel round-trip. Slash via runViewOnlyAction (CursorRef ctx); lightning via insertTexBlock's hand-rolled SelectionRef ctx - both feed the SAME texRun which reads ctx.view.state.selection directly.",
    "knownRisk": "Drive the lightning tex cell live FIRST: it is wired as onClick={() => insertTexBlock(editor)} (ActionsMenuPanel:569), NOT runGridAction('tex') - it bypasses the unified grid ctx (no canEdit thread). insertTexBlock builds a minimal ctx WITHOUT canEdit, so texRun's isCollabReadOnly returns false - the collab read-only gate is effectively INERT on the grid tex cell. In a collab read-only session the grid tex cell would attempt the insert (relying on readOnlyEnforcer to reject) where slash explicitly no-ops at view.editable. Verify the grid tex cell is greyed/blocked under collab read-only. Also verify the atom-only-selection data-loss guard on BOTH surfaces."
  },
  {
    "id": "tex@cursor#slash",
    "category": "Block inserts (6)",
    "action": "tex",
    "kind": "cursor",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "CursorRef (slash caret) -> 'ok'. Insert empty texBlock at caret.",
    "slashName": "tex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1441 (texRun); TEX_ACTION_ROW:1490; lightning delegate tex-block.ts:125 (insertTexBlock->texRun); slash commands.ts:225 (runViewOnlyAction('tex'))",
    "backbone": "",
    "expected": {
      "behavior": "Seed-from-selection. Non-empty selection -> seedCode = textBetween(from,to,'\\n', hardBreak->'\\n'), deleteSelection() then replaceSelectionWith a texBlock carrying that code. Collapsed caret -> empty texBlock (code:''). DATA-LOSS GUARD: a non-empty selection that yields seedCode==='' but a non-empty slice (an inline atom selected alone) BAILS - the atom is preserved. scrollIntoView. No-op when read-only.",
      "docDelta": "One texBlock added (attrs: uuid via the single collision-free scan, code = seedCode | ''). On a text-bearing selection the range is deleted first.",
      "texDelta": "%!vtex:begin <uuid>\\n<escaped code>\\n%!vtex:end <uuid>\\n\\n (serializeTexBlock; raw passthrough between sentinels). \\vtex sentinels are comments, no command needed.",
      "sidecarDelta": "None - texBlock is a pure in-doc node, no JSON sidecar.",
      "lifecycle": "No card produced. Caret left at the inserted block. No panel hop on either surface."
    },
    "crossSurfaceIdentity": "Both surfaces route through texRun: same seedCode harvest, same ONE uuid-collision scan, same deleteSelection->replaceSelectionWith, same data-loss guard, same %!vtex sentinel round-trip. Slash via runViewOnlyAction (CursorRef ctx); lightning via insertTexBlock's hand-rolled SelectionRef ctx - both feed the SAME texRun which reads ctx.view.state.selection directly.",
    "knownRisk": "Drive the lightning tex cell live FIRST: it is wired as onClick={() => insertTexBlock(editor)} (ActionsMenuPanel:569), NOT runGridAction('tex') - it bypasses the unified grid ctx (no canEdit thread). insertTexBlock builds a minimal ctx WITHOUT canEdit, so texRun's isCollabReadOnly returns false - the collab read-only gate is effectively INERT on the grid tex cell. In a collab read-only session the grid tex cell would attempt the insert (relying on readOnlyEnforcer to reject) where slash explicitly no-ops at view.editable. Verify the grid tex cell is greyed/blocked under collab read-only. Also verify the atom-only-selection data-loss guard on BOTH surfaces."
  },
  {
    "id": "tex@displayMath#lightning",
    "category": "Block inserts (6)",
    "action": "tex",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled' via blockApplies.",
    "slashName": "tex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1441 (texRun); TEX_ACTION_ROW:1490; lightning delegate tex-block.ts:125 (insertTexBlock->texRun); slash commands.ts:225 (runViewOnlyAction('tex'))",
    "backbone": "",
    "expected": {
      "behavior": "Seed-from-selection. Non-empty selection -> seedCode = textBetween(from,to,'\\n', hardBreak->'\\n'), deleteSelection() then replaceSelectionWith a texBlock carrying that code. Collapsed caret -> empty texBlock (code:''). DATA-LOSS GUARD: a non-empty selection that yields seedCode==='' but a non-empty slice (an inline atom selected alone) BAILS - the atom is preserved. scrollIntoView. No-op when read-only.",
      "docDelta": "One texBlock added (attrs: uuid via the single collision-free scan, code = seedCode | ''). On a text-bearing selection the range is deleted first.",
      "texDelta": "%!vtex:begin <uuid>\\n<escaped code>\\n%!vtex:end <uuid>\\n\\n (serializeTexBlock; raw passthrough between sentinels). \\vtex sentinels are comments, no command needed.",
      "sidecarDelta": "None - texBlock is a pure in-doc node, no JSON sidecar.",
      "lifecycle": "No card produced. Caret left at the inserted block. No panel hop on either surface."
    },
    "crossSurfaceIdentity": "Both surfaces route through texRun: same seedCode harvest, same ONE uuid-collision scan, same deleteSelection->replaceSelectionWith, same data-loss guard, same %!vtex sentinel round-trip. Slash via runViewOnlyAction (CursorRef ctx); lightning via insertTexBlock's hand-rolled SelectionRef ctx - both feed the SAME texRun which reads ctx.view.state.selection directly.",
    "knownRisk": "Drive the lightning tex cell live FIRST: it is wired as onClick={() => insertTexBlock(editor)} (ActionsMenuPanel:569), NOT runGridAction('tex') - it bypasses the unified grid ctx (no canEdit thread). insertTexBlock builds a minimal ctx WITHOUT canEdit, so texRun's isCollabReadOnly returns false - the collab read-only gate is effectively INERT on the grid tex cell. In a collab read-only session the grid tex cell would attempt the insert (relying on readOnlyEnforcer to reject) where slash explicitly no-ops at view.editable. Verify the grid tex cell is greyed/blocked under collab read-only. Also verify the atom-only-selection data-loss guard on BOTH surfaces."
  },
  {
    "id": "tex@displayMath#slash",
    "category": "Block inserts (6)",
    "action": "tex",
    "kind": "displayMath",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled' via blockApplies.",
    "slashName": "tex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1441 (texRun); TEX_ACTION_ROW:1490; lightning delegate tex-block.ts:125 (insertTexBlock->texRun); slash commands.ts:225 (runViewOnlyAction('tex'))",
    "backbone": "",
    "expected": {
      "behavior": "Seed-from-selection. Non-empty selection -> seedCode = textBetween(from,to,'\\n', hardBreak->'\\n'), deleteSelection() then replaceSelectionWith a texBlock carrying that code. Collapsed caret -> empty texBlock (code:''). DATA-LOSS GUARD: a non-empty selection that yields seedCode==='' but a non-empty slice (an inline atom selected alone) BAILS - the atom is preserved. scrollIntoView. No-op when read-only.",
      "docDelta": "One texBlock added (attrs: uuid via the single collision-free scan, code = seedCode | ''). On a text-bearing selection the range is deleted first.",
      "texDelta": "%!vtex:begin <uuid>\\n<escaped code>\\n%!vtex:end <uuid>\\n\\n (serializeTexBlock; raw passthrough between sentinels). \\vtex sentinels are comments, no command needed.",
      "sidecarDelta": "None - texBlock is a pure in-doc node, no JSON sidecar.",
      "lifecycle": "No card produced. Caret left at the inserted block. No panel hop on either surface."
    },
    "crossSurfaceIdentity": "Both surfaces route through texRun: same seedCode harvest, same ONE uuid-collision scan, same deleteSelection->replaceSelectionWith, same data-loss guard, same %!vtex sentinel round-trip. Slash via runViewOnlyAction (CursorRef ctx); lightning via insertTexBlock's hand-rolled SelectionRef ctx - both feed the SAME texRun which reads ctx.view.state.selection directly.",
    "knownRisk": "Drive the lightning tex cell live FIRST: it is wired as onClick={() => insertTexBlock(editor)} (ActionsMenuPanel:569), NOT runGridAction('tex') - it bypasses the unified grid ctx (no canEdit thread). insertTexBlock builds a minimal ctx WITHOUT canEdit, so texRun's isCollabReadOnly returns false - the collab read-only gate is effectively INERT on the grid tex cell. In a collab read-only session the grid tex cell would attempt the insert (relying on readOnlyEnforcer to reject) where slash explicitly no-ops at view.editable. Verify the grid tex cell is greyed/blocked under collab read-only. Also verify the atom-only-selection data-loss guard on BOTH surfaces."
  },
  {
    "id": "tex@graphicsBlock#lightning",
    "category": "Block inserts (6)",
    "action": "tex",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "tex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1441 (texRun); TEX_ACTION_ROW:1490; lightning delegate tex-block.ts:125 (insertTexBlock->texRun); slash commands.ts:225 (runViewOnlyAction('tex'))",
    "backbone": "",
    "expected": {
      "behavior": "Seed-from-selection. Non-empty selection -> seedCode = textBetween(from,to,'\\n', hardBreak->'\\n'), deleteSelection() then replaceSelectionWith a texBlock carrying that code. Collapsed caret -> empty texBlock (code:''). DATA-LOSS GUARD: a non-empty selection that yields seedCode==='' but a non-empty slice (an inline atom selected alone) BAILS - the atom is preserved. scrollIntoView. No-op when read-only.",
      "docDelta": "One texBlock added (attrs: uuid via the single collision-free scan, code = seedCode | ''). On a text-bearing selection the range is deleted first.",
      "texDelta": "%!vtex:begin <uuid>\\n<escaped code>\\n%!vtex:end <uuid>\\n\\n (serializeTexBlock; raw passthrough between sentinels). \\vtex sentinels are comments, no command needed.",
      "sidecarDelta": "None - texBlock is a pure in-doc node, no JSON sidecar.",
      "lifecycle": "No card produced. Caret left at the inserted block. No panel hop on either surface."
    },
    "crossSurfaceIdentity": "Both surfaces route through texRun: same seedCode harvest, same ONE uuid-collision scan, same deleteSelection->replaceSelectionWith, same data-loss guard, same %!vtex sentinel round-trip. Slash via runViewOnlyAction (CursorRef ctx); lightning via insertTexBlock's hand-rolled SelectionRef ctx - both feed the SAME texRun which reads ctx.view.state.selection directly.",
    "knownRisk": "Drive the lightning tex cell live FIRST: it is wired as onClick={() => insertTexBlock(editor)} (ActionsMenuPanel:569), NOT runGridAction('tex') - it bypasses the unified grid ctx (no canEdit thread). insertTexBlock builds a minimal ctx WITHOUT canEdit, so texRun's isCollabReadOnly returns false - the collab read-only gate is effectively INERT on the grid tex cell. In a collab read-only session the grid tex cell would attempt the insert (relying on readOnlyEnforcer to reject) where slash explicitly no-ops at view.editable. Verify the grid tex cell is greyed/blocked under collab read-only. Also verify the atom-only-selection data-loss guard on BOTH surfaces."
  },
  {
    "id": "tex@graphicsBlock#slash",
    "category": "Block inserts (6)",
    "action": "tex",
    "kind": "graphicsBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "tex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1441 (texRun); TEX_ACTION_ROW:1490; lightning delegate tex-block.ts:125 (insertTexBlock->texRun); slash commands.ts:225 (runViewOnlyAction('tex'))",
    "backbone": "",
    "expected": {
      "behavior": "Seed-from-selection. Non-empty selection -> seedCode = textBetween(from,to,'\\n', hardBreak->'\\n'), deleteSelection() then replaceSelectionWith a texBlock carrying that code. Collapsed caret -> empty texBlock (code:''). DATA-LOSS GUARD: a non-empty selection that yields seedCode==='' but a non-empty slice (an inline atom selected alone) BAILS - the atom is preserved. scrollIntoView. No-op when read-only.",
      "docDelta": "One texBlock added (attrs: uuid via the single collision-free scan, code = seedCode | ''). On a text-bearing selection the range is deleted first.",
      "texDelta": "%!vtex:begin <uuid>\\n<escaped code>\\n%!vtex:end <uuid>\\n\\n (serializeTexBlock; raw passthrough between sentinels). \\vtex sentinels are comments, no command needed.",
      "sidecarDelta": "None - texBlock is a pure in-doc node, no JSON sidecar.",
      "lifecycle": "No card produced. Caret left at the inserted block. No panel hop on either surface."
    },
    "crossSurfaceIdentity": "Both surfaces route through texRun: same seedCode harvest, same ONE uuid-collision scan, same deleteSelection->replaceSelectionWith, same data-loss guard, same %!vtex sentinel round-trip. Slash via runViewOnlyAction (CursorRef ctx); lightning via insertTexBlock's hand-rolled SelectionRef ctx - both feed the SAME texRun which reads ctx.view.state.selection directly.",
    "knownRisk": "Drive the lightning tex cell live FIRST: it is wired as onClick={() => insertTexBlock(editor)} (ActionsMenuPanel:569), NOT runGridAction('tex') - it bypasses the unified grid ctx (no canEdit thread). insertTexBlock builds a minimal ctx WITHOUT canEdit, so texRun's isCollabReadOnly returns false - the collab read-only gate is effectively INERT on the grid tex cell. In a collab read-only session the grid tex cell would attempt the insert (relying on readOnlyEnforcer to reject) where slash explicitly no-ops at view.editable. Verify the grid tex cell is greyed/blocked under collab read-only. Also verify the atom-only-selection data-loss guard on BOTH surfaces."
  },
  {
    "id": "tex@latexComment#lightning",
    "category": "Block inserts (6)",
    "action": "tex",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "tex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1441 (texRun); TEX_ACTION_ROW:1490; lightning delegate tex-block.ts:125 (insertTexBlock->texRun); slash commands.ts:225 (runViewOnlyAction('tex'))",
    "backbone": "",
    "expected": {
      "behavior": "Seed-from-selection. Non-empty selection -> seedCode = textBetween(from,to,'\\n', hardBreak->'\\n'), deleteSelection() then replaceSelectionWith a texBlock carrying that code. Collapsed caret -> empty texBlock (code:''). DATA-LOSS GUARD: a non-empty selection that yields seedCode==='' but a non-empty slice (an inline atom selected alone) BAILS - the atom is preserved. scrollIntoView. No-op when read-only.",
      "docDelta": "One texBlock added (attrs: uuid via the single collision-free scan, code = seedCode | ''). On a text-bearing selection the range is deleted first.",
      "texDelta": "%!vtex:begin <uuid>\\n<escaped code>\\n%!vtex:end <uuid>\\n\\n (serializeTexBlock; raw passthrough between sentinels). \\vtex sentinels are comments, no command needed.",
      "sidecarDelta": "None - texBlock is a pure in-doc node, no JSON sidecar.",
      "lifecycle": "No card produced. Caret left at the inserted block. No panel hop on either surface."
    },
    "crossSurfaceIdentity": "Both surfaces route through texRun: same seedCode harvest, same ONE uuid-collision scan, same deleteSelection->replaceSelectionWith, same data-loss guard, same %!vtex sentinel round-trip. Slash via runViewOnlyAction (CursorRef ctx); lightning via insertTexBlock's hand-rolled SelectionRef ctx - both feed the SAME texRun which reads ctx.view.state.selection directly.",
    "knownRisk": "Drive the lightning tex cell live FIRST: it is wired as onClick={() => insertTexBlock(editor)} (ActionsMenuPanel:569), NOT runGridAction('tex') - it bypasses the unified grid ctx (no canEdit thread). insertTexBlock builds a minimal ctx WITHOUT canEdit, so texRun's isCollabReadOnly returns false - the collab read-only gate is effectively INERT on the grid tex cell. In a collab read-only session the grid tex cell would attempt the insert (relying on readOnlyEnforcer to reject) where slash explicitly no-ops at view.editable. Verify the grid tex cell is greyed/blocked under collab read-only. Also verify the atom-only-selection data-loss guard on BOTH surfaces."
  },
  {
    "id": "tex@latexComment#slash",
    "category": "Block inserts (6)",
    "action": "tex",
    "kind": "latexComment",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "tex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1441 (texRun); TEX_ACTION_ROW:1490; lightning delegate tex-block.ts:125 (insertTexBlock->texRun); slash commands.ts:225 (runViewOnlyAction('tex'))",
    "backbone": "",
    "expected": {
      "behavior": "Seed-from-selection. Non-empty selection -> seedCode = textBetween(from,to,'\\n', hardBreak->'\\n'), deleteSelection() then replaceSelectionWith a texBlock carrying that code. Collapsed caret -> empty texBlock (code:''). DATA-LOSS GUARD: a non-empty selection that yields seedCode==='' but a non-empty slice (an inline atom selected alone) BAILS - the atom is preserved. scrollIntoView. No-op when read-only.",
      "docDelta": "One texBlock added (attrs: uuid via the single collision-free scan, code = seedCode | ''). On a text-bearing selection the range is deleted first.",
      "texDelta": "%!vtex:begin <uuid>\\n<escaped code>\\n%!vtex:end <uuid>\\n\\n (serializeTexBlock; raw passthrough between sentinels). \\vtex sentinels are comments, no command needed.",
      "sidecarDelta": "None - texBlock is a pure in-doc node, no JSON sidecar.",
      "lifecycle": "No card produced. Caret left at the inserted block. No panel hop on either surface."
    },
    "crossSurfaceIdentity": "Both surfaces route through texRun: same seedCode harvest, same ONE uuid-collision scan, same deleteSelection->replaceSelectionWith, same data-loss guard, same %!vtex sentinel round-trip. Slash via runViewOnlyAction (CursorRef ctx); lightning via insertTexBlock's hand-rolled SelectionRef ctx - both feed the SAME texRun which reads ctx.view.state.selection directly.",
    "knownRisk": "Drive the lightning tex cell live FIRST: it is wired as onClick={() => insertTexBlock(editor)} (ActionsMenuPanel:569), NOT runGridAction('tex') - it bypasses the unified grid ctx (no canEdit thread). insertTexBlock builds a minimal ctx WITHOUT canEdit, so texRun's isCollabReadOnly returns false - the collab read-only gate is effectively INERT on the grid tex cell. In a collab read-only session the grid tex cell would attempt the insert (relying on readOnlyEnforcer to reject) where slash explicitly no-ops at view.editable. Verify the grid tex cell is greyed/blocked under collab read-only. Also verify the atom-only-selection data-loss guard on BOTH surfaces."
  },
  {
    "id": "tex@paragraph#lightning",
    "category": "Block inserts (6)",
    "action": "tex",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "tex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1441 (texRun); TEX_ACTION_ROW:1490; lightning delegate tex-block.ts:125 (insertTexBlock->texRun); slash commands.ts:225 (runViewOnlyAction('tex'))",
    "backbone": "",
    "expected": {
      "behavior": "Seed-from-selection. Non-empty selection -> seedCode = textBetween(from,to,'\\n', hardBreak->'\\n'), deleteSelection() then replaceSelectionWith a texBlock carrying that code. Collapsed caret -> empty texBlock (code:''). DATA-LOSS GUARD: a non-empty selection that yields seedCode==='' but a non-empty slice (an inline atom selected alone) BAILS - the atom is preserved. scrollIntoView. No-op when read-only.",
      "docDelta": "One texBlock added (attrs: uuid via the single collision-free scan, code = seedCode | ''). On a text-bearing selection the range is deleted first.",
      "texDelta": "%!vtex:begin <uuid>\\n<escaped code>\\n%!vtex:end <uuid>\\n\\n (serializeTexBlock; raw passthrough between sentinels). \\vtex sentinels are comments, no command needed.",
      "sidecarDelta": "None - texBlock is a pure in-doc node, no JSON sidecar.",
      "lifecycle": "No card produced. Caret left at the inserted block. No panel hop on either surface."
    },
    "crossSurfaceIdentity": "Both surfaces route through texRun: same seedCode harvest, same ONE uuid-collision scan, same deleteSelection->replaceSelectionWith, same data-loss guard, same %!vtex sentinel round-trip. Slash via runViewOnlyAction (CursorRef ctx); lightning via insertTexBlock's hand-rolled SelectionRef ctx - both feed the SAME texRun which reads ctx.view.state.selection directly.",
    "knownRisk": "Drive the lightning tex cell live FIRST: it is wired as onClick={() => insertTexBlock(editor)} (ActionsMenuPanel:569), NOT runGridAction('tex') - it bypasses the unified grid ctx (no canEdit thread). insertTexBlock builds a minimal ctx WITHOUT canEdit, so texRun's isCollabReadOnly returns false - the collab read-only gate is effectively INERT on the grid tex cell. In a collab read-only session the grid tex cell would attempt the insert (relying on readOnlyEnforcer to reject) where slash explicitly no-ops at view.editable. Verify the grid tex cell is greyed/blocked under collab read-only. Also verify the atom-only-selection data-loss guard on BOTH surfaces."
  },
  {
    "id": "tex@paragraph#slash",
    "category": "Block inserts (6)",
    "action": "tex",
    "kind": "paragraph",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "tex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1441 (texRun); TEX_ACTION_ROW:1490; lightning delegate tex-block.ts:125 (insertTexBlock->texRun); slash commands.ts:225 (runViewOnlyAction('tex'))",
    "backbone": "",
    "expected": {
      "behavior": "Seed-from-selection. Non-empty selection -> seedCode = textBetween(from,to,'\\n', hardBreak->'\\n'), deleteSelection() then replaceSelectionWith a texBlock carrying that code. Collapsed caret -> empty texBlock (code:''). DATA-LOSS GUARD: a non-empty selection that yields seedCode==='' but a non-empty slice (an inline atom selected alone) BAILS - the atom is preserved. scrollIntoView. No-op when read-only.",
      "docDelta": "One texBlock added (attrs: uuid via the single collision-free scan, code = seedCode | ''). On a text-bearing selection the range is deleted first.",
      "texDelta": "%!vtex:begin <uuid>\\n<escaped code>\\n%!vtex:end <uuid>\\n\\n (serializeTexBlock; raw passthrough between sentinels). \\vtex sentinels are comments, no command needed.",
      "sidecarDelta": "None - texBlock is a pure in-doc node, no JSON sidecar.",
      "lifecycle": "No card produced. Caret left at the inserted block. No panel hop on either surface."
    },
    "crossSurfaceIdentity": "Both surfaces route through texRun: same seedCode harvest, same ONE uuid-collision scan, same deleteSelection->replaceSelectionWith, same data-loss guard, same %!vtex sentinel round-trip. Slash via runViewOnlyAction (CursorRef ctx); lightning via insertTexBlock's hand-rolled SelectionRef ctx - both feed the SAME texRun which reads ctx.view.state.selection directly.",
    "knownRisk": "Drive the lightning tex cell live FIRST: it is wired as onClick={() => insertTexBlock(editor)} (ActionsMenuPanel:569), NOT runGridAction('tex') - it bypasses the unified grid ctx (no canEdit thread). insertTexBlock builds a minimal ctx WITHOUT canEdit, so texRun's isCollabReadOnly returns false - the collab read-only gate is effectively INERT on the grid tex cell. In a collab read-only session the grid tex cell would attempt the insert (relying on readOnlyEnforcer to reject) where slash explicitly no-ops at view.editable. Verify the grid tex cell is greyed/blocked under collab read-only. Also verify the atom-only-selection data-loss guard on BOTH surfaces."
  },
  {
    "id": "tex@selection#lightning",
    "category": "Block inserts (6)",
    "action": "tex",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "SelectionRef -> blockApplies base 'ok'. SEED code from the selection.",
    "slashName": "tex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1441 (texRun); TEX_ACTION_ROW:1490; lightning delegate tex-block.ts:125 (insertTexBlock->texRun); slash commands.ts:225 (runViewOnlyAction('tex'))",
    "backbone": "",
    "expected": {
      "behavior": "Seed-from-selection. Non-empty selection -> seedCode = textBetween(from,to,'\\n', hardBreak->'\\n'), deleteSelection() then replaceSelectionWith a texBlock carrying that code. Collapsed caret -> empty texBlock (code:''). DATA-LOSS GUARD: a non-empty selection that yields seedCode==='' but a non-empty slice (an inline atom selected alone) BAILS - the atom is preserved. scrollIntoView. No-op when read-only.",
      "docDelta": "One texBlock added (attrs: uuid via the single collision-free scan, code = seedCode | ''). On a text-bearing selection the range is deleted first.",
      "texDelta": "%!vtex:begin <uuid>\\n<escaped code>\\n%!vtex:end <uuid>\\n\\n (serializeTexBlock; raw passthrough between sentinels). \\vtex sentinels are comments, no command needed.",
      "sidecarDelta": "None - texBlock is a pure in-doc node, no JSON sidecar.",
      "lifecycle": "No card produced. Caret left at the inserted block. No panel hop on either surface."
    },
    "crossSurfaceIdentity": "Both surfaces route through texRun: same seedCode harvest, same ONE uuid-collision scan, same deleteSelection->replaceSelectionWith, same data-loss guard, same %!vtex sentinel round-trip. Slash via runViewOnlyAction (CursorRef ctx); lightning via insertTexBlock's hand-rolled SelectionRef ctx - both feed the SAME texRun which reads ctx.view.state.selection directly.",
    "knownRisk": "Drive the lightning tex cell live FIRST: it is wired as onClick={() => insertTexBlock(editor)} (ActionsMenuPanel:569), NOT runGridAction('tex') - it bypasses the unified grid ctx (no canEdit thread). insertTexBlock builds a minimal ctx WITHOUT canEdit, so texRun's isCollabReadOnly returns false - the collab read-only gate is effectively INERT on the grid tex cell. In a collab read-only session the grid tex cell would attempt the insert (relying on readOnlyEnforcer to reject) where slash explicitly no-ops at view.editable. Verify the grid tex cell is greyed/blocked under collab read-only. Also verify the atom-only-selection data-loss guard on BOTH surfaces."
  },
  {
    "id": "tex@selection#slash",
    "category": "Block inserts (6)",
    "action": "tex",
    "kind": "selection",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "SelectionRef -> blockApplies base 'ok'. SEED code from the selection.",
    "slashName": "tex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1441 (texRun); TEX_ACTION_ROW:1490; lightning delegate tex-block.ts:125 (insertTexBlock->texRun); slash commands.ts:225 (runViewOnlyAction('tex'))",
    "backbone": "",
    "expected": {
      "behavior": "Seed-from-selection. Non-empty selection -> seedCode = textBetween(from,to,'\\n', hardBreak->'\\n'), deleteSelection() then replaceSelectionWith a texBlock carrying that code. Collapsed caret -> empty texBlock (code:''). DATA-LOSS GUARD: a non-empty selection that yields seedCode==='' but a non-empty slice (an inline atom selected alone) BAILS - the atom is preserved. scrollIntoView. No-op when read-only.",
      "docDelta": "One texBlock added (attrs: uuid via the single collision-free scan, code = seedCode | ''). On a text-bearing selection the range is deleted first.",
      "texDelta": "%!vtex:begin <uuid>\\n<escaped code>\\n%!vtex:end <uuid>\\n\\n (serializeTexBlock; raw passthrough between sentinels). \\vtex sentinels are comments, no command needed.",
      "sidecarDelta": "None - texBlock is a pure in-doc node, no JSON sidecar.",
      "lifecycle": "No card produced. Caret left at the inserted block. No panel hop on either surface."
    },
    "crossSurfaceIdentity": "Both surfaces route through texRun: same seedCode harvest, same ONE uuid-collision scan, same deleteSelection->replaceSelectionWith, same data-loss guard, same %!vtex sentinel round-trip. Slash via runViewOnlyAction (CursorRef ctx); lightning via insertTexBlock's hand-rolled SelectionRef ctx - both feed the SAME texRun which reads ctx.view.state.selection directly.",
    "knownRisk": "Drive the lightning tex cell live FIRST: it is wired as onClick={() => insertTexBlock(editor)} (ActionsMenuPanel:569), NOT runGridAction('tex') - it bypasses the unified grid ctx (no canEdit thread). insertTexBlock builds a minimal ctx WITHOUT canEdit, so texRun's isCollabReadOnly returns false - the collab read-only gate is effectively INERT on the grid tex cell. In a collab read-only session the grid tex cell would attempt the insert (relying on readOnlyEnforcer to reject) where slash explicitly no-ops at view.editable. Verify the grid tex cell is greyed/blocked under collab read-only. Also verify the atom-only-selection data-loss guard on BOTH surfaces."
  },
  {
    "id": "tex@selection:collab-readonly#lightning",
    "category": "Block inserts (6)",
    "action": "tex",
    "kind": "selection:collab-readonly",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "ctx.canEdit===false (collab read-only) -> 'disabled' uniformly via gateApplies layer (1).",
    "slashName": "tex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1441 (texRun); TEX_ACTION_ROW:1490; lightning delegate tex-block.ts:125 (insertTexBlock->texRun); slash commands.ts:225 (runViewOnlyAction('tex'))",
    "backbone": "",
    "expected": {
      "behavior": "Seed-from-selection. Non-empty selection -> seedCode = textBetween(from,to,'\\n', hardBreak->'\\n'), deleteSelection() then replaceSelectionWith a texBlock carrying that code. Collapsed caret -> empty texBlock (code:''). DATA-LOSS GUARD: a non-empty selection that yields seedCode==='' but a non-empty slice (an inline atom selected alone) BAILS - the atom is preserved. scrollIntoView. No-op when read-only.",
      "docDelta": "One texBlock added (attrs: uuid via the single collision-free scan, code = seedCode | ''). On a text-bearing selection the range is deleted first.",
      "texDelta": "%!vtex:begin <uuid>\\n<escaped code>\\n%!vtex:end <uuid>\\n\\n (serializeTexBlock; raw passthrough between sentinels). \\vtex sentinels are comments, no command needed.",
      "sidecarDelta": "None - texBlock is a pure in-doc node, no JSON sidecar.",
      "lifecycle": "No card produced. Caret left at the inserted block. No panel hop on either surface."
    },
    "crossSurfaceIdentity": "Both surfaces route through texRun: same seedCode harvest, same ONE uuid-collision scan, same deleteSelection->replaceSelectionWith, same data-loss guard, same %!vtex sentinel round-trip. Slash via runViewOnlyAction (CursorRef ctx); lightning via insertTexBlock's hand-rolled SelectionRef ctx - both feed the SAME texRun which reads ctx.view.state.selection directly.",
    "knownRisk": "Drive the lightning tex cell live FIRST: it is wired as onClick={() => insertTexBlock(editor)} (ActionsMenuPanel:569), NOT runGridAction('tex') - it bypasses the unified grid ctx (no canEdit thread). insertTexBlock builds a minimal ctx WITHOUT canEdit, so texRun's isCollabReadOnly returns false - the collab read-only gate is effectively INERT on the grid tex cell. In a collab read-only session the grid tex cell would attempt the insert (relying on readOnlyEnforcer to reject) where slash explicitly no-ops at view.editable. Verify the grid tex cell is greyed/blocked under collab read-only. Also verify the atom-only-selection data-loss guard on BOTH surfaces."
  },
  {
    "id": "tex@selection:collab-readonly#slash",
    "category": "Block inserts (6)",
    "action": "tex",
    "kind": "selection:collab-readonly",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "ctx.canEdit===false (collab read-only) -> 'disabled' uniformly via gateApplies layer (1).",
    "slashName": "tex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1441 (texRun); TEX_ACTION_ROW:1490; lightning delegate tex-block.ts:125 (insertTexBlock->texRun); slash commands.ts:225 (runViewOnlyAction('tex'))",
    "backbone": "",
    "expected": {
      "behavior": "Seed-from-selection. Non-empty selection -> seedCode = textBetween(from,to,'\\n', hardBreak->'\\n'), deleteSelection() then replaceSelectionWith a texBlock carrying that code. Collapsed caret -> empty texBlock (code:''). DATA-LOSS GUARD: a non-empty selection that yields seedCode==='' but a non-empty slice (an inline atom selected alone) BAILS - the atom is preserved. scrollIntoView. No-op when read-only.",
      "docDelta": "One texBlock added (attrs: uuid via the single collision-free scan, code = seedCode | ''). On a text-bearing selection the range is deleted first.",
      "texDelta": "%!vtex:begin <uuid>\\n<escaped code>\\n%!vtex:end <uuid>\\n\\n (serializeTexBlock; raw passthrough between sentinels). \\vtex sentinels are comments, no command needed.",
      "sidecarDelta": "None - texBlock is a pure in-doc node, no JSON sidecar.",
      "lifecycle": "No card produced. Caret left at the inserted block. No panel hop on either surface."
    },
    "crossSurfaceIdentity": "Both surfaces route through texRun: same seedCode harvest, same ONE uuid-collision scan, same deleteSelection->replaceSelectionWith, same data-loss guard, same %!vtex sentinel round-trip. Slash via runViewOnlyAction (CursorRef ctx); lightning via insertTexBlock's hand-rolled SelectionRef ctx - both feed the SAME texRun which reads ctx.view.state.selection directly.",
    "knownRisk": "Drive the lightning tex cell live FIRST: it is wired as onClick={() => insertTexBlock(editor)} (ActionsMenuPanel:569), NOT runGridAction('tex') - it bypasses the unified grid ctx (no canEdit thread). insertTexBlock builds a minimal ctx WITHOUT canEdit, so texRun's isCollabReadOnly returns false - the collab read-only gate is effectively INERT on the grid tex cell. In a collab read-only session the grid tex cell would attempt the insert (relying on readOnlyEnforcer to reject) where slash explicitly no-ops at view.editable. Verify the grid tex cell is greyed/blocked under collab read-only. Also verify the atom-only-selection data-loss guard on BOTH surfaces."
  },
  {
    "id": "tex@texBlock#lightning",
    "category": "Block inserts (6)",
    "action": "tex",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "tex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1441 (texRun); TEX_ACTION_ROW:1490; lightning delegate tex-block.ts:125 (insertTexBlock->texRun); slash commands.ts:225 (runViewOnlyAction('tex'))",
    "backbone": "",
    "expected": {
      "behavior": "Seed-from-selection. Non-empty selection -> seedCode = textBetween(from,to,'\\n', hardBreak->'\\n'), deleteSelection() then replaceSelectionWith a texBlock carrying that code. Collapsed caret -> empty texBlock (code:''). DATA-LOSS GUARD: a non-empty selection that yields seedCode==='' but a non-empty slice (an inline atom selected alone) BAILS - the atom is preserved. scrollIntoView. No-op when read-only.",
      "docDelta": "One texBlock added (attrs: uuid via the single collision-free scan, code = seedCode | ''). On a text-bearing selection the range is deleted first.",
      "texDelta": "%!vtex:begin <uuid>\\n<escaped code>\\n%!vtex:end <uuid>\\n\\n (serializeTexBlock; raw passthrough between sentinels). \\vtex sentinels are comments, no command needed.",
      "sidecarDelta": "None - texBlock is a pure in-doc node, no JSON sidecar.",
      "lifecycle": "No card produced. Caret left at the inserted block. No panel hop on either surface."
    },
    "crossSurfaceIdentity": "Both surfaces route through texRun: same seedCode harvest, same ONE uuid-collision scan, same deleteSelection->replaceSelectionWith, same data-loss guard, same %!vtex sentinel round-trip. Slash via runViewOnlyAction (CursorRef ctx); lightning via insertTexBlock's hand-rolled SelectionRef ctx - both feed the SAME texRun which reads ctx.view.state.selection directly.",
    "knownRisk": "Drive the lightning tex cell live FIRST: it is wired as onClick={() => insertTexBlock(editor)} (ActionsMenuPanel:569), NOT runGridAction('tex') - it bypasses the unified grid ctx (no canEdit thread). insertTexBlock builds a minimal ctx WITHOUT canEdit, so texRun's isCollabReadOnly returns false - the collab read-only gate is effectively INERT on the grid tex cell. In a collab read-only session the grid tex cell would attempt the insert (relying on readOnlyEnforcer to reject) where slash explicitly no-ops at view.editable. Verify the grid tex cell is greyed/blocked under collab read-only. Also verify the atom-only-selection data-loss guard on BOTH surfaces."
  },
  {
    "id": "tex@texBlock#slash",
    "category": "Block inserts (6)",
    "action": "tex",
    "kind": "texBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true -> 'disabled'.",
    "slashName": "tex",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1441 (texRun); TEX_ACTION_ROW:1490; lightning delegate tex-block.ts:125 (insertTexBlock->texRun); slash commands.ts:225 (runViewOnlyAction('tex'))",
    "backbone": "",
    "expected": {
      "behavior": "Seed-from-selection. Non-empty selection -> seedCode = textBetween(from,to,'\\n', hardBreak->'\\n'), deleteSelection() then replaceSelectionWith a texBlock carrying that code. Collapsed caret -> empty texBlock (code:''). DATA-LOSS GUARD: a non-empty selection that yields seedCode==='' but a non-empty slice (an inline atom selected alone) BAILS - the atom is preserved. scrollIntoView. No-op when read-only.",
      "docDelta": "One texBlock added (attrs: uuid via the single collision-free scan, code = seedCode | ''). On a text-bearing selection the range is deleted first.",
      "texDelta": "%!vtex:begin <uuid>\\n<escaped code>\\n%!vtex:end <uuid>\\n\\n (serializeTexBlock; raw passthrough between sentinels). \\vtex sentinels are comments, no command needed.",
      "sidecarDelta": "None - texBlock is a pure in-doc node, no JSON sidecar.",
      "lifecycle": "No card produced. Caret left at the inserted block. No panel hop on either surface."
    },
    "crossSurfaceIdentity": "Both surfaces route through texRun: same seedCode harvest, same ONE uuid-collision scan, same deleteSelection->replaceSelectionWith, same data-loss guard, same %!vtex sentinel round-trip. Slash via runViewOnlyAction (CursorRef ctx); lightning via insertTexBlock's hand-rolled SelectionRef ctx - both feed the SAME texRun which reads ctx.view.state.selection directly.",
    "knownRisk": "Drive the lightning tex cell live FIRST: it is wired as onClick={() => insertTexBlock(editor)} (ActionsMenuPanel:569), NOT runGridAction('tex') - it bypasses the unified grid ctx (no canEdit thread). insertTexBlock builds a minimal ctx WITHOUT canEdit, so texRun's isCollabReadOnly returns false - the collab read-only gate is effectively INERT on the grid tex cell. In a collab read-only session the grid tex cell would attempt the insert (relying on readOnlyEnforcer to reject) where slash explicitly no-ops at view.editable. Verify the grid tex cell is greyed/blocked under collab read-only. Also verify the atom-only-selection data-loss guard on BOTH surfaces."
  },
  {
    "id": "ref@atom-bearing:ref#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "atom-bearing:ref",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "A paragraph containing a \\ref among text -> 'ok'; inserting another \\ref is additive, no interplay hazard.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@atom-bearing:ref#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "atom-bearing:ref",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "A paragraph containing a \\ref among text -> 'ok'; inserting another \\ref is additive, no interplay hazard.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@atom-only:ref#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "atom-only:ref",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "A paragraph whose only content is a \\ref atom is still a paragraph -> 'ok'. ref insertion is non-destructive (inserts a NEW labelRef atom at the caret), so the atom-only-data-loss class does NOT apply - refRun never deletes the existing atom.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@atom-only:ref#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "atom-only:ref",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "A paragraph whose only content is a \\ref atom is still a paragraph -> 'ok'. ref insertion is non-destructive (inserts a NEW labelRef atom at the caret), so the atom-only-data-loss class does NOT apply - refRun never deletes the existing atom.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@blockquote#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@blockquote#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "blockquote",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@bulletList#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "bulletList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@bulletList#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "bulletList",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@codeBlock#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "codeBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok' per registry, though semantically odd. Theoretical (ref is only invoked from caret/selection, not a block-grab handle).",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@codeBlock#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "codeBlock",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok' per registry, though semantically odd. Theoretical (ref is only invoked from caret/selection, not a block-grab handle).",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@collab-read-only#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "collab-read-only",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isCollabReadOnly(ctx)===ctx.canEdit===false. TWO independent gates: (1) gateApplies forces applies()->'disabled' uniformly; (2) refRun early-returns at :1529 so even if invoked it opens NO popover. The lightning FmtBtn is also hard-gated by disabled={!canEdit} (ActionsMenuPanel.tsx:601) - a THIRD belt-and-suspenders layer.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@collab-read-only#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "collab-read-only",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isCollabReadOnly(ctx)===ctx.canEdit===false. TWO independent gates: (1) gateApplies forces applies()->'disabled' uniformly; (2) refRun early-returns at :1529 so even if invoked it opens NO popover. The lightning FmtBtn is also hard-gated by disabled={!canEdit} (ActionsMenuPanel.tsx:601) - a THIRD belt-and-suspenders layer.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@cursor#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "cursor",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "blockApplies: ref.kind==='cursor' -> base 'ok'. The slash \\ref path and the collapsed-caret case. selection:'optional'. The dominant real-world entry.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@cursor#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "cursor",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "blockApplies: ref.kind==='cursor' -> base 'ok'. The slash \\ref path and the collapsed-caret case. selection:'optional'. The dominant real-world entry.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@displayMath#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true (text-object-registry.ts:490) -> blockApplies base 'disabled'. A display-math atom block has no inline caret. Deliberately greyed via the shared blockApplies gate.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@displayMath#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "displayMath",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true (text-object-registry.ts:490) -> blockApplies base 'disabled'. A display-math atom block has no inline caret. Deliberately greyed via the shared blockApplies gate.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@exampleBlock#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "exampleBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'. Theoretical (no grab surface).",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@exampleBlock#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "exampleBlock",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'. Theoretical (no grab surface).",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@exampleItem#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "exampleItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@exampleItem#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "exampleItem",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@figureBlock#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "figureBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "SURPRISING: figureBlock is isAtomBlock:FALSE in text-object-registry.ts:411, so blockApplies returns base 'ok' - NOT 'disabled' - even though a figure is an opaque atom block with no inline caret. The REF_ACTION_ROW jsdoc:1542 says figure -> 'disabled', but figureBlock does NOT carry isAtomBlock, so the gate does NOT disable it. The doc and the data disagree for figureBlock - flag as a doc/data mismatch. Harmless because ref has no grab surface to reach figureBlock.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@figureBlock#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "figureBlock",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "SURPRISING: figureBlock is isAtomBlock:FALSE in text-object-registry.ts:411, so blockApplies returns base 'ok' - NOT 'disabled' - even though a figure is an opaque atom block with no inline caret. The REF_ACTION_ROW jsdoc:1542 says figure -> 'disabled', but figureBlock does NOT carry isAtomBlock, so the gate does NOT disable it. The doc and the data disagree for figureBlock - flag as a doc/data mismatch. Harmless because ref has no grab surface to reach figureBlock.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@graphicsBlock#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true (:620) -> 'disabled'.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@graphicsBlock#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "graphicsBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true (:620) -> 'disabled'.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@heading#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@heading#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "heading",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@latexComment#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true (:535) -> 'disabled'.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@latexComment#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "latexComment",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true (:535) -> 'disabled'.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@linkedRange#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "linkedRange",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'. Theoretical.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@linkedRange#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "linkedRange",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'. Theoretical.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@listItem#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@listItem#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "listItem",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@orderedList#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "orderedList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@orderedList#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "orderedList",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@paragraph#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> base 'ok'. A \\ref is insertable into running prose.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@paragraph#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "paragraph",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> base 'ok'. A \\ref is insertable into running prose.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@selection#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "blockApplies: ref.kind==='selection' -> base 'ok'. selection:'optional' so a live range is NOT required; the popover lands the labelRef atom at the caret. Normal lightning-cell path.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@selection#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "selection",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "blockApplies: ref.kind==='selection' -> base 'ok'. selection:'optional' so a live range is NOT required; the popover lands the labelRef atom at the caret. Normal lightning-cell path.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@texBlock#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true (:552) -> 'disabled'. Opaque raw-LaTeX block, no caret.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@texBlock#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "texBlock",
    "surface": "slash",
    "applies": "disabled",
    "kindNote": "isAtomBlock:true (:552) -> 'disabled'. Opaque raw-LaTeX block, no caret.",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@titleField#lightning",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'. Theoretical (ref has no grab surface on titleField).",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "ref@titleField#slash",
    "category": "Atom ref (1)",
    "action": "ref",
    "kind": "titleField",
    "surface": "slash",
    "applies": "ok",
    "kindNote": "isAtomBlock:false -> 'ok'. Theoretical (ref has no grab surface on titleField).",
    "slashName": "\\ref",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1528 (refRun); ActionSpec row REF_ACTION_ROW:1548",
    "backbone": "",
    "expected": {
      "behavior": "Opens the LabelRef create-mode popover at the live caret - it does NOT insert anything itself. refRun is a one-liner: collab-gate check, then ctx.openRefPopover?.(). The popover is the creator: lists every \\label{...} site; when the user picks/types a label, useRefActions.handleInsertRef (card-actions/ref.ts:324) lands the labelRef atom at the cursor. With NO popover seam (pure view-only path), refRun no-ops cleanly (optional-chain ?. swallows the absent seam) - verified by ref-title-cross-surface.test.ts:195.",
      "docDelta": "refRun ALONE produces NO docDelta (no atom inserted; the cross-surface test asserts labelRefs===0 after the action). The atom lands only on a SUBSEQUENT user pick: handleInsertRef does insertContent({type:'labelRef', attrs:{label, displayText, refCommand, targetKind}}). refCommand defaults to 'ref'.",
      "sidecarDelta": "NONE. Unlike citation/footnote, \\ref has NO card and NO sidecar - the labelRef atom is self-contained in the .tex and the popover is the only creator. No panelRouting, no cardCreation touched.",
      "texDelta": "On user pick: \\ref{<label>} (or \\getref{...}/\\getfullref{...} when refCommand is getref/getfullref). The labelRef node carries {label, displayText, refCommand, targetKind}; only label + refCommand are load-bearing for the serializer. refRun itself emits NO texDelta.",
      "lifecycle": "NO card lifecycle - \\ref mints no float, no pinned/pristine card (card-LESS by design, atom-registry.ts:63 idAttr:null). The only transient state is the LabelRef popover open/close."
    },
    "crossSurfaceIdentity": "Slash (\\ref) and lightning (Cross-ref cell) MUST be byte-identical because both route through the SINGLE refRun -> ctx.openRefPopover(). The two seam suppliers - EditorPane.tsx:2233 (slash) and ActionsMenuPanel.tsx:253 (lightning) - hand-roll the SAME caret-rect computation (selection.from -> coordsAtPos -> new DOMRect -> dispatch 'virgil-ref-create-popover'). The marker-clicks.ts:360 listener consumes that ONE event for BOTH surfaces. The resulting labelRef atom is identical attrs/shape regardless of surface. Verified by ref-title-cross-surface.test.ts.",
    "knownRisk": "DRIVE LIVE FIRST - three concerns: (1) The caret-rect computation is HAND-ROLLED in TWO places (EditorPane.tsx:2233 and ActionsMenuPanel.tsx:253) rather than centralized - they currently match byte-for-byte, but this is exactly the divergence class CHIP 8 hunts; a future edit to one will silently drift the popover anchor. Diff the two DOMRect expressions live and confirm both popovers anchor at the identical screen position. (2) figureBlock isAtomBlock:false vs the REF_ACTION_ROW jsdoc claiming figure -> 'disabled' - confirm whether ref is ever reachable on a figureBlock and whether the doc should be corrected. (3) The lightning cell is gated by THREE canEdit checks while slash relies on only the refRun early-return - verify a collab read-only doc greys the grid cell AND no-ops the slash command, with no popover ever appearing."
  },
  {
    "id": "blockquote@blockquote#lightning",
    "category": "Format marks",
    "action": "blockquote",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "toggle OFF - lifts the wrapped content back to paragraphs; toggleBlockquote is a toggle",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BLOCKQUOTE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the blockquote structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBlockquote().run(). On a plain block it wraps; on an existing blockquote it lifts content back to paragraphs (toggle). Grid active-state from editor.isActive('blockquote').",
      "docDelta": "current block(s) wrapped into a blockquote - or, on an existing blockquote, lifted back to paragraphs. Structural node change, not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{quote}...\\end{quote} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and a markdown input rule ('> ') which call the SAME toggleBlockquote - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleBlockquote; identical structural result. Only the grid path goes through the registry collab guard.",
    "knownRisk": "RECONSTRUCTED-TAIL (the blockquote row was truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleBlockquote may be a structural no-op there - the applies()-vs-effect divergence. Drive a blockquote-toggle on a heading and on a titleField live. Also: the '> ' markdown input rule is a SECOND live surface not in the registry - confirm parity with the grid."
  },
  {
    "id": "blockquote@heading#lightning",
    "category": "Format marks",
    "action": "blockquote",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "applies() unconditionally ok; toggleBlockquote may wrap or be odd on a heading but applies() does not gate it - possible divergence",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BLOCKQUOTE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the blockquote structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBlockquote().run(). On a plain block it wraps; on an existing blockquote it lifts content back to paragraphs (toggle). Grid active-state from editor.isActive('blockquote').",
      "docDelta": "current block(s) wrapped into a blockquote - or, on an existing blockquote, lifted back to paragraphs. Structural node change, not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{quote}...\\end{quote} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and a markdown input rule ('> ') which call the SAME toggleBlockquote - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleBlockquote; identical structural result. Only the grid path goes through the registry collab guard.",
    "knownRisk": "RECONSTRUCTED-TAIL (the blockquote row was truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleBlockquote may be a structural no-op there - the applies()-vs-effect divergence. Drive a blockquote-toggle on a heading and on a titleField live. Also: the '> ' markdown input rule is a SECOND live surface not in the registry - confirm parity with the grid."
  },
  {
    "id": "blockquote@listItem#lightning",
    "category": "Format marks",
    "action": "blockquote",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret inside a list item",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BLOCKQUOTE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the blockquote structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBlockquote().run(). On a plain block it wraps; on an existing blockquote it lifts content back to paragraphs (toggle). Grid active-state from editor.isActive('blockquote').",
      "docDelta": "current block(s) wrapped into a blockquote - or, on an existing blockquote, lifted back to paragraphs. Structural node change, not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{quote}...\\end{quote} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and a markdown input rule ('> ') which call the SAME toggleBlockquote - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleBlockquote; identical structural result. Only the grid path goes through the registry collab guard.",
    "knownRisk": "RECONSTRUCTED-TAIL (the blockquote row was truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleBlockquote may be a structural no-op there - the applies()-vs-effect divergence. Drive a blockquote-toggle on a heading and on a titleField live. Also: the '> ' markdown input rule is a SECOND live surface not in the registry - confirm parity with the grid."
  },
  {
    "id": "blockquote@paragraph#lightning",
    "category": "Format marks",
    "action": "blockquote",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "wraps the current block into a blockquote; valid at a collapsed caret (selection:'ignored')",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BLOCKQUOTE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the blockquote structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBlockquote().run(). On a plain block it wraps; on an existing blockquote it lifts content back to paragraphs (toggle). Grid active-state from editor.isActive('blockquote').",
      "docDelta": "current block(s) wrapped into a blockquote - or, on an existing blockquote, lifted back to paragraphs. Structural node change, not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{quote}...\\end{quote} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and a markdown input rule ('> ') which call the SAME toggleBlockquote - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleBlockquote; identical structural result. Only the grid path goes through the registry collab guard.",
    "knownRisk": "RECONSTRUCTED-TAIL (the blockquote row was truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleBlockquote may be a structural no-op there - the applies()-vs-effect divergence. Drive a blockquote-toggle on a heading and on a titleField live. Also: the '> ' markdown input rule is a SECOND live surface not in the registry - confirm parity with the grid."
  },
  {
    "id": "blockquote@selection#lightning",
    "category": "Format marks",
    "action": "blockquote",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "wraps spanned blocks",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BLOCKQUOTE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the blockquote structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBlockquote().run(). On a plain block it wraps; on an existing blockquote it lifts content back to paragraphs (toggle). Grid active-state from editor.isActive('blockquote').",
      "docDelta": "current block(s) wrapped into a blockquote - or, on an existing blockquote, lifted back to paragraphs. Structural node change, not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{quote}...\\end{quote} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and a markdown input rule ('> ') which call the SAME toggleBlockquote - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleBlockquote; identical structural result. Only the grid path goes through the registry collab guard.",
    "knownRisk": "RECONSTRUCTED-TAIL (the blockquote row was truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleBlockquote may be a structural no-op there - the applies()-vs-effect divergence. Drive a blockquote-toggle on a heading and on a titleField live. Also: the '> ' markdown input rule is a SECOND live surface not in the registry - confirm parity with the grid."
  },
  {
    "id": "blockquote@titleField#lightning",
    "category": "Format marks",
    "action": "blockquote",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "applies() ok, but wrapping the title field is a structural no-op/odd; applies() does not gate it",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BLOCKQUOTE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the blockquote structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBlockquote().run(). On a plain block it wraps; on an existing blockquote it lifts content back to paragraphs (toggle). Grid active-state from editor.isActive('blockquote').",
      "docDelta": "current block(s) wrapped into a blockquote - or, on an existing blockquote, lifted back to paragraphs. Structural node change, not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{quote}...\\end{quote} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and a markdown input rule ('> ') which call the SAME toggleBlockquote - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleBlockquote; identical structural result. Only the grid path goes through the registry collab guard.",
    "knownRisk": "RECONSTRUCTED-TAIL (the blockquote row was truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleBlockquote may be a structural no-op there - the applies()-vs-effect divergence. Drive a blockquote-toggle on a heading and on a titleField live. Also: the '> ' markdown input rule is a SECOND live surface not in the registry - confirm parity with the grid."
  },
  {
    "id": "bold@atom-bearing:citation#lightning",
    "category": "Format marks",
    "action": "bold",
    "kind": "atom-bearing:citation",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "selection spanning text+atom toggles the mark on the text runs only",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BOLD_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the bold mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBold().run(). Grid active-state from editor.isActive('strong').",
      "docDelta": "strong mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textbf{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-b (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-b keystroke) both bottom out in toggleBold so they produce a byte-identical strong mark / identical \\textbf{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-b is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). A held Mod-b while a partner holds the pen is the belt-and-suspenders case the run() guard comment at registry:2135-2138 calls out."
  },
  {
    "id": "bold@atom-only:inline-math#lightning",
    "category": "Format marks",
    "action": "bold",
    "kind": "atom-only:inline-math",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "formatApplies never inspects content; toggling a mark on a math-only line is a PM no-op but applies() still says ok",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BOLD_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the bold mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBold().run(). Grid active-state from editor.isActive('strong').",
      "docDelta": "strong mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textbf{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-b (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-b keystroke) both bottom out in toggleBold so they produce a byte-identical strong mark / identical \\textbf{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-b is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). A held Mod-b while a partner holds the pen is the belt-and-suspenders case the run() guard comment at registry:2135-2138 calls out."
  },
  {
    "id": "bold@blockquote#lightning",
    "category": "Format marks",
    "action": "bold",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BOLD_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the bold mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBold().run(). Grid active-state from editor.isActive('strong').",
      "docDelta": "strong mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textbf{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-b (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-b keystroke) both bottom out in toggleBold so they produce a byte-identical strong mark / identical \\textbf{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-b is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). A held Mod-b while a partner holds the pen is the belt-and-suspenders case the run() guard comment at registry:2135-2138 calls out."
  },
  {
    "id": "bold@exampleItem#lightning",
    "category": "Format marks",
    "action": "bold",
    "kind": "exampleItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret inside example-item text",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BOLD_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the bold mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBold().run(). Grid active-state from editor.isActive('strong').",
      "docDelta": "strong mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textbf{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-b (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-b keystroke) both bottom out in toggleBold so they produce a byte-identical strong mark / identical \\textbf{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-b is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). A held Mod-b while a partner holds the pen is the belt-and-suspenders case the run() guard comment at registry:2135-2138 calls out."
  },
  {
    "id": "bold@heading#lightning",
    "category": "Format marks",
    "action": "bold",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret/selection inside heading text - mark toggle valid; no kind filter",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BOLD_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the bold mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBold().run(). Grid active-state from editor.isActive('strong').",
      "docDelta": "strong mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textbf{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-b (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-b keystroke) both bottom out in toggleBold so they produce a byte-identical strong mark / identical \\textbf{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-b is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). A held Mod-b while a partner holds the pen is the belt-and-suspenders case the run() guard comment at registry:2135-2138 calls out."
  },
  {
    "id": "bold@listItem#lightning",
    "category": "Format marks",
    "action": "bold",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret inside list-item text",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BOLD_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the bold mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBold().run(). Grid active-state from editor.isActive('strong').",
      "docDelta": "strong mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textbf{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-b (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-b keystroke) both bottom out in toggleBold so they produce a byte-identical strong mark / identical \\textbf{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-b is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). A held Mod-b while a partner holds the pen is the belt-and-suspenders case the run() guard comment at registry:2135-2138 calls out."
  },
  {
    "id": "bold@paragraph#lightning",
    "category": "Format marks",
    "action": "bold",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "a collapsed caret in a paragraph still toggles the STORED mark",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BOLD_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the bold mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBold().run(). Grid active-state from editor.isActive('strong').",
      "docDelta": "strong mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textbf{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-b (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-b keystroke) both bottom out in toggleBold so they produce a byte-identical strong mark / identical \\textbf{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-b is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). A held Mod-b while a partner holds the pen is the belt-and-suspenders case the run() guard comment at registry:2135-2138 calls out."
  },
  {
    "id": "bold@selection#lightning",
    "category": "Format marks",
    "action": "bold",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "selection:'ignored' - range-agnostic; gateApplies base 'ok'",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BOLD_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the bold mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBold().run(). Grid active-state from editor.isActive('strong').",
      "docDelta": "strong mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textbf{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-b (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-b keystroke) both bottom out in toggleBold so they produce a byte-identical strong mark / identical \\textbf{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-b is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). A held Mod-b while a partner holds the pen is the belt-and-suspenders case the run() guard comment at registry:2135-2138 calls out."
  },
  {
    "id": "bold@titleField#lightning",
    "category": "Format marks",
    "action": "bold",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret in title text - mark would toggle; applies() unconditionally ok",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BOLD_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the bold mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBold().run(). Grid active-state from editor.isActive('strong').",
      "docDelta": "strong mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textbf{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-b (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-b keystroke) both bottom out in toggleBold so they produce a byte-identical strong mark / identical \\textbf{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-b is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). A held Mod-b while a partner holds the pen is the belt-and-suspenders case the run() guard comment at registry:2135-2138 calls out."
  },
  {
    "id": "bullet-list@blockquote#lightning",
    "category": "Format marks",
    "action": "bullet-list",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BULLET_LIST_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the bulletList structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBulletList().run(). On a plain block it wraps; on an existing bulletList it lifts items back to paragraphs (toggle). Grid active-state from editor.isActive('bulletList').",
      "docDelta": "current block(s) wrapped into a bulletList (listItem children) - or, on an existing bulletList, lifted back to paragraphs. Structural node change (wrap/lift), not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{itemize}...\\item...\\end{itemize} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and markdown input rules ('- '/'1. ') which call the SAME toggleBulletList - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleBulletList; identical structural result. Only the grid path goes through the registry collab guard; the keystroke/input-rule is gated by readOnlyEnforcer.",
    "knownRisk": "RECONSTRUCTED-TAIL (the bullet-list/ordered-list/blockquote rows were truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleBulletList may be a structural no-op there - the applies()-vs-effect divergence (an enabled cell that does nothing). Drive a bulletList-toggle on a heading and on a titleField live to see whether the cell should be greyed. Also: the markdown input rule ('- '/'1. ') is a SECOND live surface not in the registry - confirm it produces the same structure as the grid."
  },
  {
    "id": "bullet-list@bulletList#lightning",
    "category": "Format marks",
    "action": "bullet-list",
    "kind": "bulletList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "toggle OFF - lifts items back to paragraphs; toggleBulletList is a toggle",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BULLET_LIST_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the bulletList structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBulletList().run(). On a plain block it wraps; on an existing bulletList it lifts items back to paragraphs (toggle). Grid active-state from editor.isActive('bulletList').",
      "docDelta": "current block(s) wrapped into a bulletList (listItem children) - or, on an existing bulletList, lifted back to paragraphs. Structural node change (wrap/lift), not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{itemize}...\\item...\\end{itemize} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and markdown input rules ('- '/'1. ') which call the SAME toggleBulletList - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleBulletList; identical structural result. Only the grid path goes through the registry collab guard; the keystroke/input-rule is gated by readOnlyEnforcer.",
    "knownRisk": "RECONSTRUCTED-TAIL (the bullet-list/ordered-list/blockquote rows were truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleBulletList may be a structural no-op there - the applies()-vs-effect divergence (an enabled cell that does nothing). Drive a bulletList-toggle on a heading and on a titleField live to see whether the cell should be greyed. Also: the markdown input rule ('- '/'1. ') is a SECOND live surface not in the registry - confirm it produces the same structure as the grid."
  },
  {
    "id": "bullet-list@heading#lightning",
    "category": "Format marks",
    "action": "bullet-list",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "applies() unconditionally ok; toggleBulletList may fail to wrap a heading but applies() does not gate it - possible divergence",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BULLET_LIST_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the bulletList structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBulletList().run(). On a plain block it wraps; on an existing bulletList it lifts items back to paragraphs (toggle). Grid active-state from editor.isActive('bulletList').",
      "docDelta": "current block(s) wrapped into a bulletList (listItem children) - or, on an existing bulletList, lifted back to paragraphs. Structural node change (wrap/lift), not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{itemize}...\\item...\\end{itemize} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and markdown input rules ('- '/'1. ') which call the SAME toggleBulletList - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleBulletList; identical structural result. Only the grid path goes through the registry collab guard; the keystroke/input-rule is gated by readOnlyEnforcer.",
    "knownRisk": "RECONSTRUCTED-TAIL (the bullet-list/ordered-list/blockquote rows were truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleBulletList may be a structural no-op there - the applies()-vs-effect divergence (an enabled cell that does nothing). Drive a bulletList-toggle on a heading and on a titleField live to see whether the cell should be greyed. Also: the markdown input rule ('- '/'1. ') is a SECOND live surface not in the registry - confirm it produces the same structure as the grid."
  },
  {
    "id": "bullet-list@listItem#lightning",
    "category": "Format marks",
    "action": "bullet-list",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret inside an item",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BULLET_LIST_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the bulletList structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBulletList().run(). On a plain block it wraps; on an existing bulletList it lifts items back to paragraphs (toggle). Grid active-state from editor.isActive('bulletList').",
      "docDelta": "current block(s) wrapped into a bulletList (listItem children) - or, on an existing bulletList, lifted back to paragraphs. Structural node change (wrap/lift), not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{itemize}...\\item...\\end{itemize} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and markdown input rules ('- '/'1. ') which call the SAME toggleBulletList - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleBulletList; identical structural result. Only the grid path goes through the registry collab guard; the keystroke/input-rule is gated by readOnlyEnforcer.",
    "knownRisk": "RECONSTRUCTED-TAIL (the bullet-list/ordered-list/blockquote rows were truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleBulletList may be a structural no-op there - the applies()-vs-effect divergence (an enabled cell that does nothing). Drive a bulletList-toggle on a heading and on a titleField live to see whether the cell should be greyed. Also: the markdown input rule ('- '/'1. ') is a SECOND live surface not in the registry - confirm it produces the same structure as the grid."
  },
  {
    "id": "bullet-list@orderedList#lightning",
    "category": "Format marks",
    "action": "bullet-list",
    "kind": "orderedList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "converts ordered->bullet (toggle behavior)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BULLET_LIST_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the bulletList structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBulletList().run(). On a plain block it wraps; on an existing bulletList it lifts items back to paragraphs (toggle). Grid active-state from editor.isActive('bulletList').",
      "docDelta": "current block(s) wrapped into a bulletList (listItem children) - or, on an existing bulletList, lifted back to paragraphs. Structural node change (wrap/lift), not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{itemize}...\\item...\\end{itemize} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and markdown input rules ('- '/'1. ') which call the SAME toggleBulletList - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleBulletList; identical structural result. Only the grid path goes through the registry collab guard; the keystroke/input-rule is gated by readOnlyEnforcer.",
    "knownRisk": "RECONSTRUCTED-TAIL (the bullet-list/ordered-list/blockquote rows were truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleBulletList may be a structural no-op there - the applies()-vs-effect divergence (an enabled cell that does nothing). Drive a bulletList-toggle on a heading and on a titleField live to see whether the cell should be greyed. Also: the markdown input rule ('- '/'1. ') is a SECOND live surface not in the registry - confirm it produces the same structure as the grid."
  },
  {
    "id": "bullet-list@paragraph#lightning",
    "category": "Format marks",
    "action": "bullet-list",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "wraps the current block into a bulletList; valid at a collapsed caret (selection:'ignored')",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BULLET_LIST_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the bulletList structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBulletList().run(). On a plain block it wraps; on an existing bulletList it lifts items back to paragraphs (toggle). Grid active-state from editor.isActive('bulletList').",
      "docDelta": "current block(s) wrapped into a bulletList (listItem children) - or, on an existing bulletList, lifted back to paragraphs. Structural node change (wrap/lift), not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{itemize}...\\item...\\end{itemize} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and markdown input rules ('- '/'1. ') which call the SAME toggleBulletList - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleBulletList; identical structural result. Only the grid path goes through the registry collab guard; the keystroke/input-rule is gated by readOnlyEnforcer.",
    "knownRisk": "RECONSTRUCTED-TAIL (the bullet-list/ordered-list/blockquote rows were truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleBulletList may be a structural no-op there - the applies()-vs-effect divergence (an enabled cell that does nothing). Drive a bulletList-toggle on a heading and on a titleField live to see whether the cell should be greyed. Also: the markdown input rule ('- '/'1. ') is a SECOND live surface not in the registry - confirm it produces the same structure as the grid."
  },
  {
    "id": "bullet-list@selection#lightning",
    "category": "Format marks",
    "action": "bullet-list",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "wraps spanned blocks",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BULLET_LIST_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the bulletList structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBulletList().run(). On a plain block it wraps; on an existing bulletList it lifts items back to paragraphs (toggle). Grid active-state from editor.isActive('bulletList').",
      "docDelta": "current block(s) wrapped into a bulletList (listItem children) - or, on an existing bulletList, lifted back to paragraphs. Structural node change (wrap/lift), not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{itemize}...\\item...\\end{itemize} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and markdown input rules ('- '/'1. ') which call the SAME toggleBulletList - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleBulletList; identical structural result. Only the grid path goes through the registry collab guard; the keystroke/input-rule is gated by readOnlyEnforcer.",
    "knownRisk": "RECONSTRUCTED-TAIL (the bullet-list/ordered-list/blockquote rows were truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleBulletList may be a structural no-op there - the applies()-vs-effect divergence (an enabled cell that does nothing). Drive a bulletList-toggle on a heading and on a titleField live to see whether the cell should be greyed. Also: the markdown input rule ('- '/'1. ') is a SECOND live surface not in the registry - confirm it produces the same structure as the grid."
  },
  {
    "id": "bullet-list@titleField#lightning",
    "category": "Format marks",
    "action": "bullet-list",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "applies() ok, but wrapping the title field is a structural no-op/odd; applies() does not gate it",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (BULLET_LIST_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the bulletList structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleBulletList().run(). On a plain block it wraps; on an existing bulletList it lifts items back to paragraphs (toggle). Grid active-state from editor.isActive('bulletList').",
      "docDelta": "current block(s) wrapped into a bulletList (listItem children) - or, on an existing bulletList, lifted back to paragraphs. Structural node change (wrap/lift), not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{itemize}...\\item...\\end{itemize} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and markdown input rules ('- '/'1. ') which call the SAME toggleBulletList - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleBulletList; identical structural result. Only the grid path goes through the registry collab guard; the keystroke/input-rule is gated by readOnlyEnforcer.",
    "knownRisk": "RECONSTRUCTED-TAIL (the bullet-list/ordered-list/blockquote rows were truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleBulletList may be a structural no-op there - the applies()-vs-effect divergence (an enabled cell that does nothing). Drive a bulletList-toggle on a heading and on a titleField live to see whether the cell should be greyed. Also: the markdown input rule ('- '/'1. ') is a SECOND live surface not in the registry - confirm it produces the same structure as the grid."
  },
  {
    "id": "code@atom-bearing:citation#lightning",
    "category": "Format marks",
    "action": "code",
    "kind": "atom-bearing:citation",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "selection spanning text+atom toggles the mark on the text runs only",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (CODE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the inline code mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleCode().run(). Grid active-state from editor.isActive('code'). Toggles the INLINE code mark, NOT the codeBlock node.",
      "docDelta": "code mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\texttt{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-e (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-e keystroke) both bottom out in toggleCode so they produce a byte-identical code mark / identical \\texttt{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-e is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). NAMING TRAP - id 'code' is the inline code MARK, easily confused with the codeBlock text-object kind. Drive live to confirm the grid cell toggles the inline mark and never wraps a codeBlock, and that Mod-e does the same. Note the codeBlock kind below is applies:'ok' for this row because the INLINE mark toggles inside a code block (a near-no-op), distinct from the codeBlock NODE."
  },
  {
    "id": "code@atom-only:inline-math#lightning",
    "category": "Format marks",
    "action": "code",
    "kind": "atom-only:inline-math",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "formatApplies never inspects content; toggling a mark on a math-only line is a PM no-op but applies() still says ok",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (CODE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the inline code mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleCode().run(). Grid active-state from editor.isActive('code'). Toggles the INLINE code mark, NOT the codeBlock node.",
      "docDelta": "code mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\texttt{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-e (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-e keystroke) both bottom out in toggleCode so they produce a byte-identical code mark / identical \\texttt{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-e is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). NAMING TRAP - id 'code' is the inline code MARK, easily confused with the codeBlock text-object kind. Drive live to confirm the grid cell toggles the inline mark and never wraps a codeBlock, and that Mod-e does the same. Note the codeBlock kind below is applies:'ok' for this row because the INLINE mark toggles inside a code block (a near-no-op), distinct from the codeBlock NODE."
  },
  {
    "id": "code@blockquote#lightning",
    "category": "Format marks",
    "action": "code",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (CODE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the inline code mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleCode().run(). Grid active-state from editor.isActive('code'). Toggles the INLINE code mark, NOT the codeBlock node.",
      "docDelta": "code mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\texttt{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-e (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-e keystroke) both bottom out in toggleCode so they produce a byte-identical code mark / identical \\texttt{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-e is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). NAMING TRAP - id 'code' is the inline code MARK, easily confused with the codeBlock text-object kind. Drive live to confirm the grid cell toggles the inline mark and never wraps a codeBlock, and that Mod-e does the same. Note the codeBlock kind below is applies:'ok' for this row because the INLINE mark toggles inside a code block (a near-no-op), distinct from the codeBlock NODE."
  },
  {
    "id": "code@codeBlock#lightning",
    "category": "Format marks",
    "action": "code",
    "kind": "codeBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "the INLINE code MARK (toggleCode) - distinct from the codeBlock NODE. applies() unconditionally ok; toggling the inline mark inside a code block is a near-no-op but applies() does not branch",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (CODE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the inline code mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleCode().run(). Grid active-state from editor.isActive('code'). Toggles the INLINE code mark, NOT the codeBlock node.",
      "docDelta": "code mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\texttt{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-e (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-e keystroke) both bottom out in toggleCode so they produce a byte-identical code mark / identical \\texttt{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-e is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). NAMING TRAP - id 'code' is the inline code MARK, easily confused with the codeBlock text-object kind. Drive live to confirm the grid cell toggles the inline mark and never wraps a codeBlock, and that Mod-e does the same. Note the codeBlock kind below is applies:'ok' for this row because the INLINE mark toggles inside a code block (a near-no-op), distinct from the codeBlock NODE."
  },
  {
    "id": "code@exampleItem#lightning",
    "category": "Format marks",
    "action": "code",
    "kind": "exampleItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret inside example-item text",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (CODE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the inline code mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleCode().run(). Grid active-state from editor.isActive('code'). Toggles the INLINE code mark, NOT the codeBlock node.",
      "docDelta": "code mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\texttt{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-e (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-e keystroke) both bottom out in toggleCode so they produce a byte-identical code mark / identical \\texttt{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-e is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). NAMING TRAP - id 'code' is the inline code MARK, easily confused with the codeBlock text-object kind. Drive live to confirm the grid cell toggles the inline mark and never wraps a codeBlock, and that Mod-e does the same. Note the codeBlock kind below is applies:'ok' for this row because the INLINE mark toggles inside a code block (a near-no-op), distinct from the codeBlock NODE."
  },
  {
    "id": "code@heading#lightning",
    "category": "Format marks",
    "action": "code",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret/selection inside heading text - mark toggle valid; no kind filter",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (CODE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the inline code mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleCode().run(). Grid active-state from editor.isActive('code'). Toggles the INLINE code mark, NOT the codeBlock node.",
      "docDelta": "code mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\texttt{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-e (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-e keystroke) both bottom out in toggleCode so they produce a byte-identical code mark / identical \\texttt{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-e is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). NAMING TRAP - id 'code' is the inline code MARK, easily confused with the codeBlock text-object kind. Drive live to confirm the grid cell toggles the inline mark and never wraps a codeBlock, and that Mod-e does the same. Note the codeBlock kind below is applies:'ok' for this row because the INLINE mark toggles inside a code block (a near-no-op), distinct from the codeBlock NODE."
  },
  {
    "id": "code@listItem#lightning",
    "category": "Format marks",
    "action": "code",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret inside list-item text",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (CODE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the inline code mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleCode().run(). Grid active-state from editor.isActive('code'). Toggles the INLINE code mark, NOT the codeBlock node.",
      "docDelta": "code mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\texttt{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-e (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-e keystroke) both bottom out in toggleCode so they produce a byte-identical code mark / identical \\texttt{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-e is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). NAMING TRAP - id 'code' is the inline code MARK, easily confused with the codeBlock text-object kind. Drive live to confirm the grid cell toggles the inline mark and never wraps a codeBlock, and that Mod-e does the same. Note the codeBlock kind below is applies:'ok' for this row because the INLINE mark toggles inside a code block (a near-no-op), distinct from the codeBlock NODE."
  },
  {
    "id": "code@paragraph#lightning",
    "category": "Format marks",
    "action": "code",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "a collapsed caret in a paragraph still toggles the STORED mark",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (CODE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the inline code mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleCode().run(). Grid active-state from editor.isActive('code'). Toggles the INLINE code mark, NOT the codeBlock node.",
      "docDelta": "code mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\texttt{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-e (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-e keystroke) both bottom out in toggleCode so they produce a byte-identical code mark / identical \\texttt{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-e is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). NAMING TRAP - id 'code' is the inline code MARK, easily confused with the codeBlock text-object kind. Drive live to confirm the grid cell toggles the inline mark and never wraps a codeBlock, and that Mod-e does the same. Note the codeBlock kind below is applies:'ok' for this row because the INLINE mark toggles inside a code block (a near-no-op), distinct from the codeBlock NODE."
  },
  {
    "id": "code@selection#lightning",
    "category": "Format marks",
    "action": "code",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "selection:'ignored' - range-agnostic; gateApplies base 'ok'",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (CODE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the inline code mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleCode().run(). Grid active-state from editor.isActive('code'). Toggles the INLINE code mark, NOT the codeBlock node.",
      "docDelta": "code mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\texttt{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-e (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-e keystroke) both bottom out in toggleCode so they produce a byte-identical code mark / identical \\texttt{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-e is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). NAMING TRAP - id 'code' is the inline code MARK, easily confused with the codeBlock text-object kind. Drive live to confirm the grid cell toggles the inline mark and never wraps a codeBlock, and that Mod-e does the same. Note the codeBlock kind below is applies:'ok' for this row because the INLINE mark toggles inside a code block (a near-no-op), distinct from the codeBlock NODE."
  },
  {
    "id": "code@titleField#lightning",
    "category": "Format marks",
    "action": "code",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret in title text - mark would toggle; applies() unconditionally ok",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (CODE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the inline code mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleCode().run(). Grid active-state from editor.isActive('code'). Toggles the INLINE code mark, NOT the codeBlock node.",
      "docDelta": "code mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\texttt{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-e (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-e keystroke) both bottom out in toggleCode so they produce a byte-identical code mark / identical \\texttt{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-e is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). NAMING TRAP - id 'code' is the inline code MARK, easily confused with the codeBlock text-object kind. Drive live to confirm the grid cell toggles the inline mark and never wraps a codeBlock, and that Mod-e does the same. Note the codeBlock kind below is applies:'ok' for this row because the INLINE mark toggles inside a code block (a near-no-op), distinct from the codeBlock NODE."
  },
  {
    "id": "italic@atom-bearing:citation#lightning",
    "category": "Format marks",
    "action": "italic",
    "kind": "atom-bearing:citation",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "selection spanning text+atom toggles the mark on the text runs only",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (ITALIC_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the italic (em) mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleItalic().run(). Grid active-state from editor.isActive('italic').",
      "docDelta": "italic mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textit{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-i (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-i keystroke) both bottom out in toggleItalic so they produce a byte-identical italic mark / identical \\textit{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-i is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Lower-risk than the lifecycle actions; drive once to confirm grid==keystroke parity and the collab gate on both."
  },
  {
    "id": "italic@atom-only:inline-math#lightning",
    "category": "Format marks",
    "action": "italic",
    "kind": "atom-only:inline-math",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "formatApplies never inspects content; toggling a mark on a math-only line is a PM no-op but applies() still says ok",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (ITALIC_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the italic (em) mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleItalic().run(). Grid active-state from editor.isActive('italic').",
      "docDelta": "italic mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textit{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-i (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-i keystroke) both bottom out in toggleItalic so they produce a byte-identical italic mark / identical \\textit{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-i is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Lower-risk than the lifecycle actions; drive once to confirm grid==keystroke parity and the collab gate on both."
  },
  {
    "id": "italic@blockquote#lightning",
    "category": "Format marks",
    "action": "italic",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (ITALIC_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the italic (em) mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleItalic().run(). Grid active-state from editor.isActive('italic').",
      "docDelta": "italic mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textit{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-i (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-i keystroke) both bottom out in toggleItalic so they produce a byte-identical italic mark / identical \\textit{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-i is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Lower-risk than the lifecycle actions; drive once to confirm grid==keystroke parity and the collab gate on both."
  },
  {
    "id": "italic@exampleItem#lightning",
    "category": "Format marks",
    "action": "italic",
    "kind": "exampleItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret inside example-item text",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (ITALIC_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the italic (em) mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleItalic().run(). Grid active-state from editor.isActive('italic').",
      "docDelta": "italic mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textit{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-i (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-i keystroke) both bottom out in toggleItalic so they produce a byte-identical italic mark / identical \\textit{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-i is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Lower-risk than the lifecycle actions; drive once to confirm grid==keystroke parity and the collab gate on both."
  },
  {
    "id": "italic@heading#lightning",
    "category": "Format marks",
    "action": "italic",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret/selection inside heading text - mark toggle valid; no kind filter",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (ITALIC_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the italic (em) mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleItalic().run(). Grid active-state from editor.isActive('italic').",
      "docDelta": "italic mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textit{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-i (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-i keystroke) both bottom out in toggleItalic so they produce a byte-identical italic mark / identical \\textit{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-i is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Lower-risk than the lifecycle actions; drive once to confirm grid==keystroke parity and the collab gate on both."
  },
  {
    "id": "italic@listItem#lightning",
    "category": "Format marks",
    "action": "italic",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret inside list-item text",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (ITALIC_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the italic (em) mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleItalic().run(). Grid active-state from editor.isActive('italic').",
      "docDelta": "italic mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textit{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-i (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-i keystroke) both bottom out in toggleItalic so they produce a byte-identical italic mark / identical \\textit{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-i is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Lower-risk than the lifecycle actions; drive once to confirm grid==keystroke parity and the collab gate on both."
  },
  {
    "id": "italic@paragraph#lightning",
    "category": "Format marks",
    "action": "italic",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "a collapsed caret in a paragraph still toggles the STORED mark",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (ITALIC_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the italic (em) mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleItalic().run(). Grid active-state from editor.isActive('italic').",
      "docDelta": "italic mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textit{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-i (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-i keystroke) both bottom out in toggleItalic so they produce a byte-identical italic mark / identical \\textit{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-i is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Lower-risk than the lifecycle actions; drive once to confirm grid==keystroke parity and the collab gate on both."
  },
  {
    "id": "italic@selection#lightning",
    "category": "Format marks",
    "action": "italic",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "selection:'ignored' - range-agnostic; gateApplies base 'ok'",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (ITALIC_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the italic (em) mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleItalic().run(). Grid active-state from editor.isActive('italic').",
      "docDelta": "italic mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textit{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-i (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-i keystroke) both bottom out in toggleItalic so they produce a byte-identical italic mark / identical \\textit{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-i is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Lower-risk than the lifecycle actions; drive once to confirm grid==keystroke parity and the collab gate on both."
  },
  {
    "id": "italic@titleField#lightning",
    "category": "Format marks",
    "action": "italic",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret in title text - mark would toggle; applies() unconditionally ok",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (ITALIC_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the italic (em) mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleItalic().run(). Grid active-state from editor.isActive('italic').",
      "docDelta": "italic mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\textit{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-i (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-i keystroke) both bottom out in toggleItalic so they produce a byte-identical italic mark / identical \\textit{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-i is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Lower-risk than the lifecycle actions; drive once to confirm grid==keystroke parity and the collab gate on both."
  },
  {
    "id": "ordered-list@blockquote#lightning",
    "category": "Format marks",
    "action": "ordered-list",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (ORDERED_LIST_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the orderedList structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleOrderedList().run(). On a plain block it wraps; on an existing orderedList it lifts items back to paragraphs (toggle). Grid active-state from editor.isActive('orderedList').",
      "docDelta": "current block(s) wrapped into a orderedList (listItem children) - or, on an existing orderedList, lifted back to paragraphs. Structural node change (wrap/lift), not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{enumerate}...\\item...\\end{enumerate} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and markdown input rules ('- '/'1. ') which call the SAME toggleOrderedList - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleOrderedList; identical structural result. Only the grid path goes through the registry collab guard; the keystroke/input-rule is gated by readOnlyEnforcer.",
    "knownRisk": "RECONSTRUCTED-TAIL (the bullet-list/ordered-list/blockquote rows were truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleOrderedList may be a structural no-op there - the applies()-vs-effect divergence (an enabled cell that does nothing). Drive a orderedList-toggle on a heading and on a titleField live to see whether the cell should be greyed. Also: the markdown input rule ('- '/'1. ') is a SECOND live surface not in the registry - confirm it produces the same structure as the grid."
  },
  {
    "id": "ordered-list@bulletList#lightning",
    "category": "Format marks",
    "action": "ordered-list",
    "kind": "bulletList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "converts bullet->ordered (toggle behavior)",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (ORDERED_LIST_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the orderedList structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleOrderedList().run(). On a plain block it wraps; on an existing orderedList it lifts items back to paragraphs (toggle). Grid active-state from editor.isActive('orderedList').",
      "docDelta": "current block(s) wrapped into a orderedList (listItem children) - or, on an existing orderedList, lifted back to paragraphs. Structural node change (wrap/lift), not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{enumerate}...\\item...\\end{enumerate} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and markdown input rules ('- '/'1. ') which call the SAME toggleOrderedList - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleOrderedList; identical structural result. Only the grid path goes through the registry collab guard; the keystroke/input-rule is gated by readOnlyEnforcer.",
    "knownRisk": "RECONSTRUCTED-TAIL (the bullet-list/ordered-list/blockquote rows were truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleOrderedList may be a structural no-op there - the applies()-vs-effect divergence (an enabled cell that does nothing). Drive a orderedList-toggle on a heading and on a titleField live to see whether the cell should be greyed. Also: the markdown input rule ('- '/'1. ') is a SECOND live surface not in the registry - confirm it produces the same structure as the grid."
  },
  {
    "id": "ordered-list@heading#lightning",
    "category": "Format marks",
    "action": "ordered-list",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "applies() unconditionally ok; toggleOrderedList may fail to wrap a heading but applies() does not gate it - possible divergence",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (ORDERED_LIST_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the orderedList structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleOrderedList().run(). On a plain block it wraps; on an existing orderedList it lifts items back to paragraphs (toggle). Grid active-state from editor.isActive('orderedList').",
      "docDelta": "current block(s) wrapped into a orderedList (listItem children) - or, on an existing orderedList, lifted back to paragraphs. Structural node change (wrap/lift), not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{enumerate}...\\item...\\end{enumerate} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and markdown input rules ('- '/'1. ') which call the SAME toggleOrderedList - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleOrderedList; identical structural result. Only the grid path goes through the registry collab guard; the keystroke/input-rule is gated by readOnlyEnforcer.",
    "knownRisk": "RECONSTRUCTED-TAIL (the bullet-list/ordered-list/blockquote rows were truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleOrderedList may be a structural no-op there - the applies()-vs-effect divergence (an enabled cell that does nothing). Drive a orderedList-toggle on a heading and on a titleField live to see whether the cell should be greyed. Also: the markdown input rule ('- '/'1. ') is a SECOND live surface not in the registry - confirm it produces the same structure as the grid."
  },
  {
    "id": "ordered-list@listItem#lightning",
    "category": "Format marks",
    "action": "ordered-list",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret inside an item",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (ORDERED_LIST_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the orderedList structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleOrderedList().run(). On a plain block it wraps; on an existing orderedList it lifts items back to paragraphs (toggle). Grid active-state from editor.isActive('orderedList').",
      "docDelta": "current block(s) wrapped into a orderedList (listItem children) - or, on an existing orderedList, lifted back to paragraphs. Structural node change (wrap/lift), not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{enumerate}...\\item...\\end{enumerate} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and markdown input rules ('- '/'1. ') which call the SAME toggleOrderedList - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleOrderedList; identical structural result. Only the grid path goes through the registry collab guard; the keystroke/input-rule is gated by readOnlyEnforcer.",
    "knownRisk": "RECONSTRUCTED-TAIL (the bullet-list/ordered-list/blockquote rows were truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleOrderedList may be a structural no-op there - the applies()-vs-effect divergence (an enabled cell that does nothing). Drive a orderedList-toggle on a heading and on a titleField live to see whether the cell should be greyed. Also: the markdown input rule ('- '/'1. ') is a SECOND live surface not in the registry - confirm it produces the same structure as the grid."
  },
  {
    "id": "ordered-list@orderedList#lightning",
    "category": "Format marks",
    "action": "ordered-list",
    "kind": "orderedList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "toggle OFF - lifts items back to paragraphs; toggleOrderedList is a toggle",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (ORDERED_LIST_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the orderedList structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleOrderedList().run(). On a plain block it wraps; on an existing orderedList it lifts items back to paragraphs (toggle). Grid active-state from editor.isActive('orderedList').",
      "docDelta": "current block(s) wrapped into a orderedList (listItem children) - or, on an existing orderedList, lifted back to paragraphs. Structural node change (wrap/lift), not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{enumerate}...\\item...\\end{enumerate} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and markdown input rules ('- '/'1. ') which call the SAME toggleOrderedList - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleOrderedList; identical structural result. Only the grid path goes through the registry collab guard; the keystroke/input-rule is gated by readOnlyEnforcer.",
    "knownRisk": "RECONSTRUCTED-TAIL (the bullet-list/ordered-list/blockquote rows were truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleOrderedList may be a structural no-op there - the applies()-vs-effect divergence (an enabled cell that does nothing). Drive a orderedList-toggle on a heading and on a titleField live to see whether the cell should be greyed. Also: the markdown input rule ('- '/'1. ') is a SECOND live surface not in the registry - confirm it produces the same structure as the grid."
  },
  {
    "id": "ordered-list@paragraph#lightning",
    "category": "Format marks",
    "action": "ordered-list",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "wraps the current block into a orderedList; valid at a collapsed caret (selection:'ignored')",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (ORDERED_LIST_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the orderedList structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleOrderedList().run(). On a plain block it wraps; on an existing orderedList it lifts items back to paragraphs (toggle). Grid active-state from editor.isActive('orderedList').",
      "docDelta": "current block(s) wrapped into a orderedList (listItem children) - or, on an existing orderedList, lifted back to paragraphs. Structural node change (wrap/lift), not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{enumerate}...\\item...\\end{enumerate} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and markdown input rules ('- '/'1. ') which call the SAME toggleOrderedList - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleOrderedList; identical structural result. Only the grid path goes through the registry collab guard; the keystroke/input-rule is gated by readOnlyEnforcer.",
    "knownRisk": "RECONSTRUCTED-TAIL (the bullet-list/ordered-list/blockquote rows were truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleOrderedList may be a structural no-op there - the applies()-vs-effect divergence (an enabled cell that does nothing). Drive a orderedList-toggle on a heading and on a titleField live to see whether the cell should be greyed. Also: the markdown input rule ('- '/'1. ') is a SECOND live surface not in the registry - confirm it produces the same structure as the grid."
  },
  {
    "id": "ordered-list@selection#lightning",
    "category": "Format marks",
    "action": "ordered-list",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "wraps spanned blocks",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (ORDERED_LIST_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the orderedList structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleOrderedList().run(). On a plain block it wraps; on an existing orderedList it lifts items back to paragraphs (toggle). Grid active-state from editor.isActive('orderedList').",
      "docDelta": "current block(s) wrapped into a orderedList (listItem children) - or, on an existing orderedList, lifted back to paragraphs. Structural node change (wrap/lift), not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{enumerate}...\\item...\\end{enumerate} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and markdown input rules ('- '/'1. ') which call the SAME toggleOrderedList - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleOrderedList; identical structural result. Only the grid path goes through the registry collab guard; the keystroke/input-rule is gated by readOnlyEnforcer.",
    "knownRisk": "RECONSTRUCTED-TAIL (the bullet-list/ordered-list/blockquote rows were truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleOrderedList may be a structural no-op there - the applies()-vs-effect divergence (an enabled cell that does nothing). Drive a orderedList-toggle on a heading and on a titleField live to see whether the cell should be greyed. Also: the markdown input rule ('- '/'1. ') is a SECOND live surface not in the registry - confirm it produces the same structure as the grid."
  },
  {
    "id": "ordered-list@titleField#lightning",
    "category": "Format marks",
    "action": "ordered-list",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "applies() ok, but wrapping the title field is a structural no-op/odd; applies() does not gate it",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (ORDERED_LIST_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the orderedList structure. run() guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleOrderedList().run(). On a plain block it wraps; on an existing orderedList it lifts items back to paragraphs (toggle). Grid active-state from editor.isActive('orderedList').",
      "docDelta": "current block(s) wrapped into a orderedList (listItem children) - or, on an existing orderedList, lifted back to paragraphs. Structural node change (wrap/lift), not a mark.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection's blocks serialized into \\begin{enumerate}...\\item...\\end{enumerate} (or unwrapped) on the next .tex sync; no marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; StarterKit also binds the live keyboard shortcut and markdown input rules ('- '/'1. ') which call the SAME toggleOrderedList - NOT modeled on the ActionSpec. Grid run() and the keystroke/input-rule both bottom out in toggleOrderedList; identical structural result. Only the grid path goes through the registry collab guard; the keystroke/input-rule is gated by readOnlyEnforcer.",
    "knownRisk": "RECONSTRUCTED-TAIL (the bullet-list/ordered-list/blockquote rows were truncated in the source oracle JSON - reconstructed from the shared formatToggleRow shape; verify field-by-field against action-registry.ts before relying on the doc). Concern: applies() is unconditionally 'ok' for heading/titleField but toggleOrderedList may be a structural no-op there - the applies()-vs-effect divergence (an enabled cell that does nothing). Drive a orderedList-toggle on a heading and on a titleField live to see whether the cell should be greyed. Also: the markdown input rule ('- '/'1. ') is a SECOND live surface not in the registry - confirm it produces the same structure as the grid."
  },
  {
    "id": "strike@atom-bearing:citation#lightning",
    "category": "Format marks",
    "action": "strike",
    "kind": "atom-bearing:citation",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "selection spanning text+atom toggles the mark on the text runs only",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (STRIKE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the strikethrough mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleStrike().run(). Grid active-state from editor.isActive('strike').",
      "docDelta": "strike mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\sout{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-Shift-s (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-Shift-s keystroke) both bottom out in toggleStrike so they produce a byte-identical strike mark / identical \\sout{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-Shift-s is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Strike has NO grid keybinding hint in the cell title (ActionsMenuPanel:432, no shortcut shown), unlike Bold/Italic - minor UI inconsistency, not a behavioral bug."
  },
  {
    "id": "strike@atom-only:inline-math#lightning",
    "category": "Format marks",
    "action": "strike",
    "kind": "atom-only:inline-math",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "formatApplies never inspects content; toggling a mark on a math-only line is a PM no-op but applies() still says ok",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (STRIKE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the strikethrough mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleStrike().run(). Grid active-state from editor.isActive('strike').",
      "docDelta": "strike mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\sout{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-Shift-s (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-Shift-s keystroke) both bottom out in toggleStrike so they produce a byte-identical strike mark / identical \\sout{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-Shift-s is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Strike has NO grid keybinding hint in the cell title (ActionsMenuPanel:432, no shortcut shown), unlike Bold/Italic - minor UI inconsistency, not a behavioral bug."
  },
  {
    "id": "strike@blockquote#lightning",
    "category": "Format marks",
    "action": "strike",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (STRIKE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the strikethrough mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleStrike().run(). Grid active-state from editor.isActive('strike').",
      "docDelta": "strike mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\sout{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-Shift-s (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-Shift-s keystroke) both bottom out in toggleStrike so they produce a byte-identical strike mark / identical \\sout{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-Shift-s is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Strike has NO grid keybinding hint in the cell title (ActionsMenuPanel:432, no shortcut shown), unlike Bold/Italic - minor UI inconsistency, not a behavioral bug."
  },
  {
    "id": "strike@exampleItem#lightning",
    "category": "Format marks",
    "action": "strike",
    "kind": "exampleItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret inside example-item text",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (STRIKE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the strikethrough mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleStrike().run(). Grid active-state from editor.isActive('strike').",
      "docDelta": "strike mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\sout{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-Shift-s (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-Shift-s keystroke) both bottom out in toggleStrike so they produce a byte-identical strike mark / identical \\sout{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-Shift-s is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Strike has NO grid keybinding hint in the cell title (ActionsMenuPanel:432, no shortcut shown), unlike Bold/Italic - minor UI inconsistency, not a behavioral bug."
  },
  {
    "id": "strike@heading#lightning",
    "category": "Format marks",
    "action": "strike",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret/selection inside heading text - mark toggle valid; no kind filter",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (STRIKE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the strikethrough mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleStrike().run(). Grid active-state from editor.isActive('strike').",
      "docDelta": "strike mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\sout{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-Shift-s (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-Shift-s keystroke) both bottom out in toggleStrike so they produce a byte-identical strike mark / identical \\sout{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-Shift-s is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Strike has NO grid keybinding hint in the cell title (ActionsMenuPanel:432, no shortcut shown), unlike Bold/Italic - minor UI inconsistency, not a behavioral bug."
  },
  {
    "id": "strike@listItem#lightning",
    "category": "Format marks",
    "action": "strike",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret inside list-item text",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (STRIKE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the strikethrough mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleStrike().run(). Grid active-state from editor.isActive('strike').",
      "docDelta": "strike mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\sout{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-Shift-s (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-Shift-s keystroke) both bottom out in toggleStrike so they produce a byte-identical strike mark / identical \\sout{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-Shift-s is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Strike has NO grid keybinding hint in the cell title (ActionsMenuPanel:432, no shortcut shown), unlike Bold/Italic - minor UI inconsistency, not a behavioral bug."
  },
  {
    "id": "strike@paragraph#lightning",
    "category": "Format marks",
    "action": "strike",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "a collapsed caret in a paragraph still toggles the STORED mark",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (STRIKE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the strikethrough mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleStrike().run(). Grid active-state from editor.isActive('strike').",
      "docDelta": "strike mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\sout{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-Shift-s (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-Shift-s keystroke) both bottom out in toggleStrike so they produce a byte-identical strike mark / identical \\sout{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-Shift-s is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Strike has NO grid keybinding hint in the cell title (ActionsMenuPanel:432, no shortcut shown), unlike Bold/Italic - minor UI inconsistency, not a behavioral bug."
  },
  {
    "id": "strike@selection#lightning",
    "category": "Format marks",
    "action": "strike",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "selection:'ignored' - range-agnostic; gateApplies base 'ok'",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (STRIKE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the strikethrough mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleStrike().run(). Grid active-state from editor.isActive('strike').",
      "docDelta": "strike mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\sout{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-Shift-s (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-Shift-s keystroke) both bottom out in toggleStrike so they produce a byte-identical strike mark / identical \\sout{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-Shift-s is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Strike has NO grid keybinding hint in the cell title (ActionsMenuPanel:432, no shortcut shown), unlike Bold/Italic - minor UI inconsistency, not a behavioral bug."
  },
  {
    "id": "strike@titleField#lightning",
    "category": "Format marks",
    "action": "strike",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret in title text - mark would toggle; applies() unconditionally ok",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (STRIKE_ACTION_ROW = formatToggleRow); shared run :2140-2157",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Toggle the strikethrough mark. On a non-empty selection wraps/unwraps the range; at a collapsed caret flips the pending STORED mark. run() first guards isCollabReadOnly (no-op when canEdit===false), then editor.chain().focus().toggleStrike().run(). Grid active-state from editor.isActive('strike').",
      "docDelta": "strike mark added/removed on the selected inline range (or stored-mark toggled, no doc change, at a caret). No block added/removed/converted.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection serialized to \\sout{...} (or unwrapped) on the next .tex sync via code-pane-bridge; no atom marker, no %!v marker.",
      "lifecycle": "no card produced."
    },
    "crossSurfaceIdentity": "Registry exposes ONLY the lightning surface; the live keyboard binding Mod-Shift-s (StarterKit) is NOT modeled on the ActionSpec. The two live paths (grid cell run() and the Mod-Shift-s keystroke) both bottom out in toggleStrike so they produce a byte-identical strike mark / identical \\sout{...} serialization - but ONLY the grid path goes through the registry's collab guard; the keystroke is gated separately by the readOnlyEnforcer plugin filterTransaction (editor-extensions.ts:1839).",
    "knownRisk": "Keyboard-surface omission: Mod-Shift-s is live at runtime but surfaces.keyboard is FALSE (coverage assertion at registry:2875-2884 forbids surfaces.keyboard on format rows - keybindings are owned by StarterKit). Verify the keystroke path and the grid path stay behaviorally identical AND that collab read-only blocks BOTH (grid via run() guard, keyboard via readOnlyEnforcer). Strike has NO grid keybinding hint in the cell title (ActionsMenuPanel:432, no shortcut shown), unlike Bold/Italic - minor UI inconsistency, not a behavioral bug."
  },
  {
    "id": "text-color@blockquote#lightning",
    "category": "Format marks",
    "action": "text-color",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (TEXT_COLOR_ACTION_ROW); grid seam ctx.openColorPopover (ActionsMenuPanel runGridAction)",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Opens the color popover (ctx.openColorPopover) anchored to the selection/caret - it does NOT apply a color itself. On a user pick, the popover applies a textStyle color mark over the range (or sets the stored mark at a caret). run() guards isCollabReadOnly.",
      "docDelta": "On user pick: a textStyle/color mark added/removed on the selected inline range (or stored-mark at a caret). run() ALONE opens the popover, no docDelta until the pick.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection wrapped in the project's color macro (e.g. \\textcolor{...}{...}) on the next .tex sync; no marker.",
      "lifecycle": "no card produced. Transient color-popover open/close state only."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today - no cross-surface byte-identity to enforce. The popover seam (ctx.openColorPopover) is the single creator; any future surface must route through the SAME seam.",
    "knownRisk": "RECONSTRUCTED-TAIL (the text-color row was beyond the truncation point in the source oracle JSON - reconstructed from the figure/graphics popover-routed pattern + the runGridAction color seam; verify the exact run()/seam name and tex macro against action-registry.ts + the color popover before relying on the doc). Concern: like ref/figure, the popover-open is RAF/event-driven - drive live in the backgrounded preview (shim rAF->setTimeout) and confirm the color mark round-trips through .tex."
  },
  {
    "id": "text-color@exampleItem#lightning",
    "category": "Format marks",
    "action": "text-color",
    "kind": "exampleItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (TEXT_COLOR_ACTION_ROW); grid seam ctx.openColorPopover (ActionsMenuPanel runGridAction)",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Opens the color popover (ctx.openColorPopover) anchored to the selection/caret - it does NOT apply a color itself. On a user pick, the popover applies a textStyle color mark over the range (or sets the stored mark at a caret). run() guards isCollabReadOnly.",
      "docDelta": "On user pick: a textStyle/color mark added/removed on the selected inline range (or stored-mark at a caret). run() ALONE opens the popover, no docDelta until the pick.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection wrapped in the project's color macro (e.g. \\textcolor{...}{...}) on the next .tex sync; no marker.",
      "lifecycle": "no card produced. Transient color-popover open/close state only."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today - no cross-surface byte-identity to enforce. The popover seam (ctx.openColorPopover) is the single creator; any future surface must route through the SAME seam.",
    "knownRisk": "RECONSTRUCTED-TAIL (the text-color row was beyond the truncation point in the source oracle JSON - reconstructed from the figure/graphics popover-routed pattern + the runGridAction color seam; verify the exact run()/seam name and tex macro against action-registry.ts + the color popover before relying on the doc). Concern: like ref/figure, the popover-open is RAF/event-driven - drive live in the backgrounded preview (shim rAF->setTimeout) and confirm the color mark round-trips through .tex."
  },
  {
    "id": "text-color@heading#lightning",
    "category": "Format marks",
    "action": "text-color",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret/selection in heading text",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (TEXT_COLOR_ACTION_ROW); grid seam ctx.openColorPopover (ActionsMenuPanel runGridAction)",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Opens the color popover (ctx.openColorPopover) anchored to the selection/caret - it does NOT apply a color itself. On a user pick, the popover applies a textStyle color mark over the range (or sets the stored mark at a caret). run() guards isCollabReadOnly.",
      "docDelta": "On user pick: a textStyle/color mark added/removed on the selected inline range (or stored-mark at a caret). run() ALONE opens the popover, no docDelta until the pick.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection wrapped in the project's color macro (e.g. \\textcolor{...}{...}) on the next .tex sync; no marker.",
      "lifecycle": "no card produced. Transient color-popover open/close state only."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today - no cross-surface byte-identity to enforce. The popover seam (ctx.openColorPopover) is the single creator; any future surface must route through the SAME seam.",
    "knownRisk": "RECONSTRUCTED-TAIL (the text-color row was beyond the truncation point in the source oracle JSON - reconstructed from the figure/graphics popover-routed pattern + the runGridAction color seam; verify the exact run()/seam name and tex macro against action-registry.ts + the color popover before relying on the doc). Concern: like ref/figure, the popover-open is RAF/event-driven - drive live in the backgrounded preview (shim rAF->setTimeout) and confirm the color mark round-trips through .tex."
  },
  {
    "id": "text-color@listItem#lightning",
    "category": "Format marks",
    "action": "text-color",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (TEXT_COLOR_ACTION_ROW); grid seam ctx.openColorPopover (ActionsMenuPanel runGridAction)",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Opens the color popover (ctx.openColorPopover) anchored to the selection/caret - it does NOT apply a color itself. On a user pick, the popover applies a textStyle color mark over the range (or sets the stored mark at a caret). run() guards isCollabReadOnly.",
      "docDelta": "On user pick: a textStyle/color mark added/removed on the selected inline range (or stored-mark at a caret). run() ALONE opens the popover, no docDelta until the pick.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection wrapped in the project's color macro (e.g. \\textcolor{...}{...}) on the next .tex sync; no marker.",
      "lifecycle": "no card produced. Transient color-popover open/close state only."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today - no cross-surface byte-identity to enforce. The popover seam (ctx.openColorPopover) is the single creator; any future surface must route through the SAME seam.",
    "knownRisk": "RECONSTRUCTED-TAIL (the text-color row was beyond the truncation point in the source oracle JSON - reconstructed from the figure/graphics popover-routed pattern + the runGridAction color seam; verify the exact run()/seam name and tex macro against action-registry.ts + the color popover before relying on the doc). Concern: like ref/figure, the popover-open is RAF/event-driven - drive live in the backgrounded preview (shim rAF->setTimeout) and confirm the color mark round-trips through .tex."
  },
  {
    "id": "text-color@paragraph#lightning",
    "category": "Format marks",
    "action": "text-color",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "caret/selection in a paragraph",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (TEXT_COLOR_ACTION_ROW); grid seam ctx.openColorPopover (ActionsMenuPanel runGridAction)",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Opens the color popover (ctx.openColorPopover) anchored to the selection/caret - it does NOT apply a color itself. On a user pick, the popover applies a textStyle color mark over the range (or sets the stored mark at a caret). run() guards isCollabReadOnly.",
      "docDelta": "On user pick: a textStyle/color mark added/removed on the selected inline range (or stored-mark at a caret). run() ALONE opens the popover, no docDelta until the pick.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection wrapped in the project's color macro (e.g. \\textcolor{...}{...}) on the next .tex sync; no marker.",
      "lifecycle": "no card produced. Transient color-popover open/close state only."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today - no cross-surface byte-identity to enforce. The popover seam (ctx.openColorPopover) is the single creator; any future surface must route through the SAME seam.",
    "knownRisk": "RECONSTRUCTED-TAIL (the text-color row was beyond the truncation point in the source oracle JSON - reconstructed from the figure/graphics popover-routed pattern + the runGridAction color seam; verify the exact run()/seam name and tex macro against action-registry.ts + the color popover before relying on the doc). Concern: like ref/figure, the popover-open is RAF/event-driven - drive live in the backgrounded preview (shim rAF->setTimeout) and confirm the color mark round-trips through .tex."
  },
  {
    "id": "text-color@selection#lightning",
    "category": "Format marks",
    "action": "text-color",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "selection:'ignored' - the color popover applies a textStyle color mark to the range/caret",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (TEXT_COLOR_ACTION_ROW); grid seam ctx.openColorPopover (ActionsMenuPanel runGridAction)",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Opens the color popover (ctx.openColorPopover) anchored to the selection/caret - it does NOT apply a color itself. On a user pick, the popover applies a textStyle color mark over the range (or sets the stored mark at a caret). run() guards isCollabReadOnly.",
      "docDelta": "On user pick: a textStyle/color mark added/removed on the selected inline range (or stored-mark at a caret). run() ALONE opens the popover, no docDelta until the pick.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection wrapped in the project's color macro (e.g. \\textcolor{...}{...}) on the next .tex sync; no marker.",
      "lifecycle": "no card produced. Transient color-popover open/close state only."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today - no cross-surface byte-identity to enforce. The popover seam (ctx.openColorPopover) is the single creator; any future surface must route through the SAME seam.",
    "knownRisk": "RECONSTRUCTED-TAIL (the text-color row was beyond the truncation point in the source oracle JSON - reconstructed from the figure/graphics popover-routed pattern + the runGridAction color seam; verify the exact run()/seam name and tex macro against action-registry.ts + the color popover before relying on the doc). Concern: like ref/figure, the popover-open is RAF/event-driven - drive live in the backgrounded preview (shim rAF->setTimeout) and confirm the color mark round-trips through .tex."
  },
  {
    "id": "text-color@titleField#lightning",
    "category": "Format marks",
    "action": "text-color",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "applies() unconditionally ok",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts (TEXT_COLOR_ACTION_ROW); grid seam ctx.openColorPopover (ActionsMenuPanel runGridAction)",
    "backbone": "tiptap-chain",
    "expected": {
      "behavior": "Opens the color popover (ctx.openColorPopover) anchored to the selection/caret - it does NOT apply a color itself. On a user pick, the popover applies a textStyle color mark over the range (or sets the stored mark at a caret). run() guards isCollabReadOnly.",
      "docDelta": "On user pick: a textStyle/color mark added/removed on the selected inline range (or stored-mark at a caret). run() ALONE opens the popover, no docDelta until the pick.",
      "sidecarDelta": "none - backbone tiptap-chain, no sidecar.",
      "texDelta": "selection wrapped in the project's color macro (e.g. \\textcolor{...}{...}) on the next .tex sync; no marker.",
      "lifecycle": "no card produced. Transient color-popover open/close state only."
    },
    "crossSurfaceIdentity": "Single-surface (lightning only) today - no cross-surface byte-identity to enforce. The popover seam (ctx.openColorPopover) is the single creator; any future surface must route through the SAME seam.",
    "knownRisk": "RECONSTRUCTED-TAIL (the text-color row was beyond the truncation point in the source oracle JSON - reconstructed from the figure/graphics popover-routed pattern + the runGridAction color seam; verify the exact run()/seam name and tex macro against action-registry.ts + the color popover before relying on the doc). Concern: like ref/figure, the popover-open is RAF/event-driven - drive live in the backgrounded preview (shim rAF->setTimeout) and confirm the color mark round-trips through .tex."
  },
  {
    "id": "note@blockquote#grab",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "blockquote",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@blockquote#lightning",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@bulletList#grab",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "bulletList",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@bulletList#lightning",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "bulletList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@codeBlock#grab",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "codeBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@codeBlock#lightning",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "codeBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@displayMath#grab",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "displayMath",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@displayMath#lightning",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@exampleItem#grab",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "exampleItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@exampleItem#lightning",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "exampleItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@figureBlock#grab",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "figureBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@figureBlock#lightning",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "figureBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@graphicsBlock#grab",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "graphicsBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@graphicsBlock#lightning",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@heading#grab",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "heading",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "annotation -> heading line",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@heading#lightning",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "annotation -> heading line",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@latexComment#grab",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "latexComment",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@latexComment#lightning",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@linkedRange#grab",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "linkedRange",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "LINKED_RANGE_ACTIONS includes note",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@linkedRange#lightning",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "linkedRange",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "LINKED_RANGE_ACTIONS includes note",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@listItem#grab",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "listItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@listItem#lightning",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@orderedList#grab",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "orderedList",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@orderedList#lightning",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "orderedList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@paragraph#grab",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "paragraph",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "block ref -> Mode-A paragraph anchor, NO mark",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@paragraph#lightning",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "block ref -> Mode-A paragraph anchor, NO mark",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@selection#grab",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "selection",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "wantRangeAnchor -> drops a linkedAnchor mark + binds note",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@selection#lightning",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "wantRangeAnchor -> drops a linkedAnchor mark + binds note",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@texBlock#grab",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "texBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@texBlock#lightning",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@titleField#grab",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "titleField",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "note@titleField#lightning",
    "category": "Card actions (11)",
    "action": "note",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:296 (case \"note\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a UserNote anchored to the paragraph (Mode-A) or, for a range source (selection/linkedRange with text), drops a 'note' linkedAnchor and binds the card to it. Selects+pins in the notes panel.",
      "docDelta": "for a range source: linkedAnchor mark added; for a block source: no doc change (Mode-A link only)",
      "sidecarDelta": "notes.json +1 UserNote; targetKind recorded (paragraph for selection/linkedRange, the ref kind otherwise)",
      "texDelta": "range source -> \\vlid markers; block source -> no .tex change (anchor lives in sidecar)",
      "lifecycle": "card selected + pinned in notes omni, focusNewCard into card body; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('note', ref) directly - byte-identical. Mode-A vs Mode-B anchor branch hinges on ref.kind (selection/linkedRange -> range anchor; block -> paragraph anchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@blockquote#grab",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "blockquote",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@blockquote#lightning",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "blockquote",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@bulletList#grab",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "bulletList",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@bulletList#lightning",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "bulletList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@codeBlock#grab",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "codeBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@codeBlock#lightning",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "codeBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@displayMath#grab",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "displayMath",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS keeps todo",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@displayMath#lightning",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "displayMath",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "NON_PROSE_BLOCK_ACTIONS keeps todo",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@exampleItem#grab",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "exampleItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@exampleItem#lightning",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "exampleItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@figureBlock#grab",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "figureBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@figureBlock#lightning",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "figureBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@graphicsBlock#grab",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "graphicsBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@graphicsBlock#lightning",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "graphicsBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@heading#grab",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "heading",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@heading#lightning",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "heading",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@latexComment#grab",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "latexComment",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@latexComment#lightning",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "latexComment",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@linkedRange#grab",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "linkedRange",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@linkedRange#lightning",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "linkedRange",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@listItem#grab",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "listItem",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@listItem#lightning",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "listItem",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@orderedList#grab",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "orderedList",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@orderedList#lightning",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "orderedList",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@paragraph#grab",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "paragraph",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@paragraph#lightning",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "paragraph",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@selection#grab",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "selection",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "seeds todo text from selected text",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@selection#lightning",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "selection",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "seeds todo text from selected text",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@texBlock#grab",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "texBlock",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@texBlock#lightning",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "texBlock",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@titleField#grab",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "titleField",
    "surface": "grab",
    "applies": "ok",
    "kindNote": "TITLE_FIELD_ACTIONS includes todo",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  },
  {
    "id": "todo@titleField#lightning",
    "category": "Card actions (11)",
    "action": "todo",
    "kind": "titleField",
    "surface": "lightning",
    "applies": "ok",
    "kindNote": "TITLE_FIELD_ACTIONS includes todo",
    "slashName": "",
    "inputRulePattern": "",
    "keybinding": "",
    "runRef": "action-registry.ts:1022 (cardRun) -> drag-handle-actions.ts:330 (case \"todo\")",
    "backbone": "",
    "expected": {
      "behavior": "Creates a TodoItem (addTodo), seeds its text from the selected/block text if present, and writes a Mode-A textObjectId link to the paragraph (addTodoTextObjectId). Selects+pins in the todo panel.",
      "docDelta": "none (todo is purely a sidecar card with a Mode-A anchor; no mark, no atom)",
      "sidecarDelta": "todos.json +1 TodoItem; textObjectIds link records paragraphId + targetKind",
      "texDelta": "none (todo anchor lives entirely in the sidecar)",
      "lifecycle": "card selected + pinned in todo omni, focusNewCard into card; not popped (mode omni)"
    },
    "crossSurfaceIdentity": "grab and lightning both dispatch('todo', ref) directly - byte-identical. Todo is anchor-by-paragraph-uuid only (no linkedAnchor even for selection refs - note the case does NOT call createAnchor).",
    "knownRisk": ""
  }
]