# Bug batch (2026-06-30): items 2,3,4,5,6,7,12,13,15

Bug-catcher session. 10 items diagnosed via an 8-lane read-only workflow
(`bug-batch-2026-06-30`, run wf_dbab948e-c07) + direct diagnosis of bug 5 (its
lane failed the schema). Research only — **no code edited** (checkout live-driven,
HEAD 1b776636). For the bug-cleaning session.

## Status table

| # | Title | Status | Root (one line) |
|---|---|---|---|
| 2 | Notes anchor to the section divider, not the heading text (dividers on) | ROOT-CAUSE-FOUND | `useMarginaliaRegistry.measureBlock` re-implements heading-wrapper descent instead of the `text-metrics.ts` SSOT, picking up wrapper margin/divider chrome |
| 3 | Code function crashes/hangs with no internet | ROOT-CAUSE-FOUND · **DEFERRED (user handles separately)** | offline TeX-package XHR to texlive.texlyre.org never posts back; `compileLaTeX` has an unused reject arg + no timeout/`onLine` guard → hangs forever |
| 4 | `--` / `---` don't convert to dash glyphs while typing | DIAGNOSED | parse+serialize already round-trip dashes; the **editor input layer has no InputRule** (SmartQuotes covers quotes only) |
| 5 | Skill set: find the referenced work; never fabricate, surface instead | DIAGNOSED | anti-fabrication language exists **scattered**; no shared editor include, no explicit "search the Library first" step |
| 6 | Relabel suggest/comment → request (6a/6c); default AI-request (6b) | DIAGNOSED | label strings + `aiRequest:false`-on-creation; must bridge at creation |
| 7 | Remove the focus-view star icon, keep the "Focus view" text pill | DIAGNOSED | `StatusCluster.tsx` renders an SVG reticle before the text |
| 12 | Bib-card drag shows green-circle (native copy cursor) | ROOT-CAUSE-FOUND | sanctioned MIME_CITATION drag, but `Editor.tsx` has no `dragover` setting `dropEffect` (RichTextField does) |
| 13 | Citation create: Return should commit (multi-cite complication) | FIX-SKETCHED | `StagedFooter` OK is mouse-only; picker eats Enter to stage a key |
| 15 | Citation card `+range` should go after the citation, not in the bibkey row | FIX-SKETCHED | `CitationKeyRow` renders `+range` inline in the metadata row |

**Recurring classes (fix in-class, per [[design-prefer-deep-unified]]):** bug 2 = the *duplicated-geometry* disease ([[ui_geometry_bugsweep_2026_06_30_status]]) — a surface measuring its own element instead of the shared SSOT. bug 6b = the *AI-request orphan-card* class ([[ai_request_title_audit_2026_06_30_status]]) — a card flagged `aiRequest:true` whose `ai-requests.json` entry never gets written; **land 6b on top of that pipeline fix, and bridge at creation.**

---

## (2) Notes anchor to the divider, not the heading — `ROOT-CAUSE-FOUND`
> **GENERALIZED + SUPERSEDED by [MEMO_NOTE_MARGINALIA_JUMP_2026_06_30.md](MEMO_NOTE_MARGINALIA_JUMP_2026_06_30.md)** — the user later reported ALL note marginalia place above target (correct-for-a-second, then jump up), not just headings-with-dividers. Same root (marginalia measures on a separate, divergent path from the grab-handle SSOT); the divider case is one amplified instance. Fix per that memo (unify marginalia onto `resolveBlockFrame`/`text-metrics`).

When section dividers are on (`.show-dividers-N`, [globals.css:3976-4054](src/app/globals.css#L3976)), the heading wrapper gets a large `margin-top` (4.2–8.2em) and a `::before` divider at `top:-1.4em`. `measureBlock()` in [useMarginaliaRegistry.ts:216-244](src/hooks/useMarginaliaRegistry.ts#L216) has its **own** heading-wrapper descent (`querySelector("h1,h2,h3")` — note it misses h4-h6) rather than calling the shared `resolveInlineContextElement` ([text-metrics.ts:174-178](src/lib/text-metrics.ts#L174)) that the grab-handle path uses; when it falls back to `coordsAtPos` the coordinate may include the wrapper's margin offset, landing the note on the divider row. **Deep fix:** route marginalia measurement through the shared `resolveInlineContextElement` SSOT (also fixes the latent h4-h6 miss) so every surface measures the heading text's own line-box, never the wrapper+divider offset — same fix pattern as the geometry bugsweep. **Confidence medium** on the exact coordinate-space mechanism; the grab-handle path is presumed correct, so the divergence between the two paths *is* the bug. **Verify under real FSA** (anchor geometry masks in the preview — [[anchor_persistence_dev_masks_fsa]]): compare note vs grab-handle Y on the same heading with dividers on/off.

## (3) Offline code/compile hang — `ROOT-CAUSE-FOUND` · **DEFERRED**
> User will handle offline compile in a separate session — do NOT implement here. Diagnosis retained for that session.

The compile path fetches TeX packages via worker XHR from `https://texlive.texlyre.org/` ([swiftlatex.ts](src/lib/swiftlatex.ts), `TEXLIVE_ENDPOINT`). Offline, the XHR fails **in the worker without posting a message back**; `PdfTeXEngine.compileLaTeX`'s Promise has an **unused reject arg** ([public/swiftlatex/PdfTeXEngine.js:131-172](public/swiftlatex/PdfTeXEngine.js#L131)) and [useLatexCompile.ts:136-198](src/hooks/useLatexCompile.ts#L136) has no timeout/`navigator.onLine` guard → the "compiling" state never clears (soft hang; user perceived as crash). **Surgical (ship first):** `navigator.onLine` guard + a compile **timeout** that rejects with a friendly "offline / timed out" message; make the worker always `postMessage` on fetch error. **Deep:** pre-bundle common TeX packages locally in `public/swiftlatex/` for true offline compiles + offline-aware UI. **Decision:** surgical-only now vs. invest in pre-bundling (recommended staged: timeout now, pre-bundle follow-up). Confirm symptom is a hang vs a hard crash (affects copy). The deep worker-error path touches minified WASM internals — highest-uncertainty item.

## (4) `--`/`---` dash input rule — `DIAGNOSED`
The bidirectional typography already works: PARSE `dashesToGlyphs()` ([latex-parser.ts:192-195](src/lib/latex-parser.ts#L192)) converts `--`→`–`/`---`→`—` on load; SERIALIZE `typographyToLatex()` ([latex-serializer.ts:145-169](src/lib/latex-serializer.ts#L145)) converts back. **The gap is the editor INPUT layer** — [smart-quotes.ts](src/lib/tiptap/smart-quotes.ts) has quote InputRules but **no dash rules** (loaded main-surface-only, [editor-extensions.ts:1794](src/lib/editor-extensions.ts#L1794)). **Fix:** add longest-first InputRules (`---$`→`—`, `--$`→`–`) to SmartQuotes (or a new dash extension); serialize already re-emits canonical `--`/`---` to `.tex`. **DECISION (blocks impl):** the user said "`--` → em-dash," but the codebase (LITERAL_TABLE [latex-typography.ts:178-182](src/lib/latex-typography.ts#L178), parser, serializer, tests) enforces **LaTeX convention: `--`=en `–`, `---`=em `—`**. **Recommend LaTeX convention** (matches the whole round-trip) — confirm with user. Secondary: gate the rule off inside code/math to match the serializer's boundary.

## (5) Skill "find-or-surface, never fabricate" — `DIAGNOSED`
Anti-fabrication language **already exists but is scattered**: [find-citation.md:100/143/224](editor/skills/find-citation.md) ("rather than fabricating… take the failure path, status `failed`"), [authenticate-bib.md](library/skills/authenticate-bib.md) (marks `unverified`/`failed`, never invents fields), [_doctrine.md:359/375](library/skills/_doctrine.md) ("not a license to invent text"). **Gaps:** (i) no **editor-side shared include** (`editor/skills/_*.md` doesn't exist — only `library/skills/_doctrine.md`), so the rule is copy-pasted; (ii) no explicit "**search the user's Virgil Library (catalog + master.bib) FIRST**" step in the resolver skills — the user emphasizes "a request *to the library*," i.e. check what's already indexed before external lookup or creation. **Deep fix (unify):** add one canonical "**Find-or-surface, never fabricate**" clause — (a) always search Library catalog/`master.bib` then external sources; (b) if no confident match, mark the request `failed`/surface it to the user, never invent a source/DOI/metadata/text — to `library/skills/_doctrine.md`, and create an editor-side shared include (or cross-reference the library one) so `find-citation`, `answer-bib-review`, `authenticate-bib`, `triage-pdf/pending`, `ai-requests` all point to ONE rule. Note: build mirrors skills → `.claude/commands/` (rerun `npm run build:library-bundle`/`build:editor-bundle`).

## (6) Relabel + default-AI-request — `DIAGNOSED`
**LABEL-ONLY strings** (never touch internal kind ids `revision-comment`/`cutter-comment`/`report`/`report-request` — load-bearing across registry, creation, morph pairing, bridge):
- (6a) [action-icons.tsx:68-70](src/lib/actions/action-icons.tsx#L68): "Suggest edit"→"Request revision", "Suggest cut"→"Request cut", "Report"→"Request report". Add-menus: [CutterPanel.tsx:95-96](src/panels/Cutter/CutterPanel.tsx#L95), [RevisionsPanel.tsx:100-101](src/panels/Revisions/RevisionsPanel.tsx#L100). Check the command palette / any other menu strings.
- (6c) [card-registry.tsx:488](src/cards/card-registry.tsx#L488) (revision-comment) + :518 (cutter-comment) `label:"Comment"`→`"Request"` (renders in `CardKindHeader` + morph dropdown).
- (6b) default AI-request: flip `aiRequest:false`→`true` at creation ([useCutter.ts:197](src/hooks/useCutter.ts#L197) `addComment`, [useRevisions.ts:185](src/hooks/useRevisions.ts#L185), and the report-request creation in `useReports.ts`) **AND call `bridgeCardAiRequestFlag` at creation** — otherwise it recreates the orphan-card class (a card flagged true whose `ai-requests.json` entry is never written; see [[ai_request_title_audit_2026_06_30_status]] D1). **Sequence after the AI-request-pipeline fix.** Open Q: does `useReports.addReportRequest` already default true + bridge?

## (7) Remove focus-view star — `DIAGNOSED`
[StatusCluster.tsx:204-217](src/components/editor-layout/StatusCluster.tsx#L204): the `focusActive` button renders an SVG reticle/star (~:211-214) before the "Focus view" text. **Fix:** delete the SVG; keep the button, `className`, `onClick`, `aria-pressed`, `data-hint`, and the text → a text-only mode-indicator pill. Trivial, high confidence.

## (12) Bib-card native copy-cursor (retired-DnD residue) — `ROOT-CAUSE-FOUND`
The green-circle-with-cross is the **browser's native `copy` drop cursor** — the drag is a **sanctioned** MIME_CITATION inline-insert (STYLE_GUIDE Drag §, category 2, deliberately kept when anchor-drag was retired — **do NOT remove it**), but [Editor.tsx:580-650](src/components/Editor.tsx#L580) has a `handleDrop` and **no `dragover` handler setting `dropEffect`**. [RichTextField.tsx:526-533](src/components/RichTextField.tsx#L526) and [CitationCard.tsx:495-525](src/panels/Citations/CitationCard.tsx#L495) already do this correctly. **Fix:** add an Editor `dragover` handler mirroring RichTextField — `dropEffect='move'` for MIME_ARCHIVE else `'copy'` for the inline-insert MIMEs (citation/footnote/text). **Decision:** restyle (recommended) vs remove — restyle; it's a signaling gap, not an unwanted drag. Open Q: React `onDragOver` on the wrapper vs a ProseMirror `handleDOMEvents.dragover` (does PM consume dragover first?). Also swept for other native draggables — the bib/citation ones are sanctioned; this is the missing-feedback bug.

## (13) Citation create: Return to commit — `FIX-SKETCHED`
[CitationCreatePopover.tsx](src/panels/Citations/CitationCreatePopover.tsx) `StagedFooter` (~:103-160) OK button is **mouse-only**; the picker's `onInputKeyDown` ([BibEntryPickerMenu.tsx:370-392](src/components/library/BibEntryPickerMenu.tsx#L370)) consumes Enter to **stage** a key, so there's no keyboard path to **commit**. **RESOLVED model — Enter commits directly.** Enter must **finalize the citation** (single-cite fast path). The complication: the picker currently *consumes* Enter to STAGE a key (keepOpenOnPick), so change `BibEntryPickerMenu.onInputKeyDown` when hosted by `CitationCreatePopover` so **Enter = stage the current typed/active key (if any) THEN commit & close**. Additional keys for a **multi-cite** are added by **mouse/arrow pick (no Enter)**; Enter then commits whatever is staged. Escape still closes (commit-if-staged). Net: Enter = commit-(after-staging-active); the keep-open-for-more path is the non-Enter pick. Guard against an empty commit.

## (15) Citation card `+range` placement — `FIX-SKETCHED`
[CitationCard.tsx:1269-1429](src/panels/Citations/CitationCard.tsx#L1269) `CitationKeyRow` renders `+range`/postnote inline in the bottom **metadata** row (~:1390-1402) with citekey/copy/delete; the citation **display** is ~:1176-1223. **RESOLVED — trailing the citation inline.** Move `+range`/postnote OUT of the bibkey metadata row and **append it to the END of the citation display line** (not a separate row). Keep it **per-key** (multi-cite, `rowId`-scoped — current model): each key's range trails its own citation display. Lift `pgOpen`/`pgDraft` as needed but render inline after the citation text, not in the entry-state controls row. Best landed **together with (13)** (same Citations surface + multi-cite/per-key model).

---

## Decisions — RESOLVED by user (2026-06-30)
1. **(4) → LaTeX convention.** `--`→en `–`, `---`→em `—`. Matches the existing parser/serializer/tests. Implement the two longest-first InputRules exactly as diagnosed.
2. **(3) → DEFERRED.** User will handle offline compile in a separate session. Do NOT touch swiftlatex/compile in this batch — leave the diagnosis for that session.
3. **(12) → restyle** (implicit; the sanctioned drag stays). Add the Editor `dragover` `dropEffect` handler; do not remove the bib drag.
4. **(13) → Enter commits DIRECTLY.** NOT the Cmd+Enter model. Enter finalizes the citation. **Multi-cite implication (the "complication"):** the picker currently *consumes* Enter to STAGE a key (keepOpenOnPick), so the fix must change that in the create-citation context: **Enter = add the current typed/active key (if any) THEN commit & close**; adding *additional* keys for a multi-cite is done by **mouse/arrow-select (pick) without Enter**, and Enter commits whatever is staged. So Enter is "commit (staging the active key first)"; the keep-open-for-more path is the non-Enter pick. Update `BibEntryPickerMenu` `onInputKeyDown` behavior when hosted by `CitationCreatePopover` to commit rather than only stage.
5. **(15) → trailing the citation inline.** Append `+range`/postnote to the END of the citation display line (not a separate row), moved out of the bibkey metadata row. Per-key still applies for multi-cite (each key's range trails its own citation display).

## Ready to implement now (low risk): (7), (6a/6c labels), (12), (4). Moderate: (2), (6b), (15 inline). Higher-uncertainty: (13 — Enter-commit + multi-cite pick model). **DEFERRED: (3)** (separate session). **(5)** = skill-markdown doctrine unification. Live-verify (2) and the AI-request bits under real FSA, not the preview.
