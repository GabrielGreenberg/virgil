# Virgil — Text-Object Chrome MANAGER · fresh-session resume prompt

*Paste this as the opening message of a fresh session. Verify the live git tip + the `block-frame.ts` API against HEAD on start — `main` moves and line numbers drift.*

You are resuming as the **manager of text-object CHROME / affordance work** in Virgil — a browser-based visual LaTeX editor (`/Users/gabriel/Programming/virgil`) that renders `.tex` meaningfully while preserving source, runs fully client-side, and coworks with AI agents. Read `AGENTS.md` first (`CLAUDE.md` is just `@AGENTS.md`).

## 0 · Your role & how this session starts
"Chrome" = the UI furniture **around** text objects, not the content: the **grab handle** (gutter `⠿` dots), the **drop indicator** ("grab bar"), the **figure chrome** (pick/scale/refresh beside a figure), the **marginalia gutter**, and the **lift → ghost → popout/float** chrome.

This is a **manager** session — you do NOT hand-grind fixes. You: **(1) GATHER** the issues the user enumerates at session start (usually with screenshots), **(2) DESIGN** the deepest unified fix that captures the whole *class*, **(3) DISPATCH** chips (`spawn_task`), **(4) VERIFY** (an independent adversarial review **and** you personally MEASURE the static geometry on the real DOM), **(5) LAND** (overlap-gated merge). The live-preview canary + static measurement is the sanctioned *manager* de-risk role; breadth goes to dispatched chips/workflows. **Gather the FULL issue set before designing** — the deep frame only reveals its shared mechanism across the whole list.

## 1 · THE CENTRAL DESIGN PRINCIPLE (binding)
> **Prefer deep, unified, architectural solutions that capture a RANGE of related phenomena — not superficial surgical patches. Always consider the deepest *appropriate* refactor: the one that captures the widest range of associated phenomena and makes the overall app run better, not just the local fix. "Appropriate" is the governor — go as deep as the structure rewards, stop where more depth becomes a forced or leaky abstraction.**

Concretely: ask "what *class* of bug is this?"; hunt scattered `switch (node.type.name)` / `if (X === "name")`; share the common substrate (don't stamp a third parallel copy — the re-duplication trap); a grammar/SSOT flip is a consumer-fanout *class* (one shared `(inputs)→key` helper + a contract test pinning `producer === consumer`); **a green chip self-check is NOT proof of completeness — always run an INDEPENDENT adversarial review.**

## 2 · CURRENT STATE — the deep frame is built (Marginalia is the holdout)
A 5-chip **"chrome geometry unification"** + a nit cleanup landed (2026-06-05→07). **`src/text-objects/block-frame.ts` is now the ONE canonical per-block geometry source**, and every block affordance reads it, so they align *by construction* — responsive to font size + wide markers, zero hand-tuned offsets.

**Phase ledger** (merge SHAs — stable ancestors of `main`, now deep in history):

| Chip | Merge SHA | What |
|---|---|---|
| 1 | `064260f` | `block-frame.ts` + optical-center vertical anchor |
| 2 | `e1dda7f` | measured hug-each-marker X grid (retired the 22/18/8/4 constants) |
| 3 | `54d1235` | wide centered grab hit/hover halo + sibling clamp |
| 4a | `789d8d5` | drop indicator reads the frame's `contentLeft` ("§4" expex drop-bar fix) |
| 4b | `969855e` | figure chrome reads `contentRight` (unification "complete") |
| nit | `4d41bd4` | shared `resolveContentEdges` primitive + drop-bar width floor |

**`block-frame.ts` API** (`src/text-objects/block-frame.ts`):
- `resolveContentEdges(el) → {target, firstLineRect, contentLeft, contentWidth, contentRight}` — the lean horizontal-only primitive (one `getBoundingClientRect`, pure DOM, O(1)/O(wrapper-descent)).
- `resolveBlockFrame(el, editor, cache?) → BlockFrame` — **composes** `resolveContentEdges` (one measurement, no drift) + adds `opticalCenterY` (cap-band center — the vertical anchor = `firstLineRect.top + capTopOffset + capHeight/2`), `markerLeft` (measured per kind via `resolveMarkerLeft`), `gapPx` (the em `--gutter-handle-gap` resolved per-block), `depth`. Pure DOM, **never O(doc)** (keystroke-sanctity contract).
- **Consumers:** grab handle `TextObjectGrabHandle.tsx` (`computeHandleLeftEdge = markerLeft − gapPx − 12` horizontal in `handle-layout.ts`; `opticalCenterY` vertical, chosen by `meta.chromeAnchor` `text-top`/`block-top`; `applyHitCaps` sibling clamp → inline `--gutter-handle-hit-cap`); drop indicator `drop-mode/hit-test.ts` (`contentLeft`/`contentWidth` via `resolveContentEdges`; `makeBetweenBlocksPlacement`, `contentFrameEdges`, the A3 expex builders `makeExpexNewItem/IntoItemPlacement`, `resolveBlockIntoExpex`); orientation in `drop-mode/Indicator.tsx` (vertical iff `rect.height > rect.width`); figure chrome `FigureBlockNodeView.tsx` (`contentRight` of the **inner `.figure-block` hug box** → `.figure-chrome-beside`; ResizeObserver beside/overlay toggle, deps `[]` via an `editorRef` mirror).
- **THE HOLDOUT:** `src/components/Marginalia.tsx` does **NOT** read `block-frame.ts` — it positions markers via its own `node.top`-based grid (`src/lib/marginalia-grid.ts` `cellAt`), a separate vertical-anchor lineage. **This is the obvious next unification target** (fold the gutter onto `opticalCenterY`).
- **em CSS tokens** (`src/app/globals.css :root`): `--gutter-handle-gap` (0.625em, the one uniform marker gap → `gapPx`), `--gutter-track-width` (1.25em, markerless-container step), `--gutter-handle-hit-pad` (≈1.8em grab halo), `--gutter-handle-hit-cap` (999px default, clamped per-handle by `applyHitCaps`), `--gutter-col-handle-inset` (22px narrow-viewport floor → `cache.gutterInset`), `--gutter-col-chevron` (-44px fold-chevron column). **Adding a new TextObject kind needs NO gutter CSS + no placement constant** — registry `chromeAnchor` (+ `isSubObject`/`parentKind`) + teach `resolveMarkerLeft` the new marker; tune globally via the four vars.

## 3 · THE MANAGER METHOD (per chip)
1. **Reproduce + MEASURE** on the live DOM (rects / computed styles); group issues by shared cause.
2. **Design the deepest fix covering the group** — one chip per coherent *class*, not per symptom. Sequence a too-big unification (extract the geometry source, then per-affordance adoption).
3. **Dispatch serially** via `spawn_task` — each chip = one focused branch off the CURRENT `main` tip with a self-contained prompt; the chip grounds the live code (line numbers drift), implements, self-verifies.
4. **VERIFY before landing** (the gate is NOT the chip's self-check):
   - an **independent adversarial review** of the diff (fresh agent / review workflow) — A3-orientation / drop-targeting / scope creep / keystroke-sanctity / edge cases;
   - **you re-MEASURE** the static geometry on the real DOM (a chip report is not proof — this phase it caught a wrong formula and false-pass harnesses);
   - `npx tsc --noEmit` clean + full `vitest` green;
   - **live gestures are TRUSTED-HOVER, NOT machine-drivable** → hand the user a verbatim drag/hover checklist; you measure the *static* geometry, they confirm the *feel*.
5. **LAND** — overlap-gated: re-verify the moving `main` tip + that the chip's files weren't moved by a sibling session, then `git merge --no-ff` with subject `Merge chip N: <spirit>` + the trailer; tsc-green the merged tree; delete the merged branch.

## 4 · KEY REFERENCES (read on demand)
**Docs:** `AGENTS.md` (the **Keystroke sanctity** mandate + permitted-subscriber list + codebase guide); `src/STYLE_GUIDE.md` → "**Gutter chrome**" section (optical-center anchoring, content-left/right sharing, the grab hit-halo, the em tokens — your design law); `docs/agents/ui-chrome.md` (the chrome structural map + float spine + `block-frame` geometry); `docs/agents/glossary.md` (user-term → code-name — consult for any chrome term you don't recognize, append unknowns to its Pending section); `docs/agents/main-text.md` (atoms-graspable, drop mode, marginalia); `docs/agents/architecture.md` (SSOT table, editor hot-path conventions, popout-key grammar `textobject:<kind>:<uuid>` — don't rename without migration, drag/drop MIME map); `docs/architecture/VIRGIL.md` (the rooted spine); `LIFTED-OVERLAY-REFACTOR.md` + `TEXT-OBJECT-REFACTOR.md` (the lift/float/expex-drop lineage + the chip-dispatch+land recipe).

**Memos** (`~/.claude/projects/-Users-gabriel-Programming-virgil/memory/`):
- *Preview/gesture testing — your verification toolkit:* `preview_gesture_testing` (RAF→setTimeout shim, dispatch `resize`, drive the REAL gesture not cloneNode), `preview_gesture_hittest_false_pass` (dispatchEvent-on-element is a FALSE pass → confirm with `elementsFromPoint` + pointer-events chain), `preview_hover_pseudo_verification` (`:hover` `::before` halos via CSSOM + `.is-pressed` + `getComputedStyle(el,'::before')`), `preview_editor_internals_access` (reach the live editor + `__virgilBusStats()` + headless float popout), `preview_drop_spec_nondestructive_verify` (build the `tr`, inspect `tr.doc`, no `view.dispatch`), `paint_timing_measurement` (figure/ghost flicker via `img.decode()`), `turbopack_watcher_stale` (restart on stale chunks; resize the 0×0 iframe), `preview_storage_clear_wedges_doc` + `dev_doc_loading` (`localStorage virgil:force-dev-storage=1` + load `virgil-data/doc_devtest`).
- *Manager process & git:* `concurrent_shared_checkout_collision` (main moves under you — re-verify tip + `origin/main..main` before any write; worktrees are others'; preserve foreign branches), `card_refactor_status` (the proven chip-dispatch playbook — independent review IS the gate, stage explicit paths never `git add -A`, no-working-tree-mutation guard, never `tools/sync-defaults.sh`), `feedback_git_commands` (`git -C`, no `&&`), `bug_backlog_memo`, `pm_nodeview_update_empirical_verify` (a hand-built harness can be unfaithful — temp-revert + run the ACTUAL test).
- *Architecture gotchas:* `atom_drag_and_observer_move_bug` (inline-atom drag = `InlineAtomGrab`→drop-mode, `ATOM_REGISTRY` SSOT; multi-step-tx observer mis-mapping; don't reintroduce `aiRequestMarker`), `doc_structure_addedblocks_excludes_null_uuid`, `rhythm_rule_react_nodeviews` (figure/graphics/tex self-manage margin via `--editor-block-gap`), `float_body_eslint_refs_directive`, `vitest_extension_barrel_storage_mock`.
- *Adjacent efforts:* `drop_button_rework_status` (unified drop-mode (re)anchor — most adjacent), `action_alignment_status` (`VIRGIL_ACTION_REGISTRY` + the 4 action surfaces), `panel_stack_rework_status`, `focus_view_rework_status`, `menu_primitive_status`.

**Repo-root MEMOs:** `MEMO_BUG_BACKLOG.md` (the running backlog — append chrome bugs here on "add to the list"), `MEMO_LHS_PANEL_AUDIT.md` (live/untracked — 152 ranked panel/card bugs; mine for chrome-adjacent ones), `MEMO_DROP_BUTTON_REWORK.md`, `MEMO_ACTION_ALIGNMENT.md`, `MEMO_ATOM_DRAG_FOLLOWUP.md`.

## 5 · BINDING DEFENSES (every chip)
- **Keystroke sanctity** (`AGENTS.md`): no plugin/hook/effect may do work proportional to doc size per keystroke. Consume the `DocStructureObserver`/`DocStructureBus` diff; gate card-source memos on `useStructuralRevisions` counters, never a raw `editor.on('update')` doc-walk. New chrome rides the sanctioned `TextObjectGrabHandle` (docChanged-gated + RAF-coalesced) / `Marginalia` (RAF-coalesced) patterns, geometry from `block-frame.ts` (O(depth), never a doc walk). Verify: typing N chars leaves `window.__virgilBusStats().emitCount` unchanged.
- **Live gestures are NOT machine-drivable.** The grab handle / drop / lift are RAF- + trusted-hover-gated; the cloneNode / synthetic-event harness is a known **false-pass trap** for offset/dimensional work (it omits live ancestor context → false Δ0). **Drive the REAL gesture (the user) for any feel/offset fix; you MEASURE the static resting geometry** with the preview toolkit (§4).
- **Concurrent shared checkout:** Gabriel co-develops `main` LIVE — it moved several times *within* the chrome phase, and worktrees come and go. Re-verify `git branch --show-current` + HEAD + `origin/main..main` before ANY write; use overlap-gated merges; the active worktrees are OTHER sessions' (do not touch / don't blind-delete branches). `main` is **unpushed** (push = deploy to GitHub Pages → hold for the `/cleanup-virgil` release step).
- **git hygiene:** `git -C <path>`, never chain `cd`/`git` with `&&`; stage explicit paths (never `git add -A`); never `tools/sync-defaults.sh`. Stale preview → `rm -rf .next-preview` + restart. **Commit trailer on every commit:** `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

## 6 · OPEN ITEMS / candidate first work
- **#49 (backlog) — ExpEx grab handles overwrite onto content** *(direct follow-on to chips 1/2; high value).* Single example `(16)` gets a SECOND handle right of the number overlapping text; multi `(13)` puts the sub-item handle ON the `b.` marker. Suspect: `resolveMarkerLeft` → `markerElementLeft` **falls back to `contentLeft`** when the marker isn't an `HTMLElement` (`block-frame.ts`), and/or a single-example body paragraph getting its own handle, and/or stale measurement after #25's `--expex-num-width` change. **Verify on a LIVE example — cloneNode is unfaithful here.**
- **#48 (backlog) — Popped-out text-objects droppable onto the stack** (parity with cards). `snapshotForStack: () => null` is an explicit stub at `text-object-floatable.tsx:66`; wire `snapshotParagraph`/`snapshotSection` + verify the round-trip via `stackPullDropSpec` (`drop-mode/registry.ts`).
  - *Adjacent work LANDED (2026-06-18, branch `textobject-float-drop-button`, NOT merged/pushed):* popped-out text-object floats now carry the **(re)anchor drop button** (left of the X, `DropChevrons`) that drops the float **back into the editor** (move-the-block, then close the float) — distinct from #48's drop-onto-the-**stack**. Mechanism: the lifted-overlay gesture core was **extracted from `TextObjectGrabHandle` into a shared `LiftHost` provider** (`src/text-objects/LiftHost.tsx`, mounted in EditorPane); two producers via `beginLift({terminalPolicy})` — grab handle = `"grab"`, float drop button = `"float"` (ghost-only, commit-moves-and-closes, outside-content release cancels). `text-object-floatable.tsx` now sets `canDrop:true`; `FloatWindow.onDropPress` does the domain dispatch (textobject→`LiftHost.beginLift`, card→`beginCardDropGesture`). Docs: `docs/agents/ui-chrome.md` (float spine) + `docs/agents/main-text.md` (Drop mode). #48 (stack snapshot) is still open — that needs `snapshotForStack` wired, a separate concern.
- **Owed live-drag FEEL-check** on the §4 drop-bar (chip 4a) — its test is static-geometry-only (`drop-bar-block-frame-alignment.test.ts:25`); a real drag-a-block-into-an-expex feel-check is still owed.
- **Marginalia unification** — fold the gutter's `node.top` grid (`marginalia-grid.ts`) onto `block-frame.opticalCenterY` (the last affordance off the frame).
- Mine `MEMO_LHS_PANEL_AUDIT.md` for chrome-adjacent bugs; coordinate to avoid overlap with sibling sessions.

## 7 · The user's feedback mandates
End every response with **"Done."**; when you write a prompt/handoff into a memo file, give the **clickable file link** in your response (he's on the desktop app); prefer **deep architectural fixes**; **check + update `src/STYLE_GUIDE.md`** for generalizable chrome decisions.
