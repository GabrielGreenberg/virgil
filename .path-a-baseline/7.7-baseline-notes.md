# Path A 7.7 — Pre-Swap Baseline Notes

**Captured:** 2026-05-07 after 7.6 finish landed.
**Purpose:** Lock in the visual + functional state of the main editor and Library Reader before 7.8 swaps `EditorPane` into EditorLayout's doc branch.

## Environment

- Branch: `main`
- Dev preview restart was needed to pick up 7.6 finish's structural change to EditorPane (Turbopack HMR didn't catch the new `editor-pane-column` wrapper). After restart, both surfaces verified.
- Test paper for Reader: `bringhurst1992` (Bringhurst's "Elements of Typographic Style" excerpt).
- Test doc for main editor: `Coherence Intro` (the per-doc test fixture).

## Main editor baseline (`virgil-data/doc_devtest`)

Verified rendering correctly via dev preview:

- ✅ **Default state** — both rails populated. Left rail icons: Search, Outline, Footnotes, Citations, Bibliography, Quotations, Examples, Filter. Right rail icons: Word Count, Notes, Todo List, Revisions, Errors, Cutter, Archived Text, Filter. Above the editor pod: docked MenuBar (centered, sticky, 32px tall) with Format / Actions popover triggers, paragraph nav, Split editor, View options. Top bar: Library tab, Coherence Intro tab, Zen, collab pill, Preferences, Help, Print, AI requests, Style dropdown, Code, Compile, PDF, Collapse toolbar.
- ✅ **Sample panel — Outline** — left rail Outline button activates a left-side panel with "Document start" + "Test heading" entries plus per-row "words" + "2" indicator and an inner kebab.
- ✅ **ViewMenu (kebab in docked MenuBar)** — opens dropdown with: DISPLAY group (Paragraph titles, % comments, Current section, Labels, all checked), Marginalia submenu, Highlights submenu, Show dividers for… submenu, Margins…, Fonts…, Close all panels. Both **Margins** and **Fonts** items are present (FULL_CHROME default; `showEditItems: true`).
- ✅ **Editor pod content** — "Start writing here..." paragraph, "1 Test heading" with section indicator + "Chapter 1" label, expand/collapse-all-sections controls visible.
- ✅ **No console errors** on load or panel toggles.

## Library Reader baseline (`bringhurst1992`)

Verified rendering correctly:

- ✅ **Bib card top strip** — `@book{bringhurst1992}` chip + author/title/publisher line + "more" / "edit" actions.
- ✅ **AI requests row** — pdf/idx/bib status checkmarks + Index/Deep index/Bib review/Doc review checkboxes.
- ✅ **Text/PDF toggle** — top-right segmented control.
- ✅ **PageScrollStrip** — left margin shows page-number jump anchors (1, 2, 3, 4, 5, 6, 7+) — clickable; tied to `\pgmark{N}` markers in the LaTeX source.
- ✅ **Editor pane** — title, author, year, then "Introduction" section, then body text. Section indicator (gray "Section ▾" lozenge) attached to the heading.
- ✅ **Outline panel** — auto-mounted on right with section list (Title, Introduction, The grand design, Margins, The footnote as a quiet companion, Closing). Word counts per section.
- ✅ **Right rail** — vertical text labels for the 6 reader-allowed panels: outline, footnotes, examples, citations, bibliography, notes.
- ✅ **No docked MenuBar** — confirmed `menuBar` prop is absent in Reader's `<EditorPane>` mount → MenuBar mount is dormant.
- ✅ **No formatting toolbar** — `READER_CHROME.showFormattingToolbar = false` → suppressed inside the (absent) MenuBar and the (absent) detached versions.
- ✅ **`editor-pane-column` wrapper present** — confirmed structurally via DOM query: `editor-pane-root > editor-pane-column > editor-pane-pod`. Reader doesn't pass `menuBar`, so the MenuBar mount inside the column is gated off and only the pod renders.

## DOM structure invariants (for 7.8 to preserve)

- Reader path (`PaperRender.tsx:219`):
  ```
  EditorPane mount with chrome={READER_CHROME}
    └─ editor-pane-root (flex row)
        └─ editor-pane-column (flex col)
            └─ editor-pane-pod (flex 1; relative; data-marginalia-host)
                ├─ Marginalia
                └─ paper-render
                    ├─ MarginActionToolbar (Note button only — actionToolbarKinds: ["note"])
                    └─ VirgilEditor (editable=false)
  ```
- Main app path (currently EditorLayout's doc branch — to be replaced by EditorPane mount in 7.8):
  ```
  data-tool-strip="text" (sticky 32px MenuBar band)
    └─ MenuBar (atHome, FULL_CHROME equivalent)
  Editor column with VirgilEditor + Marginalia + MarginActionToolbar
  Floating panels block (popped panels)
  Three detached toolbar portals (Actions / Formatting / Menu)
  Root DockOutline / CardLiftOutline
  ```

## Behaviors confirmed (no-regression baseline)

- Tab switching works: Library ↔ Coherence Intro outer tabs, Central Library ↔ paper inner tabs.
- Reader paper navigation: marshall1997, mckenzie1999, bringhurst1992 inner tabs persist.
- Outline panel toggle: click left-rail Outline button → panel expands with section list.
- ViewMenu opens, all expected items present.
- No console errors on:
  - Initial app load.
  - Tab switch.
  - Library tab → paper switch.
  - Reader-side rail icons interaction (Outline, etc.).

## Items not exhaustively tested in this baseline

(Coverage relied on structural typecheck + visual sampling rather than every-panel-individually click-through.)

- Each individual panel kind (15 total — sampled Outline only).
- Detached toolbar drag-out behavior (mouse drag is hard via eval; trusted EditorLayout's existing portals + the structural typecheck).
- Margin edit mode + guides.
- Print mode + PrintAppendices.
- PDF/Code view branches.
- Zen mode.
- Drag-paragraph-into-panel-icon (the iconDropMimes routing).
- Multi-paper sidecar isolation in Reader.

7.8's verification battery should cover these explicitly (the master plan's 10-item battery includes pixel-level diff against this baseline plus full-panel walkthrough).

## How 7.8 should diff against this baseline

When 7.8 mounts EditorPane in EditorLayout's doc branch:

1. **Reload the main editor** (Coherence Intro). The post-swap rendering should match the "Main editor baseline" section above — same layout, same panel rails, same docked MenuBar, same content. Visual differences are bugs.
2. **Reload the Reader** (Library → bringhurst1992). Should be functionally unchanged — Reader already used EditorPane pre-swap; 7.8 doesn't change its mount.
3. **Confirm DOM invariants** above hold for both surfaces.
4. **Run the master plan's 10-item verification battery** (in `path-a-finish-7.6-to-7.8.md` or this plan).

## Memory note

Per `turbopack_watcher_stale.md`, structural changes to EditorPane may not propagate via HMR — restart the preview server when 7.8 lands the new mount, before claiming the swap visually verified.
