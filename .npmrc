# Node is pinned (`.nvmrc` = 22; CI + deploy run 24). Node 25 ships a built-in
# `localStorage` global that shadows jsdom's and fails ~126 tests with
# "localStorage.clear is not a function" — loud at INSTALL, not at test time.
engine-strict=true
